# Ductus Decision Log

Decision-of-record for the Ductus language. One atomic decision per numbered entry: `N. <sentence>: <example> (§ref into SPEC.md)`.
This log is the lossless WHAT; SPEC.md is the normative elaboration holding the WHY — follow an entry's (§) reference for rationale and full nuance. SPEC.md must conform to this log; divergence between the two is a defect.
Ordering: generic → specific, topic-contiguous (## headers); reading any prefix yields a coherent partial model. Position carries this gradient; numbers are stable identities — never renumbered, never reused. New decisions take the next free integer and are placed by topic; amendments keep their number; revoked numbers are retired (gaps allowed).
Edit protocol: change this log FIRST, then update the referenced SPEC.md section to conform.
If you discover a contradiction, ambiguity, or incoherence in either document: stop and disclose; never silently resolve it.
Quarantine: the contradictions/ambiguities/incoherencies discovered in SPEC.md during serialization are recorded in DECISION_LOG_FINDINGS.md with their contested wordings (see that file for the live count); contested content is deliberately NOT serialized here until ruled on.

<!-- BEGIN LOG -->

## Identity & Philosophy

1. The specification is the authoritative source for the type system, evaluation model, and runtime semantics. (§1.1)
2. Implementation details (compiler internals, optimizations, runtime representation) are out of scope except where they constrain user-visible semantics. (§1.1)
3. Lexical and syntactic structure is specified directly in this specification and decision log; there is no separate grammar document. (§1.1)
4. This specification is the single authoritative source for the language's syntax and semantics. (§1.1)
5. The specification carries no postponed design decisions: every described feature is specified definitively and every excluded feature is excluded definitively with an idiomatic alternative. (§1.2)
6. Where the specification stops short it does so only by an explicit boundary — standard-library delegation or implementation-defined behavior — never a deferred decision. (§1.2)
7. Ductus is a general-purpose, statically typed language in which compositional reactive systems are first-class. (§1.3)
8. The language commits to no domain-specific primitives. (§1.3)
9. Every value has a concrete type known to the compiler at code generation time. (§1.3)
10. Most types are inferred from context; annotations are required only where inference is ambiguous or to pin a choice deliberately. (§1.3)
11. External state — module-level declarations, type definitions, signals, function parameters, and record fields as a property of types — is always immutable. (§1.3)
12. There is no module-level `mut` and no globally-mutable state. (§1.3)
13. Controlled local mutation is available inside function bodies through `mut` bindings. (§1.3)
14. Mutation is bounded to the function body that declared it; callers never observe a function's internal mutations except through its declared return value. (§1.3)
15. Time-varying external behavior is expressed through the reactive system, not through assignment. (§1.3)
16. Every value has exactly one real owner at any moment. (§1.3)
17. Function parameters default to borrow-equivalent access: the function gets a read-only alias of the caller's value for the call's duration, and the caller's binding survives. (§1.3)
18. Consumption of an argument is opt-in via `own` in the signature and `move` at the call site. (§1.3)
19. Returning transfers ownership to the caller when the result is a real owner or the signature opts into `-> own T`. (§1.3)
20. A default return rooted in a borrowed input is itself borrow-equivalent. (§1.3)
21. There is no garbage collector and no language-level reference counting; the runtime may use refcounting internally for specific types such as `string`. (§1.3)
22. There is no shared mutable state. (§1.3)
23. From the caller's perspective every user-defined function is referentially transparent: same inputs produce the same outputs with no externally observable side effects beyond the declared return value. (§1.3)
24. A function may use `mut` bindings, indexed assignment, and `while`/`for` loops internally; these are invisible at the call site. (§1.3)
25. Any expression not involving a signal or external input is compile-time evaluable. (§1.3)
26. Records, enums, traits, nodes, and connections are nominal: identity is by name, not structure. (§1.3)
27. Tuples, closure types, and trait-constraint intersections are explicit structural exceptions to nominal typing. (§1.3)
28. Behavior abstraction uses nominal traits with explicit `satisfies`/`fulfill` declarations. (§1.3)
29. Trait coherence is enforced structurally via orphan rules. (§1.3)
30. Methods and free functions are interchangeable syntactic forms for the same operation: `f(x, y)` may equivalently be called as `x.f(y)`. (§1.3)
31. Operator application uses the `|>` token. (§1.3)
32. `>>` is bitwise right shift only, never operator application. (§1.3)
33. Bug-class failures trap and abort the process; there is no catch mechanism. (§1.3)
34. Recoverable conditions are expressed as `Option`/`Result` values with `?` propagation. (§1.3)
35. The failure track (bug vs recoverable) is chosen at the operation site, not retroactively. (§1.3)

## Source Form & Lexical Rules

36. Naming conventions: concrete primitive types (`i32`, `bool`) and built-in placeholder keywords (`numeric`, `integer`, `float`, `signed`, `unsigned`) are lowercase; trait and user-defined type names are PascalCase; functions, variables, and fields are snake_case. (§1.4)
37. Keywords are always lowercase with no capitalized form; this rule is normative and takes precedence over any conflicting grammar. (§1.4)
38. The declaration keywords are `node`, `connection`, `trait`, `type`, `fn`, `operator`, `effect`, `signal`, `attr`, `recurrent`, `derived`, `stream`, `sink`, `const`, `let`, `mut`, `repeat`. (§1.4)
39. The clause keywords are `parts`, `incoming`, `outgoing`, `expose`, `when`, `satisfies`, `fulfill`, `default`, `from`, `to`, `pairs`, `on`, `where`, `desired`, `observed`, `ring`, `gate`, `keyed`, `at`, `dynamic` (the supply-mode marker). (§1.4)
40. `pair` and `exposition` are reserved instance-field names; the instance fields `from`, `to`, `incoming`, `outgoing`, `parts` double as clause keywords. (§1.4)
41. The control-flow keywords are `if`, `else`, `match`, `for`, `in`, `while`, `break`, `continue`, `return`. (§1.4)
42. `here` and `module` are the scope-anchor namespace keywords. (§1.4)
43. `subject` is the instance-value keyword. (§1.4)
44. `as` is the naming/alias keyword, used for placement names, import aliases, and `repeat` view names. (§1.4)
45. `as` is not a cast operator; explicit conversion uses call syntax `T(value)`. (§1.4)
46. The operator-context keywords are `is`, `and`, `or`, `not`, and `weak` (the weak-reference operator). (§1.4)
47. The sole reserved type identifier is `Subject`, the subject-type alias usable in trait and `fulfill` type positions; it is a capitalized type alias, not a keyword. (§1.4)
48. Identifiers may contain `#` as a leading, infix, or terminating character, and `#` behaves like a letter in every position — normative, taking precedence over any conflicting grammar: `#default`, `audio#main`, `note#`. (§1.4)
49. The semicolon `;` is not a token; a `;` anywhere in Ductus source is a lex error. (§1.4)
50. Ductus delimits with newlines and indentation; there is no statement separator. (§1.4)
51. Every separated list (call arguments, generic parameters, tuple components, `parts:`/`incoming:`/`outgoing:` entries) uses the comma, the newline, or both. (§1.4)
52. A line comment begins with `//` and runs to end of line; there is no block-comment form; this rule is normative and takes precedence over any conflicting grammar. (§1.4)
53. "Compiler" means the static analysis phase, "runtime" means execution, and "codegen" is the boundary at which all types must be concrete. (§1.4)
54. An identifier begins with a Unicode letter, `_`, or `#`, continues with those plus Unicode digits, and may not begin with a digit: `audio#main` ✓, `2nd` ✗. (§1.4)
55. Indentation is significant; its unit is the ASCII space, and a tab in leading whitespace is a lex error. (§1.4)
56. Layout is suspended inside `(...)`/`[...]` and string literals, letting those span lines freely. (§1.4)
57. Blank lines and comment-only lines do not affect indentation. (§1.4)
58. A `trait`/`type`/`enum`/`node`/`connection` body is always an indented block, never written inline after the `:`. (§1.4)
59. A function body and `if`/`else` and `match` arms may be a single inline expression after the `:` or an indented block: `fn double(x: i32) -> i32: x * 2`. (§1.4)
60. Every keyword is reserved in every position and can never be an ordinary identifier; no keyword class is contextual. (§1.4)
61. A keyword in a field-like position (`from`, `to`, `parts`, `pair`, `exposition`, `desired`, `observed`) is the keyword used in that context with compiler-assigned meaning — like `this`/`super` in other languages — not a user-definable field. (§1.4)
62. The language's syntax is specified directly in this log and SPEC.md; there is no separate grammar document and no normative content is delegated to one. (§1.4)

## Modules, Packages & Visibility

63. `public` visibility exports a declaration across package boundaries to dependent packages. (§10.1)
64. `shared` visibility reaches any file within the declaring package. (§10.1)
65. `shared` is the default visibility level — no keyword is required for it; `public` and `private` are explicit keywords. (§10.1)
66. `private` visibility reaches only the declaring module — the folder containing the declaration. (§10.1)
67. Visibility levels are linearly ordered by permissiveness `private < shared < public`; a declaration's level determines the maximum reach of any reference to it. (§10.1)
68. A reference to a declaration from outside its visibility reach is a compile error at the reference site. (§10.1)
69. The unit of `private` is the module (folder), not the file; files in the same folder share scope regardless of visibility level. (§10.1)
70. A package is the unit of distribution with a single package root; the root folder is itself the root module, addressed in absolute paths via the `root` keyword. (§10.2)
71. Each subfolder of the package root is a distinct module addressed by its folder path: `<package_root>/audio/` is `root::audio`. (§10.2)
72. A folder is a module iff it directly contains `.duc` source files. (§10.2)
73. There is no module marker file, no in-file module declaration, and no module manifest; folder location alone determines module membership. (§10.2)
74. A folder without `.duc` files is a pure path-segment folder: it cannot be the target of a `use` or qualified path and has no declarations: `use root::audio::*` ✗. (§10.2)
75. Path-segment folders do not prevent subfolders from being modules; paths resolve through them: `use root::audio::effects::reverb::Reverb` ✓. (§10.2)
76. There is no special parent/child submodule relationship; every module folder is independent and references between modules are ordinary cross-module references. (§10.2)
77. Mixing code and non-code files in the same folder is permitted; the language imposes no convention beyond the `.duc`-presence rule. (§10.2)
78. Files inside the same folder share scope: each file sees all sibling declarations automatically, with no `use` statement or path qualification. (§10.2.1)
79. The unit of identity in the type system is the module; files inside it are purely organizational. (§10.2.1)
80. A `private` declaration is reachable from any file inside its module and from no other module of the same package nor any external package. (§10.2.2)
81. A `shared` declaration is reachable from any file in the same package; cross-module access requires a `use` statement or a path-qualified reference. (§10.2.2)
82. A `public` declaration is reachable within its package and additionally from any file in any package depending on the source package. (§10.2.2)
83. Within a single module all three visibility levels behave identically; visibility matters only for references outside the declaring module. (§10.2.2)
84. Absolute path bases are `root` (current package root module), a bare name matching a manifest-declared external dependency, and `std` (the standard library root). (§10.2.3)
85. The `std` path base is built-in and implicitly available to every package without manifest declaration: `std::vec::Vec`. (§10.2.3)
86. All `use` statements use absolute paths starting from a path base; there is no relative-path "current module" form for imports. (§10.2.3)
87. The `module::` qualifier inside node or connection bodies resolves only the enclosing module's own top-level declarations as a disambiguation anchor; it is not an import mechanism and cannot reach other modules. (§10.2.3)
88. Every visibility-bearing grammar position accepts `public`, `shared`, `private`, or absence, which denotes `shared`. (§10.3)
89. The older `pub` keyword is replaced throughout by the three-level model. (§10.3)
90. Visibility of methods within a trait declaration is uniform with the trait's visibility; there is no per-method visibility. (§10.3)
91. Free functions and `const` declarations carry a visibility specifier directly on the declaration: `private const SECRET_KEY: u64 = 0xDEADBEEF`. (§10.3)
92. `operator` declarations carry visibility specifiers under the same rules as functions. (§10.3)
93. Module-level `signal`, `derived`, and `recurrent` declarations accept visibility specifiers on the declaration line. (§10.3)
94. Node and connection type declarations carry a visibility specifier on the type declaration. (§10.3)
95. Any top-level instantiation (`Foo bar:`, `signal x = ...`, `let y = ...`) accepts a visibility specifier governing cross-module reachability of the instance name. (§10.3)
96. Visibility specifiers attach to any named declaration: any name introduced into a scope may carry a specifier governing its cross-scope reach. (§10.3)
97. A `use` statement imports a name from a different module into the current file's scope so the file can refer to it unqualified: `use root::audio::Synthesizer`. (§10.4)
98. `use` statements are required only for cross-module references; sibling files in the same module need none. (§10.4)
99. A `use` statement is per-file; there is no mechanism to share imports across sibling files. (§10.4)
100. `use` has no visibility modifier; it controls only how the current file refers to other names. (§10.4)
101. A name brought in via `use` does not become a declaration in the importing file; it remains the original declaration in the original module under a shorter local reference. (§10.4)
102. The imported declaration's visibility governs whether a `use` is permitted: importing a `private` declaration from a different module is a compile error. (§10.4)
103. Importing a `shared` declaration works only within its package; importing a `public` declaration works from any package depending on the source package. (§10.4)
104. A `use` targets the module path of the declaration's folder, never the file within it. (§10.4)
105. A `use` may rename the imported name with `as`: `use root::audio::Synthesizer as Synth`. (§10.4)
106. Selection lists on `use` paths use parentheses: `use root::ops::(add, sub, mul)`. (§10.4.1)
107. A glob import brings in every visible name from the source: `use root::variants::*`. (§10.4.1)
108. Two glob imports bringing colliding names into the same scope are a compile error at the `use` site that introduces the second collision. (§10.4.1)
109. A `use` selection-list item may be a multi-segment path, carry its own `as` alias, or be a `*` glob: `use root::audio::(synth::Oscillator, Filter as Filt, fx::*)`. (§10.4.1)
110. There is no re-exporting `use`; exposing a declaration under another path requires an explicit re-declaration such as a `public alias type` or a wrapper function. (§10.4.2)
111. `alias type` declarations accept visibility specifiers under the same rules as other declarations: `public alias type Synthesizer = root::audio::internal::Synthesizer`. (§10.4.2)
112. `use` may appear only at file scope; a `use` inside a function body, block, or other inner scope is a compile error. (§10.4.3)
113. A type or constructor name is not a first-class value; `let x = path::TypeName` is not a value binding. (§10.4.3)
114. A file-scope `use … as` introduces a local short name for an imported type: `use root::audio::Synthesizer as synth`. (§10.4.3)
115. The cross-module use-and-reference graph must be acyclic; any cycle, binary (A→B→A) or longer, is rejected at compile time. (§10.4.4)
116. The cycle-rejection error identifies the cycle's members. (§10.4.4)
117. Type-reference cycles between sibling files of one module are permitted; the module's files are compiled as a single unit. (§10.4.4)
118. Initializer-reference cycles between sibling files (top-level const values, signal initial values, module-scope attr defaults) are forbidden and rejected at compile time. (§10.4.4)
119. The acyclicity rule governs the static reference graph only and is distinct from runtime reactive-graph cycle rules. (§10.4.4)
120. Top-level initializers (`const`, `signal`, placement-time-evaluated node/connection portions) run in topological order of the cross-module reference graph: if module A references module B, B initializes first. (§10.4.5)
121. Within a module, files initialize topologically by cross-file initializer references: if file A's initializer references a name from file B, B's initializer runs first. (§10.4.5)
122. Among declarations in the same file, the textually earlier one initializes first. (§10.4.5)
123. The topological initialization order is computed at compile time and fixed; a program never observes initialization in any other order. (§10.4.5)
124. Records and newtypes carry an independent constructor visibility as a parenthesized modifier on the type visibility keyword: `public(private) type Email`. (§10.5)
125. When the inner constructor specifier is omitted, constructor visibility defaults to the type's visibility. (§10.5)
126. The inner constructor specifier may never be more permissive than the outer type visibility: `private(public)` is a compile error. (§10.5)
127. With a `(private)` constructor, the type name is usable across the type's scope but `TypeName(...)` construction is unreachable outside the constructor's scope — the language's mechanism for construction-time invariants. (§10.5.1)
128. Enum visibility applies uniformly to the enum type and all its variants; there is no per-variant visibility specifier. (§10.6)
129. Each record field declares its own visibility independently: `private password_hash: string`. (§10.7)
130. A field's visibility may never exceed the enclosing type's; a `public` field on a `private` type is a compile error. (§10.7)
131. Access to a field from outside its visibility scope is a compile error at the access site. (§10.7)
132. `fulfill` blocks have no separate visibility specifier. (§10.8)
133. An implementation's effective visibility is `min(trait_visibility, type_visibility)` under `private < shared < public`: it is callable wherever both the trait and the type are visible. (§10.8)
134. The orphan rule operates on package-of-declaration, not visibility: a `fulfill` is legal if the trait or the type is declared in the current package, regardless of either's visibility level. (§10.9)
135. A `private` trait or type still counts as in-package for orphan purposes; a `fulfill` of a private trait for a foreign type is valid, accessible only inside the declaring module. (§10.9)
136. A call `x.f()` resolves `f` against names visible in the current scope; visibility filters which names are reachable from the call site. (§10.10)
137. Every cross-module reference is explicit: the name is brought into scope via `use` or the call uses a path-qualified form like `root::module::function_name(args)`. (§10.10)
138. Trait-method calls follow the same name-resolution rule with the additional reach constraint that the implementation's effective visibility is the minimum of the trait's and the type's. (§10.10)

## Type System Core

139. Placeholders are compile-time-only types standing for not-yet-resolved concrete types; they exist solely during type checking and are eliminated before code generation. (§2.1)
140. A placeholder attaches to a value, not a binding; a binding is a transparent alias with no type independent of its value: `let x = 5` carries an integer placeholder. (§2.1.1)
141. A placeholder is resolved to a concrete type at each use site — a position where the value participates in an operation requiring a concrete type. (§2.1.2)
142. Use sites include explicit type annotations, arguments to concretely-typed parameters, operands paired with concretely-typed operands, assignments to concretely-typed fields, and returns from concretely-typed functions. (§2.1.2)
143. Each use site resolves a placeholder independently; the same value may resolve to different concrete types at different sites: `let a: i32 = x` and `let b: i64 = x` from `let x = 5`. (§2.1.2)
144. Each independent resolution verifies the compile-time-known value fits the resolved target type. (§2.1.2)
145. Placeholder resolution at a use site considers all information available at that site, including other operands' types in the same expression and the value-fits check. (§2.1.2)
146. A placeholder's integer/float kind tags it for defaulting but does not prevent resolution to a compatible type of a different kind when the value fits exactly: `let f: f64 = x` with `x` bound to `5`. (§2.1.2)
147. A binding whose right-hand side has a concrete type carries that concrete type with no placeholder: `let y = some_function()` is concretely typed. (§2.1.3)
148. A use site that cannot resolve its placeholder produces a compile error at the use site, not propagated back to the binding. (§2.1.4)
149. Unused bindings require no placeholder resolution and produce no placeholder error, though they may produce a separate unused-binding warning. (§2.1.4)
150. A value used at multiple use sites demanding mutually incompatible concrete types is surfaced as errors at the conflicting use sites. (§2.1.4)
151. No runtime value has a placeholder type and no machine representation corresponds to a placeholder. (§2.1.5)
152. The compiler infers types for variables, function parameters, function return types, and generic type parameters using a bidirectional inference algorithm. (§2.2)
153. Inference treats every omitted annotation as a fresh placeholder, generates constraints from expression structure, and solves them to concrete types or trait-constrained type variables. (§2.2.1)
154. Inference errors are reported at the sites where constraints cannot be satisfied. (§2.2.1)
155. Generic function bodies are typechecked at definition time, not deferred to call sites. (§2.2.2)
156. Constraints inferred from a generic body's operations attach to the function's signature, and call sites must satisfy them: `fn lerp(a, b, c): a + (b - a) * c` requires `+`, `-`, `*`. (§2.2.2)
157. A function with omitted parameter types is generic; each omitted parameter type desugars to a distinct fresh generic parameter: `fn make_pair(a, b)` becomes `fn make_pair[T0, T1](a: T0, b: T1)`. (§2.2.3)
158. Unification of synthesized type parameters is a result of body inference, never part of the desugaring. (§2.2.3)
159. Implicit and explicit generic forms produce the same code and the same semantics; the choice is stylistic. (§2.2.3)
160. Mixed signatures are permitted: some parameter types explicit, others omitted, the omitted ones receiving fresh parameters. (§2.2.3)
161. Body operations resolve to trait methods and the trait becomes the parameter's constraint: `+` resolves to `Add::add`, where `Add` denotes `Add[Subject]`. (§2.2.4)
162. The compiler may collapse inferred constraint sets to umbrella traits when unambiguous: `Add & Sub & Mul & Neg & Zero & One` may become `Numeric`. (§2.2.4)
163. Generic arguments — type and const-generic alike — are supplied with turbofish syntax `::[…]` or omitted and resolved by the compiler. (§2.2.5)
164. Positional generic arguments follow declaration order, and a prefix may be supplied with trailing arguments omitted: `resize::[2](stream)`. (§2.2.5)
165. The wildcard `_` in a turbofish is a positional hole resolved exactly like an omitted argument, used to skip an interior parameter: `resize::[2, _, _](stream)`. (§2.2.5)
166. Named generic arguments use `Name = value` in any order, omitting the rest: `resize::[K = 2](stream)`. (§2.2.5)
167. Named generic arguments use `=`, not the `:` of named value arguments, because inside a generic list `:` separates a parameter from its type. (§2.2.5)
168. A single turbofish list is either all-positional (with optional wildcards and trailing omission) or all-named, never mixed. (§2.2.5)
169. An omitted, wildcarded, or unnamed generic argument resolves in order: inferred from value arguments (and, for a parameter bare in the return type, from the expected type), else its declared default, else a compile error requiring explicit supply. (§2.2.5)
170. The `::` prefix on a type-argument list disambiguates generic application from indexing; without it `foo[T](args)` would be ambiguous. (§2.2.5)
171. `_` in a type-annotation position denotes a type the compiler infers from context, resolved as a placeholder at its use site: `let r: Result[i32, _] = compute()`. (§2.2.6)
172. The type wildcard is permitted in any type-expression position: generic arguments, tuple components, annotated return types, and trait-bound positions where inference can resolve the constraint. (§2.2.6)
173. An uninferable type wildcard produces an error reported at the wildcard's location, identifying the inference failure and the missing context. (§2.2.6)
174. The type wildcard `_` is distinct from the pattern wildcard `_`; the parser disambiguates by syntactic position. (§2.2.6)
175. Generic functions are realized via monomorphization: each unique combination of concrete type arguments produces a separate specialized function. (§2.3)
176. There is no type erasure, no boxing, and no dynamic dispatch for static generic calls. (§2.3)
177. Users pay no runtime cost for generic abstraction beyond the cost of the specialized operations themselves. (§2.3)
178. Each unique tuple of concrete type arguments at a call site produces a distinct instantiation, even if the machine code happens to be similar: `lerp(i32, i32, i32)` and `lerp(i32, i32, f64)` are separate instantiations. (§2.3.1)
179. Backend codegen may deduplicate identical machine code as an optimization; the deduplication is semantically invisible. (§2.3.1)
180. Monomorphization is per-call-site across module boundaries: calls from different modules with different concrete types produce separate instantiations in each calling module. (§2.3.2)
181. Generic function bodies must be available, in source or intermediate representation, to every module that calls them; generic definitions are not closed binary units. (§2.3.2)
182. Polymorphic recursion — a recursive call requiring a different type instantiation than its caller — is rejected at compile time. (§2.3.3)
183. Direct same-type recursion is permitted and reuses the same instantiation. (§2.3.3)
184. The polymorphic-recursion ban is permanent, structural to monomorphization; cases that need it use explicit dynamic dispatch via `dyn`. (§2.3.3)
185. Dead code elimination operates per-instantiation: a generic with no call sites produces no output, and only the called type combinations are emitted. (§2.3.4)
186. The semantic unit for codegen is the instantiation, not the generic. (§2.3.4)
187. Trait method calls in monomorphized code resolve to direct calls to the concrete implementation, with no vtable, indirection, or runtime dispatch cost. (§2.3.5)
188. Coherence guarantees exactly one `fulfill` block per (trait, type) pair within the module graph. (§2.3.5)
189. `dyn` trait objects are the only path through which trait method calls incur runtime indirection. (§2.3.5)
190. A const-generic parameter may declare a default expression referencing other generic parameters of the same declaration: `const N: usize = A + B`. (§2.3.6)
191. A const-generic default expression is evaluated in the const evaluation context at instantiation time. (§2.3.6)
192. A const-generic default expression must produce a value of the declared parameter type; otherwise it is a compile error. (§2.3.6)
193. A default expression may reference only generic parameters that precede it in the parameter list; forward references are compile errors. (§2.3.6)
194. An omitted const-generic parameter takes its evaluated default at the call site: `merge(stream_a, stream_b)` yields `N = A + B`. (§2.3.6)
195. An explicit turbofish value overrides a const-generic default, subject to type-correctness: `merge::[T = Event, N = 1024](stream_a, stream_b)`. (§2.3.6)
196. Type-parameter and const-generic defaults resolve in left-to-right declaration order, each with access to the resolved values of preceding parameters. (§2.3.6)
197. The dependency graph among defaults must be a DAG, enforced by the compiler during instantiation. (§2.3.6)
198. A multi-line generic parameter list permits a trailing comma after its final parameter: `const N: usize = A + B,` precedes the closing `]` of `operator merge[...]`. (§2.3.6)
199. Binary-size mitigations (backend deduplication, outlining, opt-in `dyn`) are implementation levers that do not change the language's semantic model. (§2.3.7)
200. Compile-time evaluation is a semantic feature the language relies on, not an optimization; expressions are evaluated at compile time whenever possible. (§2.4)
201. Literals, constructions and operations over compile-time-known operands, and calls to pure functions with compile-time-known arguments are compile-time known. (§2.4.1)
202. A binding of a compile-time-known expression is compile-time known. (§2.4.1)
203. In `for x in iterable:` with compile-time-known `iterable`, `x` is compile-time known on each iteration and the body is reproduced once per element. (§2.4.1)
204. A range `a..b` is compile-time known iff both bounds are. (§2.4.1)
205. An array literal `[e1, …, eK]` is compile-time known iff every element is. (§2.4.1)
206. The compiler determines compile-time-knowability automatically; users do not annotate it for `let` bindings. (§2.4.1)
207. `let` is the general, immutable binding form; the compiler determines compile-time-knowability of its RHS automatically. (§2.4.1.1)
208. A `let` bound to a non-reactive expression is compile-time known and may be tree-shaken, inlined, or otherwise optimized. (§2.4.1.1)
209. A `let` bound to a reactive or runtime expression participates in the reactive graph and exists at runtime: `let z = read_sensor()`. (§2.4.1.1)
210. `const` asserts a binding is compile-time-only; the compiler enforces the assertion and guarantees no runtime existence: `const TAU = 2.0 * PI`. (§2.4.1.1)
211. A `const` binding may carry an explicit type annotation: `const MAX_ITEMS: usize = 1024`. (§2.4.1.1)
212. A `const` RHS must not involve any signal, derived value, external input, or reactive expression; violation is a compile error at the declaration site identifying the reactivity source. (§2.4.1.2)
213. A `const` occupies no runtime memory location: references from non-`const` code inline the value, and an unreferenced `const` does not appear in compiled output. (§2.4.1.2)
214. A `const` of type `string` is a compile-time-assigned index into the string-pool seed: static program-lifetime bytes, no dynamic allocation, no refcount traffic on the read path. (§2.4.1.2)
215. A `const` has no address; operations requiring a runtime address (passing by reference, storing a pointer, FFI sharing) are compile errors. (§2.4.1.2)
216. A type is `const`-eligible if all its values are fully representable at compile time with no dynamic allocation and no mutable runtime state; static program-lifetime representation is permitted. (§2.4.1.3)
217. All primitive types are `const`-eligible: `i8`–`i128`, `u8`–`u128`, `isize`, `usize`, `f32`, `f64`, `bool`, `char`, `string`, `never`. (§2.4.1.3)
218. Fixed-size arrays, records, enums (including payload-carrying), tuples, and newtypes are `const`-eligible when all their element, field, payload, component, or wrapped types are. (§2.4.1.3)
219. Heap-allocated collections (`Vec`, `HashMap`), signal-bearing or reactive types, types containing function references or closures with captured runtime state, and `dyn` trait objects are not `const`-eligible. (§2.4.1.3)
220. `const`-eligibility is checked at the declaration site; a `const` whose RHS produces a non-eligible type is rejected with an error identifying the offending type. (§2.4.1.3)
221. `const` is permitted at module top level, inside function bodies, and inside type, trait, node, and connection bodies. (§2.4.1.4)
222. Type-associated constants are accessed via path syntax: `Vec3::ZERO`. (§2.4.1.4)
223. `const` declarations follow the same visibility model as other declarations, defaulting to `shared`: `public const TAU = ...`. (§2.4.1.4)
224. A non-reactive `let` is effectively eligible for `const`-style optimization but unasserted and unenforced; it may or may not exist at runtime depending on observation. (§2.4.1.5)
225. A `let` bound to an expression using `const` values is itself compile-time known; no promotion to `const` is needed for downstream `const` use. (§2.4.1.5)
226. Tooling may suggest converting an eligible `let` to `const`, but the compiler never requires it. (§2.4.1.5)
227. Expressions involving signals or any value derived from a signal are not compile-time known. (§2.4.2)
228. Expressions involving external I/O, host-boundary calls, or any construct whose value is determined by the runtime environment are not compile-time known. (§2.4.2)
229. Runtime/reactive dependency propagates transitively: a call with a reactive argument produces a reactive result, and a binding to a reactive expression is itself reactive. (§2.4.2)
230. A compile-time-known value is checked to fit each demanded type; a non-fitting value is a compile error: `let x: u8 = 300` ✗. (§2.4.3)
231. Value-fits-type checking applies to any compile-time-known value however computed, including through arithmetic and pure calls: `let v: u8 = y + 100` with `y = 200` ✗. (§2.4.3)
232. Integer values require exact fit in the target type. (§2.4.3)
233. Float literal values fit any float type, rounded to nearest representable. (§2.4.3)
234. Integer-to-float fit follows the lossless conversion rules: `let x: f32 = 5` ✓. (§2.4.3)
235. Compile-time-known values can serve as type-level arguments; a const-generic parameter accepts a value rather than a type: `fn fill(arr: i32[fib(10) + 1])`. (§2.4.4)
236. Anything compile-time evaluable can appear in a type position requiring a value. (§2.4.4)
237. A negative integer literal `-N` is parsed as a single signed literal token for type-checking: `let x: i8 = -5` checks `-5` against `i8`'s range, not unary minus applied to `5`. (§2.4.5)
238. Runtime unary-minus rules apply unchanged to runtime values; only literal parsing is special. (§2.4.5)
239. The compiler tracks reactivity provenance through expressions and can identify the signal(s) a reactive value depends on. (§2.4.6)
240. Supplying a reactive expression where compile-time evaluation is required (a `const` RHS, a const-generic argument, an array length) is a compile error whose diagnostic names the source signal. (§2.4.6)
241. A reactive value in an ordinarily-typed binding is not an error on reactivity grounds; reactive values are checked by ordinary type-compatibility, and value-fits checking applies only to compile-time-known values. (§2.4.6)
242. When a reactive value produces an error, the diagnostic mentions signal provenance as context, but the underlying error is a type mismatch, not a fit-check failure. (§2.4.6)
243. Limits on compile-time evaluation (recursion depth, step count, memory) are implementation concerns; the compiler enforces configurable limits and reports which limit was exceeded and at what call site. (§2.4.7)
244. Compile-time floating-point evaluation uses the target's IEEE 754 format exactly; compile-time and runtime float computation of the same expression must be bit-identical. (§2.4.7)
245. A const-generic parameter carries a compile-time-known value and is declared with the `const` keyword in a generic parameter list: `type Buffer[T, const N: usize = 1024]`. (§2.5)
246. A const-generic argument fills a const-generic parameter at a use site — supplied, inferred, or defaulted — and may be written as an expression. (§2.5)
247. A const-generic parameter's declared type must be an integer type or `bool`. (§2.5.1)
248. Floating-point const-generic parameters are forbidden: `type Spinner[const ANGLE: f64]` ✗. (§2.5.1)
249. The float ban applies to type-level parameters only; a regular `const` of float type is unaffected. (§2.5.1)
250. When every dependency is known, a const-generic argument may be any compile-time-known expression of the parameter's type, evaluated to a concrete value before type-checking proceeds: `stream ring[BUF * 2] events: Event = ...`. (§2.5.2)
251. Concrete const-generic arguments admit the full compile-time vocabulary — integer arithmetic including division and modulo, comparisons, conditionals, and calls to pure functions: `fn recent(h: i32[fib(10) + 1])`. (§2.5.2)
252. Regular `const`s are admitted in const-generic arguments on the same footing as literals. (§2.5.2)
253. A reactive value may never flow into a const-generic argument; doing so is a compile error. (§2.5.2)
254. A concrete const-generic argument is checked against the parameter's type and bounds by value-fits-type checking. (§2.5.2)
255. A const-generic expression that still mentions an unbound parameter is symbolic; only a restricted, decidable set of operations is admitted in symbolic position. (§2.5.3)
256. A symbolic integer expression must be a polynomial over in-scope const-generic parameters and compile-time-known integers, built from `+`, `-`, `*` only: `RingStream[T, N * 2]`. (§2.5.3)
257. A parameter may multiply another parameter in a symbolic polynomial: `N * K`. (§2.5.3)
258. A symbolic boolean expression must use only `and`, `or`, `not`, and `is`/`is not` between booleans, over in-scope `bool` parameters and compile-time-known booleans: `Mode[A and B, A or B]`. (§2.5.3)
259. Integer `/` and `%` are rejected in symbolic const-generic position and permitted the instant every operand is concrete: `RingStream[T, N / 2]` ✗ while `N` is unbound. (§2.5.3)
260. Integer comparisons (`<`, `>`, `<=`, `>=`, `is`, `is not` on integers) are rejected in symbolic const-generic position: `Mode[N > 0, true]` ✗. (§2.5.3)
261. `if`/`match` and calls to user-defined functions are rejected in symbolic const-generic position. (§2.5.3)
262. All floating-point operations are rejected in symbolic const-generic position. (§2.5.3)
263. Two const-generic types are the same type iff their type arguments are pairwise identical and each pair of const-generic arguments has the same canonical form. (§2.5.4)
264. An integer-polynomial argument canonicalizes to a sum of monomials with integer coefficients, in a fixed total order: `RingStream[T, N + 1]` and `RingStream[T, 1 + N]` are the same type. (§2.5.4)
265. Like terms in an integer-polynomial argument are combined in canonical form: `2 * N` and `N + N` share `2·N`. (§2.5.4)
266. A boolean argument canonicalizes to a normal form (identical truth tables): `Mode[A and B, …]` and `Mode[B and A, …]` are the same type. (§2.5.4)
267. A concrete const-generic argument canonicalizes to its value. (§2.5.4)
268. Const-generic positions are invariant: no subtyping or coercion between differing canonical forms even when one value is provably larger; `RingStream[T, N]` is never assignable to `RingStream[T, N + 1]`. (§2.5.4)
269. A const-generic parameter is inferred only from a position where it appears as a bare parameter: `RingStream[T, N]`, not `RingStream[T, N + 1]`. (§2.5.5)
270. A bare occurrence in the return type supports inference when the expected type is known from context, e.g. a `let` annotation. (§2.5.5)
271. The compiler evaluates const-generic expressions once their parameters are known; it never solves them. (§2.5.5)
272. A parameter appearing only inside an expression (never bare) cannot be inferred and must be supplied or carry a default: `resize::[2](small)` supplies `K` of `N * K`. (§2.5.5)
273. The compiler does not back-solve a parameter from a result type: from `-> RingStream[T, N * K]` and expected `RingStream[T, 16]` it will not deduce `K`. (§2.5.5)
274. A parameter appearing both bare and inside an expression is inferred from the bare occurrence; the expression occurrence is then checked for consistency against that value. (§2.5.5)
275. The result of const-generic inference is checked against the parameter's bounds, if any. (§2.5.5)
276. Type and const-generic parameters may interleave in any order in a generic parameter list: `resize[const K: usize, T, const N: usize]`. (§2.5.5)
277. A const-generic parameter may be bounded by a `where` clause on the declaration — the same clause carrying trait bounds — attached to the signature before the body's `:`. (§2.5.6)
278. A `where` clause holds a comma-separated predicate list and may mix trait bounds and value bounds: `where T: Numeric, N >= 1`. (§2.5.6)
279. A bound predicate is a boolean expression over the const-generic parameters and compile-time-known values using `<`, `<=`, `>`, `>=`, `is`, `is not` and `and`, `or`, `not`. (§2.5.6)
280. Bound-predicate comparison operands may use the full compile-time vocabulary — `/`, `%`, function calls, even on parameters — because predicates are only evaluated, never compared for type identity: `where N % 2 is 0`. (§2.5.6)
281. Bounds are checked at instantiation, once every parameter is concrete, using the compile-time evaluation rules including overflow rules. (§2.5.6)
282. A false bound predicate is a compile error at the instantiation site, naming the parameter values and the violated predicate: `window::[K = 32](buf16)` ✗ when `K <= N` and `N = 16`. (§2.5.6)
283. When a generic instantiates another generic with still-symbolic arguments, the bound check is deferred until those arguments become concrete during monomorphization. (§2.5.6)
284. The declaration site checks only that each predicate is a well-formed boolean over in-scope parameters, never that it holds. (§2.5.6)
285. The compiler does not prove bounds symbolically and does not use one declaration's bound to discharge another's obligation. (§2.5.6)
286. A body relying on a value relationship type-checks structurally regardless of any `where` clause; the obligation is discharged at instantiation alongside the declared bounds. (§2.5.6)
287. A matching `where` bound never changes whether a definition compiles; it documents the contract and yields a clearer, earlier instantiation error when violated. (§2.5.6)
288. A `where` clause on a `type` declaration sits between the generic parameter list and the body colon: `type EvenBuffer[T, const N: usize] where N % 2 is 0:`. (§2.5.6)
289. A `where` clause may stand alone on an indented line between a signature and its body colon: `where K <= N, N >= 1:`. (§2.5.6)
290. A const-generic default is used when the argument is neither supplied nor inferred and may itself be an expression referencing earlier parameters: `const N: usize = A + B`. (§2.5.7)
291. A const-generic default expression obeys the const-generic expression rules: symbolic where it references unbound parameters, concrete once they are known. (§2.5.7)

## Traits

292. A trait declares an interface — method signatures, associated types, and requirements — that types explicitly conform to and provide implementations for. (§3)
293. The trait system is nominal throughout: a type satisfies a trait only via an explicit conformance declaration, never by accidental structural match. (§3)
294. Two types with structurally identical method sets are distinct unless both have explicitly declared and implemented the same trait. (§3)
295. A trait is declared with the `trait` keyword; its body may declare method signatures, associated types, requirements on other traits, and defaults. (§3.1)
296. A trait declaration may be empty, yielding a marker trait: `trait Marker`. (§3.1)
297. A `satisfies` clause naming a marker trait is a declarative assertion about the type, checked by the compiler only for the trait's existence. (§3.1)
298. Trait methods are declared with `fn` inside the trait body, using the type-level identifier `Subject` for the implementing type: `fn eq(a: Subject, b: Subject) -> bool`. (§3.1.1)
299. `Subject` is bound during implementation: in `fulfill Eq for i32`, `Subject` resolves to `i32`. (§3.1.1)
300. Trait method signatures name their receiver parameter explicitly; there is no implicit receiver. (§3.1.1)
301. Trait methods may have any parameter list, including no `Subject` parameter at all (associated functions such as constructors). (§3.1.1)
302. The instance value `subject` is reserved for reactive context inside node and connection bodies and is not bound in trait method signatures. (§3.1.1)
303. A trait method signature may declare any parameter with the `own` keyword; the `own` declaration is part of the trait's contract and implementations cannot strengthen or weaken it. (§3.1.1)
304. An implementation must declare as `own` exactly the parameters the trait declares as `own` — no more, no fewer — enforced at the `fulfill` block. (§3.1.1)
305. Callers invoking a trait method follow the same `own`/`move` symmetry as free-function calls. (§3.1.1)
306. A trait may declare associated types with the `type` keyword in its body; each implementation binds the concrete value: `type Item`. (§3.1.2)
307. Associated types may be referenced in the trait's method signatures and in other associated-type expressions. (§3.1.2)
308. An associated type may declare a default value, which applies when an implementation does not bind it explicitly: `type Output = Subject`. (§3.1.2)
309. Bounds on associated types in generic constraints use where-clauses with `.` member-access notation: `where P.Item: Numeric`. (§3.1.2)
310. A where-clause may equate an associated type to a specific type, constraining it to be exactly that type: `where Iter.Source = Subject`. (§3.1.2)
311. Associated-type slots follow the borrow-default convention: under `type Name = T`, appearances of `Name` in the trait's method signatures are borrow-equivalent. (§3.1.2)
312. An implementation opts an associated type into owned semantics by writing `type Name = own T`; appearances of `Name` then carry owned semantics. (§3.1.2)
313. The associated-type borrow/own convention applies uniformly to user-defined and stdlib traits; no language-privileged path bypasses it. (§3.1.2)
314. A trait may provide a default implementation for any method by including a body in the trait declaration: `fn shout(value: Subject) -> string: greet(value).to_upper() + "!"`. (§3.1.3)
315. An implementation may override a trait's default method body by providing its own body in the `fulfill` block, or accept the default by omitting the method. (§3.1.3)
316. A trait declares super-trait requirements with the `requires` keyword: `requires Person`. (§3.1.4)
317. A type satisfies a trait only if it also satisfies every required trait; the compiler enforces this where `satisfies` is declared, rejecting it if a required trait is absent from the type's direct-or-transitive `satisfies` set. (§3.1.4)
318. A child trait may not redeclare a method already declared by any directly or transitively required trait; redeclaration is a compile error at the trait declaration site. (§3.1.4)
319. The no-redeclaration rule applies to signatures only; default bodies remain overridable at the fulfill site. (§3.1.4)
320. A child trait inherits required traits' signatures and default bodies and cannot re-provide a default; the trait hierarchy offers no alternative defaults at intermediate levels. (§3.1.4)
321. Every trait method — both signature and default body — has exactly one origin: the trait that originally declared it. (§3.1.4)
322. A single `requires` clause may name multiple traits comma-separated: `requires Numeric, IntDiv, Rem`. (§3.1.4)
323. A trait may declare a default concrete type used by the defaulting mechanism: `@default(i32) trait Integer`. (§3.1.5)
324. A trait's declared default fires when a use site is constrained solely by traits with declared defaults and nothing else pins the type. (§3.1.5)
325. A trait's declared default type must itself satisfy the trait; this is compiler-enforced. (§3.1.5)
326. The concrete syntax for declaring a trait default (annotation, dedicated keyword, or body clause) is unspecified; the normative decision is that defaults are declared on the trait, not built into the compiler. (§3.1.5)
327. Any use site that would require defaulting through a trait with no declared default is a compile error. (§3.1.5)
328. Trait-declared defaults are the only defaulting mechanism: there are no compiler-internal defaults, no module-level pragmas, and no use-site overrides via alternative defaulting paths. (§3.1.5)
329. When the default mechanism does not fire, the user resolves the type through explicit annotation only. (§3.1.5)
330. Traits may declare type parameters: `trait From[T]`. (§3.1.6)
331. Trait type parameters are part of the trait's identity: each trait instance (`From[i32]` vs `From[i64]`) is a distinct constraint and a distinct dispatch target. (§3.1.6)
332. A parent trait identity is the trait's declared name independent of generic arguments; several conformance and dispatch rules operate at parent-trait granularity. (§3.1.6)
333. A type may implement multiple trait instances of the same parent trait: `fulfill From[i32] for MyNumber` and `fulfill From[i64] for MyNumber` coexist. (§3.1.6)
334. A trait type parameter may declare a default, applied when the argument is omitted: `trait Add[Rhs = Subject]`. (§3.1.6)
335. A bare reference to a generic trait with defaulted type parameters resolves to the instance with all defaults applied, uniformly in `requires`, trait bounds, `satisfies`, and `fulfill` positions: `T: Add` is sugar for `T: Add[Subject]`. (§3.1.6.1)
336. In inferred constraints the compiler infers the all-defaults instance unless the operation's operand types force a cross-type form. (§3.1.6.1)
337. Bare-name default resolution is universal across all generic traits with default type parameters, not specific to operator traits. (§3.1.6.1)
338. A non-default trait instance is referenced by supplying explicit type arguments: `Add[i64]`. (§3.1.6.1)
339. Default type parameters are applied at name-resolution time; by code generation every reference is a fully-specified trait instance. (§3.1.6.1)
340. Traits may declare required members that implementing types must provide: `attr`, `const`, `signal`, `derived`, `recurrent`, and `stream` declarations, and — for connection types — `from` and `to` endpoints. (§3.1.7)
341. A trait that declares any required cell or endpoint is kind-specific: a node trait, or a connection trait when it declares endpoints. (§3.1.7)
342. A trait that declares only methods and associated types is kind-agnostic and may be satisfied by any kind. (§3.1.7)
343. A kind-specific trait is usable as the instantiation contract of a `Type[…]` value: the receiver of a `Type[ThatTrait]` may rely on exactly the members the trait declares. (§3.1.7)
344. Required cells in a trait body are written with the same syntax they use in a node or connection body, with optional defaults: `attr enabled: bool = true`. (§3.1.7)
345. A connection trait may declare required endpoints with `from: Type` and `to: Type`: `from: Driver`. (§3.1.7)
346. A required member declared without a default must be supplied by the implementing type with a concrete value: `const kind: string = "@action/log"`. (§3.1.7)
347. A required member declared with a default may be omitted by the implementing type (the trait's default applies) or overridden by the type's own declaration: `attr enabled: bool = false`. (§3.1.7)
348. A node or connection type satisfies a trait with required cells only if every required declaration is present (or defaulted) in the type's body with matching name and type. (§3.1.7)
349. A trait carrying `from`/`to` endpoints — and any `Type[…]` bounded by it — is a connection contract the compiler can check at placement. (§3.1.7)
350. A trait declaring any required cell or endpoint cannot be satisfied by a record, enum, newtype, or primitive; the compiler rejects the `satisfies`. (§3.1.7)
351. A trait declaring a `from` or `to` endpoint cannot be satisfied by a node; the compiler rejects the `satisfies`. (§3.1.7)
352. A trait with required cells but no endpoints is satisfiable by either nodes or connections. (§3.1.7)
353. An implementing connection's `from`/`to` endpoint types must satisfy (not equal) the trait's required endpoint types. (§3.1.7)
354. A type declares conformance to a trait via a `satisfies` clause in its body: `satisfies Display, Hash`. (§3.2)
355. Implementations live in `fulfill` blocks, possibly in different files from the type, subject to the orphan rule. (§3.2)
356. `satisfies Trait` in a type body without a corresponding `fulfill Trait for Type` block reachable through the module graph is a compile error. (§3.2)
357. `fulfill Trait for Type` without a corresponding `satisfies Trait` in the type's body is a compile error. (§3.2)
358. The satisfies/fulfill pairing requirement is waived for traits with no methods and traits whose methods all have default bodies; these are automatically satisfied when all their required traits are satisfied. (§3.2)
359. A type's `satisfies` set must not contain two distinct parent-trait identities whose method names overlap; the compiler rejects the declaration. (§3.2.1)
360. The method-name-conflict diagnostic identifies the conflicting method name and the two source traits. (§3.2.1)
361. Different generic instantiations of the same parent trait do not conflict under the method-name-overlap rule: `satisfies From[i32], From[i64]` ✓. (§3.2.1)
362. Methods from same-parent instantiations are disambiguated at call sites by argument type or expected return type: `MyNumber::convert(5_i32)` resolves to `From[i32]::convert`. (§3.2.1)
363. The universal identity `From[T] for T` and a user-written `From[U] for T` are instantiations of the same parent `From` and do not conflict. (§3.2.1)
364. Effective method-set computation begins from an empty set and, for each directly-satisfied trait, computes its closure under the `requires` relation — the trait plus every trait reachable through any chain of `requires` clauses. (§3.2.1)
365. The effective method set is the union of the method declarations of all traits in all closures, each entry a (method-name, parent-trait-identity) pair. (§3.2.1)
366. Two effective-set entries sharing a method name but originating from different parent-trait identities cause the type declaration to be rejected. (§3.2.1)
367. Effective-set entries with the same method name and the same parent-trait identity (different generic instantiations) do not collide; dispatch among them is resolved by inference. (§3.2.1)
368. A method reached through multiple `requires` paths but originating from the same trait-method declaration is not a conflict; the diamond case is permitted. (§3.2.1)
369. The canonical workaround for wanting two traits with conflicting method names and different parent identities is the newtype pattern: separate newtype wrappers each satisfying one trait. (§3.2.1)
370. A `fulfill` block delivers a trait's implementation for a specific type: `fulfill Display for Person:`. (§3.3)
371. A `fulfill` block may live in any module subject to the orphan rule, not necessarily the trait's or the type's module. (§3.3)
372. Multiple `fulfill` blocks for the same (trait, type) pair are rejected by coherence: exactly one implementation exists per pair, reachable through the module graph. (§3.3)
373. Functions defined inside a `fulfill Trait for Type` block live in a (Trait, Type)-scoped namespace, not the enclosing module's free-function namespace. (§3.3)
374. One module may contain multiple `fulfill` blocks for different (trait, type) pairs whose methods share a name; these do not conflict. (§3.3)
375. A module may define a free function and a same-named method in a `fulfill` block; the two do not conflict at the definition site. (§3.3)
376. Grammar: `FulfillItem := 'fulfill' TypeExpr 'for' TypeExpr WhereClause? FulfillBody`. (§3.3)
377. A `fulfill` body holds one or more items, each an optionally annotated, doc-commented function declaration or an associated-type binding: `AssocTypeBinding := 'type' Ident '=' TypeExpr`. (§3.3)
378. `fulfill` is a reserved keyword. (§3.3)
379. Inside `fulfill` blocks, `Subject` is permitted as a synonym for the implementing type; the compiler substitutes it during type checking, and both forms produce identical signatures and compiled code. (§3.3.1)
380. In `fulfill` blocks, the trait's associated-type names may be written either with the trait's name (`Output`) or with the concrete type bound to them. (§3.3.1)
381. The receiver parameter name in trait method implementations is the implementer's choice. (§3.3.1)
382. A `fulfill` block binds the trait's associated types via `type Name = Concrete`: `type Output = i32`. (§3.3.2)
383. An associated type with a default in the trait declaration may be omitted in the `fulfill` block; the default applies. (§3.3.2)
384. An associated type without a default must be bound explicitly in the `fulfill` block. (§3.3.2)
385. Omitting an abstract method (one with no default body) in a `fulfill` block is a compile error. (§3.3.3)
386. A `fulfill` block may be conditional on its type parameters via a where-clause: `fulfill Display for Result[T, E] where T: Display, E: Display`. (§3.3.4)
387. A conditional implementation is available only when the type parameters satisfy the where-clause's required traits. (§3.3.4)
388. The compiler verifies a conditional implementation's where-clause conditions at every call site that requires the implementation. (§3.3.4)
389. A `where` clause may mix trait bounds and const-generic value bounds in one comma-separated list: `where T: Numeric, N >= 1`. (§3.3.4)
390. Trait bounds are checked where the implementation or call is resolved; const-generic value bounds are checked at instantiation against concrete values. (§3.3.4)
391. A `where` clause may carry associated-type equality constraints of the form `Path.Assoc = ConcreteType`. (§3.3.4)
392. A trait declaring no methods and no associated types — only `requires` clauses — is a pure-requirement trait. (§3.3.5)
393. A trait is automatically satisfied when all of its `requires` are satisfied and every method it declares has a default body. (§3.3.5)
394. An auto-satisfied trait needs no `fulfill` block and no `satisfies` clause on the type; the clause may still be written for documentation. (§3.3.5)
395. Auto-satisfied traits remain nominal in constraint checking: a bound like `T: Numeric` is checked against the trait's name, verifying `T`'s satisfied set includes everything the trait requires. (§3.3.5)
396. Auto-satisfaction is the only point of structural satisfaction in the trait system; any trait with one or more abstract methods requires explicit `satisfies` plus `fulfill`. (§3.3.5)
397. `fulfill` blocks have no visibility specifier; an implementation is visible exactly where the trait and the type are jointly visible — the intersection of their visibility scopes. (§3.3.6)
398. Under uniform function call syntax, a trait method is callable in three equivalent forms: `person.display()`, `display(person)`, `Display::display(person)`. (§3.4)
399. The conventional call form requires the function name to be in scope: `display(person)` needs `display` in scope. (§3.4)
400. Operator application uses the `|>` token and is not a function-call form. (§3.4)
401. `>>` is bitwise right shift only. (§3.4)
402. The trait-path form `Trait::method` requires no `use` import; the path itself satisfies the in-scope requirement for dispatch. (§3.4)
403. The trait-path form does not override visibility: the trait must still be visible at the call site under module visibility rules. (§3.4)
404. Resolution of a bare-name call `f(x)` or method-call `x.f()` prioritizes trait implementations over free functions. (§3.4.1)
405. The trait-impl search collects a candidate `T::f` for every trait `T` reachable in the current scope (imported or accessible by path) such that the receiver's type fulfills `T` and `T` declares a method named `f`. (§3.4.1)
406. Candidates from the same parent trait collapse into one logical method parameterized by the trait's generic arguments. (§3.4.1)
407. Disambiguation among generic instantiations of one parent trait uses call-site context — argument types, expected return type, explicit turbofish — exactly as for generic function dispatch. (§3.4.1)
408. A matching trait-impl candidate wins over a same-named in-scope free function, which is shadowed at that call site and callable only via path qualification: `some_module::f(x)`. (§3.4.1)
409. Within a winning parent-trait candidate, failure to resolve a unique generic instantiation by inference is a compile error at the call site asking for explicit disambiguation. (§3.4.1)
410. With no trait-impl candidate, resolution falls back to the current scope's free-function namespace, matching the first parameter type directly or via implicit widening. (§3.4.1)
411. Multiple free functions in scope under one local name cannot occur: free-function names are unique per module, and cross-module conflicts are resolved at import time, not call time. (§3.4.1)
412. When nothing matches, the compiler reports an unknown-method error carrying the receiver's type, the unmatched name, and any near-matches identified. (§3.4.1)
413. Path-qualified type-side dispatch is available: `Type::f(x)` selects the method of the `fulfill` block whose for-type is `Type`. (§3.4.1)
414. Bare-name dispatch among multiple instantiations of one parent trait selects the instantiation pinned by the argument's concrete type, using the same inference algorithm as generic functions. (§3.4.1.1)
415. When inference cannot uniquely determine the instantiation (e.g., the argument has a generic-parameter type), the compiler reports a call-site ambiguity error. (§3.4.1.1)
416. Explicit instantiation selection uses turbofish on the trait identity: `From::[T]::convert(v)`. (§3.4.1.1)
417. A `fulfill T for X` block participates in dispatch on `x: X` only when `T` is in scope; otherwise the candidate is invisible and resolution proceeds as if it did not exist. (§3.4.1.1)
418. `x.f::[T]()` is not a disambiguation form: its turbofish specifies generic type arguments, not the receiving trait. (§3.4.1.1)
419. A `where` bound may constrain a concrete type, not only a type parameter: `fn build[T](v: T) -> MyNumber where MyNumber: From[T]`. (§3.4.1.1)
420. Trait method calls in monomorphized code resolve to direct function calls. (§3.4.2)
421. The free-function/trait-impl namespace distinction exists only for call-site name resolution; a resolved call compiles to a direct call to one function identified by its fully-qualified path. (§3.4.2)
422. Traits compose into hierarchies via `requires` clauses. (§3.6)
423. The standard library pervasively uses fine-grained capability traits combined into umbrella traits. (§3.6)
424. A fine-grained trait declares exactly one method or one closely related method group: `trait Neg: fn neg(value: Subject) -> Subject`. (§3.6.1)
425. Umbrella traits combine fine-grained traits via `requires` and introduce no new methods. (§3.6.1)
426. `Neg` is required by `Signed` and `Float`, not by `Numeric`. (§3.6.1)
427. Unsigned integer types satisfy `Numeric` and `Unsigned` but not `Neg`. (§3.6.1)
428. `Ord` and `Eq` are standalone traits; no numeric umbrella trait requires them. (§3.6.1)
429. Built-in numeric types implement both `Ord` and `Eq`. (§3.6.1)
430. A function needing both arithmetic and ordering combines bounds explicitly: `T: Numeric & Ord`. (§3.6.1)
431. When multiple defaulted traits constrain one use site, the most-specific trait's default wins. (§3.6.2)
432. Trait `A` is more specific than `B` iff `A` transitively requires `B`: a `Float`-bound site defaults to `f64`, not `i32`. (§3.6.2)
433. Incomparable defaulted traits at one use site make defaulting ambiguous: compile error demanding an explicit annotation. (§3.6.2)
434. Coherence holds: every (trait, type) pair has exactly one implementation reachable through the module graph. (§3.7)
435. Coherence is enforced structurally via the orphan rule. (§3.7)
436. `fulfill Trait for Type` is permitted in module M iff `Trait` or `Type` is defined in M. (§3.7.1)
437. A `fulfill` whose trait and type are both foreign to the module is rejected at compile time. (§3.7.1)
438. The orphan rule admits no exception for private or non-exported implementations. (§3.7.1)
439. A generic `fulfill` must place at least one concrete local type in its trait-or-type part: `fulfill ForeignTrait[LocalType] for ForeignType` ✓. (§3.7.2)
440. A generic `fulfill` with no concrete local type is rejected: `fulfill ForeignTrait[T] for ForeignType` ✗. (§3.7.2)
441. Language-provided implementations are not subject to the orphan rule. (§3.7.3)
442. The fine-grained operator traits (`Add`, `Sub`, `Mul`, ...) are pre-implemented for the built-in numeric types. (§3.7.3)
443. User code cannot redefine the built-in numeric operator-trait implementations. (§3.7.3)
444. `fulfill From[T] for U` automatically provides `Into[U] for T`. (§3.7.3)
445. `TryFrom` implementations automatically provide the corresponding `TryInto` implementation. (§3.7.3)
446. The `From`-to-`Into` and `TryFrom`-to-`TryInto` auto-derivations apply uniformly to user-defined and stdlib pairs. (§3.7.3)
447. The identity conversion `From[T] for T` exists structurally for every type and is exempt from the orphan rule. (§3.7.3)
448. A user `fulfill From[T] for T` with identical source and target is a compile error; the auto-provided identity is the sole impl. (§3.7.3)
449. Stdlib provides `Copy` for primitive numerics, `bool`, `char`, `string`, `duration`, `instant`, tuples of Copy components, and `Range[T]` when `T: Copy`. (§3.7.3)
450. The orphan rule bars user redefinition of the stdlib built-in `Copy` impls. (§3.7.3)
451. `Eq`, `Ord`, `Hash`, `Clone`, `Display`, `Debug` exist for a tuple type at any arity iff every component type has the trait. (§3.7.3)
452. Tuple standard-trait derivation is component-by-component, like record `@derive`, generated per instantiation. (§3.7.3)
453. Tuple structural impls are exempt from the orphan rule. (§3.7.3)
454. In an `Option`-returning function `?` returns `None` directly; no `From`-conversion impl is involved for the `Option` absence case. (§3.7.3)
455. Stdlib auto-impls exempt from the orphan rule (such as the tuple `From` impls) live outside the user-writable `fulfill` space and cannot be user-overridden. (§3.7.3)
456. Language-privileged impls use the same trait/fulfill mechanism as user impls; no language path bypasses the trait system. (§3.7.3)
457. The language defines a small, closed category of marker traits with no methods and no associated types, enforced with compiler privilege. (§3.7.4)
458. Language-defined marker traits are not redeclarable: user code cannot define a same-named trait to reuse the privileged behavior. (§3.7.4)
459. Marker traits divide into opt-in and intrinsic sub-kinds, distinguished by how a type acquires membership. (§3.7.4)
460. Opt-in marker membership is acquired explicitly, via a `satisfies` clause or `@derive`. (§3.7.4)
461. The opt-in markers are `Copy` and `Circularity`. (§3.7.4)
462. `Copy` flags a type whose values are implicitly duplicated at every use site. (§3.7.4)
463. `Circularity` flags a connection type allowed to participate in topology cycles; the static cycle rule checks this flag. (§3.7.4)
464. Intrinsic marker membership is automatic and universal for its declaration kind, with no `satisfies` clause. (§3.7.4)
465. Intrinsic markers are not `@derive`-eligible: membership is automatic at declaration, leaving nothing to opt into. (§3.7.4)
466. Intrinsic marker membership cannot be opted out of. (§3.7.4)
467. The intrinsic markers are `Node` (every `node` type), `Connection` (every `connection` type), and `Effect` (every `effect` type). (§3.7.4)
468. Intrinsic markers work as generic bounds, in `&` composition, and as super-trait requirements: `[T: Node]`. (§3.7.4)
469. A node/connection/effect type in value position is carried by the `Type[...]` meta-type. (§3.7.4)
470. A node/connection/effect instance referenced through a marker bound is a borrow under ordinary borrow semantics. (§3.7.4)
471. Any language-defined marker trait may serve as a generic bound or `where`-clause constraint: `fn duplicate[T: Copy](value: T) -> (T, T)`. (§3.7.4)
472. `Drop` is not a marker trait: it carries the method `fn drop`. (§3.7.4)
473. User-defined empty traits are valid nominal tags but receive no compiler privilege beyond the usual `satisfies` check. (§3.7.4)
474. Adding a marker trait to either sub-kind is a language-level change; the category is not user-extensible. (§3.7.4)
475. The newtype pattern is the sanctioned orphan-rule workaround: wrap the foreign type locally, then `fulfill SomeForeignTrait for MyVec[T]`. (§3.7.5)
476. `@derive` on a type structurally generates the `fulfill` blocks for the named traits. (§3.8)
477. The structural-derivation traits are `Eq`, `Ord`, `Hash`, `Clone`, `Display`, and `Debug`. (§3.8.1)
478. Every opt-in language-defined marker trait is `@derive`-eligible. (§3.8.1)
479. The intrinsic markers `Node`, `Connection`, and `Effect` are not `@derive`-eligible. (§3.8.1)
480. `@derive(Copy)` performs the structural Copy-eligibility check — every field's type must be `Copy` — and generates no method body. (§3.8.1)
481. `@derive(Circularity)` is a bare opt-in declaration: no structural check, no method body. (§3.8.1)
482. Users cannot register new traits for `@derive`; the derivable set is fixed by the language. (§3.8.1)
483. Any trait outside the derivable set is implemented with a manual `fulfill` block. (§3.8.1)
484. `@derive(Eq)` on a record compares fields pairwise via each field type's own `Eq`. (§3.8.2)
485. `@derive(Ord)` on a record yields lexicographic ordering by field declaration order. (§3.8.2)
486. `@derive(Hash)` on a record combines each field's hash. (§3.8.2)
487. `@derive(Clone)` on a record structurally copies each field. (§3.8.2)
488. `@derive(Display)` on a record yields a default record-formatted string whose exact format is compiler-defined. (§3.8.2)
489. `@derive(Debug)` on a record yields a structural debug format. (§3.8.2)
490. `@derive(Eq)` on an enum compares variant tags, then payload fields pairwise on matching tags. (§3.8.2)
491. `@derive(Ord)` on an enum orders by variant declaration order, breaking ties by payload comparison. (§3.8.2)
492. The remaining enum derivations follow the same variant-by-variant structural pattern. (§3.8.2)
493. Newtype `@derive` may delegate to the underlying type or operate structurally, per the newtype's shape (§6.3 governs). (§3.8.2)
494. Derivation requires every field or payload type to satisfy the derived trait: `@derive(Eq)` on `type Foo: x: SomeType` needs `SomeType: Eq`. (§3.8.2)
495. A component type lacking the derived trait fails derivation with a compile error naming the offending component. (§3.8.2)
496. A manual `fulfill` block for a derived trait takes precedence, suppressing the derived impl for that (trait, type) pair. (§3.8.3)
497. After `@literal_suffix` registration the lexer recognizes `<NumericLiteral><suffix>` as a single token. (§3.9)
498. A suffixed literal resolves to a call of the registered constructor: `440hz` resolves to `from_hz(440)`. (§3.9)
499. The annotation form is `@literal_suffix("<suffix>", <constructor>)`. (§3.9.1)
500. A suffix is one or more identifier-continue characters (letters, digits, underscores, Unicode identifier characters): `μs`, `years_2k`. (§3.9.1)
501. The constructor argument is the unqualified name of an in-scope function or trait method. (§3.9.1)
502. Multiple `@literal_suffix` annotations may decorate one type, each registering a distinct suffix. (§3.9.1)
503. Duplicate (suffix, target-type) registrations within a scope are a compile error. (§3.9.1)
504. A suffix must resolve to exactly one (suffix, target-type) pair at any use site. (§3.9.1)
505. Two modules registering the same suffix for different types make any use site importing both a compile error. (§3.9.1)
506. There is no qualified-suffix disambiguation syntax in v1. (§3.9.1)
507. Reserved suffixes — every built-in numeric type name (`i8`, `i16`, `i32`, `i64`, `i128`, `u8`, `u16`, `u32`, `u64`, `u128`, `isize`, `usize`, `f32`, `f64`) and the built-in `duration` suffixes — cannot be registered; doing so is a compile error. (§3.9.1)
508. A suffix constructor takes exactly one parameter, of a concrete numeric type or bounded by `Numeric` or a sub-trait. (§3.9.2)
509. A suffix constructor returns the annotated type. (§3.9.2)
510. A suffix constructor must be pure: no side effects, no reactive cell reads. (§3.9.2)
511. A generic `Numeric`-bound constructor lets one registration serve both integer and float literals: `fn from_hz[N: Numeric](n: N) -> Frequency`. (§3.9.2)
512. Each (suffix, target-type) pair has exactly one registered constructor. (§3.9.2)
513. Registering multiple non-generic constructors for one (suffix, target-type) pair is a compile error. (§3.9.2)
514. The type checker resolves a suffix among registrations visible at the site; ambiguity or no match is a compile error. (§3.9.3)
515. The literal value is passed to the constructor; the call's return value is the literal expression's value. (§3.9.3)
516. Suffix resolution is entirely compile-time; no runtime dispatch is involved. (§3.9.3)
517. The built-in `duration` suffixes are `ns`, `us`, `μs`, `ms`, `s`, `min`, `h`, `d`. (§3.9.4)
518. Built-in duration suffixes are reserved and may not be re-registered for another type in the same scope. (§3.9.4)
519. `@literal_suffix` registrations follow normal name-visibility rules: visible in the defining module and to importers. (§3.9.5)
520. The built-in `duration` suffixes are globally visible. (§3.9.5)

## Calls & Argument Forms

521. Every call site chooses positional or named argument form per call, not per callee. (§3.5)
522. Named form pairs each argument with its parameter name as `name: value`: `clamp(value: temperature, lower: 0, upper: 100)`. (§3.5.1)
523. In named form argument order does not matter; the compiler matches arguments by parameter name. (§3.5.1)
524. In positional form arguments must appear in parameter declaration order. (§3.5.1)
525. A no-argument call is trivially both forms: `person.display()`. (§3.5.1)
526. Mixing positional and named arguments in one call is a compile error: `add(3, right: 4)` ✗. (§3.5.2)
527. The no-mixing rule applies to every invocation: free functions, trait methods, variant constructors, and all other calls. (§3.5.2)
528. The mixed-form diagnostic is reported at the call site and identifies which argument breaks the pattern. (§3.5.2)
529. Records are always constructed with named arguments; positional record construction is a compile error. (§3.5.3)
530. Tuples are always constructed positionally; named tuple construction is a compile error. (§3.5.3)
531. Newtypes are always constructed positionally with exactly one argument, the underlying value. (§3.5.3)
532. Free functions and trait methods accept either argument form at every call site. (§3.5.3)
533. Positionally-declared enum variants accept only positional argument form: `Some(T)`. (§3.5.3)
534. Named-declared enum variants accept both argument forms per call: `Rectangle(width: f64, height: f64)`. (§3.5.3)
535. Defaulted parameters may appear in any position, including before non-defaulted ones: `fn render(scale: f32 = 1.0, content: string)`. (§3.5.4)
536. In named form, defaulted parameters may be omitted and take their default: `greet(name: "Alice", suffix: "?")`. (§3.5.4)
537. In named form, non-defaulted parameters must still be supplied. (§3.5.4)
538. Positional form cannot skip a mid-list defaulted parameter; skipping requires named form: `render("hello")` ✗. (§3.5.4)
539. The defaulted-before-non-defaulted permission is uniform across functions, operators, and constructor invocations. (§3.5.4)
540. A positional call may omit trailing defaulted parameters, supplying a prefix in declaration order: `greet("Alice", "Hi")` leaves the third parameter at its default. (§3.5.4)
541. A method call `x.f(args)` always passes the receiver positionally; the argument-form rule governs only `args`. (§3.5.5)
542. The receiver's ownership follows the method signature like any other parameter, borrow-equivalent by default. (§3.5.5)
543. A receiver is consumed only when the method's first parameter is `own` and the caller writes `(move x).method(args)`. (§3.5.5)
544. Dot-call adds no implicit consume rule: `x.foo()` then `x.bar()` behaves like `foo(x)` then `bar(x)`. (§3.5.5)
545. The record-update `with` expression takes named-form field overrides: `p1 with name: "new", age: 30`. (§3.5.6)
546. There is no positional `with` form. (§3.5.6)
547. Variant patterns may be positional or named, parallel to variant construction. (§3.5.7)
548. Mixing positional and named bindings within one pattern is a compile error. (§3.5.7)
549. Record patterns are always named. (§3.5.7)
550. Tuple patterns are always positional. (§3.5.7)
551. A record pattern is exhaustive — it must bind every field; an unneeded field is bound to `_` (`field: _`), and there is no rest or ellipsis token. (§3.5.7)

## Numerics

552. The built-in numeric primitive type set is fixed at fourteen types. (§4.1)
553. The signed integer types are `i8`, `i16`, `i32`, `i64`, `i128`, and `isize`. (§4.1)
554. The unsigned integer types are `u8`, `u16`, `u32`, `u64`, `u128`, and `usize`. (§4.1)
555. The floating-point types are `f32` (IEEE 754 single) and `f64` (IEEE 754 double). (§4.1)
556. `i128` and `u128` are first-class primitive types, not library types. (§4.1)
557. `isize` and `usize` are distinct platform-pointer-sized integer types. (§4.1)
558. `isize` is the array-length and index type. (§4.1)
559. `usize` is designated for FFI/ABI interop, byte-count contexts, and low-level memory layout. (§4.1)
560. `f16` and `f128` are not included in v1. (§4.1)
561. The stdlib defines `alias type byte = u8`, `short = i16`, `int = i32`, `long = i64`, `double = f64`. (§4.2)
562. `alias type` aliases are fully transparent — shared identity, no new trait impls, no conversion cost: `int` is `i32`. (§4.2)
563. No `float` alias for `f32` is provided; users write `f32` directly. (§4.2)
564. No stdlib alias is provided for `i128`, `u128`, `isize`, `usize`, `i8`, `u16`, `u32`, or `u64`. (§4.2)
565. The explicit-width names are the canonical primary identifiers and appear unaltered throughout the stdlib and documentation. (§4.2)
566. Users may define their own `alias type` aliases anywhere; the built-in aliases have no privileged status. (§4.2)
567. Numeric literals comprise integer literals (decimal, `0x`, `0o`, `0b`) and float literals (decimal point, exponent, or float suffix), specified in §4.3.1–§4.3.2. (§4.3)
568. Integer literals are written in decimal, hexadecimal (`0x`), octal (`0o`), or binary (`0b`): `0b1010_1100`. (§4.3.1)
569. Underscores are permitted between digits of a numeric literal as visual separators: `1_000_000`. (§4.3.1)
570. An integer literal may carry an explicit type suffix separated by an underscore: `255_u8`. (§4.3.1)
571. A numeric type suffix forces the literal's type, bypassing both the placeholder mechanism and the trait-level default. (§4.3.1)
572. A suffix naming a type the value does not fit is a compile error: `300_u8` ✗. (§4.3.1)
573. An unsuffixed integer literal carries the integer placeholder and resolves at the use site per §2.1, cross-kind permitted when the value fits exactly. (§4.3.1)
574. Leading zeros are permitted in a decimal literal and carry no octal meaning: `007` is `7`. (§4.3.1)
575. A float literal requires at least one of: a decimal point with digits, an exponent, or a float suffix: `1e6`. (§4.3.2)
576. A digit is required on each side of the decimal point; leading-dot forms are rejected: `.5` ✗. (§4.3.2)
577. Float literals may carry `_f32`/`_f64` type suffixes: `3.14_f32`. (§4.3.2)
578. An unsuffixed float literal carries the float placeholder, defaulting to `f64` per §3.1.5. (§4.3.2)
579. A float exponent is written `e` or `E` with an optional sign: `2.5e-3`. (§4.3.2)
580. A float suffix attaches only to decimal literals; a based literal absorbs valid suffix characters into its digit run, so `0x1_f32` is the integer `0x1F32`. (§4.3.2)
581. A numeric literal directly followed by an identifier (no underscore) lexes as one suffixed-literal token: `100ms`. (§4.3.3)
582. The type checker resolves an identifier suffix against the built-in suffixes and user-registered suffixes. (§4.3.3)
583. The built-in duration suffixes are `ns`, `us`, `μs`, `ms`, `s`, `min`, `h`, `d`, accepted on integer and float literals: `1.5s`. (§4.3.3)
584. User-defined literal suffixes registered via `@literal_suffix` resolve through the registered constructor function. (§4.3.3)
585. The underscore distinguishes suffix kinds: `5_i32` is a forced numeric type; `5ms` is a suffixed literal. (§4.3.3)
586. The `duration` suffixes are the language's only built-in identifier-suffix forms: `100ms`. (§4.3.3)
587. `true` and `false` are the two values of `bool`. (§4.3.4)
588. `bool` is not numeric and does not participate in the numeric trait hierarchy. (§4.3.4)
589. Character literals produce `char` (32-bit Unicode scalar value), which is not numeric: `'a'`. (§4.3.4)
590. Byte literals produce fully numeric `u8` values: `b'a'`. (§4.3.4)
591. Each numeric operator corresponds to one or more trait methods in the §4.9 trait hierarchy. (§4.4)
592. Binary `+`, `-`, `*`, `%` constrain operands to `Add`, `Sub`, `Mul`, `Rem` and produce the trait's associated `Output` type. (§4.4.1)
593. Unary `-` constrains its operand to `Neg` and produces the operand's own type. (§4.4.1)
594. The `/` operator accepts `Numeric` operands and always produces a `Float` result, even on uniformly-integer operands like `10_i32 / 5_i32`: `5 / 2` is `2.5`, not `2`. (§4.4.1.1)
595. The always-float rule governs `/` on numeric operands only; non-numeric primitives with their own `/` (e.g. `duration`) are exempt. (§4.4.1.1)
596. `/` promotion is a language-level rule at the operator: operands are verified `Numeric`, implicitly widened to float per §4.5, then `Div::div` dispatches on the float type. (§4.4.1.1)
597. `i8`/`u8`/`i16`/`u16` operands to `/` widen to `f32` when the other operand is `f32`, otherwise to `f64`. (§4.4.1.1)
598. `i32`/`u32` operands to `/` widen to `f64`, never `f32`. (§4.4.1.1)
599. `i64`/`u64` operands to `/` widen to `f64` under the §4.5.4 pragmatic exception: `5_i64 / 2_i64` is `2.5_f64`. (§4.4.1.1)
600. `/` on `i128`/`u128` operands is rejected; an explicit cast to float is required first. (§4.4.1.1)
601. When both `/` operands are integers and no context pins a float type, the result is a `Float`-placeholder value defaulting to `f64`. (§4.4.1.1)
602. `/` is the only operator that performs implicit kind-crossing promotion on uniformly-integer operands. (§4.4.1.1)
603. With mixed width and kind, the integer widens to the smallest float covering its range, then floats unify upward: `i64 + f32` yields `f64`. (§4.4.1.1)
604. `\` is the integer-division operator, defined on `Integer` operands with an `Integer` result via `IntDiv`: `5 \ 2` is `2`. (§4.4.1.2)
605. `\` applied to `Float` operands is a type error. (§4.4.1.2)
606. Float-input integer-output division is obtained only by explicit conversion via `T(value)` or `From`/`Into`. (§4.4.1.2)
607. The `\` token is reused: integer division at value level, escape character inside string/character literals; position disambiguates. (§4.4.1.2)
608. `%` accepts integer and float operands and produces a result of the operands' kind. (§4.4.1.2)
609. Unary `-` is defined on signed integers and floats only; applying it to an unsigned value is a compile-time type error. (§4.4.1.2)
610. Negative literals are exempt from the unary-minus-on-unsigned rule: `-N` parses as a single signed literal per §2.4.5. (§4.4.1.2)
611. `\` truncates toward zero and `%` takes the dividend's sign: `-5 \ 2` is `-2`, `-5 % 2` is `-1`. (§4.4.1.2)
612. Bitwise operators are integer-only; applying them to float values is a type error. (§4.4.2)
613. `&`, `|`, `^`, and unary `~` dispatch via `BitAnd`, `BitOr`, `BitXor`, `BitNot` and produce the operands' same integer type. (§4.4.2)
614. `<<` and `>>` dispatch via `Shl` and `Shr` and produce the same type as the left operand. (§4.4.2)
615. The language has no bit-reinterpret operator. (§4.4.2)
616. Float bit access uses the stdlib `to_bits`/`from_bits` pair: `f32` ↔ `u32`, `f64` ↔ `u64`. (§4.4.2)
617. `to_bits`/`from_bits` are ordinary stdlib behaviors over the `raw_read`/`raw_write` intrinsics. (§4.4.2)
618. A `to_bits`/`from_bits` round-trip lowers to a zero-cost bitcast. (§4.4.2)
619. `to_bits` is a bit reinterpret, not a value conversion: `(1.0_f32).to_bits()` is `1065353216`. (§4.4.2)
620. `&` and `|` carry type-level meanings elsewhere; inside value-level expressions they are bitwise operators, selected by positional context. (§4.4.2)
621. Bitwise `|` and operator-application `|>` are distinct tokens; `|>` is the loosest-binding operator while bitwise operators bind tighter than the logical operators, per the §4.4.7 precedence table. (§4.4.2)
622. `>>` shifts arithmetically (sign-extending) on signed types and logically (zero-extending) on unsigned types, dispatched via the type's `Shr` impl. (§4.4.2)
623. No `>>>` operator exists; `>>` has no other value-level meaning. (§4.4.2)
624. Operator precedence, loosest to tightest: `|>`; `or`; `and`; `not`; bitwise `|`/`^`/`&`; `..`; comparison; shifts `<<`/`>>`; additive; multiplicative; prefix `-`/`~`/`weak`; postfix `?`/`!`/`.`/`[]`/`()`; `::`. (§4.4.7)
625. Bitwise operators bind tighter than the logical operators but looser than comparison (the C convention): `a & b is c` parses as `a & (b is c)`. (§4.4.7)
626. The range operator `..` binds looser than arithmetic: `0..n + 1` parses as `0..(n + 1)`. (§4.4.7)
627. `not` binds looser than comparison and negates the whole comparison: `not a is b` parses as `not (a is b)`. (§4.4.7)
628. Binary operators are left-associative, comparison and `..` are non-associative, and prefix operators are right-associative. (§4.4.7)
629. Each arithmetic policy variant — wrapping `…%`, saturating `…|`, checked `…?` — binds at its base operator's tier (additive, multiplicative, or prefix negation). (§4.4.7)
630. Comparison operators `<`, `<=`, `>`, `>=` produce `bool` and are defined on both integer and float kinds. (§4.4.3)
631. Mixed-kind comparisons widen per §4.5 before comparing. (§4.4.3)
632. Float comparison follows IEEE 754 NaN semantics: `NaN < x` is `false`. (§4.4.3)
633. Comparison chaining is rejected by the type system though the grammar admits it: `a < b < c` ✗. (§4.4.3)
634. Equality operators are the keyword forms `is` and `is not`, constraining operands to `Eq` and producing `bool`. (§4.4.4)
635. Symbolic `==` and `!=` are not equality operators; they are reserved against future use. (§4.4.4)
636. Mixed-kind equality widens per §4.5 before comparing. (§4.4.4)
637. Float equality via `is`/`is not` is permitted despite IEEE 754 hazards: `0.1 + 0.2 is not 0.3`. (§4.4.4)
638. Approximate float comparison is delegated to the stdlib, e.g. `approx_eq(a, b, epsilon)`. (§4.4.4)
639. In an expression mixing integer and float operands, the integer widens implicitly to the float type before the operation. (§4.4.5)
640. During generic-body inference per §2.2.2, each operator implies a fixed trait constraint on its operands and result per a normative mapping table. (§4.4.6)
641. The shift right operand may be any unsigned integer type narrower than or equal to `u32` (implicitly widened); other types require an explicit cast. (§4.4.6)
642. Wrapping and saturating operator forms constrain operands to the corresponding `Wrapping...`/`Saturating...` traits and produce the operands' type. (§4.4.6)
643. Checked operator forms constrain operands to the corresponding `Checked...` traits and produce `Option[T]`. (§4.4.6)
644. A bare trait name in an operator constraint denotes the default-parameter instance per §3.1.6: `Add` means `Add[Subject]`. (§4.4.6)
645. For operands of different types, an in-scope cross-type instance (`Add[T2] for T1`) may be inferred; otherwise the operand types must unify via §4.5 widening. (§4.4.6)
646. Inference attaches the union of operator-implied constraints across the body to the generic signature; call sites must satisfy it. (§4.4.6)
647. Umbrella traits may substitute for fine-grained constraint sets in reported signatures only when unambiguous; the underlying constraints remain fine-grained. (§4.4.6)
648. Implicit widening fires only when the conversion is provably lossless (one pragmatic exception, §4.5.4); all other conversions require explicit forms. (§4.5)
649. Same-signedness integer widening is implicit: `i8` widens to `i16`/`i32`/`i64`/`i128`/`isize`. (§4.5.1)
650. Unsigned-to-wider-signed widening is implicit (always representable): `u8` → `i16`. (§4.5.1)
651. Same-width signed/unsigned crossing requires an explicit cast: `i32` ↔ `u32`. (§4.5.1)
652. Signed-to-wider-unsigned conversion is never implicit: `i8` → `u16` ✗. (§4.5.1)
653. Integer narrowing is never implicit. (§4.5.1)
654. Integer-to-float widening is implicit exactly when the integer's full range fits the float's mantissa: widths ≤ 16 bits into `f32`, ≤ 32 bits into `f64`. (§4.5.2)
655. `i128`/`u128` never widen implicitly to any float type. (§4.5.2)
656. `f32` → `f64` widening is implicit. (§4.5.3)
657. `f64` → `f32` conversion requires an explicit cast. (§4.5.3)
658. `i64`/`u64` → `f64` is permitted as implicit widening despite possible loss of low-order bits above 2⁵³. (§4.5.4)
659. The `i64`/`u64` → `f64` case is the only deviation from strict lossless implicit widening. (§4.5.4)
660. Conversions outside the implicit-widening tables use explicit `T(value)` or, when fallible, `TryFrom`/`TryInto` returning `Result`. (§4.5.5)
661. Float-to-integer conversion is never implicit, in any direction. (§4.5.5)
662. Cross-type conversions involving user-defined types go through `From`/`Into`. (§4.5.5)
663. Mixed-kind `+`, `-`, `*`, `%` widen operands to a common type per §4.5, then dispatch the trait method on that type. (§4.5.6)
664. Every arithmetic operation has four variants — default trapping, wrapping, saturating, and checked. (§4.6)
665. Default arithmetic operators trap on overflow at runtime in all build modes; no debug-traps/release-wraps distinction exists. (§4.6.1)
666. The overflow-trap diagnostic identifies the operation, the operand values, and the source location. (§4.6.1)
667. Overflow traps cannot be caught as values. (§4.6.1)
668. Wrapping operators `+%`, `-%`, `*%`, `\%`, `%%`, and unary `-%` perform modular two's-complement arithmetic: `255_u8 +% 1 is 0_u8`. (§4.6.2)
669. `\%` wraps the sole integer-division overflow case, signed minimum divided by −1: `(-128_i8) \% (-1_i8) is -128_i8`. (§4.6.2)
670. Unary wrapping negation of a signed type's minimum yields the minimum itself without trapping (`-128_i8` negates to `-128_i8`). (§4.6.2)
671. `%%` (wrapping remainder) is defined for completeness. (§4.6.2)
672. No `/%` operator exists; `/` always produces `Float`, whose overflow follows IEEE 754. (§4.6.2)
673. `\%` defines no division-by-zero result; a zero divisor traps. (§4.6.2)
674. Saturating operators `+|`, `-|`, `*|`, `\|`, `%|`, and unary `-|` clamp to the destination type's range bounds on overflow: `255_u8 +| 1 is 255_u8`. (§4.6.3)
675. `\|` clamps signed minimum divided by −1 to the type's maximum: `(-128_i8) \| (-1_i8) is 127_i8`. (§4.6.3)
676. Unary saturating negation of a signed type's minimum yields the type's maximum (`-128_i8` negates to `127_i8`). (§4.6.3)
677. `%|` (saturating remainder) is defined for completeness. (§4.6.3)
678. No `/|` operator exists. (§4.6.3)
679. Saturating division by zero is not defined; the recoverable form is `\?`. (§4.6.3)
680. Checked operators `+?`, `-?`, `*?`, `\?`, `%?`, and unary `-?` return `Option[T]`: `Some(result)` or `None` instead of trapping. (§4.6.4)
681. `\?` returns `None` on overflow or division by zero. (§4.6.4)
682. `%?` returns `None` on overflow or a zero divisor. (§4.6.4)
683. `/?` widens integer operands per §4.4.1.1 and dispatches `CheckedDiv` on the float type, returning `Option[Float]`. (§4.6.4)
684. On float operands `/?` returns `None` when IEEE 754 would produce NaN or ±Infinity, e.g. `0.0/0.0`. (§4.6.4)
685. Compile-time constant overflow is a compile error regardless of operator variant: `const x: u8 = 200_u8 +% 100_u8` ✗. (§4.6.5)
686. Float overflow produces signed infinity (`f64::INFINITY`/`f64::NEG_INFINITY`). (§4.6.6)
687. Float underflow may produce subnormals or signed zero. (§4.6.6)
688. NaN propagates through float operations involving NaN operands. (§4.6.6)
689. Float operators have no wrapping or saturating variants. (§4.6.6)
690. Checked operators on floats are normatively defined, returning `None` when the operation produces NaN or infinity. (§4.6.6)
691. Integer division by zero traps at runtime. (§4.6.7)
692. No wrapping or saturating variant exists for division by zero. (§4.6.7)
693. Explicit numeric conversion uses call syntax on the target type: `T(value)`. (§4.7)
694. The `as` keyword is not a cast; it is reserved for naming — placement names and import aliases. (§4.7)
695. `T(x)` is the default conversion and traps at runtime on out-of-range values: `u8(x)` traps when `x` is `300_i32`. (§4.7.1)
696. `T%(x)` wraps modularly via `WrappingAs[T]`: `u8%(x)` is `44` when `x` is `300_i32`. (§4.7.1)
697. `T|(x)` clamps to `T`'s range bounds via `SaturatingAs[T]`: `u8|(x)` is `255` when `x` is `300_i32`. (§4.7.1)
698. `T?(x)` returns `Option[T]` via `CheckedAs[T]`, `None` on out-of-range: `u8?(x)`. (§4.7.1)
699. The policy suffix attaches to the target type immediately before the argument list: `u8%(x)`, never `u8(x)%`. (§4.7.1)
700. Cast-policy suffixed casts parse unambiguously because a type name is never a value operand, so `u8%`/`u8|`/`u8?` cannot read as binary operators. (§4.7.1)
701. On lossless widening conversions, `T(x)` is equivalent to implicit widening. (§4.7.2)
702. The `%`/`|`/`?` variants on lossless conversions are equivalent to `T(x)` and remain syntactically valid for generic code. (§4.7.2)
703. Float-to-integer conversion truncates toward zero. (§4.7.3)
704. `T%(x)` on out-of-range float-to-integer input is implementation-defined (typically truncation modulo the destination range). (§4.7.3)
705. `T|(x)` on float-to-integer input saturates to the destination's range bounds, treating NaN as 0. (§4.7.3)
706. `T?(x)` on float-to-integer input returns `None` for NaN, infinity, or out-of-range values. (§4.7.3)
707. Conversion forms desugar to trait methods callable via uniform call syntax: `x.wrapping_as::[u8]()` is equivalent to `u8%(x)`. (§4.7.4)
708. The `T(x)` family is the canonical user-facing cast syntax; the trait methods exist for trait-constrained generic code. (§4.7.4)
709. `T(value)` is numeric conversion when `T` is a built-in scalar: `f64(x)`. (§4.7.5)
710. Scalar `T(value)` also performs newtype extraction when the source is a newtype: `f64(meters)` yields the wrapped `f64`. (§4.7.5)
711. `T(value)` is ordinary construction when `T` is a user type: `Meters(5.0)`. (§4.7.5)
712. The conversion and construction roles never overlap: `T(value)` has exactly one meaning per `T`. (§4.7.5)
713. Conversions between user types go through `From`/`Into`/`TryFrom`/`TryInto`, not `T(value)`. (§4.7.5)
714. Special numeric operations are trait methods, callable via method-call, conventional, and trait-path syntax per §3.4. (§4.8)
715. `abs`, `min`, `max` are available on all `Numeric` types via the `Abs`, `Min`, `Max` traits. (§4.8.1)
716. `abs` on a signed type's minimum traps on overflow: `i32::MIN.abs()` traps. (§4.8.1)
717. `wrapping_abs` and `saturating_abs` method variants are provided. (§4.8.1)
718. Float `min`/`max` are NaN-propagating: if either operand is NaN, the result is NaN. (§4.8.1)
719. `min_or`/`max_or` are NaN-suppressing variants: the non-NaN operand when exactly one is NaN, NaN when both are. (§4.8.1)
720. Float-only operations comprise `sqrt`; `sin`/`cos`/`tan`/`asin`/`acos`/`atan`/`atan2`; `ln`/`log2`/`log10`/`log`; `exp`/`exp2`; `floor`/`ceil`/`round`/`trunc`; `is_nan`/`is_infinite`/`is_finite`/`is_normal`. (§4.8.2)
721. Each float-only operation has its own fine-grained trait, e.g. `Sqrt`, `Sin`, `Floor`. (§4.8.2)
722. No bare one-argument `log(x)` exists; natural log is `ln(x)`. (§4.8.2)
723. Arbitrary-base logarithm is two-argument with the base first: `log(base, x)`. (§4.8.2)
724. `floor`, `ceil`, `round`, `trunc` are defined only on floats. (§4.8.2)
725. Integer-domain rounding operations (e.g. ceiling division) are delegated to the stdlib, such as a `div_ceil` method. (§4.8.2)
726. `pow` splits into two traits: `IntPow` on `Integer` and `FloatPow` on `Float`. (§4.8.3)
727. `IntPow` takes integer base and exponent, yields an integer, and traps on overflow or negative exponent: `2.pow(-1)` ✗. (§4.8.3)
728. `FloatPow` takes a float base and any-numeric exponent (integers promote to float), yielding float: `(2.0_f64).pow(-1)` is `0.5`. (§4.8.3)
729. The typer selects the `pow` trait by the receiver's type: `2.0.pow(0.5)` uses `FloatPow`. (§4.8.3)
730. The `Integer` umbrella includes `IntPow`; the `Float` umbrella includes `FloatPow`. (§4.8.3)
731. Numeric constants are associated values on the concrete types, accessed via path syntax: `f64::PI`. (§4.8.4)
732. Float types provide `PI`, `E`, `TAU`, `LN_2`, `LN_10`, `INFINITY`, `NEG_INFINITY`, `NAN`; integer types provide `MIN` and `MAX`. (§4.8.4)
733. Numeric constants are `const` declarations per §2.4.1.1: no runtime storage, inlined at use sites. (§4.8.4)
734. The numeric trait hierarchy instantiates §3.6's fine-grained-plus-umbrella pattern. (§4.9)
735. Each operator has its own fine-grained trait whose method name matches the conventional operator name, e.g. `Add::add`. (§4.9.1)
736. The fine-grained set is normative as a pattern — one trait per capability — not as an exact list; the stdlib may add further fine-grained traits. (§4.9.1)
737. Binary arithmetic traits are generic over `Rhs` defaulting to `Subject` with associated `Output` defaulting to `Subject`: `fn add(a: Subject, b: Rhs) -> Output`. (§4.9.1)
738. `Div` is non-generic and declared on the `Float` umbrella only: `fn div(a: Subject, b: Subject) -> Subject`. (§4.9.1)
739. `CheckedDiv` is generic for consistency, but instances are auto-derived only for `Float` types where `Div` is implemented. (§4.9.1)
740. Checked arithmetic trait methods return `Option[Output]`: `fn checked_add(a: Subject, b: Rhs) -> Option[Output]`. (§4.9.1)
741. `Shl`/`Shr` trait methods take the shift count as `u32`: `fn shl(value: Subject, n: u32) -> Subject`. (§4.9.1)
742. The cast traits are destination-parameterized: `WrappingAs[T]`, `SaturatingAs[T]`, and `CheckedAs[T]` returning `Option[T]`. (§4.9.1)
743. `Zero` and `One` are nullary-constructor traits: `fn zero() -> Subject`. (§4.9.1)
744. `Eq` declares the single method `eq`; both `is` and `is not` flow through it. (§4.9.1)
745. `a is not b` desugars at parse time to `not (a is b)`; `is not` has no trait method of its own. (§4.9.1)
746. `Ord` is a methodless umbrella requiring `Lt`, `Le`, `Gt`, `Ge`, auto-satisfying once those four are satisfied. (§4.9.1)
747. `Le`, `Gt`, `Ge` each declare one defaulted method over `Lt::lt`/`Eq::eq` (e.g. `ge(a, b)` is `not lt(a, b)`) and auto-satisfy once their `requires` are met. (§4.9.1)
748. `Lt` and `Eq` are the only ordering-closure traits with abstract methods; fulfilling both cascades `Le`, `Gt`, `Ge`, and `Ord`. (§4.9.1)
749. `@derive(Ord)` generates the full fulfill-block set structurally; manual implementation needs only `fulfill Lt for X` and `fulfill Eq for X`. (§4.9.1)
750. `Ord` and `Eq` are standalone traits outside the numeric umbrellas, available to non-numeric types. (§4.9.1)
751. `IntDiv`'s dispatch method is named `intdiv`: `fn intdiv(a: Subject, b: Rhs) -> Output`. (§4.9.1)
752. The bitwise trait methods are the single-word `bitand`, `bitor`, `bitxor`, `bitnot`: `fn bitand(a: Subject, b: Subject) -> Subject`. (§4.9.1)
753. Umbrella traits combine fine-grained traits via `requires`, declare no methods, and are automatically satisfied when all required traits are satisfied. (§4.9.2)
754. `Numeric` requires `Add`, `Sub`, `Mul`, `Zero`, `One`, the wrapping/saturating/checked add, sub, and mul variants, and `Abs`, `Min`, `Max`. (§4.9.2)
755. `Integer` requires `Numeric`, `Rem`, `IntDiv`, the bitwise and shift traits, the wrapping/saturating int-div and rem variants, `CheckedIntDiv`, `CheckedRem`, and `IntPow`. (§4.9.2)
756. `Signed` requires `Integer`, `Neg`, `WrappingNeg`, `SaturatingNeg`, `CheckedNeg`. (§4.9.2)
757. `Unsigned` requires `Integer` only and does not require `Neg`. (§4.9.2)
758. `Neg` and its wrapping/saturating/checked variants are excluded from `Numeric`; they appear on `Signed` and `Float`. (§4.9.2)
759. The signed/unsigned distinction is exactly the presence or absence of `Neg` in a type's effective method set. (§4.9.2)
760. The umbrella defaults are `Numeric` → `i32`, `Integer` → `i32`, `Float` → `f64`, `Signed` → `i32`, `Unsigned` → `u32`. (§4.9.3)
761. The fourteen built-in numeric types auto-implement their fine-grained traits per §3.3; umbrella satisfaction follows transitively. (§4.9.4)
762. All integer types auto-implement the arithmetic, bitwise, shift, wrapping, saturating, and checked fine-grained traits, the cast traits, and `Zero`, `One`, `Abs`, `Min`, `Max`, `Ord`, `Eq`, `IntPow`. (§4.9.4)
763. Integer types do not implement `CheckedDiv`; it is float-only because `/` widens integers to float. (§4.9.4)
764. Among integer types, only signed ones implement `Neg`, `WrappingNeg`, `SaturatingNeg`, `CheckedNeg`. (§4.9.4)
765. Integer types satisfy `Integer`, `Numeric`, and `Signed` or `Unsigned` as appropriate. (§4.9.4)
766. Float types auto-implement `Add`, `Sub`, `Mul`, `Div`, `Rem`, `Neg`, the checked add/sub/mul/div/neg variants, the float-only operation and inspection traits, and `Zero`, `One`, `Abs`, `Min`, `Max`, `Ord`, `Eq`, `FloatPow`. (§4.9.4)
767. Floats do not implement the wrapping or saturating arithmetic traits. (§4.9.4)
768. Float `WrappingAs[T]` is implemented for integer and wider-float destinations, but not for narrower-float destinations. (§4.9.4)
769. Float `SaturatingAs[T]` and `CheckedAs[T]` are implemented for integer, narrower-float, and wider-float destinations. (§4.9.4)
770. Floats satisfy `Float` and `Numeric` but not `Signed`, which requires `Integer`. (§4.9.4)
771. Built-in numeric types implement same-type trait instantiations only: `fulfill Add[i32] for i32`, never `Add[i64] for i32`. (§4.9.4)
772. Cross-type arithmetic between built-in numeric types requires explicit conversion. (§4.9.4)
773. User-defined types may implement cross-type instantiations such as `Add[i32] for Distance`. (§4.9.4)
774. User-defined numeric-like types implement whichever fine-grained traits are appropriate; umbrella satisfaction follows. (§4.9.4)

## Type Composition: `&`, `dyn`, `Type[...]`

775. The `&` type-intersection operator appears in exactly three contexts: trait conjunction in generic bounds, trait intersection behind `dyn` at value position, and record intersection in `type` definitions. (§5)
776. In a generic parameter list or where-clause, `T: A & B` constrains `T` to types for which both `fulfill A for T` and `fulfill B for T` exist: `fn pick[T: Drivable & Insurable](item: T)`. (§5.1)
777. `&` in a generic bound is constraint conjunction, not a type expression. (§5.1)
778. A conjoined constraint is resolved statically at every use site; instantiations are monomorphized with no runtime dispatch cost. (§5.1)
779. A conjoined constraint set is checked at the call site for each concrete instantiation. (§5.1)
780. Constraint conjunction is commutative and associative: `A & B`, `B & A`, and `(A & B) & C` are equivalent constraint sets. (§5.1)
781. A trait may appear at value position — variable, parameter, field, or return type — only when wrapped in `dyn`. (§5.2)
782. A trait object dispatches method calls dynamically through a vtable. (§5.2)
783. A single-trait trait-object type is written `dyn Trait`: `let x: dyn Drivable = dyn some_value`. (§5.2.1)
784. A multi-trait trait-object type requires parentheses around the trait intersection: `dyn (Drivable & Insurable)`. (§5.2.1)
785. Without parentheses `dyn` binds tighter than `&`: `dyn Drivable & Insurable` parses as `(dyn Drivable) & Insurable`. (§5.2.1)
786. A trait object is not a valid operand of trait intersection, so `(dyn Drivable) & Insurable` is ill-formed. (§5.2.1)
787. `dyn` is mandatory at every trait-object value position; a bare trait type is rejected: `let x: Drivable` ✗ when `Drivable` is a trait. (§5.2.2)
788. A bare trait intersection at value position without `dyn` is likewise rejected: `let x: Drivable & Insurable` ✗ when both operands are traits. (§5.2.2)
789. Each method invocation on a trait object costs one indirect call through the vtable. (§5.2.3)
790. A trait object stores a vtable pointer adjacent to the value's data. (§5.2.3)
791. A trait is object-safe iff every method in its effective method set — its own plus those inherited through `requires` — is vtable-dispatchable and its associated types are bound at the `dyn` site. (§5.2.4)
792. Object safety is strict and whole-trait: a single non-dispatchable method makes the entire trait non-object-safe, with no per-method carve-out. (§5.2.4)
793. A method is vtable-dispatchable only if its first parameter is typed `Subject`: `fn make() -> Subject` lacks a receiver and is not dispatchable. (§5.2.4)
794. A method is vtable-dispatchable only if `Subject` appears nowhere besides the receiver — in no other parameter and not in the return type: `fn clone(value: Subject) -> own Subject` ✗. (§5.2.4)
795. A method is vtable-dispatchable only if it declares no type parameters of its own. (§5.2.4)
796. A trait with associated types is object-safe only when they are bound at the `dyn` site: `dyn Iterator[Item = i32]` ✓; bare `dyn Iterator` is a compile error. (§5.2.4)
797. A non-object-safe trait used in a `dyn` position is a compile error at the use site, naming the trait, the offending method, and the violated rule. (§5.2.4)
798. A trait whose `requires` closure includes a non-object-safe trait is itself non-object-safe. (§5.2.4)
799. The object-safety rules also govern the structural closure type `fn(P) -> R`, treated as a single-method callable with the closure value as receiver and concrete `P`/`R`. (§5.2.4)
800. There is no per-method escape hatch that admits a trait as `dyn` while marking individual methods unavailable on the object. (§5.2.4)
801. Guidance: to make a trait that bundles dispatchable and non-dispatchable methods usable as `dyn`, factor the non-dispatchable methods into a sibling trait — the dispatchable remainder is then object-safe, and the spec sanctions this factoring as the right design, not a workaround. (§5.2.4)
802. Erasing a concrete value to a trait object is explicit and never implicit, written with the `dyn` prefix on the value: `let shape: dyn Drawable = dyn circle`. (§5.2.5)
803. The target trait set of a `dyn` coercion is taken from context: the binding's annotation, the parameter type at a call, or the collection's element type. (§5.2.5)
804. A `dyn` coercion whose target trait set no context determines is a compile error requesting an annotation. (§5.2.5)
805. At the coercion site the value's concrete type must fulfill every trait in the target set. (§5.2.5)
806. At the coercion site every trait in the target set must be object-safe. (§5.2.5)
807. `dyn value` consumes the value into the trait object as a category-B storage operation — an implicit move, no `move` keyword. (§5.2.5)
808. For a `Copy` concrete type, `dyn value` copies the value instead of consuming it. (§5.2.5)
809. A `dyn` value owns its erased payload and is itself a real owner. (§5.2.5)
810. Position disambiguates `dyn`: in type position it denotes the trait-object type; in value position it is the coercion. (§5.2.5)
811. A `type` declaration whose right-hand side is a record-record intersection produces a new nominal record combining both operands' fields: `type InsuredCar = Car & Insured`. (§5.3)
812. A record-intersection type is nominally distinct from both operands; values are not implicitly assignable in either direction. (§5.3)
813. Converting between a record-intersection type and its operands requires explicit construction or a `From` impl. (§5.3)
814. Record intersection is a definitional combinator, not subtyping; the language has no nominal subtyping. (§5.3)
815. A same-named field with identical type and identical visibility in both operands merges into a single field. (§5.3.1)
816. A same-named field with different types in the two operands makes the intersection a compile error naming the field and the two types. (§5.3.1)
817. A same-named field with the same type but different visibility makes the intersection a compile error naming the field and the two visibility specifiers. (§5.3.1)
818. A record-intersection type inherits operand traits only via explicit `@derive`, which generates `fulfill` blocks delegating to the operand types' implementations: `@derive(Display, Hash)` on `type InsuredCar = Car & Insured`. (§5.3.2)
819. `alias type X = A & B` over traits is valid and names a constraint for bound positions: `fn process[T: X]` is equivalent to `fn process[T: A & B]`. (§5.4)
820. `alias type X = dyn (A & B)` is valid and names a dynamic-dispatch trait-object type. (§5.4)
821. `alias type X = A & B` over records is rejected at compile time; record intersection requires a `type` declaration to create the new identity. (§5.4)
822. Intersection is well-defined only within `{trait & trait}` and `{record & record}`; all cross-kind combinations and `Enum & Enum` are rejected at compile time. (§5.5)
823. Intersections involving tuples, function types, or primitive types are rejected; those kinds are not subject to intersection. (§5.5)
824. An invalid intersection is reported at the `&` expression with the operand kinds named. (§5.5)
825. There are no variance markers and no subtyping between generic instantiations: `Container[Cat]` and `Container[Animal]` are unrelated types. (§5.6)
826. Intersection of two distinct generic instantiations receives no special rule; the ordinary kind rules for the operands apply. (§5.6)
827. A trait conjunction that no type satisfies is not an error at the constraint declaration; it fails at call sites where no concrete type satisfies all conjuncts. (§5.6)
828. `Type[C]` is the type of a type value — a value whose value is a type satisfying the constraint `C`. (§5.7)
829. `dyn` lifts a trait to value position as an erased instance while `Type[…]` lifts it as the type itself: `dyn Drivable` is some Drivable value; `Type[Drivable]` is some Drivable type. (§5.7)
830. A trait constraint argument is the existential form: `Type[Drivable]` admits any type satisfying the trait. (§5.7.1)
831. A concrete or generic-parameter argument is the singleton form admitting exactly that type: `Type[PostCard]`, `Type[T]`. (§5.7.1)
832. `Type[…]` appears only in value position — attr, parameter, return value, `let`/`const` binding, or generic value argument. (§5.7.2)
833. `Type[…]` never appears in bound position; a generic bound is always a trait: write `[T: Drivable]`, never `[T: Type[Drivable]]`. (§5.7.2)
834. A type value is resolved at compile time and is never erased behind a vtable. (§5.7.3)
835. An existential `Type[C]` slot desugars to a compiler-introduced generic parameter: `attr item: Type[Drivable]` becomes `[~F: Drivable]` with `attr item: Type[~F]`. (§5.7.3)
836. Each supply site picks a concrete type and monomorphizes the enclosing type for it: `ListView | item=PostCard` infers `~F = PostCard`. (§5.7.3)
837. Two anonymous `Type[C]` slots desugar to two independent parameters and may hold different types. (§5.7.3)
838. Forcing two type-value slots to hold the same type requires a named parameter used in both: `[T: Drivable]` with `Type[T]` in two places. (§5.7.3)
839. Runtime selection among type values of the same slot type is allowed: a `match` may yield a `Type[C]`. (§5.7.3)
840. Runtime selection among different node structures is the role of `given` (§13.9.13), not of type values. (§5.7.3)
841. A type value may be stored in persistent slots: record fields, attrs, and cells. (§5.7.4)
842. Whatever a receiver does with a `Type[C]` value — construct it, place it — must be justified by what `C` guarantees. (§5.7.5)
843. A bare `Type[Node]` admits any node type but can only be held, passed, stored, and returned — not placed. (§5.7.5)
844. Placing a type value requires its constraint to be a trait declaring the instantiation interface: required attrs/consts/cells and, for connections, endpoints. (§5.7.5)

## Records, Enums & Newtypes

845. Records (product types), enums (sum types), and newtypes (wrapper types) are the three user-defined nominal compound types, with identity by name rather than structure. (§6)
846. All three compound kinds participate uniformly in the trait system. (§6)
847. A record is a nominal product type with a fixed set of named fields and carries data only. (§6.1)
848. Behavior associated with a record type is expressed via free functions and trait implementations, not record methods. (§6.1)
849. A record is declared as `type Name[generics]:` followed by a body of field declarations: `type Point[T]:` with fields `x: T` and `y: T`. (§6.1.1)
850. A field declaration gives a name and a type — any type expression. (§6.1.1)
851. A record's generic parameters are in scope within its field declarations. (§6.1.1)
852. A record body may contain at most one `satisfies` clause listing the traits the type promises to implement. (§6.1.1)
853. Every `satisfies`-listed trait must have a matching `fulfill` block reachable through the module graph, except pure-requirement umbrella traits, which are satisfied automatically when their requirements are. (§6.1.1)
854. A record may declare no fields; the bodiless `type Marker` is a zero-field nominal tag. (§6.1.1)
855. Record fields have no defaults; default-bearing construction uses a constructor function with default parameters (the smart-constructor pattern §10.5.1), not the record type. (§6.1.2)
856. A record value is constructed by calling the type name with named field arguments: `Window(title: "Main")`. (§6.1.3)
857. Named construction arguments may appear in any order. (§6.1.3)
858. Every field must be supplied at construction. (§6.1.3)
859. Supplying the same field twice at construction is a compile error. (§6.1.3)
860. Supplying an unknown field name at construction is a compile error. (§6.1.3)
861. Positional record construction is not supported. (§6.1.3)
862. Generic records require concrete type arguments at construction, inferred from the field arguments or given explicitly via turbofish: `Point::[i32](x: 1, y: 2)`. (§6.1.3)
863. A multi-line construction argument list permits a trailing comma after the final argument. (§6.1.3)
864. A record field is read with dot notation: `record.field_name`. (§6.1.4)
865. The compiler distinguishes field access from method call by the syntax following the dot. (§6.1.4)
866. Record fields are read-only and cannot be reassigned after construction; binding immutability applies transitively to fields. (§6.1.4)
867. The `with` expression produces a new record derived from a base, with selected fields overridden or merged from other records: `base with name: "new"`. (§6.1.5)
868. Single-line `with` lists merge sources and field overrides comma-separated: `base with other1, other2, name: "new"`. (§6.1.5)
869. Multi-line `with` puts field overrides in a colon-introduced indented body, optionally after merge sources: `base with other1:` then `name: "new"` lines. (§6.1.5)
870. Single-line and multi-line are the only two `with` surface forms; mixing them in one expression is a parse error. (§6.1.5)
871. The components of a `with` expression are evaluated left to right. (§6.1.5)
872. The result of `with` is always a new record of the base's type. (§6.1.5)
873. Merge sources and field overrides apply left-to-right, with later assignments winning on conflict. (§6.1.5)
874. A field set by no merge source or override keeps the base's value. (§6.1.5)
875. Every merge source must have the exact same type as the base; cross-type merge is a compile error: `car with insured_record` ✗. (§6.1.5)
876. In a reactive declaration context `with` gains per-field rules (§13.2.9.8): bare reactive names alias, reactive expressions become implicit derived cells, and non-reactive values become static fields. (§6.1.5)
877. Every override field name must exist in the base's type; overriding a non-existent field is a compile error. (§6.1.5)
878. Override values must be type-compatible with the field's declared type. (§6.1.5)
879. Each record field carries its own visibility specifier, independent of the type's and the constructor's visibility: `private password_hash: string`. (§6.1.6)
880. A field with no visibility specifier defaults to `shared` — readable within the package. (§6.1.6)
881. A field's visibility may not exceed the enclosing type's; a `public` field on a `private` type is a compile error. (§6.1.6)
882. Access to a field from outside its visibility scope is a compile error reported at the access site, not at the record's declaration. (§6.1.6)
883. A record constructor's visibility is controlled independently of the type's via the `outer(inner)` form: `public(private) type Email`. (§6.1.7)
884. With a private constructor the type name remains visible while `TypeName(...)` construction is unreachable outside the constructor's scope. (§6.1.7)
885. Constructor visibility may not exceed type visibility; an inner specifier more permissive than the outer is a compile error. (§6.1.7)
886. `@derive` on a record generates structural implementations for a fixed trait set: `@derive(Eq, Ord, Hash, Clone, Display, Debug)`. (§6.1.8)
887. Record derivation operates field-by-field: every field's type must itself satisfy the derived trait, else a compile error identifies the offending field. (§6.1.8)
888. Deriving `Ord` requires `Eq` to be available on the same type — derived in the same annotation or manually fulfilled; deriving `Ord` without `Eq` is a compile error identifying the missing dependency. (§6.1.8)
889. A record body cannot contain `fn` declarations; functions on a record live as free functions or `fulfill`-block methods. (§6.1.9)
890. An enum is a nominal sum type — a tagged union whose value is exactly one declared variant at any time. (§6.2)
891. An enum is declared with the `enum` keyword and a body of variant declarations: `enum Option[T]:` with variants `Some(T)` and `None`. (§6.2.1)
892. Variant names are PascalCase, like type names. (§6.2.1)
893. A variant payload may be declared positionally, types only: `Circle(f64)`. (§6.2.1)
894. A variant payload may be declared with named fields: `Rectangle(width: f64, height: f64)`. (§6.2.1)
895. A variant with no payload is a unit variant: `North`. (§6.2.1)
896. A single variant's payload declaration must be all-positional or all-named; mixing is a compile error: `Mixed(width: f64, f64)` ✗. (§6.2.1)
897. Different variants of one enum may use different payload forms independently. (§6.2.1)
898. An enum may declare no variants; a zero-variant enum is uninhabited — no values — a type-level bottom. (§6.2.1)
899. A variant payload field, named or positional, may carry a default applied when omitted at construction, like a function parameter: `Rectangle(width: f64 = 1.0, height: f64 = 1.0)`. (§6.2.1)
900. A variant with a defaulted payload field may omit it at construction, taking the default: `Shape::Rectangle(width: 10.0)` leaves `height` at `1.0`. (§6.2.1)
901. A positionally-declared variant can omit only trailing defaulted payload fields; positional construction cannot skip a mid-list default (§3.5.4). (§6.2.1)
902. A named-payload variant supports both named and positional forms at construction sites and in patterns: `Shape::Rectangle(10.0, 20.0)` ✓. (§6.2.1.1)
903. A positional-payload variant supports only the positional form: `Shape::Circle(radius: 5.0)` ✗. (§6.2.1.1)
904. Mixing named and positional arguments within one variant call is an error: `Shape::Rectangle(width: 10.0, 20.0)` ✗. (§6.2.1.1)
905. Adding names to a previously positional variant is a non-breaking change; both forms become valid. (§6.2.1.1)
906. Removing names from a previously named variant is a breaking change; named-form call sites stop compiling. (§6.2.1.1)
907. The stdlib declares `Option::Some`, `Result::Ok`, and `Result::Err` with positional payloads. (§6.2.1.2)
908. An enum's generic parameters are in scope within all variants' payload declarations. (§6.2.1.2)
909. An enum may include a `satisfies` clause parallel to records, requiring matching `fulfill` blocks. (§6.2.2)
910. Trait conformance applies to the enum as a whole, not per-variant; one implementation handles all variants and produces a uniform result type. (§6.2.2)
911. A variant value is constructed by naming the variant and supplying its payload arguments: `Shape::Circle(5.0)`. (§6.2.3)
912. By default every variant reference is path-qualified with the enum name via `::`: `Result::Ok`, `Direction::North`. (§6.2.3)
913. Unqualified variant names are unavailable by default and require explicit import: `use Result::(Ok, Err)` enables `Ok(42)`. (§6.2.3)
914. `use Enum::*` imports all of an enum's variants unqualified: `use Direction::*` enables bare `North`. (§6.2.3)
915. Selection lists in `use` paths are written with parentheses: `use Result::(Ok, Err)`. (§6.2.3)
916. Context disambiguates parentheses: after `::` they are a selection list; after a value expression they are a call. (§6.2.3)
917. Importing colliding variant names from two enums is an error at the `use` statements, not a call-site ambiguity: `use Direction::*` then `use Heading::*` ✗. (§6.2.3)
918. A generic enum's type arguments at variant construction are inferred from the payload arguments or the expected type: `let n: Option[i32] = Option::None`. (§6.2.3)
919. `match` is the canonical consumer of an enum value; each arm pairs a pattern with an expression. (§6.2.4)
920. Positional variant patterns bind payload fields in declaration order: `Rectangle(width, height)`. (§6.2.4)
921. Named variant patterns bind by field name with `field_name: bound_name`: `Rectangle(width: w, height: h)`. (§6.2.4)
922. Mixing positional and named bindings within one pattern is a compile error: `Rectangle(width, height: h)` ✗. (§6.2.4)
923. Named-form patterns require the variant to be declared with named payload fields: `Some(value: x)` ✗ for positionally-declared `Some(T)`. (§6.2.4)
924. Patterns nest for compound values: `(Ok(x), Err(e))`. (§6.2.4)
925. The wildcard pattern `_` matches without binding. (§6.2.4)
926. A catch-all pattern — a bare identifier with no constructor — matches any value and binds it. (§6.2.4)
927. A `match` must cover every variant of the matched enum and, for compound matches, every combination; exhaustiveness is verified at compile time. (§6.2.5)
928. A non-exhaustive `match` is a compile error identifying the unreached variants. (§6.2.5)
929. A catch-all arm (`_:` or a bare identifier) covers all remaining variants, making a match trivially exhaustive. (§6.2.5)
930. `match` is a value selector: it evaluates the scrutinee, selects one arm, evaluates that arm to a value, and discards the rest. (§6.2.5)
931. Selecting which node/connection subtree is exposed and kept live is the role of `given` (§13.9.13), not `match`. (§6.2.5)
932. `given` shares `match`'s arm shape and exhaustiveness rule, differing in operation: it builds all arms and freezes unselected ones rather than discarding them. (§6.2.5)
933. Visibility applies to an enum as a whole; all variants share the enum's visibility. (§6.2.6)
934. There is no per-variant visibility specifier. (§6.2.6)
935. Enums support `@derive` for the same fixed trait set as records, with derivation operating variant-by-variant. (§6.2.7)
936. Derived enum `Eq` compares variant tags and, for matching tags, payload fields pairwise. (§6.2.7)
937. Derived enum `Ord` orders variants by declaration order, with ties broken by payload comparison. (§6.2.7)
938. Derived enum `Hash` combines the variant tag and the payload fields. (§6.2.7)
939. Derived enum `Clone` structurally copies each variant's payload. (§6.2.7)
940. Derived enum `Display` and `Debug` output is a compiler-defined structural format. (§6.2.7)
941. Enum derivation requires every payload field type to satisfy the derived trait; failure is a compile error identifying the offending payload field. (§6.2.7)
942. A newtype is a wrapper type creating a new nominal identity over an existing type. (§6.3)
943. A newtype is declared with `type` and a body containing a `wraps` clause naming the underlying type: `type UserId:` with `wraps i64`. (§6.3.1)
944. A newtype may be generic: `type MyVec[T]:` wrapping `Vec[T]`. (§6.3.1)
945. A `wraps` body may include `satisfies` clauses and metadata but no field declarations; a body mixing `wraps` with fields is rejected. (§6.3.1)
946. A `wraps` declaration creates a new nominal identity, unlike `alias type`, which is transparent substitution: `UserId` is distinct from `i64`. (§6.3.1)
947. A newtype is constructed by calling its type name with the underlying value as a single positional argument: `UserId(42)`. (§6.3.2)
948. Newtype construction has no named-argument form and no multi-argument form. (§6.3.2)
949. The underlying value is extracted with the conversion form naming the wrapped type: `i64(id)` unwraps `UserId` to `i64`. (§6.3.2)
950. Numeric-to-`string` is not a `T(value)` conversion — `string(i64(id))` ✗ — onward conversion of an extracted value uses stdlib functions such as `to_string()`. (§6.3.2)
951. By default a newtype inherits no traits from its underlying type. (§6.3.3)
952. Newtype trait inheritance is opt-in via `@derive`: `@derive(Eq, Hash, Display)` on a `wraps string` newtype. (§6.3.3)
953. Each derived newtype trait gets a compiler-generated `fulfill` block delegating to the underlying type's implementation: with `@derive(Add)`, `Distance(1.0) + Distance(2.0)` is `Distance(3.0)`. (§6.3.3)
954. Operators between a newtype and a different type require manual implementation: `Distance + i32` is a compile error without a `fulfill Add[i32] for Distance` block. (§6.3.3)
955. The orphan rule permits such cross-type operator fulfills in the newtype's defining module. (§6.3.3)
956. `@derive` implicitly declares `satisfies` for its listed traits; no separate `satisfies` clause is written. (§6.3.3)
957. Manual `fulfill` blocks still require their corresponding `satisfies` clauses in the type body. (§6.3.3)
958. Deriving a trait the underlying type does not satisfy is a compile error at the annotation site: `@derive(Display)` over a non-`Display` underlying type ✗. (§6.3.3)
959. Deriving `Ord` on a newtype requires the underlying type to satisfy `Eq`. (§6.3.3)
960. A newtype's constructor visibility is independently controllable from its type visibility: `public(private) type Email:` with `wraps string`. (§6.3.4)
961. The smart-constructor pattern makes the type name publicly visible while restricting construction to the defining module, forcing every value-producing path through the constructor's visibility scope. (§6.3.4)
962. Implementing a foreign trait for a local newtype wrapping a foreign type satisfies the orphan rule: `fulfill SomeForeignTrait for MyVec[T]` where local `MyVec[T]` wraps `Vec[T]`. (§6.3.5)

## Conversions

963. Infallible user-defined conversions use the `From`/`Into` trait pair. (§7)
964. Fallible user-defined conversions use the `TryFrom`/`TryInto` trait pair. (§7)
965. The conversion-trait system is layered on the ordinary trait system and complements built-in numeric implicit widening and the `T(value)` conversion form. (§7)
966. `From[T]` declares `fn convert(value: T) -> Subject`. (§7.1)
967. `Into[T]` declares `fn into(value: Subject) -> T`. (§7.1)
968. `TryFrom[T]` declares an associated `type Error` and `fn try_from(value: T) -> Result[Subject, Error]`. (§7.1)
969. `TryInto[T]` declares an associated `type Error` and `fn try_into(value: Subject) -> Result[T, Error]`. (§7.1)
970. Each fallible conversion impl declares its own `Error` associated type, so different conversions can produce different error kinds. (§7.1)
971. `Into` and `TryInto` are sealed traits: usable in trait bounds and method dispatch but never implementable by users. (§7.2)
972. Whenever `From[T] for U` exists, the language auto-provides `Into[U] for T`. (§7.2)
973. Whenever `TryFrom[T] for U` exists, the language auto-provides `TryInto[U] for T` with the same `Error` associated type. (§7.2)
974. The ban on manually written `Into`/`TryInto` impls applies uniformly to user-defined and language-built-in `From`/`TryFrom` pairs. (§7.2)
975. Auto-derived `Into`/`TryInto` impls require no `satisfies` declaration on the source type. (§7.2)
976. The language auto-implements the identity conversion `From[T] for T` for every type. (§7.3)
977. A user-written `fulfill From[T] for T` with source and target identical is a compile error. (§7.3)
978. The identity self-conversion is required to be a no-op. (§7.3)
979. A `T: Into[U]` bound accepts a bare `U` via the identity conversion. (§7.3)
980. Structural impls such as the identity `From[T] for T` are exempt from the orphan rule. (§7.3)
981. User `fulfill From[T] for U` and `fulfill TryFrom[T] for U` obey the orphan rule with generic-parameter coverage: a concrete local type must appear in the source or destination position: `fulfill From[i64] for f64` ✗. (§7.4)
982. Local-type coverage through a generic argument satisfies the orphan rule for conversions: `fulfill From[Vec[MyType]] for SomeType` ✓ when `MyType` is local. (§7.4)
983. A conversion between two foreign types is written by wrapping one side in a local newtype and converting through it. (§7.4)
984. A synthesized `Into[U] for T` impl exists at the module of its `From[T] for U` impl and is bound by the same orphan rule. (§7.4)
985. Stdlib pre-populates `From` impls for every lossless numeric widening of §4.5: same-signedness integer widening, unsigned-to-wider-signed, `f32` → `f64`, and exact integer-to-float cases. (§7.5)
986. The pragmatic conversions `From[i64] for f64` and `From[u64] for f64` are included among the built-in `From` impls. (§7.5)
987. Built-in `TryFrom` impls cover numeric narrowing, signed/unsigned crossings, and lossy integer-to-float, each with an appropriate `Error` type. (§7.5)
988. Users cannot declare `From`/`TryFrom` impls between primitive types (orphan rule); stdlib uses no special path unavailable to user types. (§7.5)
989. For lossless numeric conversions, `U(x)` and `x.into::[U]()` produce the same result and both are valid. (§7.6)
990. For lossy numeric conversions, the `T(x)` variant family selects a value-level range policy (trap/wrap/saturate/optional) while `try_into` expresses trait-level fallibility via `Result[T, Error]`. (§7.6)
991. Newtype extraction `T(value)` bypasses the conversion-trait system entirely; the wrapped value is exposed directly. (§7.6)
992. `T(value)` does not apply to user-defined conversions on non-newtype types; those use `.into()`, `From::convert()`, or `.try_into()`. (§7.6)
993. User-defined `From` impls never enable implicit conversion; invocation is explicit: `let f: Fahrenheit = some_c.into()`. (§7.7)
994. The implicit-conversion surface is exactly the built-in lossless widenings of §4.5; user types never participate in implicit expression-level type adjustment. (§7.7)
995. Auto-derivation of `Into` from `From` is not an implicit conversion: the synthesized method still requires explicit call-site syntax. (§7.7)
996. Three explicit invocation forms cover all `From`/`Into` impls: `x.into::[U]()`, `Into::into(x)`, and `From::convert(x)`. (§7.8)
997. A bare value converts without any invocation only for built-in lossless widenings: `let x: f64 = 5_i32`. (§7.8)
998. The trait-path form works only when the target type is inferable from context: `let x: f64 = Into::into(5_i32)`; otherwise the turbofish method form is used. (§7.8)
999. Fallible conversion uses the same explicit invocation forms returning `Result`: `let r: Result[i32, _] = big_value.try_into::[i32]()`. (§7.8)
1000. On failure, `?` propagates the failure value up the call stack, terminating the enclosing function early. (§7.9)
1001. At a `?` site the source's error type must equal the enclosing function's error type or be convertible to it via `From`. (§7.9)
1002. `expr?` evaluates to the source's success type at that site, bound locally or used inline. (§7.9)
1003. `?` imposes no constraint between the source's success type and the enclosing function's return success type. (§7.9)
1004. When source and destination error types are identical, `?` applies no conversion. (§7.9)
1005. When the source error type converts via `From`, the compiler inserts the `From::convert` call automatically at the propagation site. (§7.9)
1006. With no `From` relationship between error types, `?` is a compile error identifying the source and destination error types and the missing `From` impl. (§7.9)
1007. The error-type rule is the only relationship `?` enforces: no implicit success-type coercion and no fallback through other trait machinery. (§7.9)

## Error Handling

1008. Failure handling uses a two-track model — traps versus typed values — chosen at the operation site when code is written. (§8)
1009. A failure encoded on one track cannot be silently converted to the other. (§8)
1010. Trap-track failures represent bugs and invariant violations. (§8.1)
1011. Arithmetic overflow on a default operator is a trap-track failure. (§8.1)
1012. Integer division by zero is a trap-track failure. (§8.1)
1013. An out-of-range `T(x)` conversion is a trap-track failure. (§8.1)
1014. An out-of-range array index is a trap-track failure. (§8.1)
1015. `abs` on the signed minimum is a trap-track failure. (§8.1)
1016. A negative integer exponent on an integer base is a trap-track failure. (§8.1)
1017. `unwrap` or `expect` on `Option::None` or `Result::Err` is a trap-track failure. (§8.1)
1018. Runtime stack overflow is a trap-track failure. (§8.1)
1019. Allocation failure is a trap-track failure. (§8.1)
1020. An explicit `panic` call is a trap-track failure. (§8.1)
1021. Traps halt execution and produce diagnostics. (§8.1)
1022. Traps are not catchable as values. (§8.1)
1023. A non-exhaustive `match` is a compile error, never a runtime trap. (§8.1)
1024. A deliberate runtime panic for an "unreachable" `match` case is written as an explicit catch-all arm calling `panic`. (§8.1)
1025. Value-track failures flow through the type system as `Option[T]` (occurrence-only failure) and `Result[T, E]` (failure with contextual information). (§8.1)
1026. The value track also comprises `?` short-circuit propagation, the user-implementable `Try` trait, `From`-conversion of failure types during propagation, and the `+?`-family arithmetic operator variants producing `Option`-typed results. (§8.1)
1027. A trap never becomes a `Result::Err` value. (§8.1)
1028. A `Result::Err` never aborts the program. (§8.1)
1029. There is no `try`/`catch` mechanism for traps. (§8.1)
1030. `panic` is a prelude built-in available unqualified in every scope. (§8.2.1)
1031. `panic` has the signature `fn panic(message: string) -> never`. (§8.2.1)
1032. Calling `panic` triggers an immediate trap carrying the supplied diagnostic message. (§8.2.1)
1033. Because `panic` returns `never`, it may appear wherever a value of any type is expected: `None: panic("expected Some, got None")`. (§8.2.1)
1034. `never` is a built-in primitive type with no values, spelled in lowercase. (§8.2.2)
1035. `never` unifies with every type during type-checking: `if condition: 5 else: unreachable()` types as `i32`. (§8.2.2)
1036. A firing trap prints a diagnostic naming the triggering operation (with operand values where available), the source location, and a stack trace. (§8.2.3)
1037. After the trap diagnostic, the process exits. (§8.2.3)
1038. No unwinding hook can intercept a trap and convert it to a value. (§8.2.3)
1039. Diagnostics for user-triggered `panic` calls include the user-supplied message. (§8.2.4)
1040. Trap-diagnostic formatting is implementation-defined; the normative requirement is information sufficient to identify what trapped, where, and through what call chain. (§8.2.4)
1041. `Option` and `Result` are ordinary stdlib generic enums with no language-level special-casing of their identity. (§8.3)
1042. `Option[T]` is the stdlib enum with variants `Some(T)` and `None`. (§8.3)
1043. `Result[T, E]` is the stdlib enum with variants `Ok(T)` and `Err(E)`. (§8.3)
1044. The `?` and error-conversion interactions are mediated by the stdlib `Try` trait, not by compiler knowledge of `Option`/`Result`. (§8.3)
1045. `Option` and `Result` are discriminated with standard exhaustive `match`. (§8.3.1)
1046. Ductus provides no `if let` and no check-and-unwrap sugar. (§8.3.1)
1047. The postfix `?` operator dispatches through the stdlib trait `Try`. (§8.4)
1048. `Try` declares `type Success`, `type Failure`, and `fn branch(value: Subject) -> TryBranch[Success, Failure]`. (§8.4)
1049. `TryBranch[S, F]` is an enum with variants `Continue(S)` and `Break(F)`. (§8.4)
1050. `Option[T]` fulfills `Try` with `Failure = ()`: `branch(Some(x))` is `Continue(x)`; `branch(None)` is `Break(())`. (§8.4)
1051. `Result[T, E]` fulfills `Try` with `Failure = E`: `branch(Ok(x))` is `Continue(x)`; `branch(Err(e))` is `Break(e)`. (§8.4)
1052. Any user-defined type may implement `Try` to make `?` available on it. (§8.4)
1053. `expr?` desugars to a `match` on `Try::branch(expr)`: `Continue(value)` yields `value`; `Break(failure)` returns early — `Err(From::convert(failure))` in a `Result`-returning function (converting the inner error value) or `None` in an `Option`-returning function. (§8.4.1)
1054. The postfix `!` operator unwraps an `Option[T]` to its `T`: `let car = target!`. (§8.4.2)
1055. `!` never propagates a failure and never traps. (§8.4.2)
1056. `opt!` compiles only when the compiler can prove `opt` is always `Some` at that point; otherwise it is a compile error. (§8.4.2)
1057. A compiled `!` performs no runtime check and costs nothing at runtime. (§8.4.2)
1058. `!` has no "trust me" escape that bypasses the compile-time proof. (§8.4.2)
1059. The `!` provability rule is normative and closed, so the same programs are well-formed under every conforming compiler. (§8.4.2)
1060. An `Option`-typed expression is provably `Some` iff every value that can reach it is a `Some(...)` construction or a qualifying `Handle` resolution. (§8.4.2)
1061. A `Handle` resolution qualifies for `!` iff its topology-analysis candidate referents are all statically placed: non-`repeat`, non-dismountable graph entities. (§8.4.2)
1062. Reaching values for `!` are computed over the expression's definitions and the data flow into it. (§8.4.2)
1063. Any other provenance — a reaching `None` construction, a `Handle` with any dismountable candidate, an `Option` of unknown origin crossing a function boundary — makes `!` a compile error. (§8.4.2)
1064. Conforming compilers accept exactly the closed `!` provability set: no implementation-specific extensions, no omissions. (§8.4.2)
1065. A `Handle` into a `repeat` view is permanently non-provable for `!`, since a keyed scope is always dismountable. (§8.4.2)
1066. Postfix value-expression `!` is distinct from the `!` cardinality sigil in `parts:`/`incoming:`/`outgoing:` type-cardinality position; the two never collide. (§8.4.2)
1067. The compiler verifies the required error `From` impl at every `?` use site, rejecting with an error when no conversion path exists. (§8.5)
1068. Using `?` on an `Option` inside a `Result`-returning function, or on a `Result` inside an `Option`-returning function, is a compile error. (§8.6)
1069. The cross-track `?` ban is categorical: it holds even when `From` impls exist that could bridge the failure types. (§8.6)
1070. Crossing failure tracks is explicit-only, via stdlib methods such as `option.ok_or(err)` and `result.ok()`. (§8.6)
1071. Stdlib provides a standard method set on `Option` and `Result`; the specified list is non-exhaustive. (§8.7)
1072. All standard `Option`/`Result` methods are stdlib free functions reached through uniform call syntax; the enum types carry no methods of their own. (§8.7)
1073. `option.unwrap()`, `unwrap(option)`, and `std::option::unwrap(option)` are equivalent invocation forms. (§8.7)
1074. Module-path qualification such as `std::option::unwrap(option)` disambiguates among multiple in-scope same-named free functions. (§8.7)
1075. No type-qualified free-function form exists: `Option::unwrap(option)` ✗. (§8.7)
1076. The `fn(T) -> U` parameters in stdlib method signatures are closure types accepting any signature-matching callable, monomorphized per call site. (§8.7)
1077. `unwrap(value)` on `Option[T]` returns the contained `T` or traps on `None`. (§8.7.1)
1078. `expect(value, msg)` on `Option` is `unwrap` with a caller-supplied trap message. (§8.7.1)
1079. `unwrap_or(value, default)` on `Option` returns the contained value or the given default. (§8.7.1)
1080. `unwrap_or_else(value, f: fn() -> T)` on `Option` returns the contained value or a computed default. (§8.7.1)
1081. `map[U](value, f: fn(T) -> U) -> Option[U]` applies `f` to the success value. (§8.7.1)
1082. `and_then[U](value, f: fn(T) -> Option[U]) -> Option[U]` chains optional computations. (§8.7.1)
1083. `or_else(value, f: fn() -> Option[T]) -> Option[T]` supplies a fallback computation. (§8.7.1)
1084. `ok_or[E](value, err: E) -> Result[T, E]` converts an `Option` to a `Result`, using `err` on `None`. (§8.7.1)
1085. `is_some` and `is_none` are non-consuming `bool` discriminator predicates on `Option`. (§8.7.1)
1086. `unwrap(value)` on `Result[T, E]` returns the success value or traps on `Err`. (§8.7.2)
1087. `expect(value, msg)` on `Result` is `unwrap` with a caller-supplied trap message. (§8.7.2)
1088. `unwrap_or(value, default)` and `unwrap_or_else(value, f: fn(E) -> T)` on `Result` return the success value or a given/computed default. (§8.7.2)
1089. `map[U](value, f: fn(T) -> U) -> Result[U, E]` transforms the success value. (§8.7.2)
1090. `map_err[F](value, f: fn(E) -> F) -> Result[T, F]` converts the error type. (§8.7.2)
1091. `and_then[U](value, f: fn(T) -> Result[U, E]) -> Result[U, E]` chains fallible computations. (§8.7.2)
1092. `or_else[F](value, f: fn(E) -> Result[T, F]) -> Result[T, F]` provides an error-recovery chain. (§8.7.2)
1093. `ok(value) -> Option[T]` and `err(value) -> Option[E]` convert a `Result` to an `Option`, discarding the other arm. (§8.7.2)
1094. `is_ok` and `is_err` are non-consuming `bool` discriminator predicates on `Result`. (§8.7.2)
1095. The `Option`-vs-`Result` choice is a convention, not a language rule; the compiler accepts either signature. (§8.8)
1096. Convention: use `Option[T]` when failure carries no information beyond occurrence and `Result[T, E]` when callers may inspect failure information. (§8.8)
1097. Convention: when in doubt between `Option` and `Result`, prefer `Result`. (§8.8)
1098. The reactive system uses the same two-track failure model as the rest of the language. (§8.9)
1099. A trap inside a `derived` expression's computation propagates as a normal trap; the reactive system does not catch traps. (§8.9)
1100. A `derived` whose expression has type `Result[T, E]` or `Option[T]` produces a reactive value of that type, consumed with standard `match` or `?`. (§8.9)
1101. The reactive layer adds no error mechanism beyond the existing type system. (§8.9)

## Strings, Tuples, Arrays & Time

1102. `string`, tuples, and fixed-size arrays `T[N]` are language-level compound types with dedicated syntax and language-specified behavior, not user-defined types. (§9)
1103. `string` is a built-in primitive type at the same level as `i32` or `bool`, not a stdlib type with privileged literal syntax. (§9.1)
1104. The lowercase keyword `string` is reserved. (§9.1)
1105. The complete set of primitive non-numeric types is `bool`, `char`, `string`, `duration`, and `instant`; no other non-numeric primitives exist. (§9.1.1)
1106. Byte sequences are `u8[N]` arrays; there is no dedicated byte-string primitive. (§9.1.1)
1107. The language commits to exactly one string type, encoded as UTF-8; UTF-16 strings, ASCII-only strings, and unencoded byte strings are stdlib concerns. (§9.1.1)
1108. The language commits to monotonic time only; wall-clock dates, calendar arithmetic, and timezones are stdlib concerns. (§9.1.1)
1109. `char` is a Unicode scalar value — an integer in `0..=0xD7FF` ∪ `0xE000..=0x10FFFF`; the surrogate range `0xD800..=0xDFFF` is excluded. (§9.1.2)
1110. The type system rejects `char` values outside the Unicode-scalar range at construction time, so every `char` value is always a valid scalar. (§9.1.2)
1111. `char` has a fixed 32-bit representation per value. (§9.1.2)
1112. Character literals use single quotes: `'a'`, `'\n'`, `'\u{1F600}'`, `'\x41'`. (§9.1.2)
1113. A character literal contains exactly one Unicode scalar; multi-character literals are a compile error. (§9.1.2)
1114. Character literals use the same escape conventions as string literals. (§9.1.2)
1115. `From[char] for u32` converts every `char` to `u32` losslessly. (§9.1.2)
1116. `TryFrom[u32] for char` yields `Err` for surrogate-range values and values above `0x10FFFF`. (§9.1.2)
1117. `char` satisfies `Eq`, `Ord`, `Hash`, `Display`, `Debug`, and `Clone`. (§9.1.2)
1118. `char` comparison and ordering follow numeric Unicode scalar value order. (§9.1.2)
1119. `char` literals use the same escape set as strings (§9.1.3), so `'\''` and `'\"'` are both valid. (§9.1.2)
1120. String literals take plain, raw, escape-sequence, and interpolation forms: `"hello world"`, `r"no \n escapes"`, `r#"with "quotes""#`, `"user {name} has {count} items"`. (§9.1.3)
1121. The recognized string escape sequences are `\n`, `\r`, `\t`, `\0`, `\\`, `\"`, `\'`, `\{`, `\xHH`, and `\u{HHHHHH}`; `char` literals accept the same set except `\{`. (§9.1.3)
1122. Every string-literal form produces a value of type `string`. (§9.1.3)
1123. A plain string may contain literal newlines; the embedded newline becomes part of the value. (§9.1.3)
1124. A raw string performs no escape processing and no interpolation; every character — backslashes, braces, newlines — is literal. (§9.1.3)
1125. UTF-8 is the internal string encoding; the type system guarantees every string value is valid UTF-8, so no invalid-UTF-8 string can exist at runtime. (§9.1.4)
1126. String constructors and conversions taking untrusted input either reject ill-formed input or return a fallible result. (§9.1.4)
1127. Strings are not indexable — there is no `s[i]` operator; content access requires explicit view methods. (§9.1.5)
1128. `s.bytes()` returns a sequence of raw `u8` values, indexable in O(1); the exact return type is a stdlib concern. (§9.1.6)
1129. `s.chars()` returns a sequence of `char` values, iterable in O(n) total traversal because UTF-8 decoding is required; the exact return type is a stdlib concern. (§9.1.6)
1130. `s.byte_len() -> isize` returns the length in bytes in O(1). (§9.1.6)
1131. `s.char_count() -> isize` returns the number of Unicode scalars in O(n). (§9.1.6)
1132. `s.slice(start: isize, end: isize) -> string` slices at validated character positions with cost O(end). (§9.1.7)
1133. `s.byte_slice(start: isize, end: isize) -> string` slices at byte positions with O(1) cost per boundary. (§9.1.7)
1134. `byte_slice` traps when a boundary lands mid-codepoint in a multi-byte UTF-8 sequence. (§9.1.7)
1135. Both slicing methods return a new string value. (§9.1.7)
1136. Invalid slice boundaries — a mid-codepoint byte index or an out-of-range position — trap at runtime. (§9.1.7)
1137. Strings are immutable with no in-place mutation; every content-modifying operation returns a new string value: `let upper = s.to_upper()`. (§9.1.8)
1138. The runtime may share immutable backing storage between string values; sharing is an implementation detail invisible to the user. (§9.1.8)
1139. `+` concatenates strings via a stdlib `Add` implementation with both operands and result typed `string`: `"hello" + " " + "world"`. (§9.1.8)
1140. The orphan rule prevents users from declaring their own `Add` for `string`. (§9.1.8)
1141. Interpolation `{expr}` evaluates the expression and converts the result to `string` via the `Display` trait. (§9.1.9)
1142. Interpolating a value whose type does not satisfy `Display` is a compile error at the interpolation site. (§9.1.9)
1143. Interpolation expressions are arbitrary expressions (method calls, arithmetic, field access), not just bare identifiers: `"value: {amount * tax_rate}"`. (§9.1.9)
1144. A literal brace in a plain string is written `\{`, which yields `{` and suppresses interpolation there. (§9.1.9)
1145. Tuples are structurally typed: two tuples with the same component types in the same order are the same type: `(1, 2)` and `(3, 4)` are both `(i32, i32)`. (§9.2)
1146. Tuples and closure types are the only two structural-typing carve-outs in the otherwise nominal type system. (§9.2)
1147. The type expression `(T1, T2, ...)` denotes a tuple type directly; no type declaration is required. (§9.2)
1148. Tuple field access uses zero-based numeric postfix syntax: `t.0`. (§9.2.1)
1149. Tuple field indices must be integer literals; runtime indexing with a variable expression (`t.i`) is not permitted. (§9.2.1)
1150. Tuple field access is bounds-checked at compile time: `t.3` on a 3-tuple is a compile error. (§9.2.1)
1151. Tuple patterns are admitted in `let` bindings, `match` arms, and every other pattern position: `let (a, b, c) = (1, "hello", 3.14)`. (§9.2.2)
1152. Tuple patterns nest to arbitrary depth: `let ((a, b), c) = ((1, 2), 3)`. (§9.2.2)
1153. The wildcard `_` in a tuple pattern ignores a component without binding it: `let (x, _, z) = some_tuple`. (§9.2.2)
1154. Tuple patterns are always positional; tuples have no field names and no named pattern form. (§9.2.2)
1155. The unit type is `()`, with a single value also written `()`. (§9.2.3)
1156. A function without a final expression returns the unit value implicitly. (§9.2.3)
1157. The `-> ()` return annotation may be omitted: `fn print_hello():` equals `fn print_hello() -> ():`. (§9.2.3)
1158. `()` is admitted in pattern position to match unit-typed values. (§9.2.3)
1159. A 1-tuple requires a trailing comma: `(42,)` has type `(i32,)`; `(42)` is a parenthesized `i32`, not a tuple. (§9.2.4)
1160. A tuple of arity two or more permits an optional trailing comma: `(1, 2,)` equals `(1, 2)`. (§9.2.4)
1161. Generic parameters appear in tuple types via standard generic syntax: `fn swap[A, B](t: (A, B)) -> (B, A)`. (§9.2.5)
1162. Each unique tuple-type instantiation produces its own specialized code under monomorphization. (§9.2.5)
1163. Variadic generics — abstraction over tuples of arbitrary arity — are not part of Ductus; generics range over fixed-arity tuple component types only. (§9.2.5)
1164. `fulfill SomeTrait for (T1, T2, ...)` is permitted iff the trait is local or at least one element type `Ti` is local: `fulfill MyTrait for (i32, string)` ✓. (§9.2.6)
1165. A `fulfill` of a foreign trait for a tuple whose element types are all foreign is an orphan-rule error: `fulfill Display for (i32, string)` ✗. (§9.2.6)
1166. The compiler provides `Eq`, `Ord`, `Hash`, `Clone`, `Display`, and `Debug` for tuples structurally at any arity by component-by-component derivation. (§9.2.6)
1167. A tuple type has a derivable trait iff every component type has it; the implementation is generated per instantiation during monomorphization. (§9.2.6)
1168. Structural tuple-trait derivation is automatic-when-eligible rather than opt-in, since tuples have no declaration site to annotate. (§9.2.6)
1169. There is no arity cap on structural tuple-trait derivation and no hand-written per-arity stdlib impls. (§9.2.6)
1170. Manual tuple `fulfill` blocks have no arity limit. (§9.2.6)
1171. Any non-derivable trait is implemented for a specific tuple type with a manual `fulfill` block, subject to the orphan rule. (§9.2.6)
1172. Tuple-to-record conversion is always explicit; tuples are structural and records nominal, and the compiler never converts implicitly between them. (§9.2.7)
1173. Tuple-to-record conversion is written field-by-field or via a user `From` impl: `Vec3(x: t.0, y: t.1, z: t.2)`. (§9.2.7)
1174. Arrays are fixed-size contiguous sequences of a single element type, and the element count is part of the type. (§9.3)
1175. The dedicated array type syntax is `T[N]`: `i32[5]`. (§9.3.1)
1176. There is no exposed canonical `Array[T, N]` type name; the underlying array representation is internal and not addressable in user code. (§9.3.1)
1177. Multi-dimensional array types parse left-to-right: `T[N][M]` is an M-element array of `T[N]`. (§9.3.1)
1178. Zero-length array types `T[0]` are valid. (§9.3.1)
1179. An array is constructed with a bracketed element list `[e1, …, eK]`, producing a `T[K]` value of the unified element type: `[1, 2, 3]` is an `i32[3]`. (§9.3.1)
1180. An empty array literal `[]` has size 0 and takes its element type from context: `let xs: i32[0] = []`. (§9.3.1)
1181. `T[args]` in type position is interpreted by what `T` resolves to: a primitive or other non-generic type forms an array type (`i32[5]`); a generic type instantiates (`Vec[i32]`). (§9.3.2)
1182. Array-vs-generic disambiguation depends only on the kind of `T`, never on the kind of the arguments; there is no parser-level ambiguity. (§9.3.2)
1183. The array length type is `isize` — signed and platform-sized; there is no `usize` length type. (§9.3.3)
1184. `usize` remains available for explicit use where a non-negative invariant is needed (allocation sizes, FFI); the language does not block this. (§9.3.3)
1185. `arr[i]` traps at runtime if `i < 0 || i >= length`. (§9.3.5)
1186. When both index and length are compile-time known, out-of-range indexing is a compile error: `arr[10]` on `i32[5]` ✗. (§9.3.5)
1187. Recoverable indexing uses stdlib methods such as `arr.get(i)` returning `Option[T]`, or a `?` indexing variant if one is provided. (§9.3.5)
1188. The dynamic growable vector is a stdlib concern; only fixed-size arrays receive dedicated language syntax, and the vector's name and syntax are outside the specification. (§9.3.6)
1189. Stdlib collections receive no special powers; their mutating operations are ordinary `own subject -> Subject` methods. (§9.3.6)
1190. Whether a collection call mutates in place or yields a fresh value follows entirely from the caller's ownership, never from the type; there is no separate mutable-collection type. (§9.3.6)
1191. The only primitive a stdlib collection rests on is the allocator intrinsic, available to any program; users can implement an equivalent collection with no privileged access. (§9.3.6)
1192. A `usize` array index requires an explicit cast to `isize` (`arr[isize(n)]`); a same-width signed↔unsigned crossing is never implicit. (§9.3.4)
1193. Index-widening legality is platform-dependent: an index fitting `isize` on a wider platform may need a cast on a narrower-`isize` one, so portable code casts proactively. (§9.3.4)
1194. `duration` (a span of time) and `instant` (a point in monotonic time) are first-class lowercase primitive types. (§9.4)
1195. The operators on `duration` and `instant` are language-defined primitives, not dispatched through the numeric `Div` trait machinery, which is why shapes like `duration / duration` -> `f64` are well-formed. (§9.4)
1196. `duration` is internally i64 nanoseconds, giving a range of approximately ±292 years at single-nanosecond precision. (§9.4.1)
1197. Negative durations are permitted. (§9.4.1)
1198. The duration literal suffixes are `ns`, `us`, `μs` (microseconds), `ms`, `s`, `min`, `h`, and `d`: `500ns`, `250ms`, `1.5s`, `5min`, `2h`, `1d`. (§9.4.1.1)
1199. Both integer and float numeric literals may carry duration suffixes. (§9.4.1.1)
1200. Float duration literals convert to nanoseconds with rounding-to-nearest at compile time: `1.5s` is `1_500_000_000ns`. (§9.4.1.1)
1201. Integer duration literals scale exactly. (§9.4.1.1)
1202. Duration suffixes are reserved by the language; `@literal_suffix` may not re-register them in any scope. (§9.4.1.1)
1203. `duration + duration` yields the sum as `duration`. (§9.4.1.2)
1204. `duration - duration` yields the difference as `duration`, which may be negative. (§9.4.1.2)
1205. `duration * Numeric` scales and yields `duration`; `Numeric * duration` is equally permitted. (§9.4.1.2)
1206. `duration / Numeric` scales down and yields `duration`. (§9.4.1.2)
1207. `duration / duration` yields the ratio as `f64`. (§9.4.1.2)
1208. `duration % duration` yields the remainder as `duration`. (§9.4.1.2)
1209. Unary negation `-duration` yields `duration`. (§9.4.1.2)
1210. `<`, `<=`, `>=`, `>`, `is`, and `is not` on two durations yield `bool`. (§9.4.1.2)
1211. The `Numeric` operand in duration scaling may be any integer or float type. (§9.4.1.2)
1212. Duration multiplication by an integer is exact. (§9.4.1.2)
1213. Duration division by an integer or float rounds to nearest at the nanosecond level: `2s / 3` is 666,666,667 ns. (§9.4.1.2)
1214. Duration multiplication by a float rounds to nearest at the nanosecond level. (§9.4.1.2)
1215. `duration / 0` with an integer-typed zero traps as integer division by zero. (§9.4.1.2)
1216. `duration / 0.0` with a float-typed zero produces ±inf or NaN per IEEE 754, and converting that result back to the i64-nanosecond representation traps. (§9.4.1.2)
1217. Dividing a duration by a zero-duration value traps, treated as the i64 zero-divisor case. (§9.4.1.2)
1218. Checked variants `/?` and `%?` on durations return `Option[duration]`, or `Option[f64]` for `duration / duration`. (§9.4.1.2)
1219. `duration + Numeric` and `Numeric + duration` are not defined; attempting them is a compile error. (§9.4.1.2)
1220. `duration - Numeric` and `Numeric - duration` are not defined; attempting them is a compile error. (§9.4.1.2)
1221. `duration * duration` is not defined; attempting it is a compile error. (§9.4.1.2)
1222. Default duration arithmetic traps on overflow: a result that does not fit i64 nanoseconds aborts the process. (§9.4.1.3)
1223. Checked duration variants `+?`, `-?`, `*?`, `/?`, `%?` return `Option[duration]`. (§9.4.1.3)
1224. Construction of `duration` from raw integer/float values and conversion to numeric counts are stdlib concerns, not language built-ins. (§9.4.1.4)
1225. Stdlib is expected to provide `duration::from_nanos(n)`, `from_micros(n)`, `from_millis(n)`, `from_secs(n)`, `from_minutes(n)`, `from_hours(n)`, and `from_days(n)`. (§9.4.1.4)
1226. `d.as_nanos() -> i64` is lossless, nanoseconds being the internal representation. (§9.4.1.4)
1227. `d.as_micros() -> i64`, `d.as_millis() -> i64`, and `d.as_secs() -> i64` truncate sub-unit components. (§9.4.1.4)
1228. Stdlib is expected to provide float conversion variants such as `d.as_secs_f64() -> f64`, precision-bound by f64. (§9.4.1.4)
1229. `instant` is a monotonic point in time — from a clock that never goes backward — measured against an implementation-defined epoch (typically program start or system boot). (§9.4.2)
1230. `instant` has no literal syntax. (§9.4.2)
1231. `instant` has no direct construction from raw nanoseconds and no operation exposing the underlying value as an absolute count. (§9.4.2)
1232. Internally an `instant` is i64 nanoseconds since the implementation-defined epoch, exposed only via comparisons and difference operations. (§9.4.2)
1233. `instant - instant` yields the elapsed time as `duration`. (§9.4.2.1)
1234. `instant + duration` yields a future `instant`. (§9.4.2.1)
1235. `instant - duration` yields a past `instant`. (§9.4.2.1)
1236. `<`, `<=`, `>=`, `>`, `is`, and `is not` on two instants yield `bool`. (§9.4.2.1)
1237. `instant + instant` is not defined. (§9.4.2.1)
1238. `instant * Numeric` and `instant / Numeric` are not defined. (§9.4.2.1)
1239. All direct arithmetic between `instant` and `Numeric` is not defined. (§9.4.2.1)
1240. The language core provides no `instant` construction; stdlib is expected to provide `instant::now() -> instant`. (§9.4.2.2)
1241. No conversion from `instant` to absolute values is provided. (§9.4.2.2)
1242. Comparison and difference are the canonical operations on `instant`; no other introspection is provided. (§9.4.2.2)
1243. A saved `instant` has no meaningful interpretation in a later run started from a different epoch; cross-run serialization of instants is not reliable. (§9.4.2.3)
1244. Conversion of instants to and from a stable absolute representation for persistence is a stdlib concern, not provided by the language core. (§9.4.2.3)
1245. `duration` and `instant` are i64-sized, satisfy the direct in-cell storage criteria, and may directly type `signal`, `attr`, `recurrent`, and `derived` declarations. (§9.4.3)
1246. `Option`/`Result` wrappings of `duration`/`instant` store directly in a reactive cell when discriminant plus payload fits the platform atomic word, and otherwise use index-based pool storage. (§9.4.3)
1247. The compiler chooses the cell-storage strategy; the source-level wrapped type is permitted on all platforms. (§9.4.3)

## Ownership & Mutability

1248. Category A (value ownership: function call, function return, let-rebinding, for-loop iteration variable) defaults to borrow-equivalent: the callee or new binding gets read-only access and the source binding survives. (§11.1)
1249. Category A consumption is opt-in, requiring `own` in the signature and `move` at the call site. (§11.1)
1250. Category B (structural storage: record construction, indexed assignment, field assignment via `mut`, whole-value reassignment of `mut`, attr initialization with a value RHS at placement) consumes the RHS into the storage slot implicitly, with no `move` keyword. (§11.1)
1251. Category C (reactive wiring: placement attribute assignment with a reactive RHS, connection arguments, attr/derived references in reactive expressions) binds names to reactive cells rather than stack-owned values; multiple aliases to one cell may coexist. (§11.1)
1252. Category C operations are not §11 ownership operations; their values flow through the reactive system specified by §13 and §14. (§11.1)
1253. Category D (reactive cell writes: `signal.write(v)`, `stream.emit(v)`, attr reassignment from a recurrent advance) consumes the written value into the cell implicitly, with no `move` keyword. (§11.1)
1254. Category A is the only ownership category requiring an explicit call-site consumption marker. (§11.1)
1255. Nothing outside a function body is mutable: module-level bindings, record fields, function parameters, and enum variants cannot be declared `mut`. (§11.1)
1256. At every moment, every value has exactly one real owner. (§11.1)
1257. Exception: `Signal[T]` parameters and reactive composite bindings name reactive cells, so multiple live aliases to the same cell may coexist without violating single ownership. (§11.1)
1258. Materialization of a reactive composite (at the boundaries of §13.2.9.7) produces a concrete instance subject to standard single-ownership rules from that point on. (§11.1)
1259. Single writer: a `mut` binding is the only path through which its underlying value may be mutated. (§11.1)
1260. The single-writer rule is enforced entirely at compile time, with no runtime check. (§11.1)
1261. Mutation is globally invisible: no caller can observe a callee's mutations except through the callee's declared return value. (§11.1)
1262. Exactly two binding forms exist for runtime values: `let x = expr` (immutable) and `mut x = expr` (mutable, function bodies only). (§11.2)
1263. A `let` binding name cannot be reassigned. (§11.2)
1264. Field or element assignment through a `let` binding is a compile error. (§11.2)
1265. A `mut` binding may be reassigned and its value mutated in place via indexed assignment, field assignment, or whole-value reassignment. (§11.2)
1266. `mut` declarations are legal only inside function bodies (including nested block scopes); they are forbidden at module top level and inside type, trait, node, and connection bodies. (§11.2)
1267. A `mut` declaration outside a function body is a compile error reported at the declaration site. (§11.2)
1268. `const` and `mut` are mutually exclusive: `const` asserts compile-time-only and immutable, `mut` is necessarily runtime and mutable. (§11.2)
1269. `let` is a function-body construct only; module scope contains no `let` bindings. (§11.2)
1270. Module scope contains `const`, `signal`, `attr`, `derived`, and `recurrent` declarations. (§11.2)
1271. The ownership rules of §11.3–§11.9 apply to function bodies and to parameter/return wiring at function boundaries, not to module-scope declarations. (§11.2)
1272. Each use site of a top-level `const` reifies a fresh compile-time value. (§11.2)
1273. Consts do not enter the ownership system: a const has no runtime owner to consume or transfer. (§11.2)
1274. Const eligibility requires static representability of the type, not `Copy`-ness or non-heap-backedness: heap-backed `string` is const-eligible, represented as a static string-pool index. (§11.2)
1275. Dynamic collections such as `Vec` and `HashMap` are not supported as const types. (§11.2)
1276. Applying `move` to a const or passing a const to an `own` parameter is diagnosed as an error: consumption does not apply to compile-time-only values. (§11.2)
1277. Every binding carries an initial value at its declaration — `let`, `mut`, top-level `let`/`const`, and `signal` alike; there is no uninitialized, implicitly-undefined, or implicitly-zero state. (§11.2)
1278. Either binding form may shadow a prior binding of the same name in the same scope, making the prior binding inaccessible by that name from the shadow point forward: `let input = preprocess(input)`. (§11.2.1)
1279. A `let` may shadow a `mut` and vice versa; the new binding's mutability is governed solely by its own declaration form. (§11.2.1)
1280. The real owner of a value is the binding that holds its storage and is responsible for dropping it when its scope ends. (§11.3)
1281. `let y = x` or `mut y = x` kills the name `x` (single-name-per-cluster); if `x` was the real owner, `y` becomes the new real owner: after `let w = v`, `print(v)` ✗. (§11.3)
1282. `for x in v:` binds `x` as a borrow-equivalent alias rooted in each successive element of `v` for the body of one iteration. (§11.3)
1283. `for own x in v:` consumes `v` and binds `x` as a real owner of each element. (§11.3)
1284. Returning a real-owner local transfers its ownership directly to the call site's receiving binding. (§11.3)
1285. Using a binding after its name has been killed is a compile error reported at the use site. (§11.3)
1286. Rebinding moves no storage at runtime; renames are compile-time bookkeeping only. (§11.3)
1287. Any binding, real owner or borrow-equivalent alias, may read its value without consuming it; reading needs no keyword and leaves ownership and cluster membership unchanged. (§11.3.1)
1288. Reading comprises field access, indexed access, read-only pattern matching, built-in operator inspection (`is`, `<`), borrow-default method calls, and passing to borrow-default parameters. (§11.3.1)
1289. Consuming comprises passing to an `own` parameter with explicit call-site `move`, returning a real-owner local, storing into a record field, tuple component, or enum payload (implicit move), and writing into a reactive cell (implicit move): `consume(move r)`. (§11.3.1)
1290. Reassigning a `mut` binding is whole-value assignment: the new value is consumed implicitly (category B, no `move`) and the old value is dropped: `buf = make_other_buffer()`. (§11.3.2)
1291. Reassignment is not shadowing: it modifies the existing binding rather than introducing a new one. (§11.3.2)
1292. A binding going out of scope drops its value only if it is the cluster's current real owner; borrow-equivalent aliases never drop the underlying value. (§11.3.3)
1293. Dropping a `Copy` value is a no-op. (§11.3.3)
1294. Dropping a non-`Copy` value whose resources require cleanup invokes the type's drop behavior; user-defined drop is specified through the trait system per §14.7. (§11.3.3)
1295. A cluster is the transitive closure of borrow-equivalent aliases rooted in a single real-owner binding, computed and tracked entirely at compile time. (§11.3.4)
1296. Borrow-equivalent aliases arise from five sources: default parameters, `let`/`mut` rebinds of an alias, default for-loop iteration variables, default-convention function returns, and borrow-default trait associated-type slots. (§11.3.4)
1297. A trait associated-type slot under the borrow-default convention is rooted in the enclosing method call's argument cluster. (§11.3.4)
1298. A cluster's lifetime is the union of its members' scopes; the cluster ends when the root's scope ends or the root is consumed by `move`. (§11.3.4)
1299. A borrow-equivalent alias cannot be stored in a record field, tuple component, enum payload, or indexed slot. (§11.3.4)
1300. A borrow-equivalent alias cannot be written into a reactive cell (`signal.write`, `stream.emit`, recurrent advance, attr reassignment). (§11.3.4)
1301. A borrow-equivalent alias cannot be captured by an escaping closure. (§11.3.4)
1302. A cluster member may be returned under the default return convention, extending the cluster into the caller's scope. (§11.3.4)
1303. While a cluster has any alias member beyond its root, the root's value cannot be moved (passed to an `own` parameter, returned, or reassigned into a different slot). (§11.3.4)
1304. While a cluster has any alias member beyond its root, the root's value cannot be mutated (`mut` reassignment, indexed assignment, field assignment). (§11.3.4)
1305. Once every alias goes out of scope, the cluster collapses to its root, which may again be moved or mutated. (§11.3.4)
1306. A function-parameter borrow that does not escape into a `let` or for-loop binding is bounded by the call expression. (§11.3.4)
1307. Rule (P): rebinding a borrow-equivalent alias (`let y = x`, `mut y = x`) kills the name `x` and transfers the borrow-equivalent role to the new binding: `let y = x` makes `y` an alias in the same cluster. (§11.3.5)
1308. Under Rule (P) the source value is untouched: no data moves out of the cluster's root and no ownership transfer occurs at the value level. (§11.3.5)
1309. Function return slots are transient binding sites that default to the borrow-equivalent convention, like parameter, let-rebinding, and trait associated-type slots. (§11.3.6)
1310. A default-convention return rooted in an input makes the caller's result binding a borrow-equivalent alias in that input's cluster, extending the cluster across the call boundary: `let r = first(buf)` roots `r` in `buf`'s cluster. (§11.3.6)
1311. While a default-returned alias is live, the rooted-in input may not be moved or mutated. (§11.3.6)
1312. The compiler infers return rootedness from the function body's control flow. (§11.3.6)
1313. When multiple inputs may contribute to a return, the result is rooted in the union of contributing clusters, extending move/mutation locks across all rooted-in inputs. (§11.3.6)
1314. Declaring `-> own T` opts the return into anchoring, giving the caller a real owner independent of the inputs: `fn first_owned(v: Vec[Record]) -> own Record`. (§11.3.6)
1315. Anchoring a `Copy`-typed return is a free implicit copy at the return site. (§11.3.6)
1316. Anchoring a `Clone`-but-not-`Copy` return inserts an implicit `.clone()` at the return site; the user does not write it and the cost is exposed in the elaborated form and diagnostics. (§11.3.6)
1317. Anchoring a return whose type is neither `Copy` nor `Clone` is a compile error. (§11.3.6)
1318. Only storage sites (record fields, indexed slots, enum payloads, reactive cells) default to owned; every transient binding site defaults to borrow-equivalent. (§11.3.6)
1319. There is no stdlib-privileged borrow-returning carve-out; element accessors such as `element_at(v: Vec[T], i: isize) -> T` are borrow-equivalent under the ordinary default convention. (§11.3.6)
1320. A return-value alias may feed a category B/D storage site only if the function declares `-> own T` or the caller's binding is explicitly `.clone()`d. (§11.3.6)
1321. `Copy` is a language-defined marker trait with no methods and no associated types: `trait Copy`. (§11.4)
1322. Values of a `Copy` type are duplicated implicitly at every use site (assignment, argument passing, return) without ownership transfer; the original binding remains usable. (§11.4)
1323. Non-primitive types opt into `Copy` via `@derive(Copy)` or explicit `satisfies Copy`; the two forms have identical semantics. (§11.4)
1324. All primitive numeric types (`i8`–`i128`, `u8`–`u128`, `isize`, `usize`, `f32`, `f64`), `bool`, and `char` automatically implement `Copy`. (§11.4.1)
1325. `string`, `duration`, and `instant` automatically implement `Copy` (`duration` and `instant` are i64-sized scalars). (§11.4.1)
1326. A tuple implements `Copy` when all of its components are `Copy`. (§11.4.1)
1327. `Range[T]` is `Copy` when `T: Copy`, via a conditional `Copy` impl provided by the stdlib. (§11.4.1)
1328. Users may write conditional `Copy` impls of the same pattern for their own generic types. (§11.4.1)
1329. Closure types automatically implement `Copy`; they capture only `Copy` values. (§11.4.1)
1330. `@derive(Copy)` requires every field's type to be `Copy`; a non-`Copy` field fails derivation with a compile error identifying the offending field. (§11.4.2)
1331. A newtype may derive `Copy` when its wrapped type is `Copy`. (§11.4.2)
1332. For `Copy` types every operation that would otherwise transfer ownership instead produces an independent value: `let q = p` leaves `p` usable. (§11.4.3)
1333. `Copy` duplication is conceptually value-by-value; the runtime mechanism (register copy, memcpy) is implementation-chosen while user-visible behavior is language-guaranteed. (§11.4.3)
1334. A `mut` binding to a `Copy` type supports reassignment and field assignment exactly as for non-`Copy` types. (§11.4.4)
1335. Mutation through one `mut` binding of a `Copy` value never affects other bindings, which hold independent copies: after `working.value = 5`, `original.value` is `0`. (§11.4.4)
1336. `Clone` is the explicit deep-duplication trait, declared as `fn clone(value: Subject) -> own Subject`. (§11.5)
1337. `clone` takes its parameter under the default borrow-equivalent convention and returns an independent owned copy; the source is not consumed. (§11.5)
1338. `Clone` duplication requires an explicit `.clone()` call at every duplication site. (§11.5)
1339. `Clone` is a derivable trait: `@derive(Clone)`. (§11.5.1)
1340. Derived `Clone` on a record clones each field; every field's type must itself be `Clone`. (§11.5.1)
1341. Derived `Clone` on an enum clones the payload of the active variant. (§11.5.1)
1342. Every `Copy` type is auto-derived `Clone`; its `.clone()` returns the same result as direct use. (§11.5.2)
1343. Heap-allocated structures (`Vec`, `HashMap`), arrays, and records containing them are `Clone` (when their fields support it) but not `Copy`. (§11.5.2)
1344. Calling `.clone()` on a non-`Copy` value yields a second owned copy, allocating as the type requires: `let backup = buf.clone()`. (§11.5.3)
1345. `Clone` is the trait the compiler invokes to anchor `-> own T` rooted returns; implementations are invoked implicitly at return sites in addition to explicit `.clone()` call sites. (§11.5.4)
1346. `string` realizes its `Copy` semantics through refcounted shared backing: assigning, passing, or returning a string increments a refcount without copying bytes. (§11.6)
1347. Dropping a `string` decrements the refcount and deallocates the backing when it reaches zero. (§11.6)
1348. String duplication is constant-time regardless of string length: `let t = s` is constant-time. (§11.6)
1349. Arrays are not `Copy`, regardless of element type or compile-time size. (§11.6.1)
1350. A function parameter has exactly one of two ownership conventions: default borrow-equivalent (`fn f(v: T)`) or opt-in consumption (`fn f(own v: T)`). (§11.7)
1351. A default parameter is a borrow-equivalent cluster member rooted in the caller's argument binding, with lifetime equal to the call expression. (§11.7.1)
1352. A `Copy`-typed default parameter is a fresh independent value via implicit copy at the call boundary; the caller's binding behaves identically to the non-`Copy` case. (§11.7.1)
1353. A default parameter is immutable. (§11.7.1)
1354. Declaring a function parameter `mut` is a compile error: `fn process(mut buf: Vec[f32])` ✗. (§11.7.2)
1355. `mut local = param` is legal when `param` is an `own` parameter: ownership transfers to `local` and `param`'s name is killed. (§11.7.3)
1356. `mut local = param.clone()` on a default parameter produces a fresh real owner; the parameter's cluster is untouched. (§11.7.3)
1357. `mut local = param` where `param` is a default (borrow-equivalent) parameter is a compile error. (§11.7.3)
1358. For `Copy` parameter types, `mut local = param` and `mut local = param.clone()` are equivalent; the compiler flags the `.clone()` as redundant with a style lint. (§11.7.3)
1359. An `own T` parameter consumes its argument; the caller's binding is inaccessible after the call: `let s = into_sorted(move v)` then `print(v)` ✗. (§11.7.4)
1360. Inside the body an `own` parameter is a real owner: it may be `mut`-rebound, moved, stored in structural slots, or dropped at function exit. (§11.7.4)
1361. `own` is grammatically a keyword preceding the binding name in parameter position and may mark any subset of parameters: `fn g(a: T, own b: U) -> V`. (§11.7.4)
1362. `own` on a `Copy`-typed parameter is legal but redundant (the caller is unaffected); the compiler may report it with an optional style lint. (§11.7.4)
1363. A trait method's `own` declarations are part of its signature contract: implementations must declare exactly the parameters the trait declares `own`, no more and no fewer. (§11.7.4)
1364. The compiler maintains an elaborated form of every signature making conventions, cluster/return lifetime relationships, and anchoring points explicit: `fn f(v: T) -> T` elaborates to `fn f(borrow v: T) -> borrow_rooted_in(v) T`. (§11.7.5)
1365. Borrow-rootedness of returns is inferred from the function body; `own` parameters and `-> own T` are written by the user in source. (§11.7.5)
1366. The elaborated form is normative for diagnostic templates: every ownership-related error references elaborated-form entities rather than the user-facing surface. (§11.7.5)
1367. A user may write the elaborated form explicitly in signatures; the explicit and default forms must be equivalent and the compiler verifies the mapping. (§11.7.5)
1368. The unmarked call form `f(x)` always means no consumption: the caller's binding survives the call regardless of the callee's internal use of its alias. (§11.8)
1369. There is no borrow sigil at call sites — no `&v` or `borrow(v)` surface syntax; the compiler inserts the borrow-equivalent aliasing automatically: `length(v)`. (§11.8.1)
1370. Use-after-move is a compile error reported at the use site; the diagnostic identifies the `move` expression that consumed the binding. (§11.8.2)
1371. Use-after-move detection is local: the callee's signature plus the call-site `move` identify the consumption without analyzing the callee's implementation. (§11.8.2)
1372. `x.f(args)` is sugar for `f(x, args)`: the receiver is the first positional argument, follows the method's first-parameter convention, and the dot-call introduces no implicit ownership rule of its own. (§11.8.3)
1373. Consuming a method receiver uses prefix parenthesization: `(move v).into_sorted()`. (§11.8.3)
1374. Field access `x.field` and indexed access `x[i]` are not function calls and never consume, regardless of any signature. (§11.8.3)
1375. Tightening a parameter from default `T` to `own T` makes every existing `move`-less call site a compile error requiring `move` to be added. (§11.8.4)
1376. Loosening a parameter from `own T` to default `T` makes every existing `move` call site a compile error requiring `move` to be removed. (§11.8.4)
1377. `move <l-value identifier>` is legal only as an immediate sub-expression of a function-call argument list: `f(move x)`, `g(a, move b, c)`. (§11.8.5)
1378. `move` in a let-RHS is a parse error: `let y = move x` ✗. (§11.8.5)
1379. `move` in a return expression is a parse error: `return move x` ✗. (§11.8.5)
1380. `move` in a mut-RHS is a parse error: `mut z = move x` ✗. (§11.8.5)
1381. `move` attached to a dotted expression is a parse error; prefix parenthesization is required: `move v.method()` ✗, `(move v).method()` ✓. (§11.8.5)
1382. An `own` parameter whose call-site argument is a named binding lacking `move` is a compile error. (§11.8.5)
1383. A call-site `move` whose corresponding parameter is not declared `own` is a compile error. (§11.8.5)
1384. An rvalue argument or receiver (a temporary with no name, such as a call result or constructor expression) feeds an `own` parameter or `own` receiver by direct consumption with no `move`; `move` marks the consumption of a named binding only, and a temporary has no name to render inaccessible: `a.clone().push(move x)`. (§11.8.5)
1385. The user-facing surface has no `&T` syntax; borrow-equivalent aliases are introduced only implicitly by the slot-default conventions. (§11.9)
1386. The compiler's elaborated form exposes the inferred alias lifetimes for diagnostics and tooling. (§11.9)
1387. Stdlib types use the same alias mechanism as user-defined types; there is no language-privileged carve-out for the standard library. (§11.9)
1388. A borrow-equivalent alias may not be stored in a record field, enum variant payload, or tuple component (category B); compound types contain only owned values. (§11.9.1)
1389. Storing a value derived from an alias requires declaring the producing function's return `-> own T` or an explicit `.clone()` to create a real owner. (§11.9.1)
1390. A borrow-equivalent alias may not be stored in a reactive cell (category D): `signal.write`, `stream.emit`, attr reassignment, and recurrent advance require a real owner. (§11.9.1)
1391. A borrow-equivalent alias may not be captured by an escaping closure. (§11.9.1)
1392. Aliases may appear only in: default function parameters, `let`/`mut` rebindings of an alias source, default for-loop iteration variables, default-convention function returns, and borrow-default trait associated-type slots. (§11.9.1)
1393. A default-parameter alias's lifetime is bounded by the call expression. (§11.9.1)
1394. Alias lifetimes extend per site: let-rebind to the binding's scope, for-loop variable to the loop body, function return to the caller's scope (rooted in the inputs), associated-type slot to its use-site's scope. (§11.9.1)
1395. `Handle[T]` is not an alias but a `Copy` value weakly designating a graph entity; it is exempt from every alias restriction and may be stored in cells, fields, payloads, or tuples. (§11.9.1)
1396. A handle's resolution `Option[&T]` contains a borrow and is subject to storage categories B and D like any alias-bearing value. (§11.9.1)
1397. While a cluster has any alias member, the root may not be moved — passed to an `own` parameter, returned, or reassigned into a different slot. (§11.9.2)
1398. While a cluster has any alias member, the root may not be mutated — reassigned through `mut`, or assigned by index or field. (§11.9.2)
1399. Once every alias goes out of scope the cluster collapses back to its root, which may again be moved or mutated. (§11.9.2)
1400. Borrowing a binding in one sub-expression while consuming it in another sub-expression of the same expression is a compile error: `combine(v, extract(move v))` ✗. (§11.9.2)
1401. The borrow/consume conflict is reported at the offending sub-expression. (§11.9.2)
1402. Multiple borrow-equivalent aliases of one source may coexist; all are read-only: `compare(v, v)` ✓. (§11.9.3)
1403. Each let-rebind within a cluster kills the prior name and creates a new alias of the same source (single-name-per-cluster). (§11.9.3)
1404. Several distinct aliases of one source may be live across a single call expression: `read_two(y, items)` ✓. (§11.9.3)
1405. A default (borrow-equivalent) parameter may be passed to another default-parameter function with no extra syntax; the alias chain extends: `count_elements(v)`. (§11.9.4)
1406. A borrow-equivalent parameter cannot be consumed inside the function body: `consume_vec(move v)` ✗. (§11.9.4)
1407. Returning a borrow-equivalent parameter under the default convention propagates its cluster to the caller as a borrow-equivalent alias with no `Clone` gate; an owned return requires `-> own T` anchoring (§11.3.6): `return v`. (§11.9.4)
1408. Every `let`/`mut` rebinding of an alias inside a function body follows Rule (P): the new name aliases the same source and the old name dies: `let saved = v`. (§11.9.4)
1409. A `Copy` value passed by the default convention behaves as a fresh independent copy: the caller's binding remains usable and the parameter's value cannot be observed by the caller. (§11.9.5)
1410. The compiler may internally elide the alias machinery for `Copy` types where useful. (§11.9.5)
1411. A function generic over `T: SomeTrait` declares its parameter under the default convention regardless of whether `T` is `Copy`; the compiler specializes per instantiation. (§11.9.5)
1412. A closure is a `fn` literal — a `fn` declaration with the name omitted: `fn(params) -> ret: body`. (§11.10)
1413. A closure literal's parameter and return types may be omitted when inferable from the expected closure type at the use site. (§11.10)
1414. A closure body is a single expression or an indented block, exactly as for a named `fn`. (§11.10)
1415. Closures capture by value: each captured value is stored inside the closure at the moment of definition. (§11.10)
1416. Every value captured by a closure must have a `Copy` type; the capture is a snapshot at definition time: `fn(sample: f32): sample * gain` captures `gain` ✓. (§11.10.1)
1417. Capturing a non-`Copy` value is a compile error: `fn(): sum(buf)` ✗ for `buf: Vec[f32]`. (§11.10.1)
1418. Non-`Copy` values flow through closures as parameters under the default borrow-equivalent convention: `fn(b: Vec[f32]): sum(b)`. (§11.10.1)
1419. A closure that consumes a non-`Copy` argument declares the parameter `own`: `fn(own b: Vec[f32]): into_sorted(move b)`. (§11.10.1)
1420. A closure value is invoked with ordinary call syntax on its binding: `let total = closure(buf)`. (§11.10.1)
1421. The compiler captures the minimal set of subvalues the closure body references: `fn(): contact.age + 1` captures only `contact.age`. (§11.10.2)
1422. The `Copy` constraint applies to the captured subvalue's type, not the root binding's type. (§11.10.2)
1423. Closures may capture from `let` bindings: `fn(): stable + 1` ✓. (§11.10.3)
1424. Capturing from a `mut` binding is a compile error: `fn(): counter + 1` ✗ for `mut counter`. (§11.10.3)
1425. The capture-must-be-`Copy` restriction applies only to the captured environment; closure bodies follow normal function-body rules (local `mut`, consuming calls, construction, iteration). (§11.10.4)
1426. An escaping closure cannot capture borrow-equivalent aliases: cluster members are not `Copy` and an escaping closure may outlive the cluster root. (§11.10.5)
1427. A closure escapes when it is stored, returned, or captured by another escaping closure; the compiler distinguishes escaping from non-escaping by closure-flow analysis. (§11.10.5)
1428. A closure type is written `fn(P1, P2, …) -> R` and is inhabited by closure literals and free functions alike: `let d: fn(i32) -> i32 = double`. (§11.10.6)
1429. Closure types are structural — defined by signature alone, with no declaration and no nominal identity — the second structural carve-out after tuples. (§11.10.6)
1430. A bare parenthesized list `(T1, T2)` is a tuple type; only the `fn` prefix forms a function type. (§11.10.6)
1431. `fn((T1, T2)) -> R` is a one-parameter function type taking a tuple, distinct from the two-parameter `fn(T1, T2) -> R`. (§11.10.6)
1432. Parameter and return ownership conventions are part of a closure type: `fn(own T) -> own R` consumes its parameter and returns an owned value. (§11.10.6)
1433. A callable inhabits a closure type only if its ownership conventions match the type's. (§11.10.6)
1434. A closure value is itself `Copy`: read, passed, and duplicated freely, never consumed. (§11.10.6)
1435. There is no consumed-once or mutable-capture closure variant; captures can be neither mutable nor non-`Copy`. (§11.10.6)
1436. In parameter and return positions a closure type is monomorphized per concrete closure (distinct captures are distinct concrete types), behaving as an implicit generic with no indirection: `fn apply(f: fn(i32) -> i32, x: i32)`. (§11.10.6)
1437. Holding closures of the same signature but different concrete types in one place requires erasure to `dyn fn(P) -> R`, paying vtable indirection: `Vec[dyn fn(Event) -> ()]`. (§11.10.6)
1438. A closure type exposes one operation — its call — with the closure value as the erased receiver and concrete `P`/`R`, satisfying every object-safety rule of §5.2.4. (§11.10.6)
1439. Every closure type is object-safe without exception; `dyn fn(P) -> R` is always well-formed. (§11.10.6)
1440. Coercion from `fn(P) -> R` to `dyn fn(P) -> R` is governed by §5.2.5. (§11.10.6)
1441. A closure cannot be the value type of a `signal`, `attr`, `recurrent`, or `derived` cell. (§11.10.6)
1442. A `mut` binding may carry an explicit type annotation: `mut handlers: Vec[dyn fn(Event) -> ()] = Vec::new()`. (§11.10.6)
1443. Field assignment through a `mut` root binding is permitted: `r.field = new_value`. (§11.11)
1444. Indexed assignment through a `mut` root binding is permitted: `arr[5] = 1.5`. (§11.11)
1445. Field and indexed assignment desugar to operator-trait method calls. (§11.11)
1446. The exact assignment traits (`FieldAssign`, `IndexAssign`, or analogous) are stdlib concerns specified outside the language spec. (§11.11)
1447. The assignment desugaring preserves the single-writer invariant: no other binding to the same underlying value may exist while the mutation occurs. (§11.11)
1448. The RHS of an indexed, field, or whole-value assignment is consumed implicitly into the storage slot — no `move` keyword: `arr[5] = v` kills `v`'s name. (§11.11)
1449. Implicit RHS consumption also applies to record construction and to reactive cell writes (category D). (§11.11)
1450. Function-call argument positions (category A) are the only places where consumption requires the explicit `move` keyword. (§11.11)
1451. The RHS of a storage assignment must be a real owner or a `Copy` value; a borrow-equivalent alias as RHS is a compile error. (§11.11)
1452. The alias-as-RHS diagnostic suggests `.clone()` or restructuring to source the value from a real owner. (§11.11)
1453. Reading a field or element from any binding, `let` or `mut`, is unrestricted. (§11.11)
1454. A `mut` binding may be reassigned entirely; the previous value is dropped: `buf = make_other_buffer()`. (§11.11.1)
1455. Whole-value reassignment moves the new value into the binding, consuming it from its source. (§11.11.1)
1456. Index, field, and whole-value assignments mutate a value the `mut` binding already owns and are in place by construction. (§11.11.2)
1457. The consume-and-produce form `fn f(own subject, …) -> Subject` is the canonical way a type exposes mutation under single ownership, for stdlib and user types alike. (§11.11.2)
1458. The language has no mutable-reference parameter. (§11.11.2)
1459. When a consume-and-produce call's subject is uniquely owned and the result is rebound to the same location with no other reference to the consumed binding in between, the compiler reuses the input's storage with no copy: `v = (move v).push(move x)`. (§11.11.2)
1460. In-place storage reuse is a guarantee of the ownership model, deterministic because both conditions are statically decidable — not a heuristic optimization. (§11.11.2)
1461. The §12.7.2 iterator optimization is the in-place reuse rule applied to the for-loop's internal binding. (§11.11.2)
1462. A consume-and-produce call whose value must outlive the call produces a fresh value, leaving the original intact. (§11.11.2)
1463. Keeping a value while obtaining a transformed copy requires an explicit `.clone()` first; there is no hidden allocation: `let b = a.clone().push(move x)`. (§11.11.2)
1464. A method call on a `mut` binding whose receiver parameter is `own` and which returns `Subject` may omit the receiver move and rebind: `v.m(args)` desugars to `v = (move v).m(args)`. (§11.11.3)
1465. The receiver sugar covers only the receiver; an `own` non-`Copy` argument still requires an explicit `move`: `v.push(move item)`. (§11.11.3)
1466. `Copy` arguments in the sugared form need no `move`: `v.push(3)`. (§11.11.3)
1467. The receiver sugar applies only to `mut` bindings and only when the receiver parameter is declared `own`. (§11.11.3)
1468. A default (borrow-equivalent) receiver call is an ordinary non-consuming call and is never rewritten by the sugar. (§11.11.3)
1469. A record, enum, or newtype may itself be `Copy` if it carries `@derive(Copy)` and all its fields/payloads are `Copy`. (§11.12)
1470. Mutability is a property of the binding, never of the type; a type cannot declare itself mutable. (§11.12)
1471. A record's field is assignable in place through a `mut` binding to the containing record, provided the field's type permits assignment: `r.field = v`. (§11.12)
1472. An enum variant's payload and a newtype's wrapped value have no in-place place form and are never assigned through the binding; a `mut` enum or newtype is updated only by reassigning the whole value to a freshly constructed one (storage may be reused in place per §11.11.2): `state = Active(p2)`. (§11.12)
1473. The record-body ban on `fn` declarations does not restrict mutation; any holder of a `mut` binding to a record may assign its fields. (§11.12)
1474. The `public(private)` constructor pattern restricts only initial construction; a holder of a `mut` binding may still mutate the fields, subject to field visibility. (§11.12.1)
1475. Restricting post-construction mutation is done with field-level visibility (`private field_name: T`), which prevents external code from naming the field in an assignment. (§11.12.1)
1476. Trait method signatures follow the same ownership conventions as free functions — default borrow-equivalent, `own` opt-in: `fn into_sorted(own value: Subject) -> Subject`. (§11.13)
1477. Trait dispatch is unaffected by ownership semantics; whether a method consumes or borrows is determined solely by the trait method's signature. (§11.13)
1478. Implementations cannot strengthen or weaken `own`: a trait-declared `own` parameter must be `own` in every `fulfill`, and a default parameter must stay default. (§11.13)
1479. A `fulfill` block's signature is checked structurally against the trait's signature. (§11.13)
1480. Trait objects (`dyn T`) may invoke methods declared with any ownership convention. (§11.13)
1481. In trait-object calls, the alias lifetime of a default-convention receiver remains call-scoped, as for direct calls. (§11.13)
1482. A `derived` (and analogous) expression body is pure-evaluated: it runs as a pure function of its inputs each time the inputs change. (§11.14)
1483. A `derived` body may invoke ordinary functions that use `mut` internally; the body's produced value enters the reactive graph. (§11.14)
1484. A value bound into the reactive system as a signal, derived, or reactive store becomes external state, no longer owned by any single function's scope. (§11.14)
1485. Reactive values are updated only through the reactive system's defined update mechanisms, never through `mut` assignment. (§11.14)
1486. Reactive cell writes — `signal.write(v)`, `stream.emit(v)`, attr reassignment from a recurrent advance — are category D: the RHS is consumed implicitly, with no `move` written. (§11.14)
1487. Reactive wiring (category C) — placement attribute assignment from a reactive RHS, connection arguments referencing attrs or signals — does not consume and produces multiple aliases to the same cell. (§11.14)
1488. The category-B/C/D distinction at placement attribute assignment is type-directed: a reactive-typed RHS produces wiring; a value-typed RHS is consumed into the attr's slot. (§11.14)
1489. Function parameters flowing into reactive declarations follow `own`/`move` like any other category A consumption. (§11.14)
1490. The reactive boundary is one of the "global" scopes referenced by §11.1's principles; value crossing is fully specified in §13.12. (§11.14)

## Iteration & Loops

1491. The language's iteration constructs are integer ranges, the `for` loop in default and `for own` forms, the `while` loop, `break` and `continue`, loop expressions with `else:`, and the `Iterator`, `Iterable`, and `IntoIterable` traits. (§12)
1492. A `for` loop obtains an iterator through `Iterable` (default form) or `IntoIterable` (`for own` form), then produces successive values through `Iterator`. (§12.1)
1493. All iteration dispatches through the trait protocol; no type has built-in iteration logic specific to it. (§12.1)
1494. `for` and `while` loops are expressions whose value is determined by the body's `break` expressions and an optional `else:` clause. (§12.1)
1495. A loop is an expression in every position: its type follows §12.6's value-shaping rules whether or not the value is consumed; using a loop where its value is unused does not coerce it to `()` or create a separate statement form. (§12.1)
1496. Loop bodies are ordinary function-body fragments: they may mutate `mut` bindings declared inside or outside the loop and perform indexed and field assignment per §11's ownership rules. (§12.1)
1497. The range expression syntax is `start..end` over integer expressions: `0..10`. (§12.2)
1498. Ranges are half-open, exclusive on the upper bound: `0..10` contains 0 through 9. (§12.2)
1499. There is no inclusive-range form (`..=`) in v1; iteration through `N` is written `start..(N + 1)`. (§12.2)
1500. A range has type `Range[T]` where `T` is the bounds' integer type: `let r = 0..1024` is `Range[i32]` via the default integer placeholder. (§12.2)
1501. Both range bounds must have the same integer type; mixed-kind operands require explicit conversion. (§12.2)
1502. Ranges are ordinary values that may be bound, passed, and returned: `fn process_range(r: Range[i32]) -> isize`. (§12.2)
1503. `Range[T]` implements both `Iterable` and `IntoIterable`. (§12.2)
1504. `Range[T]` is `Copy`, so the default and `for own` forms are functionally indistinguishable on ranges. (§12.2)
1505. Range iteration yields successive integers starting at `start` and stopping before `end`. (§12.2)
1506. Ranges have no step parameter in v1; iteration always advances by one, and striding is written as explicit arithmetic: `let actual_i = i * 2`. (§12.2.1)
1507. Range bounds are evaluated once at range construction; later mutation of variables used in the bound expressions does not affect the range: after `mut n = 10; let r = 0..n; n = 100`, `r` still iterates 0..10. (§12.2.2)
1508. Range bound expressions must produce integer values. (§12.2.2)
1509. Overflow in a range bound expression traps at range construction per §4.6.1. (§12.2.2)
1510. A range with `start >= end` is empty and yields no iterations: `for i in 5..3:` runs the body zero times. (§12.2.3)
1511. Negative range bounds are valid when `T` is signed: `for i in -10..10:` yields -10 through 9. (§12.2.3)
1512. A negative range-bound literal with unsigned `T` is rejected by the value-fits check of §2.4.3. (§12.2.3)
1513. `for x in v:` is the default, borrow-equivalent form: the source survives the loop. (§12.3)
1514. `for own x in v:` is the consuming form: the source is consumed at loop entry and is not accessible to the surrounding scope afterward. (§12.3)
1515. The iteration source expression is evaluated exactly once, at loop entry. (§12.3.1)
1516. The default form constructs its iterator by invoking `Iterable::iterator(v)`, passing `v` borrow-equivalently so `v` becomes a cluster source. (§12.3.1)
1517. Under the default form the iterator is held in an internal `mut` binding, and both the iterator and the source are live throughout the loop. (§12.3.1)
1518. Each default-form iteration step calls `Iterator::next(iter, v)`, threading the source through every call and returning `(Option[Item], NewIter)`. (§12.3.1)
1519. A default-form yielded `Item` is borrow-equivalent rooted in the source's cluster. (§12.3.1)
1520. After each `next` call the loop's internal iterator binding is reassigned to the returned `NewIter`. (§12.3.1)
1521. A `Some(value)` result from `next` binds `value` to the iteration variable and runs the body. (§12.3.1)
1522. A `None` result from `next` exits the loop. (§12.3.1)
1523. When a default-form loop exits — natural completion, `break`, or enclosing function return — the iterator is dropped and the cluster's iteration alias collapses. (§12.3.1)
1524. After a default-form loop, the source is unchanged and remains owned by its original binding. (§12.3.1)
1525. The `for own` form invokes `IntoIterable::consuming_iterator(move v)`, consuming the binding and moving the underlying value into the iterator. (§12.3.1)
1526. Each `for own` iteration step calls `Iterator::next(iter, ())`, supplying unit as the source per the `Iter.Source = ()` constraint. (§12.3.1)
1527. Under `for own`, each yielded `Item` is an owned value moved out of the iterator's storage, and the iteration variable is a real owner. (§12.3.1)
1528. When a `for own` loop exits, the iterator is dropped and elements not yet yielded are dropped per their `Drop` semantics. (§12.3.1)
1529. The iteration variable is bound fresh on each iteration as an immutable `let`-style binding of the iterated `Item` type; reassigning it in the body is a compile error. (§12.3.2)
1530. `for mut x in iterable:` is a parse error; the iteration variable cannot be declared `mut`. (§12.3.2)
1531. A mutable per-iteration value is obtained by rebinding inside the body: `mut local = x`. (§12.3.2)
1532. The iteration variable's type is `Iter::Item`, where `Iter` is `Iterable::Iter` under the default form and `IntoIterable::Iter` under `for own`. (§12.3.3)
1533. Under the default form with a `Copy` element type, the iteration variable is an implicit Copy of the current element: `for sample in buf:` with `buf: f32[1024]` gives `sample: f32`. (§12.3.3)
1534. Under the default form with a non-`Copy` element type, the iteration variable is a borrow-equivalent alias rooted in the source's current element. (§12.3.3)
1535. A default-form borrow-equivalent iteration variable cannot be consumed into ownership (Rule P): `consume(move r)` ✗. (§12.3.3)
1536. Binding a default-form iteration alias to a new name creates a new alias in the same cluster and kills the old name: `let saved = r`. (§12.3.3)
1537. Under `for own` with a non-`Copy` element type, each iteration moves one element out of the consumed source's storage and the body has full ownership of it: `destinations = (move destinations).push(move r)`. (§12.3.3)
1538. The body executes in a fresh nested scope each iteration; bindings declared inside the body are dropped at the end of every iteration. (§12.3.4)
1539. The iteration variable is in scope only within the loop body. (§12.3.4)
1540. Bindings declared outside the loop are in scope inside the body, persist across iterations, and can be mutated if declared `mut`: the accumulator `total = total + sample`. (§12.3.4)
1541. Consuming an outer binding inside a loop body is a compile error because later iterations would use the already-moved value: `consume(move v)` in a loop body ✗. (§12.3.5)
1542. The loop use-after-move check is conservative: every `move` of an outer binding inside a loop body is reported as a potential use-after-move at the move site. (§12.3.5)
1543. Guidance: to consume a value inside a loop body, `.clone()` it per iteration, restructure to consume after the loop, or move it into a single-iteration loop. (§12.3.5)
1544. Under the default form, the owner may not move or mutate the iterated source while the loop is active: `v[0] = 5` inside `for x in v:` ✗ (live cluster member). (§12.3.6)
1545. Iteration aliases collapse when the loop exits, after which the owner may freely move or mutate the source. (§12.3.6)
1546. Under `for own`, referencing the source binding inside the body is a use-after-move error, not a cluster conflict, because the binding was consumed at loop entry. (§12.3.6)
1547. A `for` loop is compile-time-unrolled iff its iterable is compile-time known per §2.4.1; otherwise it runs at runtime. (§12.3.7)
1548. There is no syntactic marker selecting unrolling — no `const for`, no `inline for`, no modifier; the iterable alone decides the kind. (§12.3.7)
1549. Unrolling reproduces the body once per element, binding the iteration variable to the corresponding compile-time value. (§12.3.7)
1550. An unrolled body participates in ordinary §2.4.1 propagation: expressions depending only on the iteration variable and other compile-time values are themselves compile-time known. (§12.3.7)
1551. Unrolled loops do not dispatch through `Iterable` or `IntoIterable`; element binding happens at compile time, not by calling `next`. (§12.3.7)
1552. Exactly two iterable forms produce compile-time-known iteration in v1: a range with both bounds compile-time known, and an array literal whose every element expression is compile-time known. (§12.3.7)
1553. Iterating over the elements of a tuple and iterating over a list of types are not provided. (§12.3.7)
1554. Loops over variable-extent types run at runtime: `Vec[T]`, `SmallVec[T, N]`, `RingBuf[T, N]`, `String`/`s.chars()`, `HashMap`, and ranges with runtime bounds. (§12.3.7)
1555. A pure function with a `for` in its body has one source; whether that loop unrolls is decided at each call site by what flows into the iterable expression. (§12.3.7)
1556. A function that requires its loop to unroll expresses the requirement in its signature via a const-generic parameter: `fn process[const N: usize](buf: T[N]):`. (§12.3.7)
1557. A const-generic parameter is compile-time known at every call site, so an internal `for i in 0..N` over it is guaranteed to unroll. (§12.3.7)
1558. There is no body-internal assertion form for requiring compile-time iteration; lifting the parameter to a const-generic is the only mechanism. (§12.3.7)
1559. A loop is runtime iff its iterable is runtime; there is no silent flip between compile-time and runtime based on context. (§12.3.7)
1560. A `for` in a node type's `expose:` block or in a placement body is auto-enforced compile-time per §13.1's static-graph rule; a runtime iterable there is a compile error. (§12.3.7)
1561. Runtime-varying child cardinality in a placement uses `repeat` (§13.5.4), not `for`; the compile-time `for` covers only const-generic-parameterized, per-instance-fixed multiplicity. (§12.3.7)
1562. `while condition: body` repeatedly evaluates the condition and runs the body so long as the condition is true. (§12.4)
1563. A `while` condition must produce a value of type `bool`. (§12.4.1)
1564. The condition is re-evaluated before each iteration, including the first; a loop whose condition is `false` at entry never executes its body. (§12.4.1)
1565. There is no separate `loop` keyword; `while true:` is the idiomatic loop-forever form. (§12.4.2)
1566. The move-inside-loop rule of §12.3.5 applies to `while` bodies and equally to the condition expression. (§12.4.3)
1567. `break` exits the innermost enclosing loop. (§12.5)
1568. `continue` skips the rest of the current iteration and advances the innermost enclosing loop to its next iteration. (§12.5)
1569. `break expr` carries a value that becomes the loop's expression value. (§12.5.1)
1570. Plain `break` is equivalent to `break ()`. (§12.5.1)
1571. A loop body mixing plain `break` with `break value` of a non-unit value is a type error. (§12.5.1)
1572. `continue` never produces a value; it is a control-flow statement only. (§12.5.2)
1573. There is no label syntax in v1; `break` and `continue` always target the innermost enclosing loop. (§12.5.3)
1574. Guidance: to exit nested loops, use a flag variable or extract the inner loop into a function that returns early. (§12.5.3)
1575. A `break` or `continue` outside a loop is a parse error. (§12.5.4)
1576. A loop may carry an optional `else:` clause attached to its body. (§12.6.1)
1577. The `else:` expression is evaluated exactly when the loop completes naturally: iteration exhausts for `for`, or the condition becomes false for `while`. (§12.6.1)
1578. The `else:` clause is not evaluated when the loop exits via `break` or via an enclosing function return. (§12.6.1)
1579. A loop's `else:` clause is written at the loop head's indentation, dedented from the body rather than nested inside it. (§12.6.1)
1580. A loop with no `break value` and no `else:`, whose natural completion is reachable, has type `()`. (§12.6.2)
1581. A loop with no `break value` and an `else:`, whose natural completion is reachable, has the type of the `else:` expression. (§12.6.2)
1582. A loop with `break value` sites and no `else:`, whose natural completion is reachable, has type `Option[T]`. (§12.6.2)
1583. In a break-value loop without `else:` whose natural completion is reachable, each `break value` is auto-wrapped to `Some`; the user writes the bare value: `break item.id` yields `Some(item.id)`. (§12.6.2)
1584. In a break-value loop without `else:`, natural completion produces `None`. (§12.6.2)
1585. A loop with both `break value` sites and an `else:` has type `T`, the unified type of all break sites and the `else:` expression. (§12.6.2)
1586. All `break value` expressions in one loop body must produce values of the same type or be unifiable, together with the `else:` expression when present: `break 42` vs `break "hello"` ✗. (§12.6.3)
1587. A failed unification among `break value` sites and the `else:` clause is reported as a type error at the conflicting break or else site. (§12.6.3)
1588. If every path through the body diverges (`break value`, `return`, `panic`, or other diverging operation), the loop's natural completion is unreachable. (§12.6.4)
1589. When a loop's natural completion is provably unreachable (§12.6.4), the natural-completion arm unifies away via `never` (§8.2.2) and a loop with `break value` sites has type `T`, not `Option[T]`, regardless of any `else:`; this collapse is required, not implementation-discretionary. (§12.6.4)
1590. A loop with no `break value` and provably-unreachable natural completion (e.g. `while true:` with no `break`) yields no value on any path and has type `never`. (§12.6.4)
1591. The loop's expression value is determined by `break value` and `else:` only; `continue` does not contribute to it. (§12.6.5)
1592. The stdlib `Iterator` trait declares `type Item`, `type Source = ()`, and `fn next(own iter: Subject, source: Source) -> (Option[Item], Subject)`. (§12.7)
1593. `Iterator::next` consumes the iterator and returns the next item — `None` when iteration is complete — together with the advanced iterator, which the caller binds for the next call. (§12.7)
1594. `Item` and `Source` are borrow-equivalent associated-type slots per §3.1.2; consuming semantics require opting in via `type Item = own T` or `type Source = own T`. (§12.7)
1595. The associated-type slot convention is uniform across all traits; stdlib types get no special treatment. (§12.7)
1596. `Source` is the external value the iterator reads from on each `next` call; the choice of pattern is the iterator author's, expressed by declaring `Source`. (§12.7)
1597. Self-contained iterators (Range, counters, infinite generators) hold all state internally, use the default `type Source = ()`, and ignore the unit argument on each `next` call. (§12.7)
1598. Source-bearing iterators hold only cursor state and declare `type Source = TheCollection`, receiving the collection borrow-equivalently on every `next` call. (§12.7)
1599. Linear-ownership compilation triggers when the iterator type is statically known (monomorphized), the binding is a single `mut` location, and `NewIter` is rebound to that same location in a single statement with no other reference to the consumed binding. (§12.7.2)
1600. The for-loop's internal desugaring satisfies the linear-ownership conditions by construction: one statement calls `next`, destructures the returned tuple, and rebinds the iterator location. (§12.7.2)
1601. Under linear-ownership compilation, `next` receives a pointer to the iterator's state, mutates the cursor in place, and returns only the item; the consumed and returned iterator are the same memory location and no copy occurs. (§12.7.2)
1602. Linear-ownership compilation of `Iterator::next` is a required property of conforming implementations, not an optional optimization. (§12.7.2)
1603. The linear-ownership rule is the for-loop instance of the general consume-and-produce storage-reuse guarantee of §11.11.2. (§12.7.2)
1604. A self-contained `Iterator` implementation declares `type Source = ()` — or omits it for the default — and its `next` ignores the source parameter. (§12.7.3)
1605. An `Iterator` implementation advances its cursor by rebinding the consumed iterator to a local `mut` (per §11.7.3), mutating it, and returning it alongside the item: `mut local = iter`. (§12.7.3)
1606. An iterator cannot hold a borrow-equivalent reference to its collection: record fields are owned (§11.11), so a source-bearing iterator carries only a cursor. (§12.7.4)
1607. The `source` parameter of `next` aliases the caller's collection borrow-equivalently for the duration of one call. (§12.7.4)
1608. The stdlib `Iterable` trait declares `type Iter: Iterator` and `fn iterator(value: Subject) -> Iter` with `where Iter.Source = Subject`. (§12.8)
1609. The `where Iter.Source = Subject` constraint binds the iterator's source type to the iterable itself, so the for-loop supplies the original value as the source on each `next` call. (§12.8)
1610. `Iterable::iterator` takes its parameter by the default borrow-equivalent convention; the returned iterator is rooted in the value's cluster. (§12.8)
1611. The `Iterable` construction method is named `iterator`, not `iter`, per the full-name convention. (§12.8.1)
1612. The iterator returned by `Iterable::iterator` has a lifetime bounded by the for-loop expression. (§12.8.2)
1613. A type implements `Iterable` by declaring `type Iter` and an `iterator` method; `for x in d:` then dispatches to the implementation automatically. (§12.8.3)
1614. The stdlib `IntoIterable` trait declares `type Iter: Iterator` and `fn consuming_iterator(own value: Subject) -> Iter` with `where Iter.Source = ()`. (§12.9)
1615. `IntoIterable::consuming_iterator` declares its parameter `own`: the source is consumed at the call and the returned iterator owns the source's storage. (§12.9)
1616. The `where Iter.Source = ()` constraint binds the iterator's source to unit; the iterator reads from its own internal state, not an external parameter. (§12.9)
1617. The `IntoIterable` construction method is named `consuming_iterator`, signalling ownership transfer per the full-name convention. (§12.9.1)
1618. Under `IntoIterable`, each `next` call on a non-`Copy` `Item` physically moves one element out of the iterator's internal storage. (§12.9.2)
1619. Under `IntoIterable`, each `next` call on a `Copy` `Item` yields a copy of the element. (§12.9.2)
1620. When a consuming iterator is dropped at loop exit — including early exit via `break` or enclosing return — remaining unyielded elements are dropped and the underlying buffer is released. (§12.9.3)
1621. The exact `Drop` mechanism for non-`Copy` elements is specified in §14.7. (§12.9.3)
1622. For `Copy` types, drop is a no-op. (§12.9.3)
1623. A type implements `IntoIterable` by declaring `type Iter` and a `consuming_iterator` method; `for own x in d:` then dispatches automatically, consuming `d`. (§12.9.4)
1624. A user-defined type may implement `Iterable`, `IntoIterable`, both, or neither. (§12.9.5)
1625. If a type implements only `Iterable`, the `for own` form on it is a compile error. (§12.9.5)
1626. If a type implements only `IntoIterable`, the default `for` form on it is a compile error. (§12.9.5)
1627. There is no reclaim after consumption: once `consuming_iterator(move v)` is called, `v` is consumed and its elements are either yielded to the body or dropped with the iterator. (§12.9.5)
1628. Guidance: a caller that needs the source after iteration uses the default `for` form, or restructures to consume-and-rebuild through a transforming function. (§12.9.5)
1629. Stdlib provides `Iterable` and `IntoIterable` implementations for built-in iterable types using the same mechanism available to user code; no language privilege applies to stdlib iterators. (§12.10)
1630. Stdlib collections such as `Vec[T]` and `HashMap[K, V]` implement both traits with container-specific iterator types following the source-bearing pattern of §12.7.4. (§12.10)
1631. Stdlib iteration implementations obey the orphan rule (§3.7): user code cannot redefine `Iterable` for `Vec`. (§12.10)
1632. Arrays `T[N]` implement both `Iterable` and `IntoIterable`; the user picks the form at the call site. (§12.10.1)
1633. While an array is iterated under the default form, indexed writes are forbidden per §11.9.2: `arr[i] = v` ✗. (§12.10.1)
1634. Indexed reads of an array are allowed during default-form iteration: `arr[i]` ✓. (§12.10.1)
1635. Ranges and their iterators are stack-allocated; range iteration has no heap overhead. (§12.10.2)
1636. A conforming implementation must compile monomorphized loops over built-in iterables to machine code equivalent to hand-written indexed loops; this is a required property, not an optimization. (§12.11)
1637. The required loop code has no heap allocation, no iterator-object lifecycle overhead, and no per-iteration function-call cost: the cursor is held in registers and `next` is inlined. (§12.11)
1638. The iteration variable may be a pattern that destructures each yielded value: `for (id, name) in pairs:`. (§12.12.1)
1639. Iteration-variable patterns follow the pattern rules of §6.2.4 and §9.2.2. (§12.12.1)
1640. Refutable patterns are not permitted as iteration variables; the pattern must bind successfully for every yielded value. (§12.12.1)
1641. There is no `for pattern if guard in iterable:` inline-filter form; filtering is written with `continue` in the body. (§12.12.1)
1642. Trait method bodies, including default bodies in trait declarations (§3.1.3), may contain loops under the standard mutation and ownership rules. (§12.12.2)
1643. A generic function body containing a loop is type-checked at definition per §2.2.2. (§12.12.3)
1644. A generic loop's iterable type must satisfy `Iterable` (default form) or `IntoIterable` (`for own` form) at the call site for each monomorphization. (§12.12.3)
1645. Associated-type constraints use `.` member-access notation: `fn total[T: Iterable](source: T) -> T.Iter.Item`. (§12.12.3)
1646. A `derived` expression's body may contain loops; each evaluation of the derived runs the loop fresh. (§12.13)
1647. A loop's local mutations inside a derived body are not observable outside the evaluation; only the derived's final value enters the reactive graph. (§12.13)
1648. The collection or range iterated in a derived body may itself be reactive; each update of that value re-evaluates the derived and re-runs the loop. (§12.13)
1649. A `while` condition may depend on reactive values, but reactive updates do not interrupt an in-progress loop iteration. (§12.13)
1650. An empty iterable (e.g. `0..0` or an empty container) produces no iterations: the body does not execute and the expression value follows the §12.6.2 table. (§12.14.1)
1651. An iterator whose `next` always returns `Some(_)` produces an infinite loop; there is no language-level prevention, and responsibility lies with the iterator implementation. (§12.14.2)
1652. `break` inside the body is the user's mechanism for terminating a loop over a never-completing iterator. (§12.14.2)
1653. `Iterator::next` is pure in the type-system sense; the cursor advance across invocations (via consume-and-return) does not violate purity. (§12.14.3)
1654. Iterators must not perform externally observable I/O. (§12.14.3)
1655. Externally-driven sequences belong to the reactive system's signals (§13); iterators are for collection traversal. (§12.14.3)

## Reactive Model & Principles

1656. Ordinary computation is pure and immutable; change is confined to two contexts: local mutation within a function body and the reactive system. (§13)
1657. The reactive expression forms are `observe` and `where`. (§13)
1658. The reactive composition constructs are `node`, `connection`, parts, `operator`, and `effect`. (§13)
1659. A reactive graph is built declaratively from `signal`, `attr`, `recurrent`, `derived`, `node`, and `connection` declarations. (§13.1)
1660. Placement syntax constructs reactive instances. (§13.1)
1661. Composition is structural: the reactive graph's shape is known at compile time. (§13.1)
1662. The set of instances — signals, attrs, recurrents, nodes, connections — is created at startup and fixed for the lifetime of the runtime instance. (§13.1)
1663. Hot reload is an exception to the fixed instance set: it replaces the program source and applies a diff atomically. (§13.1)
1664. `repeat` is the explicit dynamic-lifecycle construct: it materializes and drops keyed scopes per a runtime source. (§13.1)
1665. Conditional activation never adds or removes instances: a gated instance is constructed unconditionally and merely frozen while inactive, never unmounted. (§13.1)
1666. Gates govern only activation; runtime-varying existence is exclusively `repeat`'s domain. (§13.1)
1667. Topology — the wiring among existing instances — may change at runtime. (§13.1)
1668. A connection's `to` may be a reactive node reference that re-points over time; the connection instance persists across re-pointing. (§13.1)
1669. Re-pointing a connection's destination creates or destroys no instance; only the wiring moves. (§13.1)
1670. A function or cell can only yield a node that was already placed, so every node reference resolves within the statically-known instance set. (§13.1)
1671. Endpoint typing and topology-cycle analysis are performed on the candidate topology — every node type a `to` could resolve to — not on a single frozen wiring. (§13.1)
1672. Reactive expressions (derived declarations, attr default expressions, recurrent expressions) are pure expressions over signal, attr, recurrent, and derived values. (§13.1)
1673. Reactive expressions contain no `mut` bindings, no loops, and no statement-level imperative constructs. (§13.1)
1674. Imperative work needed by a reactive expression is delegated to a call of a pure function, which may use `mut` internally. (§13.1)
1675. Signal and attr writes mark dependent cells dirty without immediate recomputation. (§13.1)
1676. `runtime.commit()` evaluates the dirty set in topological order. (§13.1)
1677. `runtime.commit()` advances triggered recurrent cells per their expressions in lockstep. (§13.1)
1678. `runtime.commit()` atomically publishes the new snapshot. (§13.1)
1679. Writes accumulate between commits; one commit processes their union. (§13.1)
1680. derived↔derived expression cycles are forbidden. (§13.1)
1681. Recurrent self-reference and recurrent↔recurrent cross-reference are permitted in reactive expressions. (§13.1)
1682. A topology cycle among nodes via connections is valid only if it traverses at least one connection type satisfying the `Circularity` trait. (§13.1)
1683. Reactive cells (signal, attr, recurrent, derived) may hold values of any type. (§13.1)
1684. The runtime stores a cell's value directly in-cell when it fits the platform atomic word, otherwise via index-based pool storage. (§13.1)
1685. Imperative data structures (`Vec`, `HashMap`, etc.) are first-class as values held inside reactive cells via pool storage. (§13.1)
1686. Cell content is governed by ordinary type rules; the reactive system governs composition and propagation. (§13.1)
1687. `fn` is pure compute and is reactive-transparent. (§13.1)
1688. `operator` is a stateful reactive transform from cells to cells, pure with respect to outside reality. (§13.1)
1689. `node` and `connection` carry topology; the runtime traverses the graph via `expose:`. (§13.1)
1690. `stream` is the append-only reactive primitive for event-shaped flows. (§13.1)
1691. `effect` is the host-interpreted bridge for outside-world alignment, using the `desired:`/`observed:` reconciliation model. (§13.1)
1692. The reconciliation model subsumes request/response, long-lived resources, fire-and-forget, and event-stream shapes under one primitive. (§13.1)
1693. Hosts may interpret nodes of specific types but are not obliged to use nodes for outside-world alignment. (§13.1)
1694. A new effect type is added by declaring an `effect`; the host registers a reconciler for that effect type. (§13.1)
1695. A new topological participant is added by declaring a `node`; the host extends its interpreter for traversed node types. (§13.1)
1696. There is no built-in clock or tick primitive; hosts declare their own signal and write it at their own cadence: `signal tick: i64 = 0`. (§13.1.1)

## Reactive Declarations (Cells)

1697. There are exactly six reactive declaration kinds — `signal`, `attr`, `recurrent`, `derived`, `const`, `stream` — distinguished by who controls the value and how it changes. (§13.2)
1698. `signal`, `attr`, `recurrent`, and `derived` declare value-shaped reactive cells. (§13.2)
1699. `stream` declares an event-sequence-shaped reactive cell. (§13.2)
1700. `const` declares a compile-time constant. (§13.2)
1701. `signal name: Type = initial` declares a writable reactive cell whose initial value is supplied at the declaration: `signal mouse_x: i32 = 0`. (§13.2.1)
1702. After construction, a signal is written only through the host API. (§13.2.1)
1703. Ductus source has no syntactic form for assigning to a signal. (§13.2.1)
1704. Signals are reserved for externally-fed reactive inputs; they are never computed by Ductus source. (§13.2.1)
1705. A signal may be declared at module, node, or connection scope. (§13.2.1)
1706. A module-level signal is one cell shared program-wide; all references read the same cell. (§13.2.1)
1707. A node-level signal is per-instance: each placement of the node creates its own cell. (§13.2.1)
1708. A connection-level signal is per-instance per-connection: each placement of the connection creates its own cell. (§13.2.1)
1709. Signal semantics — host-written, not source-assignable, reactive — are identical across all three scopes. (§13.2.1)
1710. A signal's scope determines only instance multiplicity and how the host addresses the signal when writing. (§13.2.1)
1711. `attr` declares a writable reactive cell that is per-instance of its enclosing node or connection type. (§13.2.2)
1712. Attrs are written only through the host API or at placement time. (§13.2.2)
1713. An attr with a default (`attr name: Type = default`) may be overridden at placement but need not be: `attr method: string = "GET"`. (§13.2.2)
1714. An attr without a default (`attr name: Type`) must receive a value at every placement of the enclosing type: `attr url: string`. (§13.2.2)
1715. Omitting a defaultless attr's value at placement is a compile error. (§13.2.2)
1716. A required attr's placement value may come via the attribute clause, a flag, or the `/expr` slot when the attr is the type's `default attr`. (§13.2.2)
1717. A default expression is evaluated to provide the initial value when an instance is constructed without an explicit value for that attr. (§13.2.2)
1718. An attr default may reference previously-declared attrs of the same node; declaration order is significant: `attr resonance: f32 = cutoff_hz / 1000.0`. (§13.2.2)
1719. An attr default may be a constant expression, an expression over other declared attrs, or an expression over signals visible in scope. (§13.2.2)
1720. At placement, a defaulted attr may be overridden by supplying a value: `Filter f1 | cutoff_hz=500.0`. (§13.2.2)
1721. A defaultless attr's cell receives the placement-supplied value at construction and is reactive from then on, exactly as if the value were a default. (§13.2.2)
1722. A node or connection type may designate one attr as its positional default by prefixing the declaration with `default`: `default attr message: string`. (§13.2.2.1)
1723. A `default attr` is a regular attr in every respect (writable, overridable at placement, may carry a default value) plus being the target of positional `/expr` at placement. (§13.2.2.1)
1724. The positional and named forms are equivalent and interchangeable: `Log /"Hello World"` is `Log | message="Hello World"`. (§13.2.2.1)
1725. At most one `default attr` per type; declaring two is a compile error. (§13.2.2.1)
1726. Only `attr` declarations may carry the `default` marker; `recurrent`, `derived`, `const`, and `signal` cannot. (§13.2.2.1)
1727. `/expr` binds the placed type's default attr uniformly for nodes and connections: `Drives/0.8: some_car`. (§13.2.2.1)
1728. A connection's destination is supplied separately in the placement body; it is not an attr and is never targeted by `/expr`. (§13.2.2.1)
1729. `derived name: Type = expression` declares a read-only reactive value defined by an expression: `derived is_aggressive: bool = risk_tolerance > 0.7`. (§13.2.3)
1730. A derived re-evaluates, under the lazy-batched rules, when any signal, attr, recurrent, or derived its expression reads changes. (§13.2.3)
1731. A derived's expression is pure: no `mut`, no loops, no statements. (§13.2.3)
1732. Reads of signals, attrs, recurrents, and other deriveds inside a derived expression create reactive dependencies. (§13.2.3)
1733. Derived expressions may include arithmetic and comparison operations on reactive and non-reactive values, field accesses and indexed reads, `match`, and `if`/`else`. (§13.2.3)
1734. Function calls are permitted in derived expressions; functions are reactive-transparent. (§13.2.3)
1735. Closure construction is permitted in derived expressions; the closure captures values at construction time. (§13.2.3)
1736. A derived's dependency set is its provenance: the reactive cells its expression reads, including transitively through function calls. (§13.2.3)
1737. When any cell in its dependency set changes, a derived becomes dirty and recomputes at the next commit. (§13.2.3)
1738. A derived may be declared at module level (one shared cell), node level (per-instance), or connection level (per-instance per-connection). (§13.2.3.1)
1739. Module-level deriveds are referenced by bare name, with no `here::` prefix. (§13.2.3.1)
1740. A module-level derived's initial value may reference only cells visible at module scope, per the topological-init rule. (§13.2.3.1)
1741. `recurrent[N]? name: Type? = expression` declares a per-instance reactive cell whose expression may reference its own past values. (§13.2.4)
1742. The optional `[N]` declares the cell's self-history depth, bounding `name.past(k, fallback)` accesses. (§13.2.4)
1743. `[N]` must be a compile-time-known positive `usize`: a literal, a `const`, or a const-generic parameter. (§13.2.4)
1744. Omitting `[N]` defaults the self-history depth to `[1]`, leaving only `.previous` accessible. (§13.2.4)
1745. A recurrent's name is a snake_case identifier. (§13.2.4)
1746. A recurrent's type annotation is optional when inferable from the expression's result type and the fallbacks given to `.previous`/`.past`. (§13.2.4)
1747. A recurrent's expression defines the cell's value at every moment; prior self-values are readable only via `.previous(fallback)`/`.past(k, fallback)`. (§13.2.4)
1748. `.previous(fallback)`/`.past(k, fallback)` return the fallback while the accessed cell has had fewer than `k` commits: on the first commit `counter.previous(0)` is `0`. (§13.2.4)
1749. A fallback's type must match the accessed cell's element type. (§13.2.4)
1750. By default, a gated-off recurrent holds its last committed value and resumes from pre-gap history on reactivation. (§13.2.4)
1751. `@reset_on_reopen` on a recurrent clears the cell's self-history and input-history when the gating predicate transitions false→true. (§13.2.4)
1752. After a `@reset_on_reopen` clear, the next evaluation's `.previous`/`.past` return their fallbacks, exactly as at initialization. (§13.2.4)
1753. `@reset_on_reopen` fires only on the gate false→true transition; mere absence of triggers never resets history. (§13.2.4)
1754. On a gated stream consumer (an operator or derived reading a stream), `@reset_on_reopen` resets the consumer's cursor on resume to the current head, discarding the gap's backlog. (§13.2.4)
1755. While frozen, a `@reset_on_reopen` consumer of a gate-policy stream source releases its buffer hold, neither pinning the buffer nor back-pressuring producers. (§13.2.4)
1756. `@reset_on_reopen` acts only on gated instances; on a never-gated instance it never fires and is harmless, not an error. (§13.2.4)
1757. Triggers are implicit: a recurrent re-evaluates whenever any cell it references — other than via `.previous`/`.past` on itself — commits a new value: `counter.previous(0) + step_value` re-evaluates on `step_value`. (§13.2.4)
1758. A recurrent whose expression contains only self-references evaluates once and freezes; this is valid behavior, not an error. (§13.2.4)
1759. A trigger that contributes no value to the expression (e.g., a pure clock signal) requires an explicit `observe` arm. (§13.2.4)
1760. Explicit triggers are written by making the recurrent's expression an `observe` block. (§13.2.4)
1761. `@reset_on_reopen` is a decorator written on its own line directly above the `recurrent` declaration it modifies. (§13.2.4)
1762. Recurrents re-evaluating in one commit advance in lockstep: each triggered recurrent's expression reads the previous-committed values of all recurrent cells, including other triggered ones. (§13.2.4.1)
1763. All recurrents advanced in a pass commit together at the end of the pass; none sees another's just-advanced value within the pass. (§13.2.4.1)
1764. A recurrent's own `.previous`/`.past` always return previously-committed values; the value being computed in the current commit is never visible through them. (§13.2.4.1)
1765. Recurrents whose expressions did not re-evaluate in a pass do not advance and retain their existing values. (§13.2.4.1)
1766. A re-evaluated recurrent's new value is a pure function of the previous-committed values and the inputs received during the pass. (§13.2.4.1)
1767. `attr` cells change only when the host writes via `runtime.write_attr`; the runtime never advances them automatically. (§13.2.4.2)
1768. The host cannot directly write a recurrent cell at runtime; control is indirect, via the signals and attrs the recurrent's expression reads. (§13.2.4.2)
1769. `cell.previous(fallback)` is sugar for `cell.past(1, fallback)`. (§13.2.4.3)
1770. Self-history `name.past(k, fb)` reads the recurrent's own value `k` commits ago, with `k` bounded by the declared `[N]` depth (default 1): `recurrent[2] fib: i32 = fib.past(2, 0) + fib.past(1, 1)`. (§13.2.4.3)
1771. Self-history access with `k > N` is a compile error. (§13.2.4.3)
1772. Input history: `.past(k, fb)` on any reactive cell referenced in the expression reads that input's value `k` commits ago: `input.past(2, 0.0)`. (§13.2.4.3)
1773. The compiler allocates per-input history equal to the maximum `k` observed in `.past` calls on that input. (§13.2.4.3)
1774. Inputs never accessed via `.past` add no history overhead. (§13.2.4.3)
1775. Recurrent history and stream-recurrent history are the same mechanism; signals contribute commits where streams contribute events. (§13.2.4.3)
1776. History depth `k` must be a compile-time-known positive `usize` (literal, `const`, or const-generic parameter); runtime or reactive values are rejected. (§13.2.4.3)
1777. Each `.previous`/`.past` access is an ordinary function call. (§13.2.4.3)
1778. Multiple `.previous`/`.past` calls on the same cell with different fallbacks are independent; each returns its own fallback when history is absent. (§13.2.4.3)
1779. Bare references to the recurrent's own name in its expression are a compile error. (§13.2.4.3)
1780. Non-self cells are read by bare name for their current values; `.previous`/`.past` on those names access their history. (§13.2.4.3)
1781. A cell's commit fires downstream re-evaluation only when its value differs from its previously-committed value. (§13.2.4.4)
1782. Writing an unchanged value to a signal does not fire its dependents. (§13.2.4.4)
1783. Value-change gating applies uniformly to signal commits, attr writes, recurrent advancements, and derived re-evaluations. (§13.2.4.4)
1784. A recurrent that re-evaluates to the same value as its previous commit propagates no change downstream. (§13.2.4.4)
1785. Fire-on-every-event is expressed via a monotonically increasing counter signal; each increment changes the value, so dependents trigger every time. (§13.2.4.4)
1786. A recurrent may be declared at module level (one shared cell), node level (per-instance), or connection level (per-instance per-connection). (§13.2.4.5)
1787. Module-level recurrents are referenced by bare name, with no `here::` prefix. (§13.2.4.5)
1788. Multiple recurrents may share one expression evaluation via a tuple declaration: `recurrent (name1, name2): (Type1, Type2) = tuple_expression`. (§13.2.4.6)
1789. A tuple-coupled declaration creates one independent cell per component, each named and typed individually. (§13.2.4.6)
1790. The tuple expression must return a tuple of the same shape and types as the declared cells. (§13.2.4.6)
1791. All tuple cells advance atomically from a single evaluation; shared computation runs once, not once per cell. (§13.2.4.6)
1792. Each tuple cell has its own self-history accessor: `mean.previous(0.0)`, `variance.previous(1.0)`. (§13.2.4.6)
1793. The optional `[N]` on a tuple-coupled recurrent applies to every cell; all share the same depth. (§13.2.4.6)
1794. A tuple cell is read by its individual name, bare or `here::`-anchored. (§13.2.4.6)
1795. Lockstep holds across a tuple: during evaluation each cell's `.previous`/`.past` returns its previous-committed value. (§13.2.4.6)
1796. In the per-commit DAG, a tuple-coupled recurrent contributes one evaluation node with one output edge per cell, not independent evaluation nodes. (§13.2.4.6)
1797. A recurrent's expression may be an `observe` expression whose arms supply trigger sets and per-arm update expressions: `on reset: 0`. (§13.2.4.7)
1798. The active arm's expression may use `.previous`/`.past` to access the recurrent's history, like any recurrent body. (§13.2.4.7)
1799. Trigger sets in a recurrent's `observe` may be filtered explicitly via `where`. (§13.2.4.7)
1800. Recurrent cells may hold dynamic-size types as well as fixed-size types. (§13.2.4.8)
1801. Dynamic-size cell types include `Vec[T]` (persistent, structural sharing), `SmallVec[T, N]` (inline up to N, then heap), and `RingBuf[T, N]` (fixed-capacity ring buffer). (§13.2.4.8)
1802. Cell storage and cost details are delegated to the cell-types-and-storage rules of §13.12.4. (§13.2.4.8)
1803. A recurrent expression returns a new value of the declared type; the runtime handles allocation and snapshot management transparently. (§13.2.4.8)
1804. Source code never mutates a cell in place; the functional builder API (`.with(value)`, `+`) returns new collection values. (§13.2.4.8)
1805. A `[N]` depth allocates `N` history slots per cell; for dynamic-size types total memory is proportional to `N * average_value_size`. (§13.2.4.8)
1806. The compiler may warn when the static history-size product exceeds a configurable threshold. (§13.2.4.8)
1807. `const name: Type = value` declares a compile-time constant associated with a node or connection type: `const kind: string = "@action/log"`. (§13.2.5)
1808. A `const` is not reactive and not per-instance: its value is identical for every instance of the declaring type and fixed at compile time. (§13.2.5)
1809. A const's right-hand side must be evaluable at compile time; it may reference other consts (same-type or top-level), literals, and any compile-time-evaluable expression. (§13.2.5.1)
1810. A const's right-hand side may not reference reactive cells (signals, attrs, recurrents, deriveds). (§13.2.5.1)
1811. A const occupies no reactive cell and does not participate in dirty propagation. (§13.2.5.1)
1812. Consts may hold complex values — records, arrays, tuples, nested structures — because the single-cell reactive-buffer constraint does not apply to them. (§13.2.5.1)
1813. Placement bodies cannot override a const; attempting to set a const at placement is a compile error. (§13.2.5.1)
1814. The host API has no `write_const`; consts are immutable for the runtime's lifetime. (§13.2.5.1)
1815. Inside the declaring type's reactive expressions, a const is read instance-level by bare name or anchored as `here::<const>`. (§13.2.5.2)
1816. A const is readable through an instance reference as `<instance>.<const>` from function bodies or other instances' bodies. (§13.2.5.2)
1817. A const is readable at type level without any instance as `<TypeName>::<const>`: `Log::type`. (§13.2.5.2)
1818. Type-level const reads work through generic type parameters: `T::type` inside `fn tag_for[T: Action](_: T) -> string`. (§13.2.5.2)
1819. Within a node or connection body, a const's value expression may reference previously-declared consts of the same body, in declaration order. (§13.2.5.3)
1820. A const referencing a later-declared const of the same body is a compile error. (§13.2.5.3)
1821. Initialization has exactly two phases: compile-time resolution of consts, then a startup pass for reactive cells. (§13.2.6)
1822. The startup pass initializes all reactive cells in topological order over their init-time read dependencies, with no separate serialized steps for signals, attrs, recurrents, and deriveds. (§13.2.6)
1823. Top-level consts resolve in declaration order at compile time; their values are embedded in the compiled artifact with no runtime computation. (§13.2.6)
1824. Per-type consts resolve at compile time and are never allocated reactive cells. (§13.2.6)
1825. The init-time dependency graph has one node per reactive cell, with edges from each cell to the cells its initial-value expression reads. (§13.2.6)
1826. Signals evaluate their `= initial` expression during the startup pass. (§13.2.6)
1827. An attr is initialized when its containing instance is placed. (§13.2.6)
1828. An attr whose placement supplies an explicit value — via the attribute clause, a flag, or `/expr` for the default attr — evaluates and stores that value. (§13.2.6)
1829. An attr with no placement-supplied value evaluates its declared `= default` expression. (§13.2.6)
1830. An attr with neither a declared default nor a placement value is a compile error caught before startup. (§13.2.6)
1831. Recurrents evaluate their expression for the first time during the startup pass. (§13.2.6)
1832. During startup evaluation, `.previous(fallback)` and `.past(k, fallback)` return their fallback values, since no committed history exists yet. (§13.2.6)
1833. An init-time read of a `Signal[T]` cell returns the cell's value at the topological-init evaluation point — a snapshot at startup. (§13.2.6)
1834. The snapshot-at-startup read semantic also applies to attrs and signals whose initial expressions read other reactive cells. (§13.2.6)
1835. After startup, a recurrent re-evaluates when any non-self reference in its expression commits a new value. (§13.2.6)
1836. Deriveds evaluate their expression body during the startup pass. (§13.2.6)
1837. `when` predicates are evaluated within the same topological order as other cells, establishing each instance's initial gate state at startup. (§13.2.6)
1838. An instance whose `when` evaluates false at the end of startup begins inactive, its other cells holding their just-computed initial values. (§13.2.6)
1839. An initial-value expression may read any reactive cell visible in scope, regardless of declaration kind; the topological sort resolves ordering with no artificial kind-order constraint. (§13.2.6)
1840. Init-time dependency cycles are compile errors; init time has no notion of a previous commit to break them. (§13.2.6)
1841. Within a node body, an attr's default or a recurrent's expression may reference previously-declared cells of the same body. (§13.2.6)
1842. The compiler may permit forward references within a body when the init dependency graph is well-defined. (§13.2.6)
1843. At type-declaration time, attr defaults and recurrent expressions may reference same-instance cells (bare name or `here::X`), same-type consts, module-level cells, and compile-time-evaluable expressions. (§13.2.6)
1844. Cross-instance references are resolved only at placement time, never at type declaration. (§13.2.6)
1845. A recurrent's self-history fallback expressions (`.previous(fallback)`, `.past(k, fallback)`) are evaluated in the same reference context as the recurrent's own expression. (§13.2.6)
1846. A trap during any initial evaluation (signal initializer, attr default, first recurrent commit, initial derived evaluation) aborts the process; startup traps have no recovery path. (§13.2.6)
1847. An initial value referencing a cell on another instance adds cross-instance edges to the init dependency graph. (§13.2.6)
1848. Init cycles spanning instances are compile errors at the same severity as within-instance init cycles, identifying the participating instances and cells. (§13.2.6)
1849. Ductus source has no syntactic form for assigning to a signal, attr, recurrent, derived, or const after declaration; source expressions only read them. (§13.2.7)
1850. Host-driven writes occur only through `runtime.write_signal`, `runtime.write_attr`, and `runtime.transaction`. (§13.2.7)
1851. The host cannot directly write recurrents, deriveds, or consts at runtime; influence is indirect via signals and attrs. (§13.2.7)
1852. Placement-time initial values may set attrs and recurrents; consts are not settable at placement. (§13.2.7)
1853. The runtime itself writes a derived's output cell when it evaluates the derived's expression. (§13.2.7)
1854. The runtime itself commits a recurrent's computed value at the end of the commit cycle. (§13.2.7)
1855. The no-source-level-write rule applies uniformly to all six declaration kinds. (§13.2.7)
1856. `Signal[T]` is the umbrella type over the three value-cell subkinds — signal, derived, recurrent — whose value type is `T`. (§13.2.8)
1857. Streams and sinks are reactive cells but are not `Signal[T]`. (§13.2.8)
1858. `Cell[T]` is the broader abstraction over all reactive cells, including streams and sinks. (§13.2.8)
1859. `Signal[T]` is a first-class type usable in parameter positions, return types, and generic arguments. (§13.2.8)
1860. `signal X = init` declares the host-writable `Signal[T]` subkind, written via `runtime.write_signal`. (§13.2.8)
1861. `derived X = expr` declares the projected `Signal[T]` subkind whose value the runtime keeps consistent with its inputs. (§13.2.8)
1862. `recurrent[N]? X: T = expression` declares the memoryful `Signal[T]` subkind with self-history via `.previous(fallback)` and `.past(k, fallback)`. (§13.2.8)
1863. The `signal` keyword and the `Signal[T]` type are deliberately overloaded: the keyword declares only the writable subkind; the type covers all three subkinds. (§13.2.8)
1864. An operator parameter typed `Signal[T]` binds to a reactive cell at instantiation, allocating internal state tied to that cell. (§13.2.8)
1865. An operator return type `Signal[T]` represents the operator's output cell. (§13.2.8)
1866. A function parameter declared `x: T` receives a cell argument's current value, with reactive transparency. (§13.2.8)
1867. A function parameter declared `s: Signal[T]` receives the cell reference itself. (§13.2.8)
1868. Value-vs-reference cell passing has no call-site syntactic difference; resolution is by the function's declared parameter type. (§13.2.8)
1869. A `Signal[T]` received as a parameter is read-only; no source-level form writes through a `Signal[T]` reference. (§13.2.8)
1870. Generic functions and operators may abstract over `Signal[T]`'s value type: `operator passthrough[T](source: Signal[T]) -> Signal[T]`. (§13.2.8)
1871. Standard trait bounds apply to the value type: `Signal[T: Numeric]` requires `T` to satisfy `Numeric`. (§13.2.8)
1872. Field access on a `Signal[Record]` cell inside a derived projects the field, but the consumer re-evaluates whenever the record value changes in any field. (§13.2.8)
1873. A reactive composite is a record, tuple, or fixed-array binding whose fields or elements are independently reactive. (§13.2.9)
1874. Reactive composites are constructed in any reactive declaration context by binding individual fields or elements to reactive sources, static values, or reactive expressions. (§13.2.9.1)
1875. A record literal mixing reactive and static fields forms a reactive composite: `derived A = PeakResult(some_property: signal_a, some_regular_property: 15)`. (§13.2.9.1)
1876. A tuple literal mixing reactive and static elements forms a reactive composite: `derived t = (signal_a, 15, signal_b)`. (§13.2.9.1)
1877. A fixed-array literal mixing reactive and static elements forms a reactive composite: `derived arr: f32[4] = [signal_a, signal_b, 0.0, signal_c]`. (§13.2.9.1)
1878. Reactive-composite forms apply equally in `attr` declarations on node and connection instances. (§13.2.9.1)
1879. Reactive-composite use in `signal` and `recurrent` declarations is constrained by their host-write and expression-evaluation semantics respectively. (§13.2.9.1)
1880. A reactive composite is a structural grouping with no outer cell of its own. (§13.2.9.2)
1881. When a constituent cell updates, only that field is dirty; consumers reading other fields through the same composite are not invalidated. (§13.2.9.2)
1882. A composite field bound to a bare reactive name (signal/attr/derived/recurrent) becomes an alias to that cell; no new cell is allocated. (§13.2.9.3)
1883. A composite field bound to a reactive expression becomes an implicit derived cell. (§13.2.9.3)
1884. A composite field bound to a literal or compile-time constant is static: no cell, embedded constant. (§13.2.9.3)
1885. A composite field bound to a non-reactive value expression is static, evaluated once at startup. (§13.2.9.3)
1886. An aliased field is its target cell for all purposes — cell identity, hot reload, reload-time type changes: with `some_property: signal_a`, `A.some_property` is `signal_a`. (§13.2.9.3)
1887. An implicit derived field allocates a fresh cell whose ID is its composite-field path, with dependency edges added to the graph specification: `some_property: signal_a * 2 + signal_b`. (§13.2.9.3)
1888. Implicit derived field expressions evaluate under rules identical to an explicit `derived` declaration of the same path. (§13.2.9.3)
1889. Static field values are embedded in the composite's lowered representation and contribute no cell entries to the graph specification. (§13.2.9.3)
1890. Composite cell paths use `<binding>.<field_name>` for records and `<binding>.<index>` for tuples and fixed arrays: `A.some_property`, `t.0`, `arr.3`. (§13.2.9.4)
1891. Aliased fields contribute no new cell entries to the graph specification; the alias target's existing entry is referenced and hot-reload identity follows the target. (§13.2.9.4)
1892. The composite binding name itself (`A`, `t`, `arr`) is a naming prefix, not a cell; it does not appear as a standalone cell in the graph specification. (§13.2.9.4)
1893. Composite field access reads the corresponding cell, or returns the embedded constant for static fields: `let r: i32 = A.some_regular_property` yields `15`. (§13.2.9.5)
1894. Whole-composite reads — passing to a composite-typed parameter, returning from a function, or binding to a composite-typed `let` — do not allocate a snapshot. (§13.2.9.5)
1895. Field reads of a composite-typed parameter resolve through to the caller's underlying cells: in `fn report(p: PeakResult)` called as `report(A)`, `p.some_property` is `A.some_property`. (§13.2.9.5)
1896. A `let` whose declared or inferred type is the composite's type aliases the same underlying cells; reads through it resolve to live current cell values, not a binding-time snapshot: `let q = p`. (§13.2.9.6)
1897. Standard scalar auto-deref still applies to single-cell reads from a composite: `let v: f32 = A.some_property`. (§13.2.9.6)
1898. A composite binding names runtime-held cells, not stack-owned data, so multiple live aliases to the same composite may coexist without violating single ownership. (§13.2.9.6)
1899. A materialized composite is a concrete record/tuple/array instance subject to standard ownership rules from the point of materialization on. (§13.2.9.6)
1900. Reactive composites stay live across Ductus source except at exactly three materialization boundaries: non-reactive collection storage, FFI/host handoff, and serialization/persistence. (§13.2.9.7)
1901. Pushing a composite into a non-reactive container stores a concrete snapshot of current per-field values; later cell changes do not propagate to the stored element: `vec.push(A)`. (§13.2.9.7)
1902. Any value crossing into the host via the Host API is materialized; host code never sees reactive cells. (§13.2.9.7)
1903. Hot-reload state save, debug dumps, and any explicit serialization path materialize composites to concrete values. (§13.2.9.7)
1904. The `with` expression applies to reactive composites without syntactic change; overrides in a reactive declaration context follow the per-field binding rules: `derived A2 = A with some_regular_property: signal_c`. (§13.2.9.8)
1905. A `with` result is a new reactive composite binding with its own per-field reactive shape; the base composite is unchanged. (§13.2.9.8)
1906. Each `with`-produced binding has its own cell IDs for any fields that contribute cells. (§13.2.9.8)
1907. A `with` override RHS is interpreted (alias, implicit derived, or static) by the binding form receiving the result, not by RHS syntax alone. (§13.2.9.8)
1908. A plain `let` of a `with` result that does not flow into a reactive declaration yields a concrete value, reading reactive RHS cells at evaluation time. (§13.2.9.8)
1909. Reactive composites are data-only: no placement, no node/connection topology participation, no lifecycle beyond their declaration, and no `recurrent` or behavioral content. (§13.2.9.9)
1910. A reactive composite's type is the underlying record, tuple, or fixed-array type: `PeakResult`, `(f32, i32, f32)`, `f32[4]`. (§13.2.9.10)
1911. There is no `reactive` type qualifier; the type system does not distinguish reactive composites from non-reactive values of the same type. (§13.2.9.10)
1912. Reactivity is a property of the binding (recorded in the graph specification) and its per-field cells, not of the type. (§13.2.9.10)
1913. A function over a composite type works identically with a reactive binding or a concrete value; live-vs-snapshot access is determined by the caller's context, not the function signature. (§13.2.9.10)
1914. A node or connection type is carried as a value via the `Type[...]` meta-type, the mechanism for an attr template slot deferring which kind a receiver places. (§13.2.10)
1915. Node and connection instances are never values; the type value is the storable stand-in. (§13.2.10)
1916. A `Type[...]` slot is filled by naming a type in value position, and the receiving node later places it: `ItemHost host | item_type=PostItem`. (§13.2.10)
1917. A `Type[...]` slot's constraint is an ordinary trait bound that doubles as the instantiation contract: the receiver may feed exactly the inputs the bound trait declares as required members. (§13.2.10)
1918. A `Type[...]` value carries no partial application; shipping a pre-configured node requires defining or parameterizing a type that bakes the configuration in. (§13.2.10)
1919. Any node or connection type satisfying constraint `C` is a valid value for a `Type[C]` slot. (§13.2.10.1)
1920. A bare `Type[Node]` admits any node type but is not placeable, since it carries no input contract. (§13.2.10.1)
1921. A `Type[SomeTrait]` whose trait declares required attrs/cells (and, for connections, endpoints) is placeable against exactly that contract. (§13.2.10.1)
1922. `default attr` plays no special role in `Type[...]` slots; a receiver may set a placed type's default attr with the `/expr` form when the constraint guarantees it exists. (§13.2.10.1)
1923. A `Type[...]` value held in an attr is a type, not an allocated instance; cells are allocated only when the receiving node places it. (§13.2.10.2)
1924. Placing a held type produces cells with paths under the receiving node's path (e.g., `host.item_type.<field>`), or one set per iteration when placed under a loop or `repeat`. (§13.2.10.2)
1925. Dropping the receiving node drops the cells of the types it placed. (§13.2.10.2)
1926. A `Type[...]` attr is an attr-shaped singular template slot, not a child-placement mechanism; cardinality, list semantics, and per-instance scoping belong to `parts:` with placement-time children and keyed scopes. (§13.2.10.2)
1927. A `Type[...]` value's structure cannot be read or evaluated in user expressions beyond what its constraint guarantees; it is consumed by receivers that place it. (§13.2.10.3)
1928. A `Type[...]` attr cannot be `mut` and cannot be written from Ductus source after the attr is set. (§13.2.10.3)
1929. References a placed type captures (e.g., to the receiver's exposed attrs via its placement name) are bound by reference and re-read on per-commit re-evaluation. (§13.2.10.3)
1930. Cell allocation for a placed type happens once at placement, preserving the static node set of the static-graph rule. (§13.2.10.3)
1931. Generic constraints on a `Type[...]` slot behave as standard generic bounds. (§13.2.10.3)
1932. A value conditional may select among `Type[C]` values of the same slot type — `match` yielding a `Type[C]` chooses one type, which the receiver places once, unlike `given`, which builds every arm's subtree. (§13.2.10.3)
1933. `observe` is a reactive expression form that selects an active arm by which trigger most recently fired and evaluates that arm's expression reactively. (§13.2.11)
1934. An observe arm is an `on` clause listing one or more trigger cells, an optional `where` filter, and a colon followed by the arm expression: `on T3 where C: expr_filtered`. (§13.2.11.1)
1935. A `default:` arm has no `on` clause; its expression supplies the observe's value while no `on` arm has yet activated. (§13.2.11.1)
1936. All observe arm expressions must produce the same type, as in `match` expressions. (§13.2.11.1)
1937. An observe expression's value is a `Cell[T]` whose concrete reactive type (`Signal[T]` or `Stream[T]`) is determined by the use context. (§13.2.11.1)
1938. An `observe` expression is written as `observe:` followed by an indented block of `on`-clause arms and an optional `default:` arm. (§13.2.11.1)
1939. An arm becomes a selection candidate when any cell in its `on` trigger set commits a new value (signal) or emits an event (stream). (§13.2.11.2)
1940. An arm's candidacy is filtered by its `where` clause when present. (§13.2.11.2)
1941. When multiple arms are candidates in the same commit, the first candidate in declaration order wins, mirroring `match` semantics. (§13.2.11.2)
1942. The winning arm becomes the active arm; a subsequent commit selecting a different arm changes the active arm. (§13.2.11.2)
1943. While an arm is active, its expression is fully reactive: any referenced cell is dependency-tracked and re-evaluates the arm — without requiring the `on` trigger to re-fire. (§13.2.11.3)
1944. An active arm's `on` trigger is also one of its reactive references, so re-firing the trigger re-evaluates the arm. (§13.2.11.3)
1945. When a different arm activates, the previous arm's references stop being tracked and the new arm's references become active. (§13.2.11.3)
1946. An observe's value can change in a commit where no `on` trigger fires; arm selection and intra-arm reactivity are independent concerns. (§13.2.11.3)
1947. A parenthesized multi-cell trigger set activates its arm when ANY listed cell fires — logical OR: `on (T1, T2, T3): expr`. (§13.2.11.4)
1948. All cells of a multi-cell trigger set are also reactive references of the arm while it is active. (§13.2.11.4)
1949. A `default:` arm, when present, must be the last arm in declaration order; a `default:` arm preceding an `on` arm is a compile error. (§13.2.11.5)
1950. In a signal context, the `default:` arm is required when every `on` arm's trigger set consists entirely of stream cells. (§13.2.11.5)
1951. The `default:` arm is optional when at least one `on` arm has a signal in its trigger set. (§13.2.11.5)
1952. A signal's initial value counts as its first emission for observe arm selection. (§13.2.11.5)
1953. At startup, the first signal-triggered arm in declaration order activates and supplies the observe's value. (§13.2.11.5)
1954. In a stream context, the `default:` arm is always optional; the stream may begin empty. (§13.2.11.5)
1955. In a context expecting `Signal[T]` — a `Signal`, `derived`, or `recurrent` binding — an observe produces `Signal[T]`. (§13.2.11.6)
1956. In a stream context, an observe produces the concrete stream type, `RingStream[T, N]` or `GateStream[T, N]`, per the stream context's policy and capacity. (§13.2.11.6)
1957. Type mismatch across observe arms is a compile error. (§13.2.11.6)
1958. `observe` may be the RHS of a `derived`, `signal`, `recurrent`, `recurrent[N] stream`, or `stream` declaration. (§13.2.11.7)
1959. `observe` may appear as a sub-expression inside a larger reactive expression, and anywhere a `Cell[T]` value is valid. (§13.2.11.7)
1960. `observe` may be a function-call argument; its reactive dependencies propagate through the reactive-transparent call site. (§13.2.11.7)
1961. Inside a recurrent declaration, observe arm expressions may access the enclosing recurrent's self-history via `.previous(fallback)` / `.past(k, fallback)`. (§13.2.11.7)
1962. An arm's `on` clause may carry a trailing `where` filter restricting activation: `on tick where counter.previous(0) < 100: counter.previous(0) + 1`. (§13.2.11.8)
1963. The arm-level `where` is the general stream filter, producing a filtered trigger `T where C` that the arm treats as an ordinary trigger cell. (§13.2.11.8)
1964. `T where C` emits only when `T` itself fires AND `C` evaluates true at that moment, sampling cells `C` references at their current values. (§13.2.11.8)
1965. A change to a cell referenced by `C` between `T` emissions does not make the filtered trigger emit. (§13.2.11.8)
1966. An active arm whose `where` later evaluates false stays active; `where` gates selection at trigger-emission moments, not continued activeness. (§13.2.11.8)
1967. An active arm is supplanted only when a different arm's (possibly filtered) trigger emits and that arm wins declaration-order selection. (§13.2.11.8)

## Nodes & Parts

1968. A node is a reactive entity: it holds values (attrs, recurrents), computes values (deriveds), and communicates with other nodes through typed connections. (§13.3.0)
1969. Each node type is a nominal type whose body declares its members; each placement of a node type creates an instance with its own cells. (§13.3.0)
1970. Containment places sub-nodes inside a parent as parts; the parent owns the parts and their lifetimes are bound to the parent's. (§13.3.0)
1971. Connections are typed directed links carrying their own reactive content, not passive pointers. (§13.3.0)
1972. The reactive graph is the running structure of all node instances and the connections between them; once constructed, its shape is fixed. (§13.3.0)
1973. Reactivity does not separate records from nodes — a record's fields can themselves be reactive (a reactive composite); placement and structure do. (§13.3.0)
1974. A record is a value that can exist anywhere in a program; a node exists only as a placed graph instance with its own graph identity, topology, lifecycle, and runtime-managed per-instance cells. (§13.3.0)
1975. A node type is declared `node TypeName[GenericParams]?:` with an indented body. (§13.3.1)
1976. A node body may contain `satisfies`, `parts:`, `incoming:`, `outgoing:`, `when:`, `expose:`, `const`, `signal`, `attr`, `default attr`, `recurrent[N]?`, `derived`, `stream`, and `effects:` items. (§13.3.1)
1977. All node body items are optional; a node with no attrs, deriveds, parts, or connections is legal. (§13.3.1)
1978. A node has at most one `default attr` (positional default): `default attr name: Type = default`. (§13.3.1)
1979. A node declares trait conformance with a `satisfies` clause: `satisfies Drivable`. (§13.3.2)
1980. Trait methods for a node are implemented in `fulfill` blocks: `fulfill Displayable for Driver:`. (§13.3.2)
1981. Functions on node instances are free functions taking the node type as a parameter, callable via uniform call syntax. (§13.3.2)
1982. The optional `parts:` clause constrains placement-time children: `parts: [dynamic] Type1 [cardinality]?, ...`. (§13.3.3)
1983. A node with no `parts:` clause accepts child instances of any node type. (§13.3.3)
1984. Without a `parts:` clause, the node body has no bulk access to children — only named access to children the body itself can see. (§13.3.3)
1985. With a `parts:` clause, only children whose types appear in the listed set are accepted. (§13.3.3)
1986. Part cardinality is enforced at placement: `parts: Oscillator+, Filter [=1], Amplifier?`. (§13.3.3)
1987. The `parts:` clause places no instances itself; it is the node's caller-facing part signature, bounding only what a caller supplies. (§13.3.3)
1988. A node's own emitted placements (compile-time `for` or `repeat`) are never counted against `parts:` and never appear in the `parts` namespace. (§13.3.3)
1989. Self-recursion is legal: a node may have parts of its own type when `parts:` is omitted or its own type appears in the clause. (§13.3.3)
1990. Self-recursive placements terminate: each placement is an explicit user act, and the compiler walks finite placement trees, not type recursions. (§13.3.3)
1991. A part type with no sigil and no bracket has cardinality `0..` (zero or more, unlimited). (§13.3.3.1)
1992. The `?` sigil means `0..=1` (optional): `Amplifier?`. (§13.3.3.1)
1993. The `+` sigil means `1..` (at least one): `Oscillator+`. (§13.3.3.1)
1994. The `!` sigil means exactly one, shorthand for `[=1]`. (§13.3.3.1)
1995. The bracket form `[=N]` means exactly N: `Filter [=1]`. (§13.3.3.1)
1996. The bracket form `[N..=M]` means between N and M, inclusive on both ends. (§13.3.3.1)
1997. The bracket form `[N..]` means at least N with no upper bound. (§13.3.3.1)
1998. The bracket form `[..=M]` means up to M with lower bound 0. (§13.3.3.1)
1999. A part type carries at most one cardinality specifier — sigil or bracket, not both; duplicate specifiers are a compile error. (§13.3.3.1)
2000. Sigils attach directly to the type name with no intervening whitespace: `Part?`, `Part+`, `Part!`. (§13.3.3.1)
2001. Bracket forms permit an optional space before the bracket: `Part[=1]` and `Part [=1]` are both valid. (§13.3.3.1)
2002. The `dynamic` prefix on a listed type marks runtime-varying supply mode, orthogonal to count: `parts: Header, dynamic Post`. (§13.3.3.1)
2003. `dynamic` is mutually exclusive with every cardinality specifier: `dynamic Post+` ✗. (§13.3.3.1)
2004. Runtime cardinality checks do not exist in the language. (§13.3.3.1)
2005. An unmarked part type is static: supplied only by explicitly written placements, with the count known per placement site at compile time. (§13.3.3.1)
2006. A `dynamic` part type may additionally be fed by a caller's `repeat`. (§13.3.3.1)
2007. The `dynamic` marker has the same meaning and exclusivity rule in `incoming:` and `outgoing:` clauses. (§13.3.3.1)
2008. For a static part type, `parts.<NodeType>` is a read-only array of node references whose length is an implicit const-generic fixed per placement site. (§13.3.3.2)
2009. The declared cardinality supplies compile-time bounds `min..max` that every placement site's static supply must satisfy. (§13.3.3.2)
2010. Indexing `parts.<NodeType>[i]` is legal iff `i < min_cardinality`: under `parts: Oscillator+`, `parts.Oscillator[0]` ✓, `[1]` ✗. (§13.3.3.2)
2011. `for o in parts.<NodeType>:` always works, unrolling as a fixed-extent array with each site's actual length. (§13.3.3.2)
2012. Whether codegen emits straight-line copies or a counted loop over the known list is an implementation choice. (§13.3.3.2)
2013. `parts.<NodeType>` is in placement order — a stable filter of the caller's written placement sequence that selects, never reorders. (§13.3.3.2)
2014. Every projection of the placement sequence in the language preserves placement order. (§13.3.3.2)
2015. Elements of `parts.<NodeType>` are references, so the value is transient under the ordinary borrow rules: usable in loops, indexing, and calls; storable nowhere. (§13.3.3.2)
2016. For a `dynamic` part type, `parts.<NodeType>` is a reactive cell, not an array; none of the static array forms apply. (§13.3.3.2)
2017. There is no whole-namespace parts iteration: bare `parts` is an exposition reference, not a value, and per-type access is the only body-side bulk form. (§13.3.3.2)
2018. A node type may emit child instances directly via a compile-time `for` written in its `expose:` block; the exposition is the only place structural output is emitted. (§13.3.3.3)
2019. A type-emitted `for` body is a placement-body block: any number of part and/or connection placements per iteration, under the ordinary clause-ordering and self-delimiting rules. (§13.3.3.3)
2020. A type-emitted `for` is compile-time-unrolled; its placements become children of the type itself, materialized by every instance at instantiation: `for i in 0..VOICE_COUNT: Oscillator | freq=base_freq(i)`. (§13.3.3.3)
2021. Type-emitted placements appear at the loop's written position in the exposition, with each iteration's loop variable substituted at compile time. (§13.3.3.3)
2022. The iterable of a type-emitted `for` must be compile-time-known; a runtime iterable is a compile error pointing at the iterable, with no new diagnostic class. (§13.3.3.3)
2023. A const-generic parameter may drive the emission count, making multiplicity a property of each instantiation: `node OscBank[const N: usize]:` then `OscBank[16] sixteen_bank`. (§13.3.3.3)
2024. A node's own emissions need no `parts:` entry; a type may emit children while declaring no `parts:` clause at all. (§13.3.3.3)
2025. Type-emitted children are internals: never counted against `parts:` cardinality and never in the `parts` namespace, which holds caller-supplied children only. (§13.3.3.3)
2026. Across hot reload, a changed compile-time iterable follows path-based cell identity: unchanged fully-qualified paths keep state, new loop indices allocate fresh, dropped indices are released per the standard removal rule. (§13.3.3.3)
2027. A type-emitted `for` expands once at type elaboration and applies uniformly to every instance. (§13.3.3.3)
2028. A placement-body `for` expands at each placement site and is caller supply: its placements count against the receiving type's `parts:` cardinality and appear in `parts.<NodeType>`. (§13.3.3.3)
2029. A type-emitted `for` may place connections sourced from the enclosing instance; they are self-sourced — exempt from `outgoing:`, subject to endpoint typing, and engaging at their written position in the unrolled sequence. (§13.3.3.3)
2030. A caller supplies a `dynamic` part type with `repeat` in the placement body: `repeat post in posts keyed by post.id: Post / post`. (§13.3.3.4)
2031. Feeding an unmarked (static) part type with a `repeat` is a compile error at the feeding site, naming the receiving clause. (§13.3.3.4)
2032. For a `dynamic` type, `parts.<NodeType>` is a reactive cell whose value is the current keyed, ordered collection of supplied children, updating as children mount and dismount. (§13.3.3.4)
2033. The dynamic parts cell is in source order: the written interleaving, with each feeding `repeat`'s scopes expanded in place. (§13.3.3.4)
2034. The dynamic parts cell's raw value is transient under the borrow rules: it cannot be bound, stored, or returned. (§13.3.3.4)
2035. The dynamic parts cell is consumed in exactly two ways: operators for values, `repeat` for structure. (§13.3.3.4)
2036. An operator over the dynamic cell yields an ordinary reactive value, storable and readable: `derived titles = parts.Post |> map(fn(p): p.title)`. (§13.3.3.4)
2037. The per-element closure of an operator over the cell receives each element reference transiently and must produce plain values. (§13.3.3.4)
2038. `repeat` over the dynamic cell is ordinary `repeat` over an ordinary reactive collection source: `repeat p in parts.Post:`. (§13.3.3.4)
2039. Keys are carried: each dynamically supplied element brings the key its feeding `repeat` derived, so scope identity is stable end-to-end with no `keyed by` written. (§13.3.3.4)
2040. The carried key is the first applicable path in standard key derivation; an explicit `keyed by` keeps its ordinary precedence. (§13.3.3.4)
2041. An explicitly written (static) placement of a `dynamic` type carries a stable, path-derived placement-site key. (§13.3.3.4)
2042. `for` cannot iterate a dynamic parts cell: `for` is compile-time and the set is not a compile-time fact. (§13.3.3.4)
2043. Indexing a dynamic parts cell is rejected: `parts.<NodeType>[i]` ✗ — a positional index into a keyed, changing set is not a stable identity. (§13.3.3.4)
2044. Key-addressing a dynamic parts cell is rejected: `parts.<NodeType>[key]` ✗ — keys belong to the supplier, and the receiving type cannot know or name them. (§13.3.3.4)
2045. A bulk `parts` reference in `expose:` carries dynamic children like static ones: each feeding `repeat`'s scopes appear at the repeat's written position, mounting and dismounting under Model B. (§13.3.3.4)
2046. When every supplier at a placement site is provably static, the compiler may unroll that site's `repeat` and operator applications; the developer still writes against the dynamic contract. (§13.3.3.4)
2047. The same marker, cell model, and consumption rules apply to `dynamic` connection types in `incoming:` and `outgoing:`. (§13.3.3.4)
2048. `incoming:` and `outgoing:` list the connection types in which instances may participate as endpoints, with optional cardinality: `incoming: [dynamic] ConnType [cardinality]?, ...`. (§13.3.4)
2049. `incoming` connections target the node (it is the `to` endpoint); `outgoing` connections originate from it (it is the `from` endpoint). (§13.3.4)
2050. `incoming:` bounds what others may direct at the node; `outgoing:` bounds what a caller placing the node may originate from it. (§13.3.4)
2051. Connection cardinality syntax is identical to parts cardinality, and bare defaults to unlimited `0..`: `outgoing: Drives [=1], MaintainedBy?`. (§13.3.4)
2052. A connection belongs to a node's incoming set as a static fact when both its presence and its destination are fixed at compile time. (§13.3.4)
2053. Membership stops being a static fact in exactly two ways: the connection is materialized per `repeat` key (existence varies), or its destination is a reactive reference or `Handle` (target varies). (§13.3.4)
2054. An unmarked (static) `incoming:` type admits only connections whose membership is a static fact. (§13.3.4)
2055. Static incoming cardinality is checked by exact local arithmetic: the compiler counts, per instance, the statically present caller-placed and self-sourced static-destination connections directed at it. (§13.3.4)
2056. A self-sourced connection into an own part counts at the part and, being always present, satisfies the part's lower bound. (§13.3.4)
2057. `incoming.<ConnType>[i]` for `i` below the guaranteed minimum is backed exactly by the statically present connections. (§13.3.4)
2058. A connection whose membership is not a static fact requires `dynamic <ConnType>` in the `incoming:` clause of every node type in its candidate envelope. (§13.3.4)
2059. The candidate envelope is: for a dynamic destination, every type the reference's static type admits; for a repeat-materialized connection, its destination's type. (§13.3.4)
2060. The dynamic-membership check runs at the connection placement site and names both sites on failure. (§13.3.4)
2061. A `dynamic` incoming type carries no cardinality and has no bound to check; its membership is runtime data tracked reactively. (§13.3.4)
2062. Caller-written `repeat` fan-out of connections sourced from a placed instance requires `outgoing: dynamic <ConnType>` on the instance's type. (§13.3.4)
2063. A merely re-pointing destination does not dynamize the source side: the connection statically exists at its source; only its target moves. (§13.3.4)
2064. Body access to connections is `incoming.<ConnType>` and `outgoing.<ConnType>`, bare or with the explicit anchor `here::incoming.<ConnType>`. (§13.3.4.1)
2065. The member namespaces `parts`, `incoming`, and `outgoing` share one model, each with a static face and a dynamic face. (§13.3.4.1)
2066. A static connection type reads as a read-only array of connection references with const-generic length bounded by the declared cardinality. (§13.3.4.1)
2067. Indexed connection access is legal iff the index is below the guaranteed minimum: under `outgoing: Drives [=1]`, `outgoing.Drives[0]` ✓. (§13.3.4.1)
2068. `for c in outgoing.<ConnType>:` iterates a static connection array, unrolling at compile time. (§13.3.4.1)
2069. A `dynamic` connection type reads as a reactive cell of the current member set, consumed only by operators and `repeat`: `incoming.Send |> map(fn(c): c.gain * c.from.value)`. (§13.3.4.1)
2070. A derived over a dynamic connection cell re-evaluates when members mount or dismount and when any member's read cells change. (§13.3.4.1)
2071. `repeat` over a dynamic connection cell mounts one scope per current member, mounting and dismounting with it: `repeat c in incoming.Send: Ack: c.from`. (§13.3.4.1)
2072. `incoming:` and `outgoing:` declare which connections a node participates in as an endpoint; they establish none. (§13.3.4.2)
2073. A node type establishes self-sourced connections — every instance the `from` endpoint — by placing them inline in its structural sequence as exposition entries, directly or within an exposition-level `for` or `repeat`. (§13.3.4.2)
2074. There is no separate clause for self-sourced connections; each is written at the position where it engages. (§13.3.4.2)
2075. A self-sourced placement uses ordinary connection placement syntax; the source is always the enclosing instance. (§13.3.4.2)
2076. Self-sourced destinations must be nameable from the type's scope; exactly three forms are admissible: an own part, a module-level instance, or a `Handle`-typed attr. (§13.3.4.2)
2077. An own-part destination — a named part or `parts.X[i]` within the guaranteed minimum — is static: it counts as static supply at the part and can satisfy the part's `incoming:` lower bounds. (§13.3.4.2)
2078. A module-level-instance destination is a fixed shared target and is static. (§13.3.4.2)
2079. A `Handle`-typed attr destination gives per-instance parameterized wiring, configured at placement: `attr wheel: Handle[Drivable]` with entry `Drives: wheel`. (§13.3.4.2)
2080. A self-sourced connection through a handle freezes while the handle resolves to `None`. (§13.3.4.2)
2081. With a handle destination, membership at the destination is not a static fact: every node type the handle's type admits must declare `incoming: dynamic <ConnType>`. (§13.3.4.2)
2082. A node type's `parts:`, `incoming:`, and `outgoing:` clauses are its caller-facing signature; its internals — every placement the type itself makes — are not bound by them and never appear in the signature namespaces. (§13.3.4.2)
2083. Internals are checked only by connection endpoint typing and topology-cycle analysis. (§13.3.4.2)
2084. A node is the caller of its own parts: internal wiring terminating at an own part counts against the part's `incoming:` budget, the same budget external callers draw from. (§13.3.4.2)
2085. At a module-level destination, every placed instance of the source type contributes one connection to the target's `incoming:` accounting. (§13.3.4.2)
2086. Destination contributions that are static facts count toward a static `incoming:` type's exact arithmetic. (§13.3.4.2)
2087. A destination contribution that is not a static fact — source instances materialized by `repeat`, or a `Handle` destination — requires the target to declare `incoming: dynamic` for that connection type, with the error naming both sites. (§13.3.4.2)
2088. A self-sourced connection is exempt from the source node's `outgoing:` clause, which governs caller-originated connections only. (§13.3.4.2)
2089. A self-sourced connection is not exempt from endpoint typing: its `from`/`to` types must match the connection's declaration. (§13.3.4.2)
2090. Self-sourced connections are a node-type concept: connection bodies remain minimal glue with no exposition of their own; a connection needing further structure is modeled with nodes. (§13.3.4.2)
2091. The total cardinality of a node — caller-placed plus internal — is not tracked; a clause cardinality constrains only the caller-facing side, and no `[=N total]`-style annotation is specified. (§13.3.4.2)
2092. A node may declare generic parameters in the standard `[T, U, ...]` form: `node Buffer[T: Numeric]:`. (§13.3.5)
2093. Node generic parameters are in scope within the body's attr, recurrent, derived, const, parts, and connection declarations: `parts: BufferSlot[T]`. (§13.3.5)
2094. Each instantiation with a different concrete type argument produces a distinct node type with its own cells, monomorphized by the standard rules. (§13.3.5)
2095. A node body contains no `fn` declarations: nodes declare structure and reactive content; behavior lives as free functions or trait `fulfill` blocks. (§13.3.6)
2096. A node instance is owned by the graph position that places it: placement is ownership. (§13.3.6.1)
2097. The only ways to bring a node into the graph are placement syntax and a `repeat` declaration, both creating an owned node at a known graph path. (§13.3.6.1)
2098. A name bound to a node — at top level, inside another node, or as a function parameter or return — holds a reference obeying ordinary borrow semantics, not the node itself. (§13.3.6.1)
2099. A borrow cannot be placed: a node-bound name cannot be re-placed (no double placement). (§13.3.6.1)
2100. A function cannot create or place a node: placement is a declaration-only form unavailable in function bodies, and functions only receive and return borrows. (§13.3.6.1)
2101. A function may take and return node references, usable for reading cells and as connection endpoints, never for placing. (§13.3.6.1)
2102. A node reference cannot be stored in a cell, record field, enum payload, or tuple. (§13.3.6.1)
2103. The storable, non-owning designation of a node instance is `Handle[…]`; persistent wiring between instances is a connection; a node type held as a value is `Type[…]`. (§13.3.6.1)
2104. The factory pattern is parameterizing the type (generics/const-generics) and placing at the call site: `Synth[8] my_synth`. (§13.3.6.1)
2105. Deferring which node type is placed is done by passing a `Type[…]` value. (§13.3.6.1)
2106. Connection and effect instances obey the same ownership rule: brought in only by the language's placement/instantiation syntax, held only by borrow, never stored as values; their types travel via `Type[…]`. (§13.3.6.1)
2107. Operators are instantiated only via operator syntax and are carried via the structural type `operator(…) -> Cell[U]`. (§13.3.6.1)
2108. The graph-ownership rule is normative: conformant compilers enforce it at type-check time. (§13.3.6.1)
2109. `Handle[T]` is a `Copy` value, freely placed in cells, fields, tuples, and enum payloads, that weakly designates a graph entity. (§13.3.6.2)
2110. A handle never keeps its referent alive: no reference count, no reachability; entity lifetime is governed solely by the owning graph position or `repeat` scope. (§13.3.6.2)
2111. A handle may outlive its referent and then resolves to absent. (§13.3.6.2)
2112. `Handle[T]` is kind-scoped: `T` must be a node, connection, or effect type, or a trait that `requires` one of the `Node`/`Connection`/`Effect` markers. (§13.3.6.2)
2113. There are no handles to records, tuples, or other plain values; one handles the value's home (the node or cell it lives in) instead. (§13.3.6.2)
2114. A handle has no `.resolve()` method; resolution is transparent and type-directed by the position the handle occupies. (§13.3.6.2)
2115. In a `Handle[T]`-typed position, a handle denotes the inert handle value; no graph entity is read and nothing becomes reactive. (§13.3.6.2)
2116. In a read or elimination position — `match`, `?`, `!`, or where `&T` is expected — a handle denotes its resolution `Option[&T]`: `Some(&entity)` while live, `None` once gone. (§13.3.6.2)
2117. A handle resolution read is reactive: it joins the reader's provenance and re-fires when the referent mounts or dismounts. (§13.3.6.2)
2118. `Option[&T]` contains a borrow and is unstorable (the transient resolved view); `Handle[T]` contains no borrow and is the storable carrier. (§13.3.6.2)
2119. A handle is produced by the prefix operator `weak` on a node/connection/effect reference: `weak some_car`. (§13.3.6.2)
2120. Where a `Handle[T]`-typed position receives a reference directly, the `weak` coercion is inserted automatically. (§13.3.6.2)
2121. There is no `.handle()` method and no handle sigil; `weak` is the one explicit spelling. (§13.3.6.2)
2122. A handle is eliminated through exactly three forms, the ordinary `Option` eliminators: `match`, `?`, and `!`: `let s = target?.speed`. (§13.3.6.2)
2123. `!` on a handle asserts liveness and is a compile error unless liveness is provable. (§13.3.6.2)
2124. A handle is concretely a graph slot plus a generation stamp; a slot reused by a different entity fails the stamp comparison, so a stale handle resolves to `None`, never to the wrong entity. (§13.3.6.2)
2125. Because mount and dismount flip its resolution, a handle read is a dynamic dependency. (§13.3.6.2)
2126. A handle obtained by key from a `repeat` view designates the keyed scope, not one mount: when the same key reappears in the source, a handle that resolved `None` resolves `Some` again. (§13.3.6.2)
2127. `expose:` declares the node type's structural output: the ordered list of placements the runtime traverses on encountering an instance of the type. (§13.3.7)
2128. Exposition entries are node placements and connection placements; their order is semantic — the order of traversal and connection engagement. (§13.3.7)
2129. The canonical node clause order is `satisfies` → `parts:` → `incoming:` → `outgoing:` → `when:` → `expose:` → cell declarations → `effects:`. (§13.3.7)
2130. The `expose:` body is an ordered list of placements with the same syntax as inline child placements elsewhere. (§13.3.7.1)
2131. A type-bulk entry exposes the full list of supplied parts of one type in placement order: `parts.SomeA`. (§13.3.7.1)
2132. A named-part entry exposes one specific named child: `osc1`. (§13.3.7.1)
2133. A wrapper entry places a node-internal type containing supplied parts as its own children; wrappers are declared nodes accepting children via their own `parts:` clause. (§13.3.7.1)
2134. A connection entry places a self-sourced connection with full placement syntax — `ConnType: dest`, optional `/expr`, attribute clause, inline `when` — at its engagement position. (§13.3.7.1)
2135. A positioning reference `outgoing.<ConnType>` (or `outgoing.<ConnType>[i]` under the guaranteed minimum) engages caller-supplied connections at that position, overriding the anchoring fallback. (§13.3.7.1)
2136. A positioning reference carries no destination, so it never collides with the `ConnType: dest` placement shape. (§13.3.7.1)
2137. Bare `incoming.<ConnType>` as an exposition entry is a compile error: engagement order belongs to the source's traversal, and a destination cannot reposition another node's connection. (§13.3.7.1)
2138. The bare-`incoming.<ConnType>` exposition-entry error points at the expression-position idioms instead: `incoming.X[i].from` as a back-connection destination, or `repeat c in incoming.X: ...`. (§13.3.7.1)
2139. Iteration entries emit type-internal structure in place: a compile-time `for` unrolls at its written position; a `repeat` mounts its keyed scopes there. (§13.3.7.1)
2140. Lowercase-keyword iteration entries never collide with `Name:` entries; the casing rule is normative. (§13.3.7.1)
2141. Entry kind follows name resolution: a connection type makes the entry a connection placement (destination follows), a node type a node placement (body follows), and a `Type[…]` binding follows its constraint's kind. (§13.3.7.1)
2142. A single name never denotes both a node type and a connection type in one scope: type declarations of every kind share one namespace per scope. (§13.3.7.1)
2143. An individual exposition entry may carry the inline `when` modifier for single-placement gating. (§13.3.7.1)
2144. An exposition entry may be a `when` block or a `given` block; each arm body is a list of exposition entries, and the runtime exposes the active arm and freezes the rest (Model B). (§13.3.7.1)
2145. No exposition-only control-flow syntax exists; conditional exposition reuses the general gate constructs. (§13.3.7.1)
2146. The position directly under a `when`/`given` block header is arm position, admitting only variant labels or boolean guard expressions; connection entries appear only inside arm bodies, so arm labels never collide with `Name: dest` entries. (§13.3.7.1)
2147. With `expose:` omitted, exposition defaults to `expose: parts`: all supplied placements in declaration order — parts and caller-placed connections in written interleaving, feeding `repeat` scopes expanded in place. (§13.3.7.2)
2148. The default exposition covers caller-supplied content only; a type that emits internal structure necessarily writes `expose:` explicitly. (§13.3.7.2)
2149. The reserved field `.exposition` returns the ordered list of placements — node and connection entries — the instance currently exposes: `instance.exposition`. (§13.3.7.3)
2150. External readers and the runtime see identical exposition content. (§13.3.7.3)
2151. Inside the node body, bare `exposition` is the same list. (§13.3.7.3)
2152. `exposition` is read-only: its declaration is fixed by the type's `expose:` clause and the placer's supplied content, with no imperative mutation. (§13.3.7.3)
2153. The current exposition varies at runtime only through declared constructs: gate arms flipping, `repeat` scopes mounting and dismounting, dynamic supply arriving and leaving. (§13.3.7.3)
2154. The runtime traverses what `expose:` produces, not the `parts:` clause directly. (§13.3.7.4)
2155. `parts:` is the supply half (accepted child types with cardinality, filled by placement-time child placements); `expose:` is the output half (the ordered structural output). (§13.3.7.4)
2156. `parts:` exists solely to feed `expose:`: externally supplied children exist to become the node's structural output. (§13.3.7.4)
2157. Traversal proceeds in entry order: node entries by structural descent, connection entries by engagement. (§13.3.7.4)
2158. A connection placement is an exposition entry positioned in the ordered sequence alongside node entries; its position states where in the node's structure the connection takes effect. (§13.3.7.5)
2159. A connection entry engages when traversal reaches its position, after the entries before it: `Plays: next` after `Chord/'F#'`. (§13.3.7.5)
2160. Presence is participation: an exposition entry's connection is reactively live whether or not traversal has reached it. (§13.3.7.5)
2161. Effects are never exposition entries: effects are declared only in the `effects:` clause, never in `expose:`. (§13.3.7.5)
2162. A node entry is traversed by structural descent: the runtime enters the placement and walks its exposition. (§13.3.7.6)
2163. A connection entry is engaged: the runtime takes up the connection and begins interpreting the connected subgraph. (§13.3.7.6)
2164. What engagement means is domain semantics carried by the specific connection type and interpreted by the runtime/host; it is not a language keyword, and the language and IR carry no sequencing flag for it. (§13.3.7.6)
2165. One connection type may carry await-like meaning and another parallel meaning; the host interprets each connection type as it interprets the node types it traverses. (§13.3.7.6)
2166. The language fixes only that a connection first engages when its turn comes in the structural (entry) order of the exposition — distinct from per-commit topological evaluation order. (§13.3.7.6)
2167. Engagement is orthogonal to reactive liveness: unengaged and already-passed connections, and everything downstream, keep computing; traversal interprets the graph and does not power it. (§13.3.7.6)
2168. Freezing comes only from explicit declared conditions — `when`/`given` gates and an unresolved dynamic destination — never implicitly from traversal position. (§13.3.7.6)
2169. A frozen connection entry is not engaged: traversal passes over it, consistent with a gated-off exposition entry not being exposed. (§13.3.7.6)
2170. Freezing removes an entry from engagement; engagement never affects freezing. (§13.3.7.6)
2171. Terminology is fixed: activation refers to gate-state, engagement to traversal reaching an entry; a connection can be live yet unengaged, or frozen and unengageable. (§13.3.7.6)
2172. The `effects:` clause is the sole site where an effect may be instantiated; module level, operator bodies, connection bodies, and function bodies are illegal hosts. (§13.3.8)
2173. `expose:` declares topology (what the node is); `effects:` declares side effects (what it does); the two never mix. (§13.3.8)
2174. Each `effects:` entry instantiates an effect via a `|>` pipe chain: `url |> fetch`. (§13.3.8)
2175. An effect entry is named (`f = url |> fetch`, binding the instance value) or anonymous (`label |> log`, run for its side effect only). (§13.3.8)
2176. A named effect's name is in scope for the whole node body regardless of clause order. (§13.3.8)
2177. A named effect's cells are read elsewhere by flat access: `derived spinner: bool = f.in_flight`. (§13.3.8)
2178. Sink forwarding — piping a stream into an effect's sink — lives in `effects:` alongside the effect it targets: `messages |> ws.outbound`. (§13.3.8)
2179. The `effects:` clause admits `when`, `given`, and `repeat` with effect entries in place of node placements; arm and loop bodies are lists of effect entries. (§13.3.8)
2180. Arm headers use `:` and named effect entries use `=`, so the two never collide. (§13.3.8)
2181. Gating freezes the inactive arm and suspends its effects (Model B). (§13.3.8)
2182. Effects are local-only: never passed into a node from outside — there are no effect parts and no effect parameters. (§13.3.8)
2183. A caller influences a node's effects only through data in (supplied signals/attrs/streams) and streams out (the parent consumes the child's exposed outgoing stream in the parent's own `effects:`). (§13.3.8)
2184. One named effect may take another's observed cell as its piped input: `b = a.response |> persist`. (§13.3.8)
2185. Operators in an effect pipe chain remain pure transformations of output; the effect itself is hosted in `effects:`. (§13.3.8)
2186. A program-global effect lives in the `effects:` clause of a top-level node instance, since module level is not an effect host. (§13.3.8)
2187. Instantiating an effect elsewhere in the node body — a free `f = url |> fetch` or `derived x = url |> effect` — is a compile error directing the author to `effects:`. (§13.3.8)
2188. "Part" is a role, not a separate type: a part is a child node instance placed inside a parent node at construction time. (§13.4.0)
2189. A part is contained within a parent instance and addressable only through that parent: `parent.osc1`, `parent.parts.Oscillator[0]`. (§13.4.0)
2190. A node placed at module top level is an independent instance, reachable only by its module-scope name or through connections. (§13.4.0)
2191. Guidance: a child is placed as a part when the parent conceptually owns it, when the parent’s reactive expressions aggregate over the child instances, or when the compositional structure is part of the parent’s identity. (§13.4.0)
2192. Guidance: an instance is placed at top level when it stands on its own and participates in the graph through connections rather than containment. (§13.4.0)
2193. A parent declares the child types it accepts via its `parts:` clause with optional cardinality; the specific instances appear via placement. (§13.4.0)
2194. Runtime structural traversal goes through the `expose:` clause, not through `parts:` directly. (§13.4.0)
2195. Parts the exposition does not include are not traversed by the runtime and do not contribute to structural descent. (§13.4.0)
2196. Non-exposed parts remain queryable via the host API and addressable within the parent's own reactive expressions. (§13.4.0)
2197. Type-bulk access `parts.<NodeType>` is available only when `<NodeType>` appears in the parent's `parts:` clause. (§13.4.1)
2198. For a static (unmarked) part type, `parts.<Type>` is a read-only array of node references with const-generic length bounded by the declared cardinality, in placement order. (§13.4.1)
2199. A static `parts.<Type>` array is indexable under the guaranteed minimum cardinality. (§13.4.1)
2200. For a `dynamic` part type, `parts.<Type>` is a reactive cell consumed via operators or `repeat`. (§13.4.1)
2201. A `dynamic` part type's membership is runtime data by declaration, tracked reactively. (§13.4.1)
2202. Named individual access — bare `<name>`, or `paramName.<name>` from outside the node body — reaches a specific part by its placement-time name. (§13.4.1)
2203. Part names are assigned in the placement body and are visible wherever the placement scope is known. (§13.4.1)
2204. Named individual access is available with or without a `parts:` clause. (§13.4.1)
2205. There is no whole-namespace (heterogeneous) access over `parts`: per-type access is the only bulk form. (§13.4.1)
2206. A node without a `parts:` clause has no bulk parts access at all. (§13.4.1)
2207. Inside the parent's own type body (its `derived` and `recurrent` expressions) only type-bulk access is available; placement names are not visible at the type-declaration level. (§13.4.1)
2208. Named part access is available in function bodies receiving a specific instance: `c.osc1.output` with `c: Composite`. (§13.4.1)
2209. Named part access is available in other instances' placement bodies that reference the named instance. (§13.4.1)
2210. Within a placement body, lines after a part's naming may reference the just-named part by name. (§13.4.1)
2211. Static type-bulk and named access are compile-time resolved: the compiler knows every statically supplied part's identity, type, and placement-name per site. (§13.4.1)
2212. A function receiving the parent node as a parameter may iterate a part type with `for` via the parameter name (no implicit receiver): `for o in s.parts.Oscillator`. (§13.4.2)
2213. The part-iteration bind has the concrete part type in each iteration. (§13.4.2)
2214. There is no heterogeneous `for p in c.parts` over all declared part types: aggregate behavior over several part types is written per type and combined explicitly. (§13.4.2)
2215. A uniform stored representation across part types uses `dyn Trait`, not heterogeneous parts iteration. (§13.4.2)
2216. Part iteration is not a special construct: `c.parts.Oscillator` is a fixed-extent array, so `for` over it unrolls by the ordinary rule of §12.3.7 to one reference per declared part. (§13.4.2)
2217. When a function called from a reactive expression iterates parts, each part reactive cell it reads contributes to the calling expression's dependency set. (§13.4.3)
2218. When any one contributing part cell changes, the dependent derived becomes dirty. (§13.4.3)
2219. Dependency tracking is provenance-based: reactive cells read by an expression are tracked transitively through function calls. (§13.4.3)
2220. A node may contain at most one part of each name. (§13.4.4)
2221. Multiple parts of the same type with different names are permitted, subject to the declared cardinality. (§13.4.4)
2222. Statically supplied parts are not added or removed at runtime, except via hot reload. (§13.4.4)
2223. Parts of a `dynamic` type mount and dismount with their feeding `repeat`'s key set. (§13.4.4)
2224. A `dynamic` part type is not `for`-iterable, not indexable, and not key-addressable from the receiving body. (§13.4.4)
2225. Operators and `repeat` are the only consumers of a `dynamic` part type. (§13.4.4)
2226. Type-bulk and named individual access coexist on the same instance: `c.parts.Oscillator[0]` (indexed, legal under `+`) and `c.parts.Filter[0]` (legal under `[=1]`) alongside named `c.osc_a`. (§13.4.5)

## Keyed Scopes & `repeat`

2227. Every conformant runtime exposes the three keyed-scope operations — `scope_obtain`, `scope_drop`, `scope_evaluate` — underlying the language's dynamic-scope reactive constructs. (§13.5)
2228. Each instantiation of a keyed-scope template is backed by its own state cells. (§13.5)
2229. The user-facing surface of the keyed-scope mechanism is `repeat`, materializing one scope per element yielded by a reactive iterable source. (§13.5)
2230. A scope-managing construct's bound template is fixed at compile time; the scope operations parameterize only the key. (§13.5.1)
2231. `scope_obtain(key)` returns the scope for `key`, allocating from the construct's per-template pool if absent. (§13.5.1)
2232. A newly allocated scope initializes the template's state cells to their declared initial values. (§13.5.1)
2233. `scope_drop(key)` invokes `Drop` on scope `key`'s state cells in reverse declaration order and returns the pool slot. (§13.5.1)
2234. `scope_evaluate(key)` evaluates the template's deriveds and any recurrents eligible to fire within scope `key`'s state context. (§13.5.1)
2235. During `scope_evaluate(key)`, bare references and `here::` inside the template body resolve to scope `key`'s cells. (§13.5.1)
2236. References to the enclosing instance's cells inside a template body resolve to that instance's cells per the §13.7 scope chain. (§13.5.1)
2237. The construct's elaboration is responsible for sequencing the operations: `scope_obtain` for new keys, `scope_evaluate` for active keys, `scope_drop` for keys no longer active. (§13.5.1)
2238. Each scope-managing construct has its own keyed pool. (§13.5.1.1)
2239. A construct's pool element shape is the template's state-shape. (§13.5.1.1)
2240. A construct's pool index space is the construct's key domain. (§13.5.1.1)
2241. Scopes are independent: no cell sharing across keys. (§13.5.1.1)
2242. Pools follow the §14.3.3 extensible-pool model, growing as keys are added and shrinking as keys are dropped, subject to the runtime's pool-management policy. (§13.5.1.1)
2243. A template's state-shape is the set of stateful cells it declares: `signal`, `attr`, and `recurrent` declarations in its body. (§13.5.2)
2244. `derived` declarations are not part of the state-shape: they are pure functions of state cells, the loop binding, and the enclosing scope's cells. (§13.5.2)
2245. `const` declarations are static and not state. (§13.5.2)
2246. When a template's state-shape is empty, the runtime allocates no pool, and `scope_obtain(key)` and `scope_drop(key)` are no-ops. (§13.5.2)
2247. For a stateless template, `scope_evaluate(key)` evaluates the deriveds against the loop binding and the enclosing scope's cells without any per-key state context. (§13.5.2)
2248. The compiler determines state-shape at compile time and statically selects between the pool and no-pool case per construct instantiation. (§13.5.2)
2249. When state-shape is non-empty, per-key state cells participate in hot reload per §13.15.2's cell-identity rules. (§13.5.3)
2250. A scope cell's path is `<enclosing_path>.<key>.<template_field>`. (§13.5.3)
2251. `<enclosing_path>` is the fully-qualified path of the enclosing instance — the node, placement, or effect containing the scope-managing construct. (§13.5.3)
2252. Scope keys are required to be stringifiable primitives; for `repeat` the bound is the `StringifiableKey` trait. (§13.5.3)
2253. The key value itself serves as the scope's path component. (§13.5.3)
2254. Hot-reload changes to a template's body follow the standard reload-safe/reload-unsafe rules of §13.15.4, applied uniformly across all live keys. (§13.5.3)
2255. `repeat` declares one reactive scope per element of a runtime reactive source. (§13.5.4)
2256. `repeat` materializes scopes via the keyed-scope primitive: `scope_obtain` on key emergence, `scope_drop` on key disappearance, `scope_evaluate` per active key per commit. (§13.5.4)
2257. `repeat` desugars directly to the §13.5.1 primitive; the runtime sees no machinery distinct from it. (§13.5.4)
2258. `repeat` is the construct for runtime-varying existence: presence varies by mounting and dropping scopes as the source's key set changes, with state reclaimed on drop. (§13.5.4)
2259. Conditional-activation gates never add or remove instances; they only freeze an unconditionally-constructed instance while it is inactive. (§13.5.4)
2260. The `repeat` clause order is fixed: `repeat <bind> [at <index>] in <source> [as <view>] [keyed by <key-expr>]:` followed by an indented body. (§13.5.4.1)
2261. A `repeat` source is a reactive collection cell: `Signal[I]` for some `I: Iterable`, or a `dynamic` namespace cell (`parts.<T>`, `incoming.<C>`, `outgoing.<C>`). (§13.5.4.1)
2262. A `dynamic` namespace cell's value is a language-provided keyed, ordered collection — always iterable. (§13.5.4.1)
2263. The standard library fulfills `Iterable` for `Vec[T]`, `T[N]` (any const N), `HashSet[T]`, and `HashMap[K, V]`. (§13.5.4.1)
2264. User types may fulfill `Iterable` to participate as `repeat` sources. (§13.5.4.1)
2265. `Stream[T]` is not a valid `repeat` source: an event source has no current snapshot and no key set to diff. (§13.5.4.1)
2266. Driving `repeat` from a stream requires first projecting the stream into a collection-valued cell (e.g. folding events into a `Vec`/`HashMap`) and repeating over that. (§13.5.4.1)
2267. `<bind>` is a bare identifier or a tuple-destructuring pattern per §12.12.1 — the same destructuring grammar the for-loop's iteration variable accepts. (§13.5.4.1)
2268. `HashMap[K, V]`'s iterator yields `(K, V)` pairs. (§13.5.4.1)
2269. `<bind>` is typed as the iterator's element type after move-promotion — a real owner, not the borrow-equivalent alias `Iterable::iterator` yields under the default `type Item = T` convention. (§13.5.4.1)
2270. Move-promotion converts the cluster-member alias into a real owner scoped to one `scope_evaluate` invocation. (§13.5.4.1)
2271. Each `scope_evaluate` runs in isolation against the source signal's current committed snapshot, which is read-only. (§13.5.4.1)
2272. The runtime hands each `scope_evaluate` a unique pointer to its element; no other code can read or write that element during the scope's execution. (§13.5.4.1)
2273. Using the bind within the scope is zero-copy: move-promotion is a static compiler reinterpretation, not a runtime operation. (§13.5.4.1)
2274. A `Copy` bind persists into cells or fields by copy as usual, with no effect on the source. (§13.5.4.1)
2275. A non-`Copy` bind cannot be moved out of its storage: the element lives in the source's immutable committed snapshot and is re-read on the next commit. (§13.5.4.1)
2276. Persisting a non-`Copy` bind into category B/D storage materializes a copy at that point, the same materialization as §13.2.9.7 boundaries. (§13.5.4.1)
2277. Attrs, connection arguments, and other placement targets in a `repeat` body see owned types: borrow-equivalent aliases do not leak into attribute or argument positions through `repeat`. (§13.5.4.1)
2278. The `repeat` body is an indented placement-body block per §13.8.3, with §13.8.9's clause ordering and §13.8.10's whitespace-separation and self-delimiting rules. (§13.5.4.1)
2279. A scope body may contain any number of placements per key — nodes, connections, nested structure, or connections only; there is no one-placement-per-key requirement. (§13.5.4.1)
2280. Each scope's placements join the enclosing structural sequence at the `repeat`'s written position, in source order. (§13.5.4.1)
2281. In an `effects:` clause, the `repeat` body is a list of effect entries instead of placements. (§13.5.4.1)
2282. `at <index>` binds a bare identifier to each element's 0-based enumeration index (`usize`) in iterator order. (§13.5.4.1)
2283. The `at` index is ordinary per-iteration data: it updates whenever elements reorder. (§13.5.4.1)
2284. `as <view>` binds a bare identifier naming the repeat's keyed view, addressable by key from the surrounding body — for an exposition-level `repeat`, the whole node type body. (§13.5.4.1)
2285. Key derivation follows a strict ordered precedence; the compiler picks the first applicable path, leaving no call-site ambiguity. (§13.5.4.1)
2286. Key path 1: an explicit `keyed by <key-expr>`, evaluated with the bind in scope, always wins when present. (§13.5.4.1)
2287. An explicit `keyed by` result must be a `StringifiableKey`. (§13.5.4.1)
2288. `StringifiableKey` comprises `i8`–`i64`, `u8`–`u64`, `bool`, `char`, and `string`. (§13.5.4.1)
2289. Key path 2 (carried key): when the source is a `dynamic` namespace cell, each element arrives with the key its supplier derived for it, and that key is used. (§13.5.4.1)
2290. Statically written elements of a `dynamic` namespace carry placement-site keys. (§13.5.4.1)
2291. The carried-key path applies only to `dynamic` namespace sources. (§13.5.4.1)
2292. Key path 3 (`Keyed` trait): if the element type fulfills the stdlib `Keyed` trait, the key is `Keyed::key(element)`. (§13.5.4.1)
2293. `Keyed` is a stdlib trait of shape `type Key: StringifiableKey` plus `fn key(value: Subject) -> Key`. (§13.5.4.1)
2294. A type fulfills `Keyed` for at most one `Key` (standard trait coherence), so the `Key` associated type is uniquely determined. (§13.5.4.1)
2295. Key path 4 (stringifiable element): if the element type is itself a `StringifiableKey`, the element value is the key. (§13.5.4.1)
2296. When no key-derivation path applies, compilation fails with an error directing the user to fulfill `Keyed` for the element type or add `keyed by <expr>`. (§13.5.4.1)
2297. The `Keyed` path always wins over the stringifiable-element path when both apply, e.g. a newtype fulfilling both. (§13.5.4.1)
2298. `repeat` re-evaluates only when its source is dirty; the runtime then reads the source's current committed value and iterates it via `Iterable::iterator`. (§13.5.4.2)
2299. At each re-evaluation, every element's key is derived per §13.5.4.1's ordered selection. (§13.5.4.2)
2300. At each re-evaluation, the new key set is diffed against the previous key set. (§13.5.4.2)
2301. Keys in `old ∩ new` carry over: their scopes are preserved, and `<bind>` is updated to the new element. (§13.5.4.2)
2302. Keys in `old − new` are dropped: `scope_drop(key)` releases the per-key cells. (§13.5.4.2)
2303. Keys in `new − old` are added: `scope_obtain(key)` initializes the per-key cells per the state-shape. (§13.5.4.2)
2304. For each key in iterator order, the runtime updates `<bind>` — and the `at` index, when present, to the element's 0-based position — and calls `scope_evaluate(key)`. (§13.5.4.2)
2305. Reordering source elements without changing the key set performs no scope allocations or drops; only iteration order changes, and surviving scopes observe their new `at` indices. (§13.5.4.2)
2306. Unordered iterables (`HashSet[T]`, `HashMap[K, V]`) are diffed by key identity; iteration order is whatever the underlying iterator emits and does not affect scope identity. (§13.5.4.2)
2307. An `attr` is a reactive cell valid as a `repeat` source: reads of a collection-typed attr in the body yield a `Signal` of the collection (e.g. `Signal[Vec[VoiceConfig]]`), satisfying the source-type requirement with the collection supplying the iterator inside the `Signal`. (§13.5.4.3)
2308. A key reappearing across a hot reload — source edit or program rerun — preserves its scope's state, identified by the same `<enclosing>.<key>.<cell>` path. (§13.5.4.4)
2309. For a stateless template (empty state-shape), no per-key cells are allocated and the path machinery is bypassed. (§13.5.4.4)
2310. Source mutations across a reload drive the same key-set diff as a runtime mutation: scopes whose keys disappear are dropped; scopes whose keys appear are allocated fresh. (§13.5.4.5)
2311. Body changes across a reload — the template's placements and their attrs — apply uniformly to all existing scopes. (§13.5.4.5)
2312. Changes to key derivation — the `keyed by` expression or a depended-on `Keyed::key` body — are reload-safe: recomputed against current inputs without a restart. (§13.5.4.5)
2313. When a key-derivation change makes an element produce a different key, the old key's scope state is dropped and the new key's scope is freshly allocated: per-scope state is identified by key, not element identity. (§13.5.4.5)
2314. `repeat` follows the commit-time-recompute model: signals carry current value, not deltas, so a dirty source is re-iterated and re-keyed in full. (§13.5.4.6)
2315. The per-commit floor is O(N) iterate + O(N) key derivation + O(N) hash-diff against the previous key set, with N the current element count. (§13.5.4.6)
2316. Scope add/remove costs O(K + K') on top of the floor — K keys removed, K' keys added — each invoking `scope_drop` or `scope_obtain`. (§13.5.4.6)
2317. Per-scope evaluation costs the template body times the number of live keys. (§13.5.4.6)
2318. A pure reorder performs zero scope-management work: keys carry across reorderings, with no `scope_obtain` or `scope_drop` invoked. (§13.5.4.6)
2319. A pure reorder still pays the per-commit floor: reorder does not shortcut detecting that the key set is unchanged. (§13.5.4.6)
2320. A clean commit (source not dirty) costs zero work: `repeat` does not re-iterate. (§13.5.4.6)
2321. A stateless template allocates no per-key cells; the per-commit floor still applies. (§13.5.4.6)
2322. There is no delta-driven `repeat` variant over a structural-delta reactive shape; `repeat` always re-iterates and re-keys the current value. (§13.5.4.6)
2323. Programs that do not use `repeat` incur no runtime cost from the template-scope machinery. (§13.5.4.6)
2324. `repeat` is admitted in `expose:` blocks: scopes become internal children of every instance of the enclosing node, materialized at instantiation, tracking the source, at the `repeat`'s written position in the exposition. (§13.5.4.7)
2325. Exposition-level `repeat` children are internals, never counted against signature clauses. (§13.5.4.7)
2326. `repeat` is admitted in placement bodies: scopes become children of that specific placement (caller supply). (§13.5.4.7)
2327. Caller-supplied `repeat` node placements feed the receiving type's `dynamic`-marked part types. (§13.5.4.7)
2328. Caller-supplied `repeat` connection placements sourced from the placed instance require `outgoing: dynamic` on its type. (§13.5.4.7)
2329. `repeat` is admitted in `effects:` clauses: each scope materializes one effect-bearing instance per element. (§13.5.4.7)
2330. Per-element effects from an `effects:`-clause `repeat` suspend, resume, and tear down with the element key. (§13.5.4.7)
2331. `repeat` is admitted in effect `desired:` blocks: scopes become part of the effect's declared desired state, reconciled by the host. (§13.5.4.7)
2332. `repeat` is rejected in function bodies — functions produce values, not reactive structure — by the same rule as `derived`/`recurrent`/`stream`. (§13.5.4.7)
2333. `repeat` is rejected in effect `observed:` blocks; per-element scopes from an observed cell are written in an `expose:` or `desired:` block that consumes the cell. (§13.5.4.7)
2334. `repeat` is rejected in operator bodies: dynamic-scope materialization inside operators is not in scope for v1. (§13.5.4.7)
2335. `repeat` is rejected in connection bodies; dynamic-scope structure belongs in node bodies, placement bodies, `effects:` clauses, or `desired:` blocks. (§13.5.4.7)
2336. `repeat` is rejected in trait and `fulfill` blocks, which declare behavior, not graph structure. (§13.5.4.7)
2337. In each rejected context, the diagnostic identifies the misplaced `repeat` and points at the appropriate target context. (§13.5.4.7)
2338. `<key-expr>` must be reactive-pure — no reactive dependencies beyond `<bind>` — guaranteeing key stability across evaluations. (§13.5.4.8)
2339. The same reactive-purity rule applies to the `Keyed::key` method body when implicit keying goes through the `Keyed` trait. (§13.5.4.8)
2340. A `repeat` body may not close over the enclosing scope's mutable state beyond §13.12.3's closure-snapshot semantics. (§13.5.4.8)
2341. Nested `repeat` constructs are permitted; each nested level's scopes hang off the outer scope's path. (§13.5.4.8)
2342. The source iterator must terminate at each evaluation; bounded-at-commit-time collections satisfy this. (§13.5.4.8)
2343. No compiler termination check is mandated for user `Iterable` implementations; they are trusted. (§13.5.4.8)
2344. An iterator whose `next` never returns `None` hangs the iterate phase; this is a programmer error against the trait's intended use. (§13.5.4.8)
2345. The same key reached through different element values across commits identifies the same scope: the value is carried in `<bind>` and may change, while the key fixes scope identity. (§13.5.4.8)
2346. Carried keys diff identically to derived keys: with a `dynamic` namespace source the diff compares the carried key set across commits, so an element keeps its scope across reorderings and value changes. (§13.5.4.8)
2347. The `at` index never derives the key implicitly: it is excluded from the implicit key-derivation paths (`Keyed` trait and stringifiable element). (§13.5.4.8)
2348. Positional identity requires explicit opt-in by writing `keyed by <index>`; there is no implicit path to index keying. (§13.5.4.8)
2349. Without `as`, a `repeat` materializes its scopes anonymously: nothing in the surrounding body can name a particular child. (§13.5.4.9)
2350. `as <view>` binds a keyed view: a lookup table over the scopes the `repeat` currently holds, addressed by scope key. (§13.5.4.9)
2351. Names are scope entries, not instance members: a scope exposes its placements through their names, so addressing a child goes through the placement name, not an instance. (§13.5.4.9)
2352. `<view>[<key>]` selects the scope for `<key>`. (§13.5.4.9)
2353. `<view>[<key>].<name>` yields a key-addressed `Handle[T]` for the placement named `<name>` via `as <name>` at its site, with `T` that placement's node type: `posts_view[selected].avatar`. (§13.5.4.9)
2354. Every named placement in the body is addressable through the view at any nesting depth. (§13.5.4.9)
2355. The view table is flat: it maps names, not paths. (§13.5.4.9)
2356. `as`-names must be unique within one `repeat` body, across nesting; a duplicate is a compile error. (§13.5.4.9)
2357. Anonymous placements are unaddressable through the view; leaving a placement unnamed keeps it private to its scope. (§13.5.4.9)
2358. A view lookup is a weak, storable `Handle` (§13.3.6.2): `<view>[k].<name>` resolves to `Some(&node)` while key `k`'s scope is mounted and to `None` otherwise. (§13.5.4.9)
2359. A view handle is never provably live: `<view>[k].<name>!` is always a compile error; eliminate with `match` or `?`. (§13.5.4.9)
2360. A key that leaves and later returns remounts the same scope, so a stored view handle resumes resolving `Some` when its key comes back. (§13.5.4.9)
2361. Nested `repeat`s each own a separate `as` view and do not flatten into the outer one. (§13.5.4.9)
2362. A nested view is scoped to its parent key; cross-level addressing composes view by view rather than through one global table. (§13.5.4.9)

## Connections

2363. A connection is a directional, typed link between two node instances. (§13.6.0)
2364. Connections are first-class entities: each has its own identity. (§13.6.0)
2365. A connection carries its own reactive content — attrs, recurrents, deriveds — per-relationship state belonging to the relationship rather than to either endpoint: `Drives` holding `attr aggressiveness`. (§13.6.0)
2366. A connection type may satisfy traits. (§13.6.0)
2367. Every connection has a source — the `from` endpoint — and a destination — the `to` endpoint. (§13.6.0)
2368. A caller-placed connection participates in the source node's outgoing surface declared via `outgoing:`. (§13.6.0)
2369. A self-sourced connection sits outside the source's `outgoing:` surface. (§13.6.0)
2370. Caller-placed and self-sourced connections both count at the destination node's `incoming:` surface. (§13.6.0)
2371. The `from` endpoint is the enclosing instance, fixed structurally by where the connection is placed; it does not vary at runtime. (§13.6.0)
2372. The `to` endpoint is a node reference and may be a reactive value that re-points among existing nodes at runtime. (§13.6.0)
2373. When `to` re-points, the connection instance persists and retains its per-relationship state; only its target moves. (§13.6.0)
2374. Re-pointing neither creates nor drops a node — a connection never owns its endpoints. (§13.6.0)
2375. A node declares which connection types callers may wire to and from it via `incoming:` and `outgoing:` clauses, with optional cardinality constraints and the `dynamic` supply-mode marker. (§13.6.0)
2376. Connection instances appear at placement, not in `incoming:`/`outgoing:` declarations. (§13.6.0)
2377. A connection placement is an exposition entry at a position in its source's ordered structural sequence. (§13.6.0)
2378. A connection's exposition position is where it engages during traversal. (§13.6.0)
2379. Every statically placed connection exists from construction; only a dynamic destination's target may move. (§13.6.0)
2380. A connection's exposition entry belongs to the source's exposition, yet the connection is owned by no single endpoint. (§13.6.0)
2381. A node cannot store a direct reference to another node instance: `attr target: SomeNode` ✗. (§13.6.0)
2382. A node-instance reference is a borrow, and a borrow may not be stored in a cell. (§13.6.0)
2383. Storing a node type rather than an instance — a template slot — uses `Type[…]`. (§13.6.0)
2384. A connection declares its endpoint types in exactly one of three forms: single, cartesian, or pairs. (§13.6.1)
2385. Mixing endpoint forms is a compile error: `pairs:` alongside `from:`+`to:` ✗. (§13.6.1)
2386. Single form requires `from:` exactly once and `to:` exactly once: `from: Driver` / `to: Drivable`. (§13.6.1.1)
2387. A connection body admits `when:` (activation predicate), `const`, `signal`, `attr`, `default attr`, `recurrent[N]`, `derived`, and `stream policy[N]` declarations. (§13.6.1.1)
2388. A connection `const` is a per-type compile-time constant. (§13.6.1.1)
2389. Connection `signal`, `attr`, `recurrent`, `derived`, and `stream` members are per-instance. (§13.6.1.1)
2390. A connection type may declare at most one `default attr`. (§13.6.1.1)
2391. At placement, `/expr` targets the connection's default attr. (§13.6.1.1)
2392. A placement's destination endpoint is supplied in the placement body, never via `/expr`. (§13.6.1.1)
2393. `from` and `to` are endpoint slots, not attributes; placement attribute syntax (`name=value`, flags) does not target them. (§13.6.1.1)
2394. In single form, `from` and `to` resolve inside the body directly to the endpoint instances at their concrete types: `to.top_speed`. (§13.6.1.1)
2395. Cartesian form lists multiple from-types and/or to-types; every from×to combination is a valid placement: `from: Person, Company` with `to: Vehicle, Property`. (§13.6.1.2)
2396. A cartesian body is specialized per from×to combination by a required, statically resolved `match`: `match (from, to):`. (§13.6.1.2)
2397. The cartesian `match` must be exhaustive over the statically-known from×to product — a compile-time check. (§13.6.1.2)
2398. Within each cartesian arm, `from` and `to` are concretely typed: `(Person(p), Vehicle(v)):`. (§13.6.1.2)
2399. Cartesian form introduces no combined runtime value and no new type former. (§13.6.1.2)
2400. Pairs form lists explicit `From -> To` combinations; only listed pairs are valid placements: `pairs:` with `Driver -> Vehicle`. (§13.6.1.3)
2401. In pairs form, endpoints are accessed only via `pair`, matched per declared pair: `match pair: (Driver(d), Vehicle(v)): ...`. (§13.6.1.3)
2402. In pairs form, `from` and `to` are not independently accessible. (§13.6.1.3)
2403. Listing the same `From -> To` pair twice is a compile error. (§13.6.1.3)
2404. Asymmetric pair counts are legal — pair uniqueness, not type count, governs: `A -> X`, `A -> Y`, `B -> Y` ✓. (§13.6.1.3)
2405. A pairs-form body is uniform across pairs; attrs/deriveds cannot vary by pair. (§13.6.1.3)
2406. Differing per-pair content requires a separate connection type per pair. (§13.6.1.3)
2407. `from`, `to`, and `pair` are bound at the connection's placement, which specifies the source (the enclosing instance) and the destination. (§13.6.2)
2408. The `from` binding is fixed for the connection's lifetime. (§13.6.2)
2409. When the destination is a reactive selection or a `Handle`, `to` follows whichever node it currently resolves to. (§13.6.2)
2410. Body reads of `to.*` re-evaluate when the destination re-points — a dynamic dependency on the current target. (§13.6.2)
2411. Inside a connection body, `from` and `to` are never `Option`; they are the live endpoint instances directly. (§13.6.2)
2412. A `Handle` destination (read type `Option[&N]`) is unwrapped at the boundary: while it resolves to `Some`, the connection is active and the body sees the contained node as `to`. (§13.6.2)
2413. While a `Handle` destination resolves to `None`, the connection freezes and its body does not run at all. (§13.6.2)
2414. The freeze is the unwrap: connection body code never handles destination absence. (§13.6.2)
2415. Gates and the unresolved-destination condition are the only switches on a connection's reactive liveness. (§13.6.2)
2416. Traversal position never switches a connection's liveness: an unengaged or already-passed connection keeps computing. (§13.6.2)
2417. `from` and `to` are body-internal, visible only inside the connection type's own body; there is no external `some_conn.to` access. (§13.6.2)
2418. A connection does not surface its endpoints or its activation as readable fields. (§13.6.2)
2419. An outside observer reads a connection's own cells (attrs, deriveds), which hold their last committed value even while the connection is frozen. (§13.6.2)
2420. Liveness-awareness about a node is the job of a `Handle`, not of a connection's surface. (§13.6.2)
2421. A connection may declare generic parameters: `connection Contains[T]:` with `from: Container[T]`, `to: T`. (§13.6.3)
2422. A connection's generic parameters scope over its `from`, `to`, attrs, recurrents, and deriveds. (§13.6.3)
2423. Each unique instantiation of a generic connection is a distinct connection type. (§13.6.3)
2424. A connection body contains no `fn` declarations. (§13.6.4)
2425. Functions on connections are free functions taking the connection type, dispatched via uniform call syntax. (§13.6.4)
2426. Trait methods on connections are implemented in `fulfill` blocks. (§13.6.4)
2427. A connection body contains no `repeat` declarations. (§13.6.4)
2428. Dynamic-scope structure belongs in `expose:` blocks, placement bodies, or effect `desired:` blocks, never in connection bodies. (§13.6.4)
2429. A connection body hosts no effect instantiations. (§13.6.4)
2430. Effects are instantiated only in a node's `effects:` clause. (§13.6.4)
2431. A connection holds no outside-world resource of its own. (§13.6.4)
2432. `Circularity` is a language-defined marker trait with no methods: `trait Circularity`. (§13.6.5)
2433. A connection type opts into participating in topology cycles by declaring `satisfies Circularity`. (§13.6.5)
2434. Every topology cycle in the construction-time node graph must traverse at least one connection whose type satisfies `Circularity`. (§13.6.5)
2435. A cycle consisting only of non-`Circularity` connections is a compile error. (§13.6.5)
2436. Satisfying `Circularity` has no effect beyond opting the connection type into cycle participation; that is its sole purpose. (§13.6.5)
2437. Which connection types satisfy `Circularity` is domain-defined. (§13.6.5)

## Name Resolution in Reactive Bodies

2438. Name resolution proceeds outward through enclosing scopes, innermost first — standard lexical scoping. (§13.7)
2439. A node or connection body is an ordinary scope; its members are in scope within the body's reactive expressions. (§13.7)
2440. There is no receiver concept: a bare name binds to the nearest enclosing scope that declares it. (§13.7)
2441. Two explicit scope anchors exist: `here::x` resolves in the current scope, `module::x` in the module top level. (§13.7)
2442. Routine member access uses a bare name or `here::`; `subject` is reserved for the value role. (§13.7)
2443. Within a node or connection body, the scope chain innermost-first is: local bindings, the instance body scope, the module top-level scope. (§13.7.1)
2444. Local bindings comprise `let` bindings and `for`-loop variables inside a reactive expression. (§13.7.1)
2445. The instance body scope comprises `attr`/`signal`/`recurrent`/`derived`/`stream` cells, `parts`, and the reserved fields `from`, `to`, `incoming`, `outgoing`, `pair`, `exposition`. (§13.7.1)
2446. The module top-level scope comprises module-level `signal`, `derived`, `recurrent`, `stream`, `const`, and `let` declarations. (§13.7.1)
2447. A bare name resolves to the nearest scope in the chain that declares it: `local_gain * master_gain` reads a member and a module cell anchor-free. (§13.7.1)
2448. `here::x` resolves `x` in the current scope, bypassing inner local bindings and ignoring same-named module-level declarations. (§13.7.2)
2449. `here::` is meaningful in any scope (function, operator, trait, node/connection body), always denoting the binding in that very scope rather than an outer one. (§13.7.2)
2450. Type-level positions (trait declarations, `fulfill` blocks) use the `Subject` alias for the subject type, not `here::`. (§13.7.2)
2451. Bare and `here::`-anchored references resolve to the same cell when there is no collision: `expertise_level` and `here::expertise_level`. (§13.7.2)
2452. `module::x` resolves `x` in the enclosing module's top-level scope, bypassing body and local scopes: `on module::tick:`. (§13.7.3)
2453. `module::` reaches only the current module's top level; cross-module access uses the module-path mechanism. (§13.7.3)
2454. A bare name declared in both the instance body scope and the module top-level scope is ambiguous — a compile error: `derived a: f32 = gain` ✗. (§13.7.4)
2455. The body/module collision is resolved by anchoring: `here::gain` selects the member, `module::gain` the module-level cell. (§13.7.4)
2456. Local bindings (`let`, `for`-vars) shadow outer scopes normally; the ambiguity error applies only to the body-vs-module collision. (§13.7.4)
2457. The reserved fields `from`, `to`, `incoming`, `outgoing`, `pair`, `parts`, and `exposition` are instance-body-scope members resolving by bare name in expression position: `incoming.ShowsCount[0].count`. (§13.7.5)
2458. Reserved-field keywords carry clause-header meaning only in declaration position (statement level, trailing colon: `from:`). (§13.7.5)
2459. In expression-operand position, a reserved-field keyword denotes the corresponding instance field: `from.expertise_level`. (§13.7.5)
2460. Reserved words cannot be declared as cell names, so reserved fields never collide with user names. (§13.7.5)
2461. `here::` remains available as the explicit form for reserved fields: `here::from`, `here::incoming`. (§13.7.5)
2462. `in` is not a reserved instance field; it serves solely as the `for`-loop separator: `for x in incoming.ShowsCount`. (§13.7.5)
2463. A cell reference — bare, `here::`-anchored, or `module::`-anchored — joins the reactive dependency graph: `derived x: f32 = y + 1` dirties `x` when the resolved `y` changes. (§13.7.6)
2464. Dependency edges are emitted per instance: body-scope references resolve to that specific instance's cells, never another instance's. (§13.7.6)
2465. `module::` references resolve to the single shared module-level cell. (§13.7.6)
2466. `subject` is the instance value, available in node/connection bodies for handling the whole instance as a value: `total_output(subject)`. (§13.7.7)
2467. `subject.some_method()` and `some_method(subject)` are the same call — dispatch is by first argument and the dot is sugar, not a receiver binding. (§13.7.7)
2468. There is no implicit receiver: the instance is always explicit, as `subject` in a body or as a named parameter in a `fulfill`-block function. (§13.7.7)
2469. A type's "methods" are ordinary scoped functions taking the instance as a developer-named first parameter: `fn display(value: Subject)`. (§13.7.7)
2470. `Subject` is the type-level alias for the subject type, usable only in type positions in trait declarations and `fulfill` blocks. (§13.7.7)
2471. `here` denotes the current (innermost) scope as a namespace, usable only as the left side of `::`; it is not a value and has no `.` form. (§13.7.7)
2472. `module` denotes the enclosing module namespace, usable only as the left side of `::`; it is not a value and has no `.` form. (§13.7.7)
2473. The four reserved identifiers never overlap in role: `subject` is a value usable in expression positions, `Subject` a type usable in type positions, `here` and `module` namespaces usable only as the left side of `::`. (§13.7.7)

## Placement

2474. Placement is the syntax for instantiating nodes, parts, and connections into a concrete reactive graph. (§13.8)
2475. Placement is distinct from value construction of records, which uses constructor syntax. (§13.8)
2476. A top-level placement creates a named instance of a node type at module scope: `Driver john_doe`. (§13.8.1)
2477. A top-level placement line is the type name, then the instance name, then optional attribute settings, then an optional `:` introducing a body of child placements: `Driver john_doe | expertise_level=10:`. (§13.8.1)
2478. A top-level placement is a declaration that names its subject positionally — type then name, with no marker between them. (§13.8.1)
2479. A top-level placement is mandatorily named. (§13.8.1)
2480. The `as` name marker is optional in a top-level placement; `Driver john_doe` and `Driver as john_doe` have identical meaning. (§13.8.1)
2481. Instance names are unique within their declaring scope; two top-level placements with the same name in one module is a compile error. (§13.8.1)
2482. Attribute settings never appear in a placement body; they live on the placement's main line or its aligned continuation lines. (§13.8.2)
2483. A single-line placement with attrs uses one leading `|` followed by one or more `name=value` settings separated by whitespace: `Counter c1 | count=100`. (§13.8.2)
2484. Multi-line attribute layout keeps the first attribute on the placement's main line; later attributes continue on lines indented exactly to the first attribute's column, with no further `|`. (§13.8.2)
2485. A placement with attrs and children writes attributes inline (or via aligned continuation) and then `:` introducing the body: `Driver john_doe | expertise_level=10:`. (§13.8.2)
2486. A placement attribute setting must name a cell declared as an `attr` on the placed type; setting any other identifier — `signal`, `recurrent`, `derived`, or `const` — is a compile error. (§13.8.2)
2487. A placement attribute value must match the attr's declared type, subject to the standard widening rules. (§13.8.2)
2488. A placement attr RHS whose provenance contains no reactive cell is consumed into the attr's storage slot at instantiation as an implicit move (§11.1 category B), with no `move` keyword required: `Server as srv | port=8080`. (§13.8.2.1)
2489. A placement attr RHS that references reactive cells (signals, attrs, recurrents, deriveds) creates an implicit derived bridging the source cells to the target attr, so the attr reactively tracks the sources: `Display as d1 | label=format(c1.count)`. (§13.8.2.1)
2490. Reactive placement wiring is §11.1 category C under the reactive-binding exception: the placement is not an ownership operation and the source cell is not consumed. (§13.8.2.1)
2491. A reactive placement value synthesizes a derived in the parent's scope; when any cell in the expression's provenance changes, the synthesized derived re-evaluates and the target attr updates. (§13.8.2.1)
2492. A value placement evaluates the RHS once and stores the result into the attr's slot. (§13.8.2.1)
2493. The compiler determines the binding category from the expression's provenance set: any reference to a reactive cell makes the expression reactive (category C); otherwise it is a value expression (category B). (§13.8.2.1)
2494. The value-vs-reactive distinction at placement is type-directed and requires no syntactic marker. (§13.8.2.1)
2495. `const` declarations are not settable at placement; attempting to set a const at placement is a compile error. (§13.8.2.2)
2496. `signal` declarations are not settable at placement: the declared initial value applies at construction and subsequent values come through the host API. (§13.8.2.2)
2497. Recurrents are not settable at placement: a recurrent's value is fully defined by its expression and self-history fallbacks, with no separate initial value to override. (§13.8.2.2)
2498. Per-instance variation of recurrent behavior is expressed via attrs the recurrent reads, which are settable at placement: `Counter c1 | start_value=100 step=5`. (§13.8.2.2)
2499. The inline attribute forms (`name=value` / `name` / `!name`) and the flag form target the same underlying attr cells; setting the same attr via two mechanisms is a duplicate-set compile error. (§13.8.2.2)
2500. Reactive bindings apply only to the `name=value` inline attribute form; the flag form has no expression slot and always sets a literal boolean, so reactive bindings do not apply to flags. (§13.8.2.2)
2501. A type's declared `default attr` is additionally settable via the positional `/expr` form, uniformly for nodes and connections. (§13.8.2.2)
2502. Connection destinations are supplied in the placement's body, never via `/expr`. (§13.8.2.2)
2503. An attr not set at placement takes its declared default. (§13.8.2.2)
2504. Consts always have their type-declared value. (§13.8.2.2)
2505. The body of a placement — the indented block introduced by `:` — is reserved exclusively for child placements: parts and connections. (§13.8.3)
2506. A child placement naming a node type listed in the parent's `parts:` clause is a part and creates an instance of that type as a child of the parent. (§13.8.3)
2507. A nested placement is named with the `as` marker, which is required in nested position: `Pin as out1`. (§13.8.3)
2508. A nested part may be anonymous: bare `Pin` is a valid unnamed part placement. (§13.8.3)
2509. A part's placement-time name is visible by bare name to subsequent placements inside the same placement body: a later connection can reference `out1`. (§13.8.3)
2510. A named part is accessible outside the parent type, in function bodies receiving the parent instance, as `chip_b.out1` — in parallel with the type-bulk form `chip_b.parts.Pin[i]`. (§13.8.3)
2511. A named part is accessible by qualified path from other instances' placement bodies that reference the parent instance: `chip_b.out1`. (§13.8.3)
2512. Placement-time part names are not available inside the parent's own type body; there, `parts.<NodeType>[i]` or `parts.<NodeType>` iteration is used. (§13.8.3)
2513. Cardinality declared in the parent's `parts:` clause is enforced at placement; violations are compile errors at the placement site. (§13.8.3)
2514. A placement body may contain a `for` loop over a compile-time-known iterable; it unrolls at compile time into one child placement per iteration: `for i in 0..VOICE_COUNT:` with body `Oscillator | freq=base_freq(i)`. (§13.8.3.1)
2515. Placement-body `for` preserves the static-graph principle: every part is determined at compile time. (§13.8.3.1)
2516. `for` in a placement body is static supply: its unrolled placements count against the receiving type's cardinality and appear in `parts.<NodeType>`, exactly like individually written placements. (§13.8.3.1)
2517. `repeat` in a placement body is dynamic supply, legal only against the receiver's `dynamic`-marked types. (§13.8.3.1)
2518. A placement-body `for` whose iterable is not compile-time-known is a compile error pointing at the iterable; no new diagnostic class is introduced — the static-graph rule itself enforces it. (§13.8.3.1)
2519. The non-compile-time-iterable diagnostic identifies the iterable and cites the runtime source via reactivity-provenance machinery when the runtime-ness flows from a reactive value. (§13.8.3.1)
2520. Anonymous parts produced by an unrolled placement loop are addressable via `parts.<NodeType>[i]` from function bodies receiving the parent instance and via bare `parts.<NodeType>` inside the parent's type body. (§13.8.3.1)
2521. Runtime-varying child multiplicity uses `repeat`, not `for`; placement-body `for` is for multiplicity parameterized by a compile-time-known value but fixed per instance. (§13.8.3.1)
2522. Multiplicity intrinsic to the type — every instance materializing the same children — is declared in the node body (§13.3.3.3); the placement-body `for` is for loops intrinsic to a specific placement. (§13.8.3.1)
2523. A connection placement creates a directional connection and is written inside the source instance's body. (§13.8.4)
2524. The source of a connection placement is always the immediately enclosing instance — determined positionally, with no prefix or sigil. (§13.8.4)
2525. The enclosing-instance source rule is uniform across nesting depth: the depth at which the connection appears does not change how the source is determined. (§13.8.4)
2526. A connection placement's position among its sibling placements is its engagement position: the connection engages when traversal reaches that point, after the siblings written before it. (§13.8.4)
2527. Position-is-engagement holds regardless of who places the connection — a caller in a placement body, or the type itself in its exposition, including exposition-level `for` and `repeat` bodies. (§13.8.4)
2528. A caller-supplied connection's position survives the type's exposition by anchoring: it anchors to its nearest preceding sibling placement and engages immediately after that sibling wherever the exposition carries it — through a bulk reference, a by-name entry, or a wrapper. (§13.8.4)
2529. A connection anchored to a `repeat` block anchors to the block as a unit and engages after the block's expanded scopes. (§13.8.4)
2530. A connection written before any sibling anchors to the body start and engages before the node's first exposed entry. (§13.8.4)
2531. Under the default `expose: parts`, anchoring reduces to traversing the full interleaved sequence as written. (§13.8.4)
2532. Anchoring is the fallback: an explicit `expose:` that references caller-supplied connections directly via `outgoing.<ConnType>` overrides the anchor for the referenced connections. (§13.8.4)
2533. A caller-placed connection's type must match a type listed in the source instance's `outgoing:` clause or in the type's traits' contributions. (§13.8.4)
2534. A self-sourced connection placed in the source type's own exposition is exempt from `outgoing:` matching but not from endpoint typing. (§13.8.4)
2535. A connection's destination is supplied in the placement's body as a node reference: a bare identifier, any (possibly reactive) expression yielding a node reference, or a `Handle[N]` read as `Option[&N]`. (§13.8.4)
2536. A reactive or `Handle` destination makes membership at the destination a runtime fact: every node type in the reference's candidate envelope must declare `incoming: dynamic <ConnType>`. (§13.8.4)
2537. On a connection placement, `/expr` sets the connection's `default attr` and the attribute clause sets named attrs; neither targets the destination. (§13.8.4)
2538. A placement-level `when` modifier may gate a connection instance; it appears between `/Expr` and the attribute clause, before the body's `:`: `Cascade when filter.signal_active: next_filter`. (§13.8.4)
2539. A placement-level `when` predicate evaluates in the scope of the enclosing source instance, not the connection being placed, whose scope is unavailable because the connection is not yet constructed. (§13.8.4)
2540. Gating on a connection's own attrs requires a type-level `when:` clause inside the connection type's body, not a placement-level `when`. (§13.8.4)
2541. The placement-level `when` predicate names the enclosing source instance directly: inside `my_app`'s body, `WiresTo when my_app.debug_enabled: monitor`. (§13.8.4)
2542. A connection is owned by neither endpoint; it runs between two instances. (§13.8.4.1)
2543. A connection is initiated from its source and terminated at its destination; "source" and "destination" are the canonical terms, and "owner" is not. (§13.8.4.1)
2544. Re-pointing a connection's destination reference moves the connection's target without affecting either node's lifetime. (§13.8.4.1)
2545. A connection type may be supplied as a value (`Type[…]`) and placed, exactly as a node type can. (§13.8.4.2)
2546. Placing a supplied connection type binds the connection's `from` to the placing (enclosing) instance and takes its `to` at the placement site: `link_kind: the_car`. (§13.8.4.2)
2547. A supplied connection type is constrained by a trait carrying its endpoints, so the compiler checks both endpoints before the concrete type is known. (§13.8.4.2)
2548. `outgoing: C` with `C` a connection-type parameter admits exactly the supplied connection type. (§13.8.4.2)
2549. Attrs of a placed supplied connection type are set with the attribute clause, exactly as for a named connection. (§13.8.4.2)
2550. The `/expr` slot appears immediately after the placed type name, any flags, and the optional `as` name, and before the attribute clause. (§13.8.5)
2551. `/expr` is the placement's positional argument: it targets the placed type's `default attr`, whether the placed type is a node or a connection. (§13.8.5)
2552. Using `/expr` on a type without a declared `default attr` is a compile error. (§13.8.5)
2553. An unparenthesized `/expr` is restricted to a single atom — a literal, identifier, or path: `C/4`, `Filter/cutoff_default`. (§13.8.5)
2554. A compound `/expr` must be parenthesized: `C/(base * 2)`. (§13.8.5)
2555. On a connection placement, `/expr` sets the connection type's declared `default attr` while the destination is supplied in the body: `Drives/0.8: some_car`. (§13.8.5.1)
2556. A connection destination is a node reference whose type satisfies the connection's `to:` clause. (§13.8.5.1)
2557. A connection destination is a reference to an existing node, not a placement: an inline placement spec is not a valid destination — the target is placed first, then pointed at. (§13.8.5.1)
2558. Any expression yielding a node reference is admissible as a destination, including a function return: `Drives: pick(mode, some_car, other_car)`. (§13.8.5.1)
2559. A connection endpoint is a borrow; because a borrow cannot be stored in a cell, a destination that must persist or re-point across time is supplied as a `Handle[N]`, whose read is `Option[&N]`. (§13.8.5.1)
2560. A `Handle` destination points at the contained node while the resolution is `Some` and the connection freezes while it is `None`. (§13.8.5.1)
2561. A destination that varies at runtime makes the connection's `to` endpoint dynamic: the connection re-points among existing nodes as the reference changes. (§13.8.5.1)
2562. Every node a destination reference could yield belongs to the statically-known instance set — a function or cell cannot create a node — so the destination always resolves within that set. (§13.8.5.1)
2563. The compiler checks the `to:` constraint against the destination reference's type, so every possible target is well-typed. (§13.8.5.1)
2564. Topology-cycle analysis for a dynamic destination ranges over the node types the reference type admits. (§13.8.5.1)
2565. Each node type a dynamic destination reference admits must declare `incoming: dynamic <ConnType>`. (§13.8.5.1)
2566. A reactive selection is what makes a destination move; a compile-time-fixed selection resolves to one node and the membership is a static fact. (§13.8.5.1)
2567. On a node placement, `/expr` sets the type's `default attr` and the expression must match that attr's declared type: `Log /"Hello World"`. (§13.8.5.2)
2568. A node-placement `/expr` is equivalent to the inline attribute form naming the default attr: `Log /"Hello World"` equals `Log | message="Hello World"`. (§13.8.5.2)
2569. A node placement body contains zero or more child placements — parts and connections — with multiple children allowed. (§13.8.6)
2570. A connection placement body — the indented block or inline single-line form after `:` — contains exactly one node reference (the destination): no child placements, no inline placement specs, no attr settings, no multiple values. (§13.8.6)
2571. `to` and `from` are reserved as endpoint slots inside connection type bodies and cannot be used as attr names on connections. (§13.8.6)
2572. `to` and `from` do not appear in connection placement bodies. (§13.8.6)
2573. The parser distinguishes attribute settings from placements lexically: attribute settings follow a single leading `|` (or aligned continuation) in `name=expr` form; placements use the placement clause form. (§13.8.6)
2574. An attribute clause follows the TypeRef (and optional flags, instance name, and `/expr`) on the same line, introduced by exactly one leading `|`. (§13.8.7)
2575. After the leading `|`, attributes are separated by whitespace; intermediate `|` characters between attributes are not permitted. (§13.8.7)
2576. `name=value` sets attribute `name` to expression `value`: `Sensor s1 | gain=0.5`. (§13.8.7)
2577. A bare `name` in the attribute clause sets boolean attribute `name` to `true`: `Sensor s1 | active`. (§13.8.7)
2578. `!name` in the attribute clause sets boolean attribute `name` to `false`: `Sensor s1 | !calibrated`. (§13.8.7)
2579. Parentheses may be used freely around attribute values for grouping or disambiguation: `Sensor s1 | gain=(base + offset)`. (§13.8.7)
2580. Multi-line attribute continuation is purely a formatting variant and does not change semantics. (§13.8.7)
2581. Setting the same attribute twice on one placement is a duplicate-set compile error, parallel to record-field duplicate-set. (§13.8.7)
2582. Attribute settings target attrs declared on the placed type directly or inherited via satisfied traits. (§13.8.7)
2583. The bare `name` and `!name` forms require the attr to be of type `bool`; using them on a non-boolean attr is a compile error. (§13.8.7)
2584. A flag is a single non-letter character adjacent to the placed type's TypeRef with no intervening whitespace, aliasing a boolean attribute of that type: `Pin' p1`. (§13.8.8)
2585. Flags apply uniformly to node and connection placements: `WiresTo'! as my_wire: chip_b.in1`. (§13.8.8)
2586. Multiple flags may appear as a run after the TypeRef: `Component?* c1`. (§13.8.8)
2587. Flags are declared on attr declarations via the `@flag('c')` annotation: `@flag('!')` on `attr reverse_polarity: bool = false`. (§13.8.8)
2588. The `@flag` annotation argument is a `char` literal. (§13.8.8)
2589. Only boolean attrs may carry `@flag`; `@flag` on a non-boolean attr is a compile error. (§13.8.8)
2590. The permitted flag characters are exactly `'`, `!`, `?`, `*`, `+`, `^`, `~`, `@`, `$`. (§13.8.8.1)
2591. Flag characters are non-letter characters outside identifier syntax; `#`, being a valid identifier character, is excluded from the flag set. (§13.8.8.1)
2592. Within a type's effective attribute surface — its own attrs plus those inherited via satisfied traits — each flag character must be unique. (§13.8.8.2)
2593. Two attrs claiming the same flag character is a compile error at the type declaration site, identifying both attrs. (§13.8.8.2)
2594. At a placement site, each flag character in the run resolves to the boolean attr it aliases and sets that attr to `true`. (§13.8.8.3)
2595. There is no flag form for setting `false`; overriding a default-`true` attr to `false` uses the inline `!name` form. (§13.8.8.3)
2596. In placement position, a non-letter character immediately following the TypeRef path with no intervening whitespace is a flag-run opener. (§13.8.8.4)
2597. A flag character that doubles as a token elsewhere carries its other meaning in any non-placement position: `'` opens a char literal, `?` is postfix Try, `@` is the annotation prefix, `!` is the attribute-false marker. (§13.8.8.4)
2598. A boolean attr may be set via at most one mechanism per placement — flag form or inline `name`/`!name`/`name=value`; using two on the same attr is a compile error: `Pin' p1 | reverse_polarity=false` ✗. (§13.8.8.5)
2599. The two-mechanism duplicate uses the same diagnostic class as duplicate-set for inline attributes. (§13.8.8.5)
2600. A placement's inline parts have the fixed order TypeRef, flags run, `as` name, `/Expr`, `when` predicate, attribute clause, `:` body — each element after TypeRef optional: `Drives'! as my_drive / 0.8 | enhanced_handling: some_car`. (§13.8.9)
2601. The inline `when` clause gates the placement; its predicate is a boolean expression in placement scope. (§13.8.9)
2602. When `/Expr` is absent, `when` slots immediately after whichever preceding element is present: `Logger when debug_enabled`. (§13.8.9)
2603. The inline `when` is the single-placement modifier; `when` blocks and `given` blocks are not inline-parts modifiers — they appear as standalone entries at `expose:`/body level, each owning an indented arm body, outside the inline ordering. (§13.8.9)
2604. A `when` predicate containing an unparenthesized `:` must be parenthesized to avoid colliding with the body-introducer `:`. (§13.8.9)
2605. Multiple placements may appear on one line separated by whitespace; there is no comma separator (and no semicolon): `C/4 G'/4 A`. (§13.8.10)
2606. Same-line multi-placement requires each placement to be self-delimiting: its end is determined without lookahead into the next placement. (§13.8.10)
2607. A bare type, including any flag run, is self-delimiting: `C`, `G'`, `Pin'!`. (§13.8.10)
2608. A single-atom `/expr` is self-delimiting: `C/4`, `Filter/cutoff_default`. (§13.8.10)
2609. An `as` name is self-delimiting, consuming exactly one identifier: `G as a  rest  C`. (§13.8.10)
2610. A placement carrying an open expression — a `when` predicate, an attribute clause, or a compound `/expr` — is not self-delimiting and must be parenthesized or placed on its own line: `(Sensor as s1 | gain=0.5) (Sensor as s2 | gain=0.7)`. (§13.8.10)
2611. Naming via `as` never requires parentheses in same-line multi-placement; parenthesizing a named placement is a readability convention, not a rule. (§13.8.10)
2612. A placement introducing its own children body via `:` owns the rest of its line and the following indented block; it cannot share its line with sibling placements. (§13.8.10)
2613. A `:`-bearing placement may carry inline children on its own line when no sibling placements share the line: `SomePart: Child1 Child2 Child3`. (§13.8.10)

## Conditional Activation (Gates)

2614. A gate is a predicate or discriminant that conditions whether a node instance, a connection instance, or an exposed subtree is active. (§13.9)
2615. Gates are the structural conditional layer, distinct from the value conditionals `if`/`else`/`match`. (§13.9)
2616. A value conditional evaluates its scrutinee, selects one arm, discards the rest, and yields a value. (§13.9)
2617. A structural conditional constructs all of its arms and freezes the unselected ones, determining which subtree is live. (§13.9)
2618. `if`/`else`/`match` remain value selection everywhere they appear, including inside `derived` and `recurrent` expressions, and never gate structure. (§13.9)
2619. Exactly four surfaces gate structure: type-level `when:`, the placement modifier `when`, the `when` block, and the `given` block. (§13.9)
2620. Gates gate propagation, never existence: a gated instance is constructed unconditionally under the static-graph rule. (§13.9)
2621. A gated-off instance is frozen: its cells hold their values and resume when the gate reopens. (§13.9)
2622. Gates never unmount or reconstruct an instance; runtime-varying existence is exclusively the domain of `repeat`. (§13.9)
2623. The compiler reasons about the graph under the assumption that any gate may open or close at any commit. (§13.9)
2624. Routing is not a host concern; gate routing lives in the source, not in the host. (§13.9)
2625. A `when` predicate is, for reactive evaluation, a derived expression of type `bool`, subject to the same purity, provenance, cycle-detection, and recurrent-read rules as any derived. (§13.9.1)
2626. The expression forms accepted in a `when` predicate are identical to those accepted in any derived. (§13.9.1)
2627. Activation is never surfaced as a readable cell or field; source code cannot read gate state as a value. (§13.9.1)
2628. Being gated off is observable only structurally (the freeze) and, for effects, via the `suspend`/`resume` reconciler hooks delivered to the host. (§13.9.1)
2629. Activation means gate-state and is distinct from engagement, which is traversal reaching a connection's position in the exposition order. (§13.9.1)
2630. A gated-off (frozen) connection entry is not engageable; this is the only interaction between gate-state and traversal. (§13.9.1)
2631. A `when` predicate evaluates in the scope of the construct it modifies: a type-level `when: from.is_emitting` reads the body's own scope. (§13.9.1)
2632. A node or connection body may declare a `when:` predicate as a schema member in colon form: `when: weight > 0.0`. (§13.9.2)
2633. At most one `when:` clause is permitted per type; multiple `when:` declarations in one body are a compile error. (§13.9.2)
2634. Traits cannot declare or require a `when` predicate; a `when:` clause in a trait declaration is a compile error. (§13.9.2)
2635. A placement may attach a colon-less `when` modifier that introduces or overrides a gate for that one instance: `Logger l1 when debug_enabled`. (§13.9.3)
2636. Connection placements accept the `when` modifier: `ShowsCount when from.count > 0: d1`. (§13.9.3)
2637. Parts placed inside a parent's body carry `when` clauses with the same grammar as top-level placements: `Logger as l2 when my_app.verbose`. (§13.9.3)
2638. In inline-parts ordering `when` sits after `/Expr` (if present) and before the attribute clause: `Filter f1 / "low-pass" when dsp_mode is DspMode::Realtime | gain=0.5`. (§13.9.3)
2639. When `/Expr` is absent, the `when` clause follows whatever placement element precedes it. (§13.9.3)
2640. A `when` predicate must have type `bool`; a non-bool predicate is a compile error. (§13.9.4)
2641. Beyond the `bool` requirement, `when` predicates follow normal expression scope rules with no special restrictions. (§13.9.4)
2642. A type-level predicate may reference the type's own cells by bare name plus anything visible at the type's declaration scope (module signals, consts, imports). (§13.9.4)
2643. A connection's type-level predicate may additionally reference `from`, `to`, and `pair`. (§13.9.4)
2644. A placement-level predicate may reference the full placement scope: siblings, parent attrs, top-level cells, and any identifier resolvable at that point. (§13.9.4)
2645. A placement-level `when` totally replaces the type-level `when` for that instance; the two predicates are never conjoined or stacked: `Pulse when my_app.debug_audio: l1`. (§13.9.5)
2646. A placement needing both predicates must combine them explicitly in the placement-level form: `Pulse when my_app.is_emitting and my_app.debug_audio: l1`. (§13.9.5)
2647. A placement with no `when` modifier inherits the type-level `when`, if any. (§13.9.5)
2648. A placement with neither a type-level nor a placement-level `when` is unconditional: always active. (§13.9.5)
2649. A gate predicate may reference cells of the gated instance itself: `when: weight > 0.0` against the instance's own `attr weight`. (§13.9.6)
2650. A cyclically self-referential gate predicate evaluates against the gated cells' previously-committed values from the prior commit. (§13.9.6)
2651. A self-conditional gate whose predicate reads a same-instance `recurrent` cannot reopen once closed; recurrent-based self-gates are inherently one-shot latches. (§13.9.6)
2652. A self-gate reading an `attr` or `signal` can reopen, because input cells keep receiving values from outside the gated subtree. (§13.9.6)
2653. The gating runtime model is Model B: frozen-when-gated, snap on activation. (§13.9.7)
2654. The runtime evaluates gate state at edge level on each propagation cycle. (§13.9.7)
2655. A gate-true edge propagates normally. (§13.9.7)
2656. Gated subgraphs do no work; a permanently-gated node costs exactly the evaluation of its `when` predicate. (§13.9.7)
2657. A construct freezes under exactly two conditions: its gate evaluating false, or, for a connection, a dynamic destination resolving to `None`. (§13.9.7)
2658. An unresolved dynamic destination behaves as gate-false, and resolution returning `Some` behaves as the false-to-true flip, including its propagation event. (§13.9.7)
2659. Traversal position never freezes a construct. (§13.9.7)
2660. Incoming connections still deliver into a gated node's input cells; input cells stay live. (§13.9.7)
2661. A gated node's `when` predicate re-evaluates whenever any cell in its provenance set changes. (§13.9.7)
2662. A gated node's recurrents do not advance or re-evaluate; they hold their last committed value. (§13.9.7)
2663. Input changes during a gated period that would have triggered a recurrent are lost: triggers are not queued and gate-open does not replay them. (§13.9.7)
2664. A recurrent does not recompute at the open snap; it advances only on its next trigger during an active period. (§13.9.7)
2665. A recurrent's self-history is preserved across a gated gap; the first post-gap evaluation reads pre-gap `.previous`/`.past` values. (§13.9.7)
2666. A recurrent carrying `@reset_on_reopen` has its history cleared at gate-reopen, so the first post-gap evaluation reads fallbacks. (§13.9.7)
2667. A gated node's deriveds do not recompute in the steady gated state; they hold their last committed value. (§13.9.7)
2668. Deriveds read by the `when` predicate stay current while gated: the runtime keeps live the minimum subgraph needed for predicate evaluation. (§13.9.7)
2669. A gated node's outputs do not propagate; its outgoing connections do not deliver to their destinations. (§13.9.7)
2670. A gated connection does not propagate at all; its destination receives nothing through that connection. (§13.9.7)
2671. A false-to-true predicate flip is a propagation event: the frozen deriveds re-evaluate against current upstream state in topological order. (§13.9.7)
2672. Gated-period values are never replayed at gate-open; they are recomputed as of now, and downstream sees a single jump from frozen to current value. (§13.9.7)
2673. The gate primitive performs no smoothing of open-snap discontinuities; smoothing is delegated to the parameter system. (§13.9.7)
2674. On a true-to-false flip the subtree freezes: its cells hold their last values and stop recomputing. (§13.9.7)
2675. At gate-close the runtime fires the `suspend` reconciler hook for every effect in the newly-frozen subtree, at the commit boundary. (§13.9.7)
2676. Only effects need close-time handling; the `suspend`/`resume` protocol concerns effects exclusively, freezing being complete for pure nodes and connections. (§13.9.7)
2677. A gated connection receives no `suspend`, because effects cannot be instantiated in connection bodies. (§13.9.7)
2678. An effect gated by a `when`/`given` block inside an `effects:` clause freezes and receives `suspend`/`resume` by the same protocol as any gated instance. (§13.9.7)
2679. An instance is effectively active iff its own gate and every ancestor gate on the path from the root are open. (§13.9.7)
2680. Closing any ancestor gate transitively freezes every descendant and suspends every effect within it, regardless of the descendants' own gate states. (§13.9.7)
2681. Reopening an ancestor restores each descendant to its own gate state; a descendant whose own gate is false stays frozen. (§13.9.7)
2682. Cell reads on gated subgraphs always return a defined value of type `T`, never `Option[T]`. (§13.9.7)
2683. Reads on a gated instance return frozen values: the last value committed during an active period, or the initial value if never active. (§13.9.7)
2684. The reactive dependency graph is built independent of gate state; gates suspend propagation at runtime and never remove edges from the static graph. (§13.9.8)
2685. In per-commit DAG construction, gated edges contribute no dirty propagation to destination output-affecting cells but do contribute to input cells and `when`-predicate provenance. (§13.9.8)
2686. Gates do not relax the `Circularity` rule: a cycle through `Link` declared `when: false` still requires a `Circularity`-satisfying connection type. (§13.9.9)
2687. The cycle constraint must hold across all reachable gate configurations; a gated edge is structurally still an edge. (§13.9.9)
2688. The compile-time cycle check is sound because gated instances are suspended, never removed: every reachable gate configuration is a subset of one static edge set. (§13.9.9)
2689. A dynamic-destination connection stays inside the compile-time cycle check: its candidate edges are a statically-known set and `Circularity` must hold for every candidate target. (§13.9.9)
2690. A dynamic destination re-points within a fixed instance set; it creates and drops nothing. (§13.9.9)
2691. Adding, removing, or modifying a `when` predicate is a reload-safe change; the predicate is structural metadata, not cell identity. (§13.9.10)
2692. On reload the new predicate participates in the next commit, and cells retain their values. (§13.9.10)
2693. A reloaded predicate takes effect prospectively only; states that would have differed historically are not recomputed retroactively. (§13.9.10)
2694. The diagnostic classes for `when` clauses are normative; concrete wording is implementation-defined. (§13.9.11)
2695. Diagnostic class: non-bool `when` predicate. (§13.9.11)
2696. Diagnostic class: multiple `when:` clauses in a single type body. (§13.9.11)
2697. Diagnostic class: `when:` in a trait declaration. (§13.9.11)
2698. Diagnostic class: unresolved reference in a `when` predicate, surfaced as a standard name-resolution failure in `when`-clause context. (§13.9.11)
2699. Diagnostic class: cycle through `when` provenance; gate predicates participate in cycle detection identically to deriveds. (§13.9.11)
2700. The `when` block selects which placements or children are active by boolean condition; all arms are constructed, the live arm propagates, the rest freeze. (§13.9.12)
2701. The `when` block appears wherever reactive structure is declared — `expose:` clauses, node bodies, placement bodies, `effects:` clauses, effect `desired:` blocks — never as an inline modifier. (§13.9.12)
2702. Block arms hold whatever entries their context admits: placements at `expose:` and in bodies, effect entries in `effects:`, desired cells and `repeat`s in `desired:`. (§13.9.12)
2703. Each arm body is its own scope: arm-local names nest under the arm and do not collide across arms. (§13.9.12)
2704. Cells declared in a `desired:` arm are nested and exempt from the flat-namespace uniqueness rule of effect desired blocks, like a `repeat`'s per-key cells. (§13.9.12)
2705. Simple form: `when cond:` introduces a then-body of placements, and an optional sibling `default:` block supplies the else-body. (§13.9.12)
2706. In the simple form, while `cond` is true the then-body is active and the `default:` body frozen; when false the roles reverse. (§13.9.12)
2707. A `when cond:` block with no sibling `default:` gates its then-body alone: active while `cond` holds, frozen otherwise, with no alternative. (§13.9.12)
2708. Multi-way form: a `when:` header with no condition takes `guard: body` arms plus an optional `default:` arm: `cond_a: ArmA`. (§13.9.12)
2709. Multi-way arms are tried in declaration order; the first arm whose guard holds is active and all others are frozen. (§13.9.12)
2710. Every line at the arm indent of a multi-way `when:` block is a guard arm, never a connection entry; connection placements appear only inside arm bodies. (§13.9.12)
2711. Guards are arbitrary boolean expressions, using the same predicate vocabulary as any `when` predicate. (§13.9.12)
2712. A `when:` block is an open selector that cannot be exhaustiveness-checked; `default:` is required unless some guard is provably total. (§13.9.12)
2713. Switching the active arm of a `when` block runs the close pass on the deactivating arm and the open snap on the activating arm. (§13.9.12)
2714. Placements in all arms of a `when`/`given` block in a node or placement body count toward `parts:` cardinality: two arms each placing one `Oscillator` violates `parts: Oscillator [=1]`. (§13.9.12)
2715. At `expose:` level, block arms do not consume `parts:` cardinality: exposition references already-placed parts and constructs only wrapper placements. (§13.9.12)
2716. A `when`/`given` block owns its indented body and occupies its own lines; it cannot share a line with sibling placements. (§13.9.12)
2717. `given` gates among constructed arms by the current variant of a sum-typed scrutinee: `given mode:` with arm `Realtime: RealtimeChain`. (§13.9.13)
2718. `given` is the structural counterpart of value `match`: same arm shape, but `given` builds all arms and freezes the inactive ones instead of discarding. (§13.9.13)
2719. `given <scrutinee>:` introduces variant-pattern arms written exactly as value-`match` arms: bare `Pattern: body` with no per-arm keyword. (§13.9.13)
2720. Inside a `given` block every line at the arm indent is an arm; connection entries appear only within arm bodies, never at arm position. (§13.9.13)
2721. Each `given` arm body is itself a list of placements indented under the arm. (§13.9.13)
2722. `given` is admitted in the same contexts as the `when` block, including `effects:` clauses, where each arm body is a list of effect entries. (§13.9.13)
2723. In an `effects:` clause an arm header `Variant:` (colon form) never collides with a named effect entry `name = …` (equals form). (§13.9.13)
2724. `given` arms bind variant payloads exactly as `match` patterns do: `Active(session):` binds `session`. (§13.9.13)
2725. A bound payload is a live reactive projection of the scrutinee's current payload, not a snapshot taken at selection time. (§13.9.13)
2726. The active `given` arm re-evaluates when its bound payload changes. (§13.9.13)
2727. At a variant flip the deactivating arm freezes at its last value and does not re-evaluate against the new scrutinee; its payload projection never reads a payload the current variant lacks. (§13.9.13)
2728. At a variant flip the newly-active arm projects the current variant's payload; the flip follows the standard gate-close and gate-open semantics. (§13.9.13)
2729. A `given` block is a closed selector: it must be exhaustive over the scrutinee's variants, checked by the `match` exhaustiveness rules. (§13.9.13)
2730. A `default:` arm in `given` is permitted only as an explicit catch-all that suppresses the exhaustiveness obligation, as in value `match`. (§13.9.13)
2731. Selecting a type value with `match` over `Type[…]` and gating structure with `given` are both legal: `match` chooses one type placed once; `given` builds every arm and switches which is live. (§13.9.13)

## Reactive Evaluation & Commit

2732. The reactive runtime exposes exactly two state operations: writes (signal/attr), which stage new values without evaluation, and commit, which evaluates and atomically publishes a new consistent snapshot. (§13.10)
2733. A write call (`runtime.write_signal`, `runtime.write_attr`, or any write inside `runtime.transaction`) stages the new value; the staged value is not yet visible to observers. (§13.10.1)
2734. No derived recomputation and no recurrent advancement happens at write time. (§13.10.1)
2735. Staged writes accumulate until the next `runtime.commit()`; only the net change from the previous commit matters. (§13.10.1)
2736. Dirty bits are determined at commit time, not per write. (§13.10.1)
2737. A write sequence ending with a cell's value equal to its previous-committed value produces no dirty bit and fires no triggers, regardless of intermediate values: `write_signal(x_id, 1); write_signal(x_id, 0); commit()` dirties nothing. (§13.10.1)
2738. Write staging and net-revert semantics are identical inside and outside a transaction. (§13.10.1)
2739. Commit step 1 computes the dirty set: a writable cell (signal, attr) is dirty iff its staged value differs from its previously-committed value. (§13.10.2)
2740. A reactive expression (derived, recurrent, or `observe` arm trigger set) re-evaluates when a referenced cell is dirty; value-change semantics are operationalized as current-commit value ≠ previous-commit value. (§13.10.2)
2741. Dirty propagation extends to all derived cells transitively dependent on dirty roots. (§13.10.2)
2742. Dirty propagation extends to all recurrents whose triggers fired this commit. (§13.10.2)
2743. After dirty-set computation, no new dirty bits are added during the rest of the commit cycle. (§13.10.2)
2744. Commit step 2 computes evaluation order by topologically sorting the per-commit DAG. (§13.10.2)
2745. The per-commit DAG's nodes are the dirty derived expressions plus each recurrent whose expression became dirty this commit. (§13.10.2)
2746. A recurrent contributes one DAG node per cell, or one node per tuple group for tuple-coupled recurrents. (§13.10.2)
2747. A recurrent wrapped in `observe` contributes the whole observe expression as its DAG node, with arm selection deferred to evaluation. (§13.10.2)
2748. In the per-commit DAG, recurrent self-history reads (`.previous`/`.past`) are treated as inputs at their previous-committed values, which breaks reactive cycles. (§13.10.2)
2749. Reads of deriveds, signals, attrs, and streams follow normal dependency edges within the commit. (§13.10.2)
2750. A dependency edge whose gate predicate evaluates false does not propagate to destination outputs. (§13.10.2)
2751. Commit step 3 evaluates each DAG node in topological order by invoking its behavior through the runtime ABI. (§13.10.2)
2752. During commit evaluation, signal and attr reads resolve to the current staged values (most recent writes since the previous commit). (§13.10.2)
2753. During commit evaluation, a derived read resolves to the this-commit value if that derived was already evaluated this commit, and to its previously-committed value if it is not in the dirty set. (§13.10.2)
2754. Recurrent self-history reads (`.previous`/`.past`) always resolve to previous-committed values (lockstep). (§13.10.2)
2755. Derived behaviors write their results into the pending snapshot. (§13.10.2)
2756. Recurrent expression results are held aside, invisible to in-pass evaluation, until the recurrent-advancement step. (§13.10.2)
2757. `observe` arm selection stage one (activation): the candidate arms are those whose trigger set fired this commit and whose `where` filter, if present, passes. (§13.10.2)
2758. `observe` arm selection stage two: the first candidate arm in declaration order wins, and its expression evaluates to produce the observe's value for the commit. (§13.10.2)
2759. Non-selected candidate arms' expressions are not evaluated. (§13.10.2)
2760. Commit step 4 writes the values computed during evaluation into the recurrent cells; after this step, recurrent reads return the newly-advanced values. (§13.10.2)
2761. Commit step 5 atomically publishes the just-committed values as the visible snapshot, so observers' subsequent reads return the new state. (§13.10.2)
2762. Commit step 6 clears the dirty bits. (§13.10.2)
2763. Writes during a commit are forbidden. (§13.10.2)
2764. When two same-level DAG nodes have no dependency between them, the tiebreaker is deterministic source declaration order: the cell declared earlier evaluates first. (§13.10.3)
2765. For same-level cells across different node instances, the tiebreaker is placement order at construction time. (§13.10.3)
2766. Recurrent expressions across multiple recurrent cells evaluate in lockstep, with no observable internal ordering between them. (§13.10.3)
2767. `runtime.transaction` batches multiple writes into one logical change: `runtime.transaction(|tx| { tx.write_signal(a_id, new_a) })`. (§13.10.4)
2768. Writes within a transaction accumulate as staged values applied atomically at transaction close. (§13.10.4)
2769. A panic during a transaction closure follows trap-track semantics: the process aborts, with no rollback. (§13.10.4)
2770. Nested transactions are flattened: only the outermost `runtime.transaction` commits, and inner `runtime.transaction` calls are no-ops with respect to commit. (§13.10.4)
2771. All writes since the outer transaction's start are committed together at the outer close. (§13.10.4)
2772. `tx.abort()` rolls back the transaction's accumulated writes, restoring staged values to their pre-transaction state, while the closure returns normally. (§13.10.4)
2773. `tx.abort()` is the only rollback path. (§13.10.4)
2774. Transaction close applies the staged writes; evaluation and visibility happen only at the next `runtime.commit()`. (§13.10.4)
2775. Outside transactions, each individual `runtime.write_*` call behaves as its own one-write transaction. (§13.10.4)
2776. A dynamic dependency is one whose depended-on cell identity can change between commits. (§13.10.5)
2777. Handle resolution — individually or in its collective dynamic-namespace form — is the only source of dynamic dependency in the language. (§13.10.5)
2778. A read through a `Handle` reaches whichever entity the handle currently resolves to, and the dependency flips on re-point, mount, and dismount. (§13.10.5)
2779. The cell or expression producing a dynamic target reference (e.g. a connection's `to`) is itself part of the reading body's provenance. (§13.10.5)
2780. When a dynamic target re-points, deriveds reading through it (e.g. a connection's `to.*` reads) are marked dirty and re-evaluate in the next commit against the new target's cells. (§13.10.5)
2781. While a connection's `to` designates a particular node, the connection's reads subscribe to that node's cells; on re-point the subscription is re-established against the new target, and a write to the old target no longer dirties the connection. (§13.10.5)
2782. The same two-part mechanism (reference as dependency; referent cells as dependencies only while selected) governs every handle read, including node-body deriveds reading `repeat`-view handles. (§13.10.5)
2783. A dismount flips a handle's resolution to `None` and drops the referent-cell subscription. (§13.10.5)
2784. Dynamic namespace cells are the collective form of handle resolution: an operator over `incoming.<C>` subscribes to the membership cell itself and, per current member, to the cells its per-element fn reads. (§13.10.5)
2785. A mount or dismount re-establishes dynamic-namespace member subscriptions exactly as a re-point does. (§13.10.5)
2786. Dynamic dependencies do not alter per-commit evaluation order: within any one commit every handle resolution is fixed, so that commit's dependency graph is static. (§13.10.5)
2787. The candidate set of possible handle targets is statically known, which keeps topology-cycle analysis a compile-time check. (§13.10.5)

## Cycle Handling

2788. Cycles are handled at two distinct layers with separate rules: reactive expression cycles between reactive cells, and topology cycles between node instances via connection placements. (§13.11)
2789. The compiler builds the reactive dependency graph by walking every `derived` body and every recurrent expression body, recording the set of reactive cells each reads. (§13.11.1)
2790. Reactive-dependency edges run from each read cell to the reading expression's output cell. (§13.11.1)
2791. Signal, attr, derived, and recurrent reads all contribute reactive-dependency edges. (§13.11.1)
2792. The reactive dependency graph is the basis for the per-commit DAG constructed at each commit. (§13.11.1)
2793. A cycle consisting only of derived-to-derived edges is a compile error (instantaneous cycle): derived `a.x` reading `b.y` while `b.y` reads `a.x` is rejected. (§13.11.2)
2794. A recurrent's expression may read the recurrent's own previous value: `on t: x + 1`. (§13.11.2)
2795. A recurrent's expression may read another recurrent cell's previous value: `on t: other.value`. (§13.11.2)
2796. A static cycle passing through a recurrent is legal: a derived may read a recurrent whose expression reads that derived back, since each commit the derived sees the recurrent's last-committed value. (§13.11.2)
2797. The per-commit DAG is formed by treating every recurrent read as an input valued at the previous commit's result, which breaks all valid reactive cycles. (§13.11.3)
2798. Reads OF a recurrent cell from any expression context — derived bodies, recurrent expressions, `where` guards — always return the previous-committed value. (§13.11.3)
2799. Reads FROM a `where` guard (the cells the guard's own expression reads) are not previous-commit inputs: the guard evaluates within the current commit to determine whether its arm fires. (§13.11.3)
2800. A recurrent cell on a cycle behaves as a one-commit delay element: reads see the previous-committed value regardless of what its expression computes this commit. (§13.11.4)
2801. Delay-element behavior is fully specified at the language level; the runtime requires no per-implementation convention beyond the `recurrent` declaration. (§13.11.4)
2802. A topology cycle is a cycle in the topology graph and is analyzed at compile time over every possible wiring. (§13.11.5)
2803. Topology-graph nodes are placed instances; each placed connection contributes a directed edge from its `from`-endpoint instance to its `to`-endpoint instance, labeled with the connection type. (§13.11.5)
2804. A connection whose destination is a dynamic reference contributes candidate edges to each node type the reference's static type admits, analyzed over node types as a sound over-approximation of their instances. (§13.11.5)
2805. Candidate-envelope inference is by static type: a concrete node type contributes itself, a trait contributes every node type satisfying it (including via a supertrait), and the bare `Node` bound contributes all node types. (§13.11.5)
2806. A topology cycle is a sequence of distinct directed edges returning to its starting node. (§13.11.5)
2807. Every topology cycle in the construction-time node graph must traverse at least one connection whose type satisfies `Circularity`; cycles consisting only of non-`Circularity` connections are compile errors. (§13.11.5)
2808. A dynamic reference can only resolve to an already-placed node of one of its envelope's types, so the candidate set is statically known. (§13.11.5)
2809. Every node type in a connection's candidate envelope must declare `incoming: dynamic` for that connection type (the membership-marker rule). (§13.11.5)
2810. The compile-time cycle check verifies that every cycle in the candidate graph traverses a `Circularity` connection, so no reachable wiring can form a non-`Circularity` cycle. (§13.11.5)
2811. Candidate-edge cycle analysis is sound but conservative in proportion to the width of the candidate set. (§13.11.5)
2812. `Circularity` is a marker trait whose semantic effect is domain-defined. (§13.11.5)
2813. Connection types that imply simultaneous source-destination activation should not satisfy `Circularity`. (§13.11.5)
2814. Topology-cycle validation and reactive-expression-cycle validation are independent passes; either kind of cycle can exist without the other. (§13.11.5)

## Reactivity Boundary & Cell Storage

2815. The reactivity boundary determines which expressions become reactive and which remain ordinary computation. (§13.12)
2816. The compiler computes for each expression a provenance set: the reactive cells (signals, attrs, recurrents, derived results) it reads, including transitively through function calls and field accesses. (§13.12.1)
2817. An expression is reactive iff its provenance set is non-empty. (§13.12.1)
2818. A derived's dependency set, used by dirty-bit propagation, is decided from its provenance set. (§13.12.1)
2819. Diagnostics rejecting reactivity where compile-time values are required give precise provenance blame (which cell makes the value reactive, and where). (§13.12.1)
2820. A provenance set is normally static; the sole exception is a read through handle resolution, where the identity of the depended-on cells changes on re-point, mount, or dismount. (§13.12.1)
2821. Under handle resolution, the resolution itself is in the provenance, and a referent's cells are in the provenance only while that referent is resolved (a member's cells only while it is a member). (§13.12.1)
2822. A function body is not itself reactive: it takes parameters as ordinary values and returns ordinary values. (§13.12.2)
2823. Reactivity emerges at the call site: a reactive expression calling `some_fn(signal_a, signal_b)` includes `signal_a` and `signal_b` in its provenance set. (§13.12.2)
2824. When a signal argument changes, the containing reactive expression re-evaluates and re-runs the function with the new concrete values; the function never observes the signal itself. (§13.12.2)
2825. Reactive transparency applies to `fn` declarations only; operators are not reactive-transparent and allocate cells per instantiation. (§13.12.2)
2826. The call-site distinction is by callee declaration kind: `some_fn(my_signal)` evaluates per-emission, while `some_op(my_signal)` instantiates an operator with internal state. (§13.12.2)
2827. A parameter declared with type `Signal[T]` binds to the cell reference itself rather than to its current value: `fn some_fn(s: Signal[T])`. (§13.12.2)
2828. Per-emission versus cell-reference parameter binding is distinguished solely by the declared parameter type, with no call-site syntactic difference. (§13.12.2)
2829. A function whose body directly reads a reactive cell passes that provenance to its return value, so callers gain the directly-read cells: `shifted(base_value)` reading signal `global_offset` yields provenance `{base_value, global_offset}`. (§13.12.2.1)
2830. Provenance analysis is transitive through function calls; module-level globals read by called functions are included. (§13.12.2.1)
2831. When a function branches on its arguments, the provenance contributions of all branches are computed independently and unioned: branch A reading X and branch B reading Y contributes `{X, Y}` to callers. (§13.12.2.2)
2832. Branch-union provenance is a conservative over-approximation that may cause unnecessary re-evaluation but never under-tracks dependencies. (§13.12.2.2)
2833. A closure constructed with a reactive value in scope captures the value at construction time as a snapshot, not the live cell: `fn(x: f32): x > current_threshold` captures the snapshotted `f32`. (§13.12.3)
2834. Calling a closure later does not observe changes made to the source signal after capture. (§13.12.3)
2835. Closures are not reactive: they do not participate in the dependency graph. (§13.12.3)
2836. Reactive cells (signal, attr, recurrent, derived) accept any value type. (§13.12.4)
2837. The compiler determines each cell's storage strategy from the value type's size and shape. (§13.12.4)
2838. A type whose size is at most the platform's atomic word width (typically 8 bytes) is stored directly in the cell. (§13.12.4)
2839. Publishing a direct-storage cell at commit requires no allocation, no refcount, and no pool. (§13.12.4)
2840. A backend may store 9–16-byte types across two consecutive cells where the platform supports it, otherwise such types fall back to index-based storage; this is a backend choice, not a language requirement. (§13.12.4)
2841. `Result[T, E]` and `Option[T]` store directly when discriminant plus maximum payload fits the atomic word: `Option[i32]` is direct (1-bit tag + 4 bytes). (§13.12.4)
2842. A type larger than the atomic word is stored as a one-word pool index into a per-type pool: the cell holds the index and the value lives in the pool. (§13.12.4)
2843. Dynamically-sized types (`string`, `Vec[T]`, `HashMap[K, V]`, and other heap-allocated dynamic-size collections) always use index-based storage. (§13.12.4)
2844. `string` cells use the string pool; other dynamic types use per-type pools generated by the compiler. (§13.12.4)
2845. Pool-entry refcounting manages lifetime: when the cell is overwritten and no consumer holds a reference, the pool slot is released. (§13.12.4)
2846. Pools are per-type, each sized at runtime construction from the IR. (§13.12.4)
2847. Committing an index-stored cell publishes the one-word pool index unchanged. (§13.12.4)
2848. Writing a pool-stored cell allocates or reuses a pool slot, copies the value in, and stages the index — work proportional to the value's size plus a pool acquire. (§13.12.4)
2849. Reading a pool-stored cell resolves the pool index to reach the value. (§13.12.4)
2850. Real-time hot paths (audio DSP, animation) should prefer cell types that fit direct storage, since pool storage involves allocation and refcounting. (§13.12.4)
2851. Direct storage has zero overhead versus plain atomic reads and writes. (§13.12.4)
2852. Index storage adds an indirection on read and a pool allocation on write. (§13.12.4)
2853. A direct-stored value narrower than the cell width occupies one cell with appropriate padding or extension. (§13.12.4)
2854. The stdlib provides three dynamic-size collection types usable as reactive cell types — `Vec[T]`, `SmallVec[T, N]`, `RingBuf[T, N]` — each using index-based pool storage. (§13.12.4)
2855. The dynamic-collection cost model is normative: implementations must achieve the documented complexity bounds. (§13.12.4)
2856. `Vec[T]` is a persistent trie with structural sharing across versions: append/push O(log32 n), read by index O(log32 n). (§13.12.4)
2857. `SmallVec[T, N]` stores up to N elements inline and uses the heap beyond: append O(n) bounded, read by index O(1). (§13.12.4)
2858. `RingBuf[T, N]` is a fixed-capacity ring that drops the oldest element when full: append O(1), read by index O(1). (§13.12.4)
2859. In `SmallVec[T, N]` and `RingBuf[T, N]`, `N` is a const-generic slot-count parameter supplied as a value at use sites, not a second type parameter: `SmallVec[i32, 8]`. (§13.12.4)
2860. A const-generic parameter is marked by the `const` keyword in the declaration: `type SmallVec[T, const N: usize]`. (§13.12.4)
2861. Generic parameter lists use commas throughout, never semicolons. (§13.12.4)
2862. `Vec[T]` is the default collection for unbounded growth. (§13.12.4)
2863. `vec.with(value)` returns a new `Vec[T]` with `value` appended, preserving the no-mutation rule. (§13.12.4)
2864. `vec + value` is the operator form equivalent to `vec.with(value)`. (§13.12.4)
2865. A recurrent's expression returns the new collection value, and the runtime publishes it as the cell's next committed value. (§13.12.4)
2866. Collection implementation strategies (persistent trie, inline+heap, fixed ring) must be observably indistinguishable from always-returns-new semantics; sharing and in-place optimization are runtime concerns transparent at the language level. (§13.12.4)
2867. If user code never references `Vec[T]`, `SmallVec[T, N]`, or `RingBuf[T, N]`, the runtime cost is zero: the reactive state buffer remains a flat fixed-size table and the extensible pool machinery is not exercised. (§13.12.4)
2868. Binary size grows only when the dynamic collection types are used. (§13.12.4)
2869. `dyn` trait objects are not permitted as reactive cell types in v1. (§13.12.4)
2870. Functions and closures are not permitted as reactive cell types in v1. (§13.12.4)
2871. For collections of reactive things, composition via parts — a parent node with N parts of the same child type, each holding its own attrs and deriveds — is the canonical mechanism. (§13.12.4)
2872. Collection-typed reactive cells work via pool storage, but each write rebuilds or replaces the whole collection — suited to batch updates, not fine-grained mutation. (§13.12.4)
2873. A fixed-extent cell type (`T[N]`, tuples, records) has compile-time-known layout: every element's offset within the cell, or within the pool slot for sizes above the word, is determined at compile time. (§13.12.4)
2874. A `for` iterating a fixed-extent cell's value compile-time-unrolls to element reads at known offsets — no runtime loop counter, bounds check, or per-iteration dispatch: `for x in buf.value():` over `Cell[f32[64]]`. (§13.12.4)
2875. Direct-storage fixed-extent cells are read with no indirection; index-storage ones with one indirection (resolving the current pool index) plus compile-time-known offsets. (§13.12.4)
2876. A fixed-extent cell's slot is pre-allocated at runtime construction (directly or as a fixed-size pool slot), the runtime writes the value into it, and making it visible is a constant-time publish — no per-emission allocation, realloc, or resize. (§13.12.4)
2877. A reactive value cannot be used where a compile-time-known value is required. (§13.12.5)
2878. A reactive array size is a compile error: `i32[some_signal]` ✗. (§13.12.5)
2879. A reactive value flowing into a const-generic argument position is a compile error: `Buffer[some_signal]` ✗. (§13.12.5)
2880. A `const` declaration whose right-hand side is reactive is a compile error. (§13.12.5)

## Reactive Error Handling

2881. The two-track failure model applies uniformly to reactive contexts. (§13.13)
2882. A derived or recurrent expression that traps during evaluation — arithmetic overflow under default operators, division by zero, out-of-range array index, or explicit `panic` — aborts the process. (§13.13.1)
2883. The runtime does not isolate traps within behavior invocations. (§13.13.1)
2884. There is no errored-cell sentinel state at the runtime level. (§13.13.1)
2885. There is no `catch_unwind` boundary and no continuation past a trap. (§13.13.1)
2886. Recoverable failures in reactive code use value-track errors: declare the derived's type as `Result[T, E]` or `Option[T]` and produce `Err(...)` or `None` explicitly for failure cases. (§13.13.2)
2887. Downstream reactive expressions propagate value-track errors through `?` or `match`. (§13.13.2)
2888. Arithmetic that may overflow but should fail recoverably uses the checked operator variants (`+?`, `-?`, ...), whose `Option[T]` results flow through the type system. (§13.13.2)
2889. Where an `Option` is provably `Some` — most often a `Handle` whose referents are all statically placed — postfix `!` unwraps it with no runtime check and no propagation. (§13.13.2)
2890. Postfix `!` is a compile error wherever the compiler cannot prove the `None` case impossible. (§13.13.2)
2891. The reactive evaluation context does not modify trap semantics: a behavior that traps aborts the process, the same as a free-function trap. (§13.13.3)
2892. The language provides no hidden recovery mechanism in reactive contexts; graceful handling requires value-track errors. (§13.13.3)

## Runtime Interface

2893. The runtime interface is the normative contract every backend must satisfy — the abstract counterpart to the IR the compiler emits. (§13.14)
2894. The shape of the runtime interface is normative; the host-language binding is implementation-defined. (§13.14)
2895. A runtime is a transactional store of reactive cells with snapshot isolation. (§13.14)
2896. Deriveds are materialized views of the runtime's transactional store. (§13.14)
2897. Side effects are reconciled at the commit boundary. (§13.14)
2898. The core verbs `write`/`push` stage external input into the runtime. (§13.14)
2899. The core verb `commit` settles all dependents glitch-free, advances the version, and publishes a consistent snapshot. (§13.14)
2900. The core verbs `acquire_snapshot` + `read` observe the latest published snapshot. (§13.14)
2901. The core verb `transaction` groups writes atomically. (§13.14)
2902. The core verb `register_reconciler` installs effect hooks. (§13.14)
2903. The core verb `teardown` shuts the reactive graph down. (§13.14)
2904. The runtime calls behaviors and reconciler hooks back into host code. (§13.14)
2905. A backend advertises optional capabilities beyond the mandatory core; a host queries what a runtime supports rather than assuming the union. (§13.14)
2906. The observability and cadence cell hints bias how a backend stores cells. (§13.14)
2907. A runtime need only honor the verbs' observable contract; the mechanism is its own choice. (§13.14)
2908. Startup step 1 loads the IR (per §15.4). (§13.14.1)
2909. Startup step 2 allocates cell storage (per §14.3). (§13.14.1)
2910. Startup initializes all reactive cells and evaluates all `when` predicates in topological order over their init-time read dependencies, per §13.2.6's startup pass rules. (§13.14.1)
2911. At startup, signal cells receive their declared initial values. (§13.14.1)
2912. At startup, attr cells receive their declared defaults or placement-supplied values. (§13.14.1)
2913. At startup, recurrent cells evaluate their expressions for the first time, with `.previous`/`.past` returning their fallback values since no history exists. (§13.14.1)
2914. At startup, derived cells are computed by evaluating their expression bodies. (§13.14.1)
2915. At startup, stream cells begin empty. (§13.14.1)
2916. At startup, `when` predicates are evaluated to determine each instance's initial gate state. (§13.14.1)
2917. Placement-level `when` predicates evaluate alongside type-level `when:` predicates in the same topological startup pass. (§13.14.1)
2918. At startup, a placement-level `when` overrides the type-level predicate and evaluates in its placement scope rather than the type's own scope. (§13.14.1)
2919. Startup initialization is not separated into per-declaration-kind phases; the topological sort alone determines order. (§13.14.1)
2920. The final startup step performs the first commit, publishing the initial snapshot; observers' subsequent `acquire_snapshot` calls return real data. (§13.14.1)
2921. The runtime is "constructing" until the first commit and "live" only after it completes. (§13.14.1)
2922. Observer reads before the first commit return a sentinel or block, per implementation choice. (§13.14.1)
2923. In steady state, host writes (`write_signal`, `write_attr`, `transaction`) only stage values and mark dirty; no evaluation runs at write time. (§13.14.1)
2924. In steady state, `runtime.commit()` settles dirty cells, advances recurrents, runs reconcilers, and publishes a new snapshot for observers. (§13.14.1)
2925. Shutdown step 1 stops accepting new signal/attr writes. (§13.14.1)
2926. Shutdown step 2 drains any in-flight commit: the current commit, if running, completes. (§13.14.1)
2927. Shutdown step 3 drops reactive cells in reverse-of-construction order per §14.7's Drop rules: connections before their endpoint instances; within an instance, attrs, recurrents, and deriveds in reverse declaration order. (§13.14.1)
2928. During shutdown, each live effect receives reconciler `teardown`. (§13.14.1)
2929. Shutdown step 4 drops top-level signals. (§13.14.1)
2930. Shutdown step 5 drops string pool entries (per §14.5). (§13.14.1)
2931. Shutdown step 6 releases cell storage. (§13.14.1)
2932. After shutdown the runtime is terminated; subsequent observer reads return a sentinel. (§13.14.1)
2933. `runtime.write_signal` is one overloaded call dispatched by arity: `write_signal(signal_id, v)` (module-level) or `write_signal(instance_id, signal_id, v)` (per-instance). (§13.14.2)
2934. A `write_signal` call is synchronous and inexpensive: it records the pending value and marks dependents dirty. (§13.14.2)
2935. No evaluation runs at `write_signal` time; evaluation happens at the next `commit`. (§13.14.2)
2936. The module-level `write_signal` form targets a module-scope signal, for which one cell exists for the entire program. (§13.14.2)
2937. The per-instance `write_signal` form targets a node- or connection-level signal on one instance; each placement creates its own cell and the write targets that one cell. (§13.14.2)
2938. Both `write_signal` arities must be called from the runtime's driving context — the single context permitted to write, evaluate, and commit. (§13.14.2)
2939. Other threads write only indirectly, by enqueueing requests for the driving context to apply — a host-application concern. (§13.14.2)
2940. Signal IDs and instance IDs are stable, assigned at compile time, and obtained from the IR. (§13.14.2)
2941. `runtime.write_attr(instance_id, attr_id, value)` stages a value for one instance's attr cell, behaving identically to per-instance `write_signal`: synchronous, staged-only, dirty propagation, no evaluation. (§13.14.3)
2942. `write_attr` applies to attrs on node instances and connection instances alike; both instance kinds share one ID space. (§13.14.3)
2943. `runtime.commit()` performs the complete commit operation of §13.10.2: it settles all dirty deriveds and recurrent expressions glitch-free in topological order and advances recurrents. (§13.14.4)
2944. `commit` fires `suspend`/`resume` for effects whose effective activation changed. (§13.14.4)
2945. `commit` is the sole point at which staged writes become observable, publishing a new consistent snapshot. (§13.14.4)
2946. `commit` is synchronous, runs on the driving context, and returns when the commit completes. (§13.14.4)
2947. Settling work in a commit is bounded by the dirty set (deriveds and recurrents with fired triggers). (§13.14.4)
2948. The cost of making a new snapshot visible is backend-defined and not asserted to be constant. (§13.14.4)
2949. A commit with no dirty cells is idempotent: a fresh snapshot is published with identical values. (§13.14.4)
2950. The host chooses the commit cadence (per audio block, frame, or event); the runtime imposes none. (§13.14.4)
2951. `runtime.transaction` provides atomic grouping of writes: `runtime.transaction(|tx| { tx.write_signal(a_id, new_a) })`. (§13.14.5)
2952. A transaction's closure executes synchronously; its writes stage atomically at closure completion. (§13.14.5)
2953. The full rules for transaction atomicity, panic-on-abort, nesting flattening, and `tx.abort()` rollback are those specified in §13.10.4; `runtime.transaction` is their invocation form. (§13.14.5)
2954. Transactions give atomicity of grouped writes only — settlement and publication of the staged writes happen at the next `runtime.commit()`. (§13.14.5)
2955. `runtime.acquire_snapshot() -> Snapshot` returns a consistent snapshot of the latest committed state. (§13.14.6)
2956. Every cell read from one snapshot is coherent with every other; no torn or cross-cell-inconsistent reads occur. (§13.14.6)
2957. Snapshot reads are wait-free. (§13.14.6)
2958. A snapshot remains valid until the observer next acquires one; a later call obtains a newer snapshot if a commit occurred in the interim. (§13.14.6)
2959. Multiple snapshots may be held concurrently; acquiring or holding a snapshot never blocks a `commit`. (§13.14.6)
2960. `runtime.register_reconciler(effect_type_name, reconciler)` registers a host-side reconciler for one effect type. (§13.14.7)
2961. `register_reconciler` must be called before the runtime goes live; registrations after the live transition are rejected. (§13.14.7)
2962. The `effect_type_name` argument is a string matching the name of an `effect` declaration in the loaded source. (§13.14.7)
2963. The *create* hook fires when a new effect instance enters the live graph; it receives the instance ID, current parameter values, and initial `desired:` values, and returns opaque reconciler-side instance state. (§13.14.7)
2964. The *update* hook fires when any parameter or `desired:` cell of an existing instance becomes dirty; it receives the instance ID, the instance state, and current parameter/desired values. (§13.14.7)
2965. The *update* hook writes its alignment outcome into observed cells via `runtime.write_signal` or `runtime.push_stream`. (§13.14.7)
2966. The *teardown* hook fires when an effect instance leaves scope; it receives the instance ID and instance state and releases external resources. (§13.14.7)
2967. The *suspend* hook fires when the instance's enclosing subtree gates off (effective activation goes false); its contract is to release the external resource while preserving the reconciler-side instance state. (§13.14.7)
2968. What "release" means under *suspend* (close, cancel, flush, or keep warm) is the reconciler's decision; the runtime guarantees only that the signal is delivered. (§13.14.7)
2969. The *resume* hook fires when the enclosing subtree gates back on; it re-acquires the external resource from the preserved instance state. (§13.14.7)
2970. `suspend` then `resume` is a round-trip that does not reinitialize instance state; only `teardown` followed by a fresh `create` does. (§13.14.7)
2971. The host-language binding of the reconciler interface is implementation-defined. (§13.14.7)
2972. Normatively, the five reconciler hooks must be invokable by the runtime at the commit boundary, with per-instance state managed by the host between invocations. (§13.14.7)
2973. For a generic effect, reconciler registration is per-effect-type-per-concrete-instantiation; the `effect_type_name` carries a mangled suffix encoding the resolved type parameters. (§13.14.7)
2974. Generic-effect instantiations without a registered reconciler are detected at startup and cause the runtime to refuse the live transition. (§13.14.7)
2975. If the program declares an effect type with no registered reconciler, startup fails with a diagnostic naming the effect type and the runtime does not enter the live state. (§13.14.7)
2976. `runtime.push_stream` pushes a value into a stream cell, dispatched by arity: `push_stream(instance_id, stream_id, v)` (per-instance) or `push_stream(stream_id, v)` (module-level). (§13.14.8)
2977. `push_stream` serves host-side reconcilers writing effect `observed:` stream cells and host code producing into top-level streams whose source is host-defined. (§13.14.8)
2978. A push to a `ring` stream always succeeds; on a full buffer the oldest unconsumed event is overwritten and `dropped_total` increments. (§13.14.8)
2979. A push to a `gate` stream succeeds only if a slot is free; on failure `rejected_total` increments. (§13.14.8)
2980. A `gate` push returns a `bool` indicating success or failure; the host decides how to handle rejection. (§13.14.8)
2981. A push is dirty-tracked: stream consumers become dirty and the pushed event becomes visible to them at the next commit. (§13.14.8)
2982. The per-instance `push_stream` form targets a stream cell scoped to one specific effect or node instance; `stream_id` identifies the stream cell on that instance's type. (§13.14.8)
2983. The module-level `push_stream` form targets a top-level, module-scope stream cell. (§13.14.8)
2984. The runtime maintains a one-to-one correspondence between live effect instances and reconciler-side instance states. (§13.14.9)
2985. Hooks align with the instance lifecycle: `create` on scope entry, `teardown` on scope exit, `update` on dirty parameters/`desired:` during a commit, `suspend` on gate-off, `resume` on gate-on. (§13.14.9)
2986. Only scope death triggers `teardown`, which drops the reconciler-side instance state. (§13.14.9)
2987. A suspended instance whose scope dies receives `teardown` directly, with no preceding `resume`. (§13.14.9)
2988. A reconciler must tolerate `teardown` arriving while the instance is suspended; teardown subsumes the release the suspend already performed. (§13.14.9)
2989. The runtime never delivers `resume` to an instance that is leaving scope. (§13.14.9)
2990. Reconciler hooks fire at the commit boundary: after the commit publishes its snapshot and before the next commit begins. (§13.14.9)
2991. Hooks run on the runtime's driving context; reconcilers must not block it with long-running operations (dispatch to host-managed workers and write results back via the interface). (§13.14.9)
2992. Reconciler writes into observed cells via `runtime.write_signal`/`runtime.push_stream` are dirty-tracked normally: they stage and become visible to program-side observers on the next commit. (§13.14.9)
2993. Reconcilers are expected to be idempotent in the reconciliation sense: applying the same desired state twice produces the same alignment outcome. (§13.14.9)
2994. The runtime may invoke `update` with unchanged desired values when a commit dirties an instance for unrelated reasons; reconcilers must not produce duplicate side effects in that case. (§13.14.9)
2995. The reconciler's host-side state is the canonical record of what has already been done; `desired:` cells describe only what should be true. (§13.14.9)
2996. The runtime delivers `suspend` only on a gate-close transition and `resume` only on a gate-open transition, never repeated for the same transition. (§13.14.9)
2997. A reconciler should treat a redundant `suspend` on an already-released resource, or a redundant `resume` on an already-acquired one, as a no-op. (§13.14.9)
2998. A `suspend` may arrive for an instance whose `desired:` already implied no resource; it is then a no-op on the resource and serves only to mark the instance frozen. (§13.14.9)
2999. Reconciler errors are reported to the program through the effect's `observed:` cells, typically an `error: Signal[Option[E]]` cell. (§13.14.9)
3000. Reconcilers do not panic the runtime; reconciler-internal errors are domain errors expressed through the value track. (§13.14.9)
3001. A reconciler panic is a host bug; the runtime's behavior is then implementation-defined (typically propagate to the host thread, mark the instance failed, write an error sentinel to a declared observed error cell). (§13.14.9)
3002. The runtime invokes each behavior by its content-addressed id with the current values of its input cells and receives the output; it never reaches inside. (§13.14.10)
3003. Behavior inputs are passed per the behavior's declared ownership modes. (§13.14.10)
3004. The behavior callback and the reconciler hooks are the whole of what the runtime calls back into host code. (§13.14.10)
3005. `runtime.teardown()` runs the shutdown sequence: drains any in-flight commit, delivers reconciler `teardown` to every live effect, drops all cells in reverse-construction order, and releases storage; afterward the runtime is terminated. (§13.14.10)
3006. The mandatory core comprises `write`/`push`, `commit`, `acquire_snapshot`/`read`, `transaction`, `register_reconciler`, `teardown`, the behavior ABI, and the reconciler hooks. (§13.14.11)
3007. Using a capability the runtime does not advertise is a load-time error, not silent misbehavior. (§13.14.11)
3008. The cross-thread observation capability permits `acquire_snapshot`/`read` from threads other than the driving context, honoring the `cross_thread_*` observability classes. (§13.14.11)
3009. A single-threaded runtime omits the cross-thread observation capability. (§13.14.11)
3010. `confined` cells never require the cross-thread observation capability. (§13.14.11)
3011. The hot reload capability exposes `runtime.reload(diff)`, applying an IR diff to the live graph: cells/scopes matched by path with state preserved, changed behaviors swapped by content-hash, added/removed nodes mounted/unmounted. (§13.14.11)
3012. The persistence capability lets the runtime serialize and restore live graph state across runs. (§13.14.11)
3013. The real-time bounds capability honors the `realtime` cadence hint with bounded, non-blocking observation. (§13.14.11)
3014. The `observability` and `cadence_hint` cell fields are inputs to capability negotiation: the runtime selects storage that honors them or rejects a graph it cannot serve. (§13.14.11)

## Hot Reload

3015. The runtime supports hot reload of the reactive graph when the host provides updated source code (per §14.8). (§13.15)
3016. Before any runtime-side reload action, the new source must compile under the full type system and reactive-system rules. (§13.15.1)
3017. If reload compilation fails for any reason — including dangling references to removed nodes — the reload is rejected and the runtime continues the previously-loaded version unaffected. (§13.15.1)
3018. Reactive cells are identified across reloads by fully-qualified declaration path — module path, instance name, cell name: `audio.synth_a.osc_1.frequency`. (§13.15.2)
3019. Anonymous or duplicated sibling placements get an ordinal suffix `:N` — the zero-based declaration-order index among same-type siblings at the same nesting depth. (§13.15.2)
3020. A cell with the same fully-qualified path and same type in old and new source is the same cell; its value is preserved across reload. (§13.15.2)
3021. A cell present only in the old source is a removal: it is dropped during reload. (§13.15.2)
3022. A cell present only in the new source is an addition: a new cell is allocated and initialized per the new source's declared initial value or default. (§13.15.2)
3023. A cell present in both sources with a different type is treated as removal of the old plus addition of the new. (§13.15.2)
3024. `@reset_on_reopen` is not part of cell identity; adding or removing it across a reload preserves the cell's value and history. (§13.15.2)
3025. A `@reset_on_reopen` change takes effect at the next gate-open. (§13.15.2)
3026. Reload is performed atomically on the runtime's driving context. (§13.15.3)
3027. Reload step 1 compiles the new source; on failure the reload is rejected with runtime state unchanged. (§13.15.3)
3028. Reload step 2 acquires a reload lock and pauses acceptance of new signal/attr writes; host requests queue. (§13.15.3)
3029. Reload step 3 lets any in-flight commit complete, ensuring a between-commits state. (§13.15.3)
3030. Reload step 4 computes the old/new graph diff: surviving cells (same path, same type), additions, removals. (§13.15.3)
3031. Reload step 5 allocates and initializes added cells per the new source. (§13.15.3)
3032. Reload step 6 invokes Drop on removed cells in reverse-declaration order: connections before endpoint instances; within an instance, attrs, recurrents, and deriveds in reverse declaration order. (§13.15.3)
3033. Reload step 7 updates the behavior table: new content-addressed IDs registered, absent behaviors deregistered, unchanged IDs carried over. (§13.15.3)
3034. Reload step 8 recomputes the value of each derived whose behavior body changed (different content-addressed ID) from current inputs. (§13.15.3)
3035. A derived whose body is unchanged keeps its value across reload. (§13.15.3)
3036. Reload step 9 commits the reloaded state. (§13.15.3)
3037. Reload step 10 releases the reload lock, resumes signal/attr writes, and applies queued writes to the new state. (§13.15.3)
3038. Changes to `when` predicates — added, removed, or modified — are reload-safe; the predicate is structural metadata, not cell identity, and cells retain their values across the reload. (§13.15.3)
3039. A changed `when` predicate takes effect prospectively at the next commit; historical gate state is not recomputed. (§13.15.3)
3040. Cell-storage layout changes that would relocate live cells are not hot-reloadable and require a full-runtime restart. (§13.15.4)
3041. Operator signature changes (parameters added, removed, or retyped; return type changed) require restart of the affected operator instances. (§13.15.4)
3042. Internal cell type changes within an operator body require restart of the affected operator instances. (§13.15.4)
3043. Changing a cell's `= initial` expression in a way that would alter its current state semantics requires restart of the affected operator instances. (§13.15.4)
3044. For operator-level changes, per-instance restart suffices: affected instances are recreated while the rest of the runtime continues without restart. (§13.15.4)
3045. Stream element-type changes (an incompatible structural change to `T`) require per-instance restart of the affected stream cells and their consumers. (§13.15.4)
3046. Stream policy changes (`ring` ↔ `gate`) require per-instance restart of the affected stream cells and their consumers. (§13.15.4)
3047. Stream capacity changes require per-instance restart of the affected stream cells and their consumers. (§13.15.4)
3048. Effect parameter-signature changes (added, removed, retyped, or reordered parameters) require per-instance restart of the affected effect instances. (§13.15.4)
3049. Effect `desired:` or `observed:` cell type changes require per-instance restart of the affected effect instances. (§13.15.4)
3050. Stream/Sink cell policy or capacity changes within an effect require per-instance restart of the affected effect instances. (§13.15.4)
3051. On per-instance restart of effect instances, the host's reconciler receives a teardown call for the affected instances. (§13.15.4)
3052. Implementations detect reload-unsafe changes during the diff phase and either reject the reload or schedule the appropriate restart (full-runtime or per-instance). (§13.15.4)
3053. The runtime diagnoses which class of reload-unsafe change occurred. (§13.15.4)
3054. Stream cell identity across reloads follows the same fully-qualified declaration path rule as other reactive cells. (§13.15.5)
3055. A stream's buffer is preserved across reload iff its type signature is byte-identical: structurally identical element type, same policy, same capacity literal. (§13.15.5)
3056. When any of element type, policy, or capacity differs across reload, the buffer resets to empty and all consumer cursors restart at the empty position. (§13.15.5)
3057. Changes to a stream's `= source` expression are reload-safe and never affect buffer preservation. (§13.15.5)
3058. Across a source-expression change, producers push into the same buffer and previously buffered events remain available to consumers until overwritten by policy. (§13.15.5)
3059. A stream declared `@reset_on_reload` always resets its buffer across reload, regardless of type-signature match: `@reset_on_reload stream ring[1024] events: LogEntry = source`. (§13.15.5)
3060. A consumer's cursor is preserved across reload when the consuming operator or derived instance is itself preserved per its own identity rule. (§13.15.5)
3061. A consumer added in the new source starts at the current head, observing only events arriving after the reload. (§13.15.5)
3062. A removed consumer's cursor is dropped. (§13.15.5)
3063. The synthesized observation cells (`pending_count`, `pressure`, `is_full`, `dropped_total`, `rejected_total`, `last_overflow_at`) survive reload with the buffer when the type signature matches, and reset when the buffer resets. (§13.15.5)
3064. Effect instance identity across reloads follows the operator identity rule: enclosing scope, effect type name, and argument bindings, tolerating positional moves within the same scope. (§13.15.6)
3065. Within a preserved effect instance, a `desired:`/`observed:` `Signal[T]` cell is preserved when its type matches and reset to its initial value on type change. (§13.15.6)
3066. Within a preserved effect instance, `Stream[T]` and `Sink[T]` cells are preserved per the stream reload rules. (§13.15.6)
3067. Adding a new cell to `desired:` or `observed:` is reload-safe; the new cell is initialized fresh. (§13.15.6)
3068. Changing the initial-value expression of a `desired:`/`observed:` Signal cell is reload-safe: existing committed values persist and the new expression applies only to fresh instances. (§13.15.6)
3069. Changing a parameter-derived `desired:` cell's derivation expression is reload-safe: the cell re-evaluates on the next commit with the new logic. (§13.15.6)
3070. Changing effect visibility, generic-parameter bounds, or other declaration-level metadata that does not affect cell shape or parameter signature is reload-safe. (§13.15.6)
3071. Effect parameter-signature changes are reload-unsafe and require per-instance restart per §13.15.4. (§13.15.6)
3072. Cell type changes in `desired:` or `observed:` are reload-unsafe and require per-instance restart per §13.15.4. (§13.15.6)
3073. Stream/Sink policy or capacity changes within an effect are reload-unsafe and require per-instance restart per §13.15.4. (§13.15.6)
3074. Per-instance effect restart invokes the reconciler's teardown hook, constructs the new instance under the new declaration, then fires the create hook for it. (§13.15.6)
3075. Renaming an effect type is removal of the old type plus addition of a new one: old instances are torn down and new-type instances are constructed fresh. (§13.15.6)
3076. The reactive system's source semantics are realized through two backend-agnostic contracts — the compiler-emitted IR and the runtime interface — each construct mapping naming a contract, not a mechanism. (§13.16)
3077. Reactive cells (signal, attr, recurrent, derived) map to graph-IR cells; the runtime stores their values and keeps them current across commits. (§13.16)
3078. Stream cells map to graph-IR `stream` primitives carrying element type, policy, capacity, cursors, and observation cells. (§13.16)
3079. Recurrent streams add history depth and per-input lookback to the `stream` primitive. (§13.16)
3080. How a runtime buffers stream events is a backend concern. (§13.16)
3081. Effect instances map to graph-IR `effect` primitives: groupings of standard cells plus a reconciler reference. (§13.16)
3082. Per-instance reconciler state is host-managed and lives outside reactive state. (§13.16)
3083. Reactive evaluation maps to the commit: apply staged writes, settle deriveds and recurrents glitch-free in topological order, fire reconciler hooks, publish a consistent snapshot for observers. (§13.16)
3084. Which thread or threads perform the commit, and how the snapshot is published, are backend concerns. (§13.16)
3085. Behaviors invoked during commit — derived, recurrent, gate, and effect-`desired` bodies — conform to the behavior ABI: invoked by content-addressed id with input-cell values, pure and stateless. (§13.16)
3086. Host-registered reconcilers are dispatched at the commit boundary; `suspend`/`resume` fire there on gate-close/gate-open of an effect's enclosing subtree, driven by the runtime's internally-computed effective activation. (§13.16)
3087. The IR carries the structural information the runtime needs: cells and dependency edges, gates, streams, effects, and behavior references. (§13.16)
3088. Source-level hot reload applies an IR diff to the live runtime: cells matched by path with state preserved, changed behaviors swapped by content-hash, added/removed nodes mounted/unmounted. (§13.16)

## Operators

3089. An operator is a reusable, cell-allocating reactive transformation declared with the `operator` keyword. (§13.17)
3090. An operator takes `Signal[T]` inputs, optionally plus non-reactive value parameters, and produces a `Cell[T]` output. (§13.17)
3091. An operator allocates internal reactive cells (recurrents and/or deriveds) per instantiation. (§13.17)
3092. `fn` is reactive-transparent: it takes value parameters, returns values, and allocates no cells. (§13.17)
3093. `operator` is not reactive-transparent: it takes cell references, allocates internal cells, and is wired into the reactive graph at its call site. (§13.17)
3094. A stateless operator wraps a pure projection over a source cell: `operator double(source: Signal[f32]) -> Signal[f32]` with body `source * 2`. (§13.17.1)
3095. A stateful operator allocates `recurrent` state per instantiation. (§13.17.1)
3096. Each instantiation of a stateful operator creates fresh internal cells; multiple call sites do not share state. (§13.17.1)
3097. An operator declaration follows the form `operator name[GenericParams]?(params...) -> Cell[T]: body`. (§13.17.2)
3098. An operator name is a snake_case identifier. (§13.17.2)
3099. An operator's declared return type is a `Cell[T]` — typically `Signal[T]`, or `Stream[T]`/`Sink[T]` for event-producing or sink operators. (§13.17.2)
3100. Operator parameters are distinguished by declared type into cell-bound `name: Signal[T]` and value `name: T`. (§13.17.3)
3101. A cell-bound parameter binds to a reactive cell at instantiation; the operator tracks that cell's changes over time via the reactive engine. (§13.17.3)
3102. Inside the body, a cell-bound parameter is treated as a cell of value type `T`; references read the cell's current value. (§13.17.3)
3103. A value parameter is snapshotted at instantiation; the value is fixed for the lifetime of that operator instance. (§13.17.3)
3104. Value-parameter snapshotting is structurally a category-B storage operation with implicit-move semantics; no `move` keyword is required. (§13.17.3)
3105. Default values are allowed on value parameters: `rate: f32 = 0.1`. (§13.17.3)
3106. Default values on `Signal[T]` parameters are not allowed in v1; a stdlib helper constructing a constant cell substitutes when needed. (§13.17.3)
3107. Defaulted-before-non-defaulted parameter ordering follows the general rule of §3.5.4, uniform across functions, operators, and constructors; operators have no special-case relaxation. (§13.17.3)
3108. A literal passed to a `Signal[T]` parameter is wrapped as an implicit constant signal cell, a compile-time-fixed `Signal[T]` value. (§13.17.3)
3109. Literal wrapping costs one cell per literal at the call site, folded by the compiler when possible. (§13.17.3)
3110. A cell passed to a `Signal[T]` parameter binds directly. (§13.17.3)
3111. An argument passed to a value parameter `T` is evaluated and snapshotted. (§13.17.3)
3112. Where a context requires `T` and the expression has type `Signal[T]`, the compiler implicitly inserts a read of the cell's current value: `derived doubled: f32 = s * 2.0`. (§13.17.3.1)
3113. An auto-dereferenced cell read is recorded as a provenance dependency, making the surrounding expression reactive on changes to that cell. (§13.17.3.1)
3114. Auto-deref applies wherever `Signal[T]` flows into a `T` position: arithmetic operands, `T`-typed function arguments, attr initial-value expressions, derived bodies, recurrent expressions. (§13.17.3.1)
3115. Auto-deref does not apply where the context expects `Signal[T]` directly — operator parameters, `Signal[T]`-typed function parameters, `|>` LHS — there the cell reference binds without dereferencing. (§13.17.3.1)
3116. Signal auto-deref is a compile-time mechanism with no runtime cost beyond the cell read itself. (§13.17.3.1)
3117. The permitted operator body items are `recurrent` declarations (with all §13.2.4 extensions), `derived` declarations, `let` bindings, and a final expression. (§13.17.4)
3118. An operator-body `let` right-hand side evaluates in a reactive context: reads of cell-bound parameters return current values, and the binding recomputes whenever any read cell changes. (§13.17.4)
3119. An operator-body `let` binding behaves as a synthesized derived for dependency-tracking purposes. (§13.17.4)
3120. `signal` declarations are forbidden in operator bodies; operator-internal cells cannot be host-writable. (§13.17.4)
3121. `attr` declarations are forbidden in operator bodies; per-instance configuration is expressed via parameters. (§13.17.4)
3122. Side-effecting statements are forbidden in operator bodies. (§13.17.4)
3123. `repeat` declarations are forbidden in operator bodies; operator state is fixed-shape. (§13.17.4)
3124. Effect instantiations are forbidden in operator bodies. (§13.17.4)
3125. An effect composing through an operator in a pipe chain is hosted in the enclosing node's `effects:` clause, never inside the operator: `url |> fetch |> render`. (§13.17.4)
3126. The final expression's type must be `T` or a `Cell[T]` matching the operator's declared return type. (§13.17.4)
3127. For a final expression of value type `T`, the compiler synthesizes a derived cell holding its value and exposes that cell as the instance's output. (§13.17.4)
3128. A final expression that is already a `Cell[T]` (a named recurrent, derived, or stream in the body) is the output directly, with no synthesis. (§13.17.4)
3129. Every operator returns a single reactive cell. (§13.17.5)
3130. Event-producing operators (e.g. `to_stream`, `filter`, `merge`) return `Stream[T]`: `operator changes[T](source: Signal[T]) -> Stream[T]`. (§13.17.5)
3131. Operators exposing write-end stream handles return `Sink[T]`. (§13.17.5)
3132. Multiple outputs are returned as a single record or tuple. (§13.17.5)
3133. Consumers project fields of a record-valued output via reactive expressions: `derived just_peak: f32 = result.peak`. (§13.17.5)
3134. Field-level reactivity on a record output is coarse-grained: a change to any field invalidates consumers of all fields. (§13.17.5)
3135. Function-call form instantiates an operator directly: `let smoothed = smooth(source_cell, rate: 0.1, clock: tick)`. (§13.17.6)
3136. Pipe form binds the `|>` LHS to the operator's first positional parameter, remaining arguments passing as in function-call form: `source_cell |> smooth(rate: 0.1, clock: tick)`. (§13.17.6)
3137. The function-call and pipe instantiation forms are observationally identical. (§13.17.6)
3138. Pipe steps chain, each `|>` step being an operator application whose result is consumed by the next: `0.0 |> clamp(min: 0.0, max: 1.0) |> smooth(rate: 0.1, clock: tick) |> ease_in_out`. (§13.17.6)
3139. By convention, the first positional parameter of any operator is the implicit pipe target; library authors place the upstream cell first. (§13.17.6)
3140. For binary operators (two upstream cells), the first cell is the pipe target and subsequent cells are passed by name: `source |> combine(other: another_signal, weight: 0.5)`. (§13.17.6)
3141. Each instantiation allocates fresh internal cells; two call sites of the same operator do not share state. (§13.17.6)
3142. The compiler emits one allocation set per operator call site at compile time; the reactive state buffer reserves space for each instance's internal cells. (§13.17.6)
3143. An operator instance is identified by its enclosing scope, the operator name, and its argument bindings. (§13.17.6.1)
3144. Identical `|>` chains in different scopes (modules, node bodies, placements) are distinct instances with independent state. (§13.17.6.1)
3145. Operator instances have no user-assignable names; a `let` of an operator's output names the output cell, not the instance. (§13.17.6.1)
3146. Reload identity uses the same scope/name/argument-bindings scheme, with tolerance for positional moves within the same scope; the binding name has no role. (§13.17.6.1)
3147. Operator instances contribute to the runtime's graph specification the same way node placements and connection placements do. (§13.17.6.2)
3148. Each instance's internal cells, including synthesized cells from the body and `let` bindings, count against the runtime's cell-storage allocation and per-type pool sizing for dynamic-size cells. (§13.17.6.2)
3149. Recursion through operators is forbidden: an operator may not transitively instantiate itself. (§13.17.6.2)
3150. The compiler enumerates operator call sites at compile time, so the static operator-instance count is bounded and known. (§13.17.6.2)
3151. `|>` connects a source cell on the left to a destination on the right; the connection kind dispatches on the RHS kind: operator call, effect call, or `Sink[T]`. (§13.17.7)
3152. With an operator-call RHS, `|>` instantiates the operator, binds the LHS to its first positional parameter, and yields the operator's declared output cell. (§13.17.7)
3153. With an effect-call RHS, `|>` instantiates the effect, binds the LHS to its first positional parameter, and yields the effect instance value. (§13.17.7)
3154. Effect instantiation via `|>` lives in a node's `effects:` clause: `f = current_url |> fetch`. (§13.17.7)
3155. With a `Sink[T]` RHS, `|>` establishes a forwarding subscription that pushes each event observed from the LHS stream into the sink. (§13.17.7)
3156. A sink-forwarding `|>` expression has the unit value `()`; the subscription lives as long as the enclosing scope. (§13.17.7)
3157. Stream forwarding into an effect's sink also lives in `effects:`: `messages_to_send |> ws.outbound`. (§13.17.7)
3158. The `|>` LHS must be an expression of a reactive cell type (`Signal[T]`, `Stream[T]`, `Sink[T]`, or other `Cell[T]`) or convertible to one. (§13.17.7)
3159. A literal `|>` LHS is wrapped as an implicit constant signal cell automatically. (§13.17.7)
3160. Piping into a sink requires the LHS concrete type to be a `Stream[T]` of matching element type. (§13.17.7)
3161. Piping a `Signal[T]` into a sink is a type error; `to_stream` converts a signal first when event semantics are desired. (§13.17.7)
3162. `|>` is low-precedence; most arithmetic and logical operators bind tighter: `a + b |> op` parses as `(a + b) |> op`. (§13.17.7)
3163. `|>` is left-associative: `a |> op1 |> op2` parses as `(a |> op1) |> op2`. (§13.17.7)
3164. Bitwise `|` binds tighter than `|>` and tighter than the logical operators per §4.4.7; `|>` remains the loosest-binding operator. (§13.17.7)
3165. Using `|>` with a `fn` RHS is a compile error: `0.0 |> some_fn` ✗. (§13.17.7)
3166. Operators may take type parameters with optional trait bounds: `operator running_total[T: Add & Copy](source: Signal[T]) -> Signal[T]`. (§13.17.8)
3167. Operator type parameters are resolved at the call site from argument types. (§13.17.8)
3168. Explicit operator type-parameter instantiation uses turbofish syntax where inference is ambiguous. (§13.17.8)
3169. Operators carry the standard three-level visibility: `public`, `shared` (the default), `private`. (§13.17.9)
3170. Module-private operators are not reachable from other modules. (§13.17.9)
3171. Public operators may be re-exported. (§13.17.9)
3172. An operator instance is a scoped reload boundary; each internal cell is identified by its declared name and type within the operator body. (§13.17.10)
3173. Changes to recurrent expression bodies, `where` guards, or final-expression bodies are reload-safe. (§13.17.10)
3174. Adding a new internal cell is reload-safe; new cells initialize fresh. (§13.17.10)
3175. Operator signature changes (parameters added, removed, or retyped; return type changed) trigger per-instance restart on reload. (§13.17.10)
3176. Internal cell type changes trigger per-instance restart on reload. (§13.17.10)
3177. Changing a cell's `= initial` expression in a way that would alter its current state semantics triggers per-instance restart. (§13.17.10)
3178. Per-instance restart recreates only the affected operator instances, never the whole runtime. (§13.17.10)
3179. Reload-unsafe changes involving buffer-layout relocation require full-runtime restart. (§13.17.10)
3180. A call site switching operators (`source |> op_a` to `source |> op_b`) drops the old instance's cells via eviction and initializes the new instance fresh, even if the signatures match. (§13.17.10)
3181. The reload diff identifies operator instances by (enclosing scope, operator name, argument bindings) rather than line/column, so a pure positional move within the same scope preserves state. (§13.17.10)
3182. Moving a call site to a different enclosing scope is treated as a call-site change; state is lost. (§13.17.10)
3183. Call sites in the same scope indistinguishable by (operator name, arguments) are matched old-to-new by syntactic order on reload. (§13.17.10)
3184. Adding a new identical call between two existing indistinguishable calls treats the new call as fresh; the existing calls preserve state. (§13.17.10)
3185. An operator instance has no `when` predicate of its own; gating is expressed on its output cell or its consumer. (§13.17.11)
3186. Operator-internal cells participate in the standard cycle-detection rules. (§13.17.11)
3187. A recurrent inside an operator acts as a delay element identical to a top-level recurrent. (§13.17.11)
3188. Each operator instance's internal cells contribute their evaluation nodes to the per-commit DAG. (§13.17.11)
3189. Operators do not cross commit boundaries; all internal evaluation happens within a single commit. (§13.17.11)
3190. Operator bodies are not reactive-transparent: reading a cell-bound parameter reads through the reactive engine, with provenance tracked at the call site. (§13.17.11)
3191. Operator calls inside an operator body create further instantiations. (§13.17.11)
3192. `fn` calls inside an operator body remain reactive-transparent. (§13.17.11)
3193. Operator-internal recurrents may hold dynamic-size types (e.g. `Vec[T]`), stored via the standard pool-with-index mechanism. (§13.17.11)
3194. Operators share the composition surface — `|>` pipe form, instance identity, parameter rules, generics, visibility — with effects. (§13.17.11)
3195. Operators produce or consume streams via the standard stream operators. (§13.17.11)
3196. Operators perform pure reactive transforms with no outside-world side effects; effects align program state with external reality. (§13.17.11)
3197. A normative diagnostic class rejects `|>` with a non-operator, non-effect right-hand side. (§13.17.12)
3198. A normative diagnostic class rejects piping into an operator that declares no positional parameter to bind. (§13.17.12)
3199. A normative diagnostic class rejects passing a non-cell, non-literal value expression to a `Signal[T]` parameter. (§13.17.12)
3200. A normative diagnostic class rejects `signal` or `attr` declarations inside an operator body. (§13.17.12)
3201. A normative diagnostic class rejects a final expression whose value type mismatches the operator's declared return value type. (§13.17.12)
3202. An operator has a structural type written `operator(P1, P2, …) -> Cell[U]`: the parameter types in order, then the output cell type. (§13.17.13)
3203. Operators are carried structurally by signature, unlike the nominal node/connection/effect kinds carried by the `Type[…]` meta-type. (§13.17.13)
3204. An operator type expression in value position (an operator parameter, attr, or return) carries or defers which operator the receiver instantiates: `kernel: operator(Signal[f32]) -> Signal[f32]`. (§13.17.13)
3205. An operator value must name an operator declaration; there are no anonymous operator literals. (§13.17.13)
3206. A supplied operator value resolves at compile time, and the receiver is monomorphized per supplied operator with no erasure, exactly as for a `Type[…]` value. (§13.17.13)
3207. A value is admissible for an `operator(P…) -> Cell[U]` parameter iff the named operator's signature matches structurally: the same parameter types in order and the same output cell type. (§13.17.13)
3208. In an operator type, cell-bound parameters appear as their cell type and value parameters as their value type. (§13.17.13)
3209. A value parameter's default is not part of the operator type and does not affect signature matching. (§13.17.13)
3210. A receiver instantiates a carried operator exactly as a named one, by applying it to arguments: `kernel(source)`. (§13.17.13)
3211. The instance of a carried operator lives at the receiver's graph position, not the supplier's. (§13.17.13)
3212. A receiver may instantiate a carried operator only against the parameter and output types its signature states; it cannot otherwise read or decompose the operator value. (§13.17.13)
3213. A carried operator may be generic, its type written with the generic parameters bound at the use site: `operator(Signal[T]) -> Signal[T]`. (§13.17.13)
3214. A carried operator composes in `|>` chains at the point it is instantiated, identically to a named operator. (§13.17.13)

## Streams

3215. A stream is a reactive primitive for append-only event sequences governed by a buffering policy. (§13.18)
3216. Streams are first-class reactive cells participating in the commit cycle, in cell identity for hot reload, and in the graph specification. (§13.18)
3217. Streams are not values flowing through ordinary expressions; they are cells with read and write surfaces distinct from signals. (§13.18)
3218. A stream carries an append-only sequence of typed events observed by zero or more consumers — operators or deriveds reading from the stream — each advancing its own cursor independently. (§13.18.1)
3219. A stream event's producer is a stream-emitting operator chain or a host-side push. (§13.18.1)
3220. A stream's storage is governed by a buffering policy. (§13.18.1)
3221. The v1 policy set is exactly `ring` and `gate`, and both policies are bounded. (§13.18.1)
3222. A bounded policy allocates a fixed-capacity region of typed slots once, at the stream's declaration site. (§13.18.1)
3223. A bounded stream's capacity is part of its concrete type. (§13.18.1)
3224. On a push to a full buffer, the `ring` policy — a true circular ring buffer — overwrites, displacing the oldest unconsumed event. (§13.18.1)
3225. On a push to a full buffer, the `gate` policy rejects the new event with failure and never overwrites; gate is bounded-but-reject, not a ring buffer. (§13.18.1)
3226. Boundedness and capacity are properties of the bounded policies, not of streams in general; a future unbounded policy (to which capacity would not apply) is not forbidden. (§13.18.1)
3227. A stream's lifetime is scope-bound: it lives as long as its declaration's enclosing scope and is freed when that scope dies. (§13.18.1)
3228. There is no garbage collection of stream events. (§13.18.1)
3229. A bounded stream's buffer is one fixed memory region for the stream's entire lifetime, reused as events arrive. (§13.18.1)
3230. `Stream[T]` holds zero or more pending events, each consumed independently, in contrast to `Signal[T]`'s single always-defined current value. (§13.18.1)
3231. A stream has no current value; consumers project a stream to a signal only explicitly. (§13.18.1)
3232. The stream declaration form is `stream policy[capacity]? name: Type? = source`. (§13.18.2)
3233. The policy keyword, one of `ring` or `gate`, is mandatory; a stream declaration has no default policy. (§13.18.2)
3234. An explicit `[capacity]` specifies the buffer's slot count and must be a compile-time-known positive `usize`: a literal, a `const`, or a const-generic parameter. (§13.18.2)
3235. Omitted capacity with a single-stream source or a known-capacity stream-producing chain defaults to that output's capacity. (§13.18.2)
3236. Omitted capacity with a reactive-expression source over streams and/or signals defaults to the sum of input capacities across all reactive inputs. (§13.18.2)
3237. In the capacity-default sum, each stream contributes its declared capacity and each signal contributes the implicit `to_stream` default of 1024. (§13.18.2)
3238. Omitted capacity on a `recurrent[N] stream` defaults to `sum_of_input_capacities + N`. (§13.18.2)
3239. In all other omitted-capacity cases (e.g., a bare `to_stream` without surrounding context), capacity defaults to 1024. (§13.18.2)
3240. A stream's name is a snake_case identifier. (§13.18.2)
3241. The element-type annotation is optional when inferable from the source expression: `stream gate[256] db_writes = pending_writes`. (§13.18.2)
3242. Valid stream sources are a stream-producing operator chain, a reference to another stream (direct forwarding), a reactive expression over streams and/or signals, or a signal-only expression. (§13.18.2)
3243. A signal-only source is equivalent to appending `|> to_stream`: `stream ring url_events: Url = current_url`. (§13.18.2)
3244. Streams may be declared wherever reactive declarations may appear: module level, node bodies, connection bodies, operator bodies, and effect bodies. (§13.18.2)
3245. Streams may not be declared inside function bodies, which are reactive-transparent. (§13.18.2)
3246. Module-level streams carry the standard three-level visibility: `public`, `shared` (the default), `private`. (§13.18.2)
3247. Streams inside node, connection, operator, or effect bodies inherit the enclosing declaration's visibility. (§13.18.2)
3248. A bounded stream's type encodes its element type, its policy, and its capacity. (§13.18.3)
3249. The abstract base `Stream[T]` carries only the element type — neither policy nor capacity. (§13.18.3)
3250. The concrete stream types are `RingStream[T, N]` (ring policy, capacity `N`) and `GateStream[T, N]` (gate policy, capacity `N`). (§13.18.3)
3251. Every stream value belongs to one of the concrete stream types at runtime. (§13.18.3)
3252. `Stream[T]` types signatures accepting any stream: `operator map[T, U](source: Stream[T], f: fn(T) -> U) -> Stream[U]`. (§13.18.3)
3253. A parameter of concrete policy type accepts only that policy; passing a gate stream where `RingStream[T, N]` is expected is a type error. (§13.18.3)
3254. A parameter typed `Stream[T]` is polymorphic over both policies. (§13.18.3)
3255. Policy-preserving operators (e.g., `map`, `filter`) declare input and output as abstract `Stream[T]`, and the implementation propagates the concrete policy from input to output. (§13.18.3)
3256. Capacity is part of the stream type for analysis and pool sizing but does not generally appear in operator constraints. (§13.18.3)
3257. An operator accepting `Stream[T]` accepts any capacity. (§13.18.3)
3258. An operator needing a capacity bound declares it explicitly: `RingStream[T, N]` for a specific `N`, or a trait bound expressing capacity relationships. (§13.18.3)
3259. A concrete stream value is implicitly usable wherever `Stream[T]` is expected; the abstract type is a supertrait of both concrete types. (§13.18.3)
3260. Concrete-to-abstract stream widening is zero-cost at runtime; only the type-system view changes. (§13.18.3)
3261. A sink is the write-side view of a stream; the stream and its sink share the same underlying buffer and differ only in access mode. (§13.18.4)
3262. A Stream is the read view: consumers observe events through cursors. (§13.18.4)
3263. A Sink is the write view: producers push events into the buffer. (§13.18.4)
3264. Sink types mirror stream types: abstract `Sink[T]`, concrete `RingSink[T, N]` and `GateSink[T, N]`. (§13.18.4)
3265. Sinks appear primarily in effect `desired:` blocks: the program writes the declared sink while the effect's host-side reconciler holds the stream view. (§13.18.4)
3266. Outside effects, a sink appears where a stream's producer-side handle is exposed explicitly, e.g. an operator returning both stream and sink views as a record. (§13.18.4)
3267. Sinks are not written via assignment; producers push by piping a stream into the sink directly via `|>` (the pipe operator's Case 3): `events_to_log |> log_sink`. (§13.18.4)
3268. Pipe-into-sink establishes a forwarding subscription: each event observed from the source stream is pushed into the sink. (§13.18.4)
3269. A pipe-into-sink expression's value is the unit type `()`. (§13.18.4)
3270. The forwarding subscription established by pipe-into-sink lives for the enclosing scope. (§13.18.4)
3271. A single sink may receive from multiple stream sources via multiple pipe-into-sink expressions (multi-producer). (§13.18.4)
3272. Under multi-producer fan-in the receiving sink's buffer is shared, and events from all producers arrive in their commit order. (§13.18.4)
3273. There is no standalone `sink` declaration keyword; a sink exists only as the write-side counterpart of a declared stream. (§13.18.4)
3274. A `stream` declaration creates both views implicitly: the declared name binds the Stream view, and the Sink view is reached through producer-side machinery. (§13.18.4)
3275. `Cell[T]` is the common trait over reactive cells carrying `T`, implemented by `Signal[T]`, `Stream[T]`, and `Sink[T]` and their concrete sub-types. (§13.18.5)
3276. `Cell[T]` types signatures accepting any kind of reactive cell: `operator monitor[T, C: Cell[T]](source: C) -> Signal[bool]`. (§13.18.5)
3277. `Cell[T]` has no required methods at the language level; it is a marker trait for participation in the reactive system as a typed cell. (§13.18.5)
3278. Concrete cell behavior — current-value reads, event observation, push semantics — is expressed through each implementing type's own methods and operators. (§13.18.5)
3279. `Readable[T]: Cell[T]` is the read-direction sub-trait, implemented by `Signal[T]` and `Stream[T]`. (§13.18.5)
3280. `Writable[T]: Cell[T]` is the write-direction sub-trait, implemented by `Sink[T]`. (§13.18.5)
3281. The `Readable`/`Writable` sub-traits are stdlib-provided; most operators use the parent `Cell[T]` or a concrete type directly. (§13.18.5)
3282. Ductus has no ad-hoc union types at value position, so the common abstraction over heterogeneous reactive cells is the trait `Cell[T]`. (§13.18.5)
3283. Every bounded stream automatically exposes derived signal cells describing its state, read via field syntax on the stream value: `events.pressure`. (§13.18.6)
3284. `pending_count: Signal[i32]` counts events in the buffer not yet observed by every cursor. (§13.18.6)
3285. `pressure: Signal[f32]` equals `pending_count / capacity`, in range `0.0..1.0`. (§13.18.6)
3286. `is_full: Signal[bool]` is `true` when `pending_count is capacity`. (§13.18.6)
3287. `dropped_total: Signal[i64]` cumulatively counts events displaced by overflow; it applies to ring only and is always `0` for gate. (§13.18.6)
3288. `rejected_total: Signal[i64]` cumulatively counts pushes rejected by overflow; it applies to gate only and is always `0` for ring. (§13.18.6)
3289. `last_overflow_at: Signal[Option[instant]]` holds the timestamp of the most recent overflow, or `None` if none has occurred. (§13.18.6)
3290. Observation cells are ordinary `Signal[T]` cells for all purposes: commit cycle, derived dependencies, hot-reload identity. (§13.18.6)
3291. Observation cells are never declared in user code; the compiler synthesizes them as part of the stream's storage. (§13.18.6)
3292. Producer self-throttling is expressed by reading the stream's `pressure` (or `is_full`) signal and gating emission upstream of the producing chain. (§13.18.6)
3293. The stdlib provides operators that combine with the observation surface, e.g. `throttle` driven by a pressure-derived gating signal. (§13.18.6)
3294. Gate-stream producers observe back-pressure via `rejected_total` (or `is_full`) to take corrective action such as retry, log, or surfacing an error. (§13.18.6)
3295. Streams participate in reactive expressions on equal footing with signals. (§13.18.7)
3296. A reactive expression containing one or more streams evaluates per event, producing a derived stream. (§13.18.7)
3297. A stream input contributes its events: the expression re-evaluates once per event, producing one output event per input event. (§13.18.7.1)
3298. A signal input contributes its commits with `to_stream` semantics: the initial value as first contribution, then every subsequently changed committed value. (§13.18.7.1)
3299. Same-value signal commits contribute nothing to a stream expression, per the value-change rule. (§13.18.7.1)
3300. The expression recomputes whenever any reactive input emits, and the output stream emits the freshly computed value as its next event. (§13.18.7.1)
3301. With multiple reactive inputs the default combining behavior is combine_latest: any input's emission yields an output event using the latest value of each other input. (§13.18.7.2)
3302. The first combine_latest output is emitted once every input has emitted at least once, a signal's initial value counting as its first emission. (§13.18.7.2)
3303. Combining behaviors other than combine_latest (`zip`, `sample`, `merge`, etc.) require explicit stdlib operators. (§13.18.7.2)
3304. The combine_latest default governs only value-producing operations: arithmetic, function calls, `map`, conditional expressions, and similar. (§13.18.7.2)
3305. LHS-driven operations are normatively per-LHS-event, not combine_latest: only the LHS stream drives emission. (§13.18.7.2)
3306. The per-LHS-event rule covers subset operators that may drop events (`where`, `filter`, `skip`, `take`) and 1:1 operators (`pairwise`). (§13.18.7.2)
3307. Reactive cells referenced in an LHS-driven operation's predicate or transformation contribute their current values at LHS-emission time; their commits between LHS events cause no re-emission. (§13.18.7.2)
3308. `pairwise` never drops events and emits exactly one output per LHS input event. (§13.18.7.2)
3309. A reactive expression emits its first output during the startup pass iff every reactive input has a value to contribute at that moment. (§13.18.7.2)
3310. A stream expression whose reactive inputs are all signals emits one event during startup. (§13.18.7.2)
3311. A stream expression with stream inputs emits its first event only once those streams have each produced at least one event, possibly later than startup. (§13.18.7.2)
3312. A reactive expression containing zero streams has signal result kind. (§13.18.7.3)
3313. A reactive expression containing one or more streams has stream result kind, and its surrounding declaration must be `stream` or `recurrent[N] stream`. (§13.18.7.3)
3314. The result kind follows from input types alone; there is no type-directed dispatch on the binding's LHS context. (§13.18.7.3)
3315. A stream-containing expression is never silently coerced to a signal; explicit projection via `to_signal(fallback)` is required. (§13.18.7.3)
3316. `derived X = expr` with a zero-stream RHS is a standard derived signal. (§13.18.7.4)
3317. `derived X = expr` with a stream-containing RHS is a compile error; use `to_signal(fallback)`. (§13.18.7.4)
3318. A stream declaration whose RHS contains at least one stream produces an output stream with per-event evaluation. (§13.18.7.4)
3319. A stream declaration whose RHS contains no streams but at least one signal produces an output stream; the signals participate via implicit `to_stream` with initial-as-first-event. (§13.18.7.4)
3320. A stream declaration whose RHS has no reactive inputs is a compile error; a stream needs at least one reactive input. (§13.18.7.4)
3321. Implicit conversion turns each signal's commits into output events; controlling the mechanism (e.g., skipping the initial value) requires explicit `to_stream` with an operator chain. (§13.18.7.4)
3322. An explicit `[capacity]` is always allowed and overrides the capacity default. (§13.18.7.5)
3323. A parenthesized stream-valued expression continues into a downstream operator chain: `stream ring filtered: i32 = (count * 2) |> filter(is_positive)`. (§13.18.7.5)
3324. Assigning a stream-valued expression to a `derived` produces a dedicated diagnostic hinting `to_signal` projection: `derived count_signal: i32 = some_stream * 2` ✗. (§13.18.7.6)
3325. A `recurrent[N] stream` is a stream declaration whose expression body may reference past events of itself and of its input streams via `.past(n, fallback)`. (§13.18.8)
3326. The recurrent stream form is `recurrent[N]? stream policy[capacity]? NAME: Type? = EXPR`. (§13.18.8.1)
3327. `[N]` is the output stream's self-history size: the maximum lookback `k` permitted in `NAME.past(k, ...)` self-references. (§13.18.8.1)
3328. `N` must be a compile-time-known positive integer: a literal, a `const`, or a const-generic parameter. (§13.18.8.1)
3329. `recurrent stream` without brackets is shorthand for `recurrent[1] stream`, one step of self-memory. (§13.18.8.1)
3330. A recurrent stream follows the standard stream declaration constraints: policy mandatory, capacity optional. (§13.18.8.1)
3331. The body may apply `.past(n, fallback)` and `.previous(init)` to any referenced stream including the declared name itself: `recurrent stream ring smoothed: f32 = (count + smoothed.previous(0.0)) / 2`. (§13.18.8.1)
3332. In `stream_name.past(n, fallback)`, `n` is a compile-time-known positive `usize` — a literal, a `const`, or a const-generic parameter — giving the lookback distance: `n=1` is the immediately-previous event, `n=2` two events back. (§13.18.8.2)
3333. `fallback` is an expression of the stream's element type `T`, returned when fewer than `n` events of the stream have been observed. (§13.18.8.2)
3334. `.past` returns `T` directly, not `Option[T]`; the fallback keeps the read always well-typed. (§13.18.8.2)
3335. `stream_name.previous(init)` is sugar for `stream_name.past(1, init)`. (§13.18.8.2)
3336. History is per-stream: `count.past(1, 0)` is the immediately-previous event of `count` regardless of other streams' timing, each stream's history advancing per its own event count. (§13.18.8.2)
3337. Each `.past(n, fallback)` invocation is an ordinary function call whose arguments are evaluated when read. (§13.18.8.3)
3338. Multiple `.past` calls on the same stream in one expression may use different fallbacks: `if condition: count.previous(0) else: count.previous(99)`. (§13.18.8.3)
3339. The output stream's self-history allocates `N` slots of `sizeof(T)`. (§13.18.8.4)
3340. The compiler statically scans the body per input stream and allocates the maximum `k` among its `.past(k, ...)` calls: `.past(1, ...)` and `.past(2, ...)` allocate 2 slots. (§13.18.8.4)
3341. An input stream not accessed via `.past` gets zero history slots. (§13.18.8.4)
3342. Per-declaration history memory cost is `N * sizeof(T_output)` plus the sum over referenced inputs of `max_k(input_i) * sizeof(T_i)`. (§13.18.8.4)
3343. `NAME.past(k, ...)` with `k > N` on the declared output is a compile error. (§13.18.8.5)
3344. `.past` or `.previous` access outside the body of a `recurrent[N] stream` declaration is a compile error. (§13.18.8.5)
3345. The `n` argument of `.past(n, fallback)` must be a compile-time-known positive integer: a literal, a `const`, or a const-generic parameter. (§13.18.8.5)
3346. A recurrent stream's special semantics are contained in its own body; downstream consumers see an ordinary `Stream[T]` to which operators apply normally: `avg |> map(fn(x): x * 2)`. (§13.18.8.6)
3347. `recurrent[N] stream` is invalid in effect `observed:` blocks, which declare bare host-written stream cells with no expression body for `.past` to reference. (§13.18.8.7)
3348. Effects needing history-aware behavior must compute it in the host's reconciler. (§13.18.8.7)
3349. Signal-typed `recurrent[N]` declarations use the same `.previous(fallback)` and `.past(k, fallback)` accessors with the same compile-time bounds. (§13.18.8.7)
3350. Signal and stream recurrents are symmetric in syntax, memory model, and semantics, differing only in value-shaped Signal versus event-shaped Stream. (§13.18.8.7)
3351. Stream-producing, stream-transforming, and stream-consuming operators are stdlib primitives. (§13.18.9)
3352. Stream operators use the standard operator-application syntax, `|>` chains or function-call form. (§13.18.9)
3353. `to_stream` is the signal-to-stream bridge: `to_stream[T, const N: usize = 1024](Signal[T]) -> RingStream[T, N]`. (§13.18.9)
3354. At stream creation, `to_stream` emits the source signal's current value as event 0. (§13.18.9)
3355. After creation, `to_stream` appends one event per commit of a new source value. (§13.18.9)
3356. Same-value commits of the source signal emit no `to_stream` event. (§13.18.9)
3357. `to_stream` capacity defaults to `1024`: `some_signal |> to_stream` is `RingStream[T, 1024]`. (§13.18.9)
3358. `to_stream` capacity is overridable via turbofish: `some_signal |> to_stream::[Url, 2048]`. (§13.18.9)
3359. `to_stream` always produces `ring` policy. (§13.18.9)
3360. A signal is converted to a non-ring stream only by applying further operators/declarations after `to_stream` (e.g., threading into a `gate`-declared output). (§13.18.9)
3361. The implicit signal-to-stream conversion in reactive expressions uses `to_stream`'s default capacity `N = 1024`. (§13.18.9)
3362. `to_signal[T](source: Stream[T], fallback: T) -> Signal[T]` yields a signal holding the latest observed event, updated on each new event. (§13.18.9)
3363. `to_signal`'s `fallback` argument is required; the signal holds `fallback` until the first event is observed. (§13.18.9)
3364. `skip[T](source: Stream[T], n: i32) -> Stream[T]` drops the first `n` observed events and passes the rest. (§13.18.9)
3365. `skip_first` is equivalent to `skip(1)`: `current_url |> to_stream |> skip_first` drops the initial-value event. (§13.18.9)
3366. `take[T](source: Stream[T], n: i32) -> Stream[T]` emits the first `n` events, after which the output stream is complete and emits no more. (§13.18.9)
3367. `take_first` is equivalent to `take(1)`. (§13.18.9)
3368. `skip` and `take` both preserve the source's policy and capacity. (§13.18.9)
3369. `count[T](source: Stream[T]) -> Signal[i64]` is the running count of observed events, starting at `0`. (§13.18.9)
3370. `fold[T, A](source: Stream[T], init: A, f: fn(A, T) -> A) -> Signal[A]` is a running accumulator whose initial value is `init`. (§13.18.9)
3371. `any[T](source: Stream[T], pred: fn(T) -> bool) -> Signal[bool]` starts `false` and becomes `true` once any event satisfies `pred`. (§13.18.9)
3372. `all[T](source: Stream[T], pred: fn(T) -> bool) -> Signal[bool]` starts `true` and stays `true` while every event so far satisfies `pred`. (§13.18.9)
3373. `map[T, U](source: Stream[T], f: fn(T) -> U) -> Stream[U]` preserves the source's policy and capacity. (§13.18.9)
3374. `filter[T](source: Stream[T], pred: fn(T) -> bool) -> Stream[T]` preserves the source's policy and capacity. (§13.18.9)
3375. `merge(a: RingStream[T, A], b: RingStream[T, B]) -> RingStream[T, N]` interleaves events from both sources in commit order. (§13.18.9)
3376. `merge`'s const-generic output capacity `N` defaults to `A + B`, the sum of the input streams' capacities. (§13.18.9)
3377. `throttle[T](Stream[T], window: duration, clock: Signal[u64]) -> Stream[T]` rate-limits events to at most one per `window`. (§13.18.9)
3378. `merge`'s capacity is overridable at the call site via turbofish: `merge::[T = Event, N = 1024](a, b)`. (§13.18.9)
3379. `merge_gate` is the gate-stream variant of `merge` with the same shape. (§13.18.9)
3380. Cross-policy merges (one ring input, one gate input) are a compile error. (§13.18.9)
3381. There is no dedicated stream-to-sink forwarding operator; forwarding is `|>` Case 3: `source_stream |> target_sink`. (§13.18.9)
3382. A pipe-established sink-forwarding subscription lives for the enclosing scope. (§13.18.9)
3383. `scan[T, A](source: Stream[T], init: A, f: fn(A, T) -> A) -> Stream[A]` emits `f(state, event)` per input event, threading state through. (§13.18.9)
3384. `scan`'s first output event is `f(init, first_input_event)`. (§13.18.9)
3385. `pairwise[T](source: Stream[T]) -> Stream[(T, Option[T])]` emits each event paired with the previous event. (§13.18.9)
3386. `pairwise`'s first output is `(first_input, None)`. (§13.18.9)
3387. `where` filters a stream (or a signal implicitly lifted via `to_stream`) by a boolean predicate, emitting only the events for which the predicate is true. (§13.18.10)
3388. The filter form is `A where C`, with stream-or-signal LHS `A` and boolean `C` evaluated per event of `A`: `numbers where numbers > 0`. (§13.18.10.1)
3389. A signal LHS to `where` undergoes implicit `to_stream`, equivalent to `to_stream` followed by `filter`: `sensor_signal where sensor_signal > 100`. (§13.18.10.1)
3390. Inside `C`, the LHS stream's name denotes the current event: `A.field` reads the event's field, and a bare reference to a primitive-typed stream is the event value. (§13.18.10.2)
3391. Other reactive cells referenced in `C` contribute their current committed values at the moment the filter evaluates. (§13.18.10.2)
3392. The filter evaluates exactly once per LHS event; the LHS stream is the sole driver of emission. (§13.18.10.2)
3393. Commits to predicate-referenced cells between LHS emissions never trigger filter re-evaluation or re-emission: `events where active_signal is true` emits nothing when only `active_signal` changes. (§13.18.10.2)
3394. `where` and the `filter` operator have identical per-LHS-event sampled-predicate semantics; the distinction is syntactic (keyword reactive expression vs closure). (§13.18.10.2)
3395. A `where` output's element type matches its input's. (§13.18.10.3)
3396. A `where` output inherits the LHS policy: a ring LHS yields a ring output and a gate LHS yields a gate output. (§13.18.10.3)
3397. A signal LHS to `where` (implicit `to_stream`) produces a ring-policy output. (§13.18.10.3)
3398. A `where` output's default capacity is the LHS stream's capacity alone; predicate-referenced cells contribute nothing to the capacity, unlike the all-inputs sum used for value-producing expressions. (§13.18.10.3)
3399. An explicit `[capacity]` on the surrounding stream declaration overrides the `where` default capacity. (§13.18.10.3)
3400. `where` is left-associative; chained filters group leftward: `clicks where clicks.area > 100 where clicks.priority is high`. (§13.18.10.4)
3401. `where` composes with stream operators at any pipeline position: `(events where events.value > 0) |> pairwise`. (§13.18.10.4)
3402. An observe arm-trigger `T where C` is a filtered stream that the arm treats as an ordinary trigger cell, indistinguishable from a bare trigger. (§13.18.10.5)
3403. A filtered trigger emits when `T` emits and `C` is true under the sampled current values of cells `C` references: `on tick where active_mode: ...`. (§13.18.10.5)
3404. Predicate-cell commits do not, by themselves, emit on a filtered trigger or activate its arm. (§13.18.10.5)
3405. Arm selection over filtered triggers is uniform: the first arm whose (filtered) trigger emits in the current commit wins. (§13.18.10.5)
3406. `where` applies only to reactive streams and signals; filtering non-reactive collections uses `Iterator` operations instead. (§13.18.10.6)
3407. The `where` predicate must evaluate to `bool`; any other predicate type is a compile error. (§13.18.10.6)
3408. Assigning a `where` expression (always stream-valued) to a `derived` binding is a compile error; a signal result requires explicit `to_signal(fallback)`. (§13.18.10.6)
3409. Stream policy is encoded in the stream's type, not as a runtime attribute. (§13.18.11)
3410. Policy-preserving operators (`map`, `filter`, `skip`) are written against abstract `Stream[T]`, and the compiler propagates the input's concrete policy: a `GateStream[T, 256]` input gives a `GateStream[U, 256]` `map` output. (§13.18.11)
3411. Policy-constraining operators declare the required policy concretely in their signature: `persist[T: Persistable](writes: GateStream[T]) -> EffectResult`. (§13.18.11)
3412. Passing a stream of the wrong policy to a policy-constrained parameter is a compile error: `RingStream[Write, 1024]` to a `GateStream[Write, _]` parameter ✗. (§13.18.11)
3413. Mixed-policy reactive expressions are permitted; the output's policy is the LHS-declared policy regardless of input policies: `stream ring X = ring_a + gate_b` is ring. (§13.18.11)
3414. Output stream policy is never inferred from inputs. (§13.18.11)
3415. A gate input feeding a ring-declared output still tracks its own `rejected_total`; its producer-facing guarantee is unaffected by the mix. (§13.18.11)
3416. A gate input's lossless guarantee does not propagate to consumers of a ring-declared combined output; end-to-end losslessness requires declaring the output `gate`. (§13.18.11)
3417. Every stream consumer maintains its own cursor marking the oldest event it has not yet observed; consumers of the same stream advance independently. (§13.18.12)
3418. A consumer is any operator instance whose signature takes a `Stream[T]` parameter; each instantiation allocates a fresh cursor. (§13.18.12)
3419. Multiple consumers of one stream see the same events, each observing the full sequence from its own point of attachment. (§13.18.12)
3420. A cursor's identity follows the consuming operator instance's identity; two `|>` chains in different scopes consuming the same stream get distinct cursors. (§13.18.12)
3421. Ring cursors are advisory: the buffer overwrites per ring policy regardless of cursor positions, so lagging consumers miss events. (§13.18.12)
3422. Gate streams are lossless per consumer: retention is slowest-cursor-driven, and no event is evicted until every consumer has observed it. (§13.18.12)
3423. When a gate buffer holds `capacity` events unobserved by the slowest cursor, further producer pushes are rejected — `rejected_total` increments and `is_full` is true. (§13.18.12)
3424. When a ring advance overwrites a cursor's position, the cursor automatically jumps forward to the oldest still-present event, the gap being observed via `dropped_total`. (§13.18.12)
3425. When a consumer's enclosing subtree is gated off, its cursor stops advancing; the default behavior is freeze-and-backlog. (§13.18.12)
3426. A frozen ring consumer's buffer keeps overwriting; on resume the cursor jumps to the oldest still-present event, the gap observed via `dropped_total`. (§13.18.12)
3427. A frozen gate consumer pins the buffer via slowest-cursor retention, so the buffer fills and producers are rejected (`rejected_total`, `is_full`) for the duration of the freeze. (§13.18.12)
3428. On resume, a frozen gate consumer drains the full retained backlog; nothing is lost to it. (§13.18.12)
3429. `@reset_on_reopen` opts a consumer out of backlog: on resume its cursor skips to the current head, discarding gap events. (§13.18.12)
3430. On a gate consumer, `@reset_on_reopen` additionally releases the buffer hold during the freeze, so the frozen consumer neither pins the buffer nor back-pressures producers. (§13.18.12)
3431. Cursors only advance; no operation rewinds a cursor to an earlier position. (§13.18.12)
3432. A stream's buffer is a fixed array of `capacity` slots of `sizeof(T)` bytes, allocated once at stream creation and freed when the stream's scope dies. (§13.18.13)
3433. A stream's head pointer and metadata counters (`pending_count`, `dropped_total`, `rejected_total`, `last_overflow_at`) are stored as ordinary reactive cells. (§13.18.13)
3434. Per-consumer cursors are stored as part of each consumer's per-instance state. (§13.18.13)
3435. Stream buffers are allocated from per-`(T, capacity)` pools; all instances with the same element type and capacity share a pool, each occupying one buffer-sized slot. (§13.18.13)
3436. The compiler enumerates stream declarations at compile time and computes per-`(T, capacity)` pool sizes statically. (§13.18.13)
3437. Hot reload may extend or shrink stream pools as declarations are added or removed, via the same per-`(T, capacity)` mechanism as other dynamic-size cell types. (§13.18.13)
3438. Streams perform no dynamic allocation: producers and consumers use pre-allocated slots, and the fixed buffer is the stream's entire memory cost. (§13.18.13)
3439. A `recurrent[N] stream X` allocates `N` output-history slots of `sizeof(T_X)` for `X.past(k, ...)` reads. (§13.18.13)
3440. A recurrent stream without an explicit `[N]` defaults to `[1]`. (§13.18.13)
3441. For each input referenced via `.past(k, ...)` in a recurrent stream body, the compiler statically determines that input's maximum `k` and allocates that many slots of `sizeof(T_input)`. (§13.18.13)
3442. Total memory of a recurrent stream declaration is `ring_buffer(X) + N * sizeof(T_X) + Σ max_k(input_i) * sizeof(T_input_i)`. (§13.18.13)
3443. Inputs not accessed via `.past` add no history overhead. (§13.18.13)
3444. All recurrent-history allocations are made once at stream creation; nothing is allocated at runtime. (§13.18.13)
3445. A stream's buffer survives hot reload iff its type signature — element type, policy, and capacity literal `N` — is byte-identical between old and new code. (§13.18.14)
3446. Element-type identity for reload preservation is structural, not same-named. (§13.18.14)
3447. On a signature-preserving reload, consumers preserved by their own identity rules continue from their previous cursor positions. (§13.18.14)
3448. A stream's source expression may change freely across reload; only the type signature gates buffer preservation. (§13.18.14)
3449. If any of element type, policy, or capacity differs at reload, the old buffer is discarded, a new empty buffer is allocated, and all cursors reset to its empty position. (§13.18.14)
3450. `@reset_on_reload` on a stream declaration opts out of buffer preservation: after any reload affecting the declaration site, the buffer resets to empty regardless of type-signature match. (§13.18.14)
3451. Adding `@reset_on_reload` applies prospectively: the adding reload still preserves the buffer per the type-signature rule, and the annotation takes effect on the next reload. (§13.18.14)
3452. Removing `@reset_on_reload` still resets the buffer on the removing reload (the old annotation governs the in-progress reload); subsequent reloads preserve. (§13.18.14)
3453. A consumer's cursor survives reload exactly when the consuming operator (or derived) instance is preserved per its own identity rule. (§13.18.14)
3454. A consumer added by a reload starts its cursor at the current head, observing only events arriving after the reload. (§13.18.14)
3455. A consumer removed by a reload has its cursor dropped. (§13.18.14)
3456. Changes to a stream's element type, policy (`ring` ↔ `gate`), or capacity are reload-unsafe, requiring per-instance restart (the stream and its consumers are recreated) or full-runtime restart. (§13.18.14)
3457. Implementations detect reload-unsafe stream changes during the reload diff phase. (§13.18.14)
3458. Recurrent output history survives reload iff the quadruple `(element type, policy, capacity, N)` is byte-identical. (§13.18.14)
3459. Rewriting a recurrent stream's `.past`-using source expression does not affect history preservation while the type signature matches. (§13.18.14)
3460. Increasing `[N]` (e.g., `recurrent[3]` to `recurrent[5]`) is reload-safe: preserved history fills the lower-index slots and new slots start empty, serving fallbacks until filled. (§13.18.14)
3461. Decreasing `[N]` (e.g., `recurrent[5]` to `recurrent[3]`) is reload-unsafe and forces per-instance restart of the affected recurrent-stream declarations. (§13.18.14)
3462. Per-input history is preserved across reload iff the input stream's type signature is byte-identical and that input's max referenced `k` does not decrease. (§13.18.14)
3463. Increasing an input's max `k` is reload-safe and extends its history allocation, older positions initializing from fallbacks. (§13.18.14)
3464. Removing all `.past` references to an input reload-safely releases its history allocation. (§13.18.14)
3465. `@reset_on_reload` on a `recurrent[N] stream` resets the output history and all per-input history in addition to the base buffer. (§13.18.14)
3466. `stream` declarations may not appear inside function bodies. (§13.18.15)
3467. A stream's source expression must contain at least one reactive input (signal, stream, or other Cell); a pure-value expression cannot be a stream source. (§13.18.15)
3468. Cursors are not first-class values: programs cannot construct, store, or pass them, and they are observable only through consumers' signal outputs. (§13.18.15)
3469. Within one commit cycle, a stream consumer observes exactly the set of events committed by the end of producer evaluation. (§13.18.15)
3470. Events emitted during a consumer's own evaluation are deferred to the next commit. (§13.18.15)
3471. Streams may not be passed to `runtime.write_signal` or `runtime.write_attr`; host-side stream writes go through `runtime.push_stream`. (§13.18.15)
3472. An output-stream lookback must satisfy `k ≤ N`: `recurrent[N] stream policy X = ... X.past(k, ...)` with `k > N` is a compile error. (§13.18.15)
3473. Stream-valued expressions cannot be assigned to signal-typed bindings — `derived X = stream_expr` and `signal X = stream_expr` are compile errors; project via `to_signal(fallback)`. (§13.18.15)
3474. A stream declaration without a policy keyword (`ring`/`gate`) is a normative error class: `stream my_events: Event = source` ✗. (§13.18.16)
3475. Operator and effect parameters typed `Stream[T]` are not satisfied by a signal (implicit `to_stream` covers bindings, not parameters); this is a normative error class: `persist(my_signal)` ✗. (§13.18.16)
3476. Assigning a stream-valued expression to a signal binding is a normative error class: `derived latest: Event = some_stream * 2` ✗. (§13.18.16)
3477. Reading a bare stream as a value is a normative error class: `derived latest: Event = events` ✗ when `events` is a stream. (§13.18.16)
3478. A policy mismatch in a pipe chain is a normative error class: ring `writes |> persist` ✗ where `persist` takes a gate stream. (§13.18.16)
3479. Assignment to a sink is a normative error class; sinks receive events only via stream forwarding: `ws.outbound = some_message` ✗. (§13.18.16)
3480. A `stream` declaration inside a function body is a normative error class: `stream ring[1024] events` inside `fn helper():` ✗. (§13.18.16)
3481. `.past`/`.previous` in a plain (non-recurrent) `stream` declaration body is a normative error class: `count.previous(0)` ✗. (§13.18.16)
3482. An output `.past(k, ...)` exceeding the declared `[N]` is a normative error class: `recurrent[3] stream ring x = x.past(5, 0)` ✗. (§13.18.16)
3483. A non-compile-time lookback `n` in `.past(n, fallback)` is a normative error class: `source.past(some_variable, 0)` ✗. (§13.18.16)

## Effects

3484. An effect is a reusable, cell-allocating reactive construct describing a desired alignment between program state and external reality, declared with the `effect` keyword. (§13.19)
3485. Effects are the mechanism by which programs interact with the outside world: network requests, persistent storage, long-lived resources, and event subscriptions. (§13.19)
3486. Effects are distinct from `fn` (reactive-transparent pure computation), `operator` (stateful cell-to-cell transform, pure with respect to outside reality), and `node` (topological graph participant): only `effect` is the host-interpreted bridge to the runtime environment. (§13.19)
3487. Every effect type intrinsically satisfies the `Effect` marker trait, so "any effect" is expressible as a bound: `[T: Effect]`. (§13.19)
3488. Like nodes and connections, an effect instance is a graph member: instantiated only in a node's `effects:` clause, held by reference, never stored as a value. (§13.19)
3489. An effect type (as opposed to an instance) is carried as a value via `Type[…]`. (§13.19)
3490. An effect declaration consists of two record-shaped blocks, `desired:` and `observed:`. (§13.19.1)
3491. Cells in `desired:` are written by the program (or flow from the effect's parameters) and read by the host. (§13.19.1)
3492. Cells in `observed:` are written by the host and read by the program through the standard reactive machinery. (§13.19.1)
3493. The reconciler reads an instance's parameters and desired cells, performs the real-world operations that align reality with the desired state, and writes the actual outcome into the observed cells. (§13.19.1)
3494. An effect declaration has the form `effect name[GenericParams]?(params...):` followed by its cell blocks. (§13.19.2)
3495. An effect's name is a snake_case identifier. (§13.19.2)
3496. The `desired:` block and the `observed:` block are each individually optional in an effect declaration. (§13.19.2)
3497. Every cell declaration in an effect block carries an explicit role keyword: `derived` or `sink` in `desired:`, `signal` or `stream` in `observed:`. (§13.19.2)
3498. Reactive declarations other than the four role-keyword cell forms (e.g. `recurrent`, `attr`, top-level `signal`) cannot appear inside an effect's body. (§13.19.2)
3499. The `desired:` and `observed:` blocks may be declared in either order; the canonical order is `desired:` first, `observed:` second. (§13.19.2)
3500. An effect does not return a value in the operator sense; it evaluates to its instance value, accessed through the binding name or expression position. (§13.19.2)
3501. Effect parameters are cell-bound or value-typed, under the same rules as operator parameters. (§13.19.3)
3502. A cell-bound parameter (`Signal[T]`, `Stream[T]`, or another `Cell[T]` type) binds to a reactive cell at instantiation. (§13.19.3)
3503. The reconciler reads a cell-bound parameter's current value — or observes its events, for stream parameters — during each invocation in which the parameter is dirty. (§13.19.3)
3504. Inside `desired:` cell expressions, a cell-bound parameter is treated as a cell of value type `T` (auto-deref). (§13.19.3)
3505. A value parameter (`name: T`) is snapshotted at instantiation and fixed for the effect instance's lifetime. (§13.19.3)
3506. Value-parameter snapshotting is a category-B storage operation with implicit-move semantics; no `move` keyword is required. (§13.19.3)
3507. Defaults are allowed on effect value parameters, under the standard parameter-ordering rules: `method: Method = Method::GET`. (§13.19.3)
3508. Defaults are not allowed on `Signal[T]` effect parameters in v1. (§13.19.3)
3509. An effect's first positional parameter is its implicit pipe target: `current_url |> fetch(method: Method::POST)`. (§13.19.3)
3510. Effect parameters may be stream-typed (`Stream[T]`, `RingStream[T, N]`, `GateStream[T, N]`): `entries: GateStream[LogEntry, 4096]`. (§13.19.3)
3511. A reactive composite parameter has each constituent field tracked independently for dirty propagation to the reconciler. (§13.19.3)
3512. A `derived` cell in `desired:` is a parameter-tracking pure reactive expression over parameters and in-scope cells, recomputed when any input changes: `derived request: Request = Request(method: method, target: url)`. (§13.19.4)
3513. Program code may read a `desired:` `derived` cell via the standard access path but cannot write it: `f.request`. (§13.19.4)
3514. A `sink` cell in `desired:` is a write-only stream end the program pushes into by piping a stream via `|>`: `sink ring[1024] outbound: Message`. (§13.19.4)
3515. A sink declaration comprises a policy keyword (`ring` or `gate`), an optional bracketed capacity defaulting to 1024, a name, and an element type. (§13.19.4)
3516. A sink declaration has no `= source` clause; its events come only from program-side pipe-into-sink expressions. (§13.19.4)
3517. Cell names within a single `desired:` block must be distinct. (§13.19.4)
3518. The reconciler reads `desired:` `derived` cells on each invocation where any of them is dirty. (§13.19.4)
3519. The reconciler consumes a sink cell's buffered events in order. (§13.19.4)
3520. A `desired:` block may contain `repeat` declarations for dynamic-scope materialization driven by reactive sources. (§13.19.4)
3521. Cells under a desired-block `repeat` are reconciled per scope, with the host applying additions and removals as the source's key set changes. (§13.19.4)
3522. Per-scope paths under a desired-block `repeat` follow the standard repeat-scope path scheme with the effect instance as the enclosing context. (§13.19.4)
3523. A `desired:` block may contain `when` and `given` selection blocks whose arms hold desired-cell declarations and `repeat`s. (§13.19.4)
3524. `when`/`given` arms in `desired:` carry standard freeze semantics (Model B): every arm is constructed, the active arm's cells are live, inactive arms' cells are frozen at their last value. (§13.19.4)
3525. A frozen desired arm contributes static values to the reconciler: it neither goes dirty nor triggers an `update`. (§13.19.4)
3526. When a gate over desired cells reopens, the arm's cells recompute, go dirty, and the reconciler updates with the now-live values. (§13.19.4)
3527. Freezing a desired arm pins that part of the desired state and stops tracking it while gated; it does not release the resource. (§13.19.4)
3528. Resource release on inactivity is the reconciler's response to the `suspend` hook, not an effect of `when`/`given` blocks in `desired:`. (§13.19.4)
3529. A `signal` cell in `observed:` declares a name, a value type, and an initial value: `signal response: Option[Response] = None`. (§13.19.5)
3530. An observed signal's initial value is what the program reads before the host's first write. (§13.19.5)
3531. The host writes observed `signal` cells via `runtime.write_signal` against the effect instance ID. (§13.19.5)
3532. A `stream` cell in `observed:` is a host-written event sequence: `stream ring[1024] inbound: Message`. (§13.19.5)
3533. An observed `stream` declaration has no `= source` clause; its source is the host's reconciler pushing events via `runtime.push_stream`. (§13.19.5)
3534. Policy and capacity on observed `stream` cells work as in regular stream declarations. (§13.19.5)
3535. `recurrent[N] stream` is not valid in `observed:` blocks; observed cells have no reactive expression body. (§13.19.5)
3536. Because `observed:` blocks admit no recurrent streams, an effect's history-aware behavior is computed in the host's reconciler. (§13.19.5)
3537. `repeat` and the `when`/`given` selection blocks are not valid in `observed:` blocks. (§13.19.5)
3538. Per-element scopes or gating driven by an observed cell are expressed by placing the `repeat` or `when`/`given` in an `expose:` block or the same effect's `desired:` block, with the observed cell as source. (§13.19.5)
3539. An observed `stream` cell begins empty. (§13.19.5)
3540. Cell names within a single `observed:` block must be distinct. (§13.19.5)
3541. Writing to an observed cell from program code is a compile error: `f.response = some(custom_response)` ✗. (§13.19.5)
3542. Host-side writes to observed cells — `runtime.write_signal` for signal cells, `runtime.push_stream` for stream cells — are keyed by effect instance ID and cell name. (§13.19.5)
3543. Host writes to observed cells are dirty-tracked in the standard commit-cycle way; downstream deriveds re-evaluate on the next commit. (§13.19.5)
3544. The identifiers `desired` and `observed` are reserved inside an effect declaration body; cells cannot bear these names: `derived desired: bool` ✗. (§13.19.6)
3545. `desired` and `observed` are reserved keywords everywhere; within an `effect` body they introduce the desired and observed blocks. (§13.19.6)
3546. An effect cannot declare cells with the same name in both `desired:` and `observed:`. (§13.19.6)
3547. `instance.field` resolves to whichever block declares `field`; the compiler enforces that exactly one block does. (§13.19.6)
3548. Effect instances expose their cells through a flat access path: `desired:` and `observed:` cells share a single namespace on the instance: `f.response`. (§13.19.7)
3549. Assigning to any effect cell — signal, stream, or derived — from program code is a compile error. (§13.19.7)
3550. Pipe-into-sink expressions are the only program-side writes to effect cells. (§13.19.7)
3551. Both the effect instantiation and any sink-forward pipe live in the `effects:` clause: `my_outgoing_messages |> ws.outbound`. (§13.19.7)
3552. Piping a `Stream[T]` into a `Sink[T]` establishes a forwarding subscription. (§13.19.7)
3553. Multiple pipes may target the same sink (multi-producer); their events arrive in commit order. (§13.19.7)
3554. Effect stream cells are consumed via stream operators, never via direct value reads; the stream value is the program-readable cursor view: `ws.inbound |> to_signal(empty_message)`. (§13.19.7)
3555. A stream cell's observation surface is read by field access on the stream value: `ws.inbound.pressure`. (§13.19.7)
3556. An effect instance is identified by its enclosing scope, its effect type name, and its argument bindings — the operator-instance scheme. (§13.19.8)
3557. Instantiations of the same effect type in different scopes — different nodes' `effects:` clauses, or different `repeat`/gate arms within one clause — produce distinct instances with independent desired/observed cells and independent host-side reconciler state. (§13.19.8)
3558. Effect instances have no user-assignable names in the language sense. (§13.19.8)
3559. A named `effects:` entry (`name = source |> effect`) names the instance value for downstream reads; the entry name plays no role in instance identity. (§13.19.8)
3560. Effect hot-reload identity follows the operator scheme, with tolerance for positional moves within the same clause. (§13.19.8)
3561. The effect's declared name serves as the type in signatures, annotations, and bounds, and as the constructor in pipe chains and call form — unlike nodes, which separate type names from placement syntax: `effect fetch` yields both type `fetch` and constructor `fetch`. (§13.19.8)
3562. Function-call form instantiates an effect equivalently to pipe form: `f2 = fetch(current_url)`. (§13.19.8)
3563. Effects may take type parameters with optional trait bounds: `effect cached_fetch[T: Cacheable](...)`. (§13.19.9)
3564. Effect type parameters are resolved at the call site from argument types, with explicit turbofish where ambiguous. (§13.19.9)
3565. Reconciler registration is per-effect-type and per-type-parameter-instantiation: each concrete instantiation of a generic effect has a distinct registration key. (§13.19.9)
3566. Effects carry the standard three-level visibility: `public`, `shared` (the default), `private`. (§13.19.10)
3567. Module-private effects are not reachable from other modules; public effects may be re-exported. (§13.19.10)
3568. Visibility applies uniformly to an effect's type and constructor. (§13.19.10)
3569. An effect instance is a scoped reload boundary, like an operator instance. (§13.19.11)
3570. For reload, each cell in an effect's blocks is identified by its declared name and type within the effect's body. (§13.19.11)
3571. Changing the initial-value expression of an observed `signal` cell is reload-safe; existing live cells retain their committed values. (§13.19.11)
3572. Adding a new cell in `observed:` or `desired:` is reload-safe; new cells initialize fresh — declared initial value for signals, empty for streams and sinks. (§13.19.11)
3573. Changing a `desired:` `derived` cell's expression is reload-safe; the cell re-evaluates on the next commit with the new logic. (§13.19.11)
3574. Effect parameter signature changes (parameters added, removed, retyped, or reordered) are reload-unsafe and force per-instance restart. (§13.19.11)
3575. Cell type changes in `desired:` or `observed:` are reload-unsafe and force per-instance restart. (§13.19.11)
3576. Policy or capacity changes on an effect's `stream` or `sink` cells are reload-unsafe and force per-instance restart. (§13.19.11)
3577. Changing a cell's role keyword (e.g. `derived` to `sink`, `signal` to `stream`) is reload-unsafe and forces per-instance restart. (§13.19.11)
3578. Per-instance restart delivers a reconciler teardown for the affected instances and constructs new instances under the new declaration. (§13.19.11)
3579. Per-instance effect restart leaves other effect instances and the rest of the runtime running. (§13.19.11)
3580. A suspended effect instance reloads by its ordinary identity rules: a reload-safe change preserves the instance and its reconciler-side state, and it stays suspended with no spurious `resume`. (§13.19.11)
3581. After a reload-safe change to a suspended instance, a later gate-open delivers `resume` against the reloaded declaration. (§13.19.11)
3582. A reload-unsafe change to a suspended instance delivers `teardown` (subsuming the already-performed suspend release) and constructs a fresh instance. (§13.19.11)
3583. A fresh instance constructed by reload begins in whatever activation state its gate evaluates to on the next commit. (§13.19.11)
3584. Changing which effect a call site invokes tears down the old instance and constructs the new one fresh, even if cell shapes overlap: `source |> fetch` to `source |> cached_fetch`. (§13.19.11)
3585. Pure positional call-site moves within the same scope preserve effect instance identity. (§13.19.11)
3586. A stream cell's buffer inside an effect is preserved across reload iff `(element type, policy, capacity)` is byte-identical. (§13.19.11)
3587. `@reset_on_reload` on an effect's stream cell forces a buffer clear on reload. (§13.19.11)
3588. An effect instance lives as long as its enclosing scope; scope death drops the instance and all its cells. (§13.19.12)
3589. On instance teardown the host's reconciler receives a teardown call with the instance ID, allowing release of external resources. (§13.19.12)
3590. An effect in a node's `effects:` clause lives as long as the node instance is mounted. (§13.19.12)
3591. An application-global effect is hosted in a top-level node instance and lasts the program's lifetime. (§13.19.12)
3592. An effect inside a `repeat` in `effects:` lives until its element key is removed from the iterated source. (§13.19.12)
3593. An effect inside a `when`/`given` block in `effects:` is suspended while the gate is closed and resumed when it reopens; gating never destroys it. (§13.19.12)
3594. A desired value implying no resource is needed (e.g. `derived target: Option[Url] = None`) makes the host tear down the resource while the effect instance stays alive, ready to re-establish. (§13.19.12)
3595. Only scope death causes effect instance teardown. (§13.19.12)
3596. Gating off an effect's enclosing subtree delivers the reconciler's `suspend` hook: the resource is released, instance state preserved. (§13.19.12)
3597. Reopening the gate delivers `resume`, re-establishing the resource. (§13.19.12)
3598. Gating is transitive: closing any ancestor gate suspends every contained effect, regardless of the effects' own gates. (§13.19.12)
3599. Effects are the sole construct the suspend/resume protocol concerns; pure nodes and connections have no `suspend`/`resume`. (§13.19.12)
3600. The runtime guarantees the `suspend` signal on gate-close and `resume` on gate-open. (§13.19.12)
3601. What releasing and re-acquiring a resource mean under suspend/resume is decided by the reconciler. (§13.19.12)
3602. No source-level activation value must be threaded through `desired:`; the suspend/resume hook is the gating mechanism. (§13.19.12)
3603. Effect instances are created by piping into them, identically to operators, with the instantiation in the node's `effects:` clause: `f = current_url |> fetch`. (§13.19.13)
3604. The LHS of `|>` binds to the effect's first positional parameter. (§13.19.13)
3605. The value of an effect-instantiating pipe is the effect instance: the composite of its `desired:` and `observed:` cells. (§13.19.13)
3606. An effect instance never auto-projects to a single cell in pipe-out position. (§13.19.13)
3607. Operators downstream of an effect take either the whole instance (parameter typed with the effect type) or a cell projected by field access: `f |> render_fetch_card` vs `f.response |> render_response`. (§13.19.13)
3608. The host registers a reconciler for each effect type via the host API, keyed by the effect's type name. (§13.19.14)
3609. A reconciler is a host-language object whose interface mirrors the effect's declaration. (§13.19.14)
3610. A reconciler has read access to an instance's parameter values and `desired:` cell values. (§13.19.14)
3611. A reconciler writes an instance's `observed:` cells via the host API: `runtime.write_signal` for signal cells, `runtime.push_stream` for stream cells. (§13.19.14)
3612. A reconciler exposes lifecycle hooks: instance creation (effect appears in the live graph), update (parameters or desired cells change), and teardown (instance leaves scope). (§13.19.14)
3613. After commit, the runtime enumerates effect instances whose parameters or desired cells became dirty during that commit. (§13.19.14)
3614. For each such instance, the runtime invokes the registered reconciler with the instance ID; the reconciler reads the new desired state and reconciles. (§13.19.14)
3615. Reconciler writes into observed cells are dirty-tracked in the standard way and take effect on the next commit. (§13.19.14)
3616. Reconcilers are expected to be idempotent: re-applying the same desired state produces the same alignment, with no duplicated side effects or leaked resources — permitting the runtime to invoke the reconciler freely. (§13.19.14)
3617. An effect type appearing in the graph with no registered reconciler triggers a startup diagnostic, and the runtime refuses to enter the live state. (§13.19.14)
3618. Reconciler errors are reported to the program through the effect's `observed:` cells: `signal error: Option[E] = None`. (§13.19.14)
3619. A reconciler does not panic the runtime; reconciler-internal errors are domain errors expressed through the value track. (§13.19.14)
3620. A node's `effects:` clause is the single, exclusive host for effect instantiation. (§13.19.15)
3621. Effect instantiation is forbidden at module level, in operator bodies, in connection bodies, and in function bodies. (§13.19.15)
3622. Within a node, effect instantiation outside the `effects:` clause is a compile error: a free `f = url |> fetch` ✗. (§13.19.15)
3623. An effect's `desired:` block may not contain another effect's instantiation: effects do not compose from effects. (§13.19.15)
3624. Effects are composed at the consumer site by feeding one effect's observed cells into another effect's parameters. (§13.19.15)
3625. Effects are not topological participants and are never placed via node-style `EffectName name /` placement syntax. (§13.19.15)
3626. Effects are instantiated by appearance in expression position: pipe chains or function-call form. (§13.19.15)
3627. An effect's body consists only of the `desired:` and `observed:` blocks containing role-keyword cell declarations. (§13.19.15)
3628. Stateful behavior wrapping an effect — recurrent state, accumulators, retry logic — is expressed via wrapping operators, not via declarations inside the effect. (§13.19.15)
3629. A normative diagnostic class covers use of a reserved cell name (`desired`/`observed`) inside an effect's blocks. (§13.19.16)
3630. A normative diagnostic class covers a cell name duplicated across `desired:` and `observed:`. (§13.19.16)
3631. A normative diagnostic class covers program-code assignment to an effect cell. (§13.19.16)
3632. A normative diagnostic class covers an effect type with no registered reconciler, raised at runtime startup. (§13.19.16)
3633. A normative diagnostic class covers effect instantiation inside a function body. (§13.19.16)
3634. A normative diagnostic class covers effect instantiation inside another effect's body. (§13.19.16)
3635. A normative diagnostic class covers a non-role-keyword declaration (e.g. `recurrent`) inside an effect's blocks. (§13.19.16)
3636. A normative diagnostic class covers an effect type used in node-style placement position. (§13.19.16)
3637. A normative diagnostic class covers effect instantiation in an `expose:` block. (§13.19.16)
3638. A normative diagnostic class covers effect instantiation at module level. (§13.19.16)
3639. A normative diagnostic class covers effect instantiation in an operator body. (§13.19.16)
3640. A normative diagnostic class covers effect instantiation in a node body outside the `effects:` clause. (§13.19.16)

## Implementation Model

3641. The implementation model is normative for implementations of Ductus, not for source-level code; program behavior is determined by §§1–13. (§14)
3642. A conforming implementation provides two compilation modes: interpreter mode and native mode. (§14.1)
3643. Interpreter mode compiles Ductus source to a compact bytecode executed by an interpreter embedded in the runtime. (§14.1)
3644. Native mode compiles Ductus source to a native executable. (§14.1)
3645. Both compilation modes share one frontend — lexer, parser, type checker, semantic analysis — producing a typed intermediate representation; the modes diverge only after that point. (§14.1)
3646. The two compilation modes must produce equivalent observable behavior (modulo performance and timing); an implementation that diverges observably between modes is non-conforming. (§14.1)
3647. The frontend's first pass lexes and parses per this specification, producing an AST. (§14.1.1)
3648. The frontend's second pass performs name resolution and type checking per §§2–10, yielding a typed AST with all generic instantiations resolved and all trait dispatch sites bound to concrete implementations. (§14.1.1)
3649. The frontend's third pass performs borrow and ownership checking per §11, catching use-after-move, borrow conflicts, and other ownership violations. (§14.1.1)
3650. The frontend's fourth pass performs reactive analysis per §13: it identifies reactive declarations, computes dependency graphs, and extracts the graph specification. (§14.1.1)
3651. The frontend's fifth pass monomorphizes per §2.3, resolving all generic instantiations before lowering. (§14.1.1)
3652. Monomorphization is never delegated to a backend; emitted code is fully concrete. (§14.1.1)
3653. In interpreter mode the bytecode emitter lowers the typed IR to a stack-based bytecode executed directly by the runtime's interpreter. (§14.1.2)
3654. No native compilation step occurs in interpreter mode. (§14.1.2)
3655. Interpreter mode supports hot reload (§14.8): individual behaviors can be replaced in a running runtime without restarting. (§14.1.2)
3656. The bytecode format is implementation-internal and not stable across Ductus versions. (§14.1.2)
3657. The bytecode format is not a distribution format. (§14.1.2)
3658. In native mode the backend lowers the typed IR to a native executable, and that binary is the distribution artifact. (§14.1.3)
3659. How an implementation generates native code — its intermediate form and toolchain — is a backend concern outside the specification. (§14.1.3)
3660. Native mode produces a single executable embedding both the compiled behaviors and the graph specification (§15.4). (§14.1.3)
3661. Native mode does not support hot reload at runtime; changing the program requires a rebuild. (§14.1.3)
3662. Emitted native code is fully monomorphic and Ductus-trait-free, with concrete type and function definitions per instantiation. (§14.1.3)
3663. The trait system is not exported into emitted native code; trait dispatch sites are resolved to direct function calls during frontend processing. (§14.1.3)
3664. A conforming implementation provides a command-line interface wrapping the compilation modes. (§14.2)
3665. The CLI's operations are normative and required; specific flag spellings may vary across implementations. (§14.2)
3666. The CLI `run` operation invokes interpreter mode, compiling to bytecode and executing immediately until program completion or interruption: `ductus run <file>`. (§14.2.1)
3667. The CLI `watch` operation runs interpreter mode with file watching; saved source changes trigger recompilation and hot reload of affected behaviors per §14.8: `ductus watch <file>`. (§14.2.1)
3668. The CLI `build` operation invokes native mode and outputs a single executable file, with `--release` enabling optimization: `ductus build <file> --release`. (§14.2.1)
3669. The CLI `check` operation runs frontend passes only, producing diagnostics without invoking either backend, and is used by editor integrations (LSP): `ductus check <file>`. (§14.2.1)
3670. The CLI `fmt` operation invokes the canonical formatter, rewriting the source in normalized form: `ductus fmt <file>`. (§14.2.1)
3671. The CLI `test` operation runs tests via interpreter mode: `ductus test <file>`. (§14.2.1)
3672. The CLI ships as a single binary that bundles or downloads on first use the Ductus frontend, the bytecode interpreter, the native-mode toolchain, and the Ductus stdlib and reactive runtime. (§14.2.2)
3673. Users never install or manage the native toolchain separately; all of its invocations are internal. (§14.2.2)
3674. Native-toolchain build output is suppressed in normal operation and surfaced only when a compilation failure prevents Ductus's output from being produced. (§14.2.2)
3675. A Ductus project is a directory tree containing `.duc` source files. (§14.2.3)
3676. No manifest file is required for single-file programs: `ductus run file.duc` works on a lone file. (§14.2.3)
3677. Multi-file projects use a manifest file specifying the entry point and any external dependencies. (§14.2.3)
3678. The manifest file format is implementation-specific. (§14.2.3)
3679. The runtime holds the value of every reactive cell and keeps it current. (§14.3)
3680. How cells are stored is a backend concern; every backend must preserve the per-cell value semantics. (§14.3)
3681. Each reactive cell holds a value of its declared type, read and written losslessly (bit-exact). (§14.3.1)
3682. A backend may represent cells however it likes as long as reads and writes are value-preserving. (§14.3.1)
3683. Types wider than 8 bytes — `i128`, `u128`, multi-field records used as reactive values — occupy multiple consecutive cells. (§14.3.2)
3684. An `i128` reactive value occupies two cells: the low 64 bits in cell N, the high 64 bits in cell N+1. (§14.3.2)
3685. The canonical layout for a record used as a reactive value is one cell per field, and conforming implementations must support it: `Vec3` with `f32` fields `x`, `y`, `z` takes three consecutive cells. (§14.3.2)
3686. In the canonical layout, sub-8-byte fields pad to a full 8-byte cell: an `f32` field pads from 4 to 8 bytes. (§14.3.2)
3687. An implementation may optionally pack multiple sub-8-byte fields into one cell as an optimization: four `bool` fields in the low bits of one cell. (§14.3.2)
3688. Field packing must not be observable from Ductus source: every cell read and write through the runtime API must match the canonical per-field layout exactly. (§14.3.2)
3689. Records with fields wider than 8 bytes follow the same per-field layout, each multi-cell field occupying its own consecutive cells within the enclosing record's allocation. (§14.3.2)
3690. Dynamic-size cell types per §13.12.4 (`Vec[T]`, `SmallVec[T, N]`, `RingBuf[T, N]`, `string`, etc.) cannot live in fixed-size cell slots; their storage uses extensible pools. (§14.3.3)
3691. The runtime allocates one pool per dynamic-size type used in the program. (§14.3.3)
3692. A dynamic-size reactive cell holds a fixed-size index into its type's pool; the variable-size value lives in the pool. (§14.3.3)
3693. On commit the runtime writes the new value into the pool and updates the cell's index so subsequent reads resolve to it. (§14.3.3)
3694. An observer that reads a cell never sees a partial write. (§14.3.3)
3695. An index read resolves to a value coherent with the snapshot it was read from. (§14.3.3)
3696. Each pool is an arena of slots; a slot holds one value of the pool's type plus the metadata needed to keep values of distinct committed states distinct until released. (§14.3.3)
3697. A pool read resolves the cell's index through the pool to the value's address — one pointer dereference — then reads the value. (§14.3.3)
3698. A value observed under a committed state remains valid until that state is released, even after a later commit updates the cell's index. (§14.3.3)
3699. For persistent data structures (e.g., `Vec[T]` as a persistent vector trie), pool slots may share internal nodes across versions. (§14.3.3)
3700. The pool tracks persistent-structure node-level refcounts and reclaims old nodes when no live state references them. (§14.3.3)
3701. For value types (`SmallVec[T, N]`, `RingBuf[T, N]`), each retained value is a complete copy, bounding per-commit work by the value's size. (§14.3.3)
3702. Pool sizes are chosen at runtime construction from the graph specification's static per-type cell count plus a configurable versioning headroom. (§14.3.3)
3703. Pools may grow at runtime when the configured headroom is exceeded; growth is amortized but not guaranteed wait-free. (§14.3.3)
3704. Hosts needing strict wait-free guarantees should configure sufficient pool headroom up front. (§14.3.3)
3705. Pool slot acquire is O(1) amortized per commit, via a free-list in the pool. (§14.3.3)
3706. Per-commit value copy into a pool costs O(value-size). (§14.3.3)
3707. Persistent structures copy O(log n) nodes per push. (§14.3.3)
3708. Per-cell memory overhead for pool-backed types is one 8-byte index slot. (§14.3.3)
3709. Stream buffers (§13.18) are pool-managed: a stream declaration with element type `T` and capacity `N` allocates a buffer of `N * sizeof(T)` bytes. (§14.3.3)
3710. All stream instances sharing the same `(T, N)` combination across the program draw from one per-`(T, N)` pool. (§14.3.3)
3711. The runtime enumerates stream declarations at compile time, groups them by `(T, N)`, and sizes each pool by the number of instances of that combination. (§14.3.3)
3712. Each stream-pool slot holds one complete buffer. (§14.3.3)
3713. A stream's metadata cells — head pointer, dropped/rejected counters, observation cells per §13.18.6 — live in the standard reactive state buffer; only the buffer's slot array lives in the per-`(T, N)` pool. (§14.3.3)
3714. Hot reload can grow or shrink stream pools as stream declarations are added or removed, via the same extensible-pool mechanism. (§14.3.3)
3715. A preserved stream (per §13.18.14's preservation rule) retains its pool slot across hot reload; a new stream allocates a new slot. (§14.3.3)
3716. Stream buffer slot arrays are not versioned per snapshot. (§14.3.3)
3717. Stream synchronization relies on the head pointer, which is part of reactive state: the runtime writes events into slot positions, then advances the committed head pointer at commit. (§14.3.3)
3718. An observer sees stream events only up to the head pointer of the snapshot it reads; writes beyond the committed head are invisible until the next commit. (§14.3.3)
3719. Under `ring` policy, previously committed slots are overwritten only at positions past any caught-up cursor. (§14.3.3)
3720. Lagging cursors that were pointing at overwritten positions jump forward per §13.18.12. (§14.3.3)
3721. Only reactive cells are reactive state: values the runtime owns and keeps current across commits. (§14.4)
3722. Values held by `signal` declarations are reactive state. (§14.4)
3723. Values held by `attr` declarations on node and connection instances are reactive state. (§14.4)
3724. Values held by `recurrent` declarations on node and connection instances are reactive state. (§14.4)
3725. A `derived` declaration's cached computed value is reactive state. (§14.4)
3726. A `stream` declaration's reactive state is its head pointer, metadata, and synthesized observation cells per §13.18.6; the buffer slot array itself lives in the per-`(T, N)` pool per §14.3.3. (§14.4)
3727. Cells declared inside an `effect`'s `desired:` and `observed:` blocks (§13.19.4, §13.19.5) are reactive state — ordinary Signal or Stream cells per their declared type. (§14.4)
3728. An `effect` is a grouping construct, not a new storage category. (§14.4)
3729. `Sink[T]` cells (§13.18.4) are the write-side view of a Stream: they share the same underlying storage and do not allocate separately. (§14.4)
3730. Local bindings, parameters, return values, iterator state, closure captures, and non-reactive record/array/tuple values are not reactive state; they are ordinary stack or heap values under §11's ownership and borrow rules. (§14.4)
3731. A record type may be reactive state in one context and an ordinary value in another within one program; the compiler decides by declaration site, not by type. (§14.4)
3732. The runtime maintains a separate string pool that stores all string content. (§14.5)
3733. The string pool is logically a refcounted-shared, append-mostly arena: each unique string is stored once and shared via reference counts. (§14.5)
3734. Reactive cells of type `string` store a u64 pool index rather than the content; the pool resolves the index to the string data. (§14.5)
3735. The string pool is shared across all snapshots: cells and the snapshots retaining them hold indices; the pool holds the data. (§14.5.1)
3736. Commit cost is O(N) in cell count, not in string content size: changing a 1-megabyte string updates a single 8-byte index. (§14.5.1)
3737. A string may be referenced by multiple cells, in the same or different snapshots, via shared indices. (§14.5.1)
3738. An observer acquiring or releasing a snapshot never touches per-string refcounts, keeping index-to-string resolution wait-free. (§14.5.1)
3739. Per-string refcount changes happen producer-side — at write/publish and at producer-side retirement of a superseded snapshot — never on the observer's read path. (§14.5.1)
3740. String allocation stores a new string in the pool and returns an index, with the refcount initialized to 1 for the cell that will hold it. (§14.5.2)
3741. Copying a string index into another cell increments the pool's refcount on that string. (§14.5.2)
3742. Overwriting a cell or retiring a snapshot holding a string index decrements that string's refcount. (§14.5.2)
3743. When a string's refcount reaches zero, the pool reclaims its storage. (§14.5.2)
3744. Lookup resolves an index to its string value and is wait-free with a proper pool structure. (§14.5.2)
3745. String-pool allocation and refcount operations are atomic but may block briefly under contention. (§14.5.2)
3746. Which execution context performs string-pool writes versus reads is a backend concern; the contract is only that index reads stay wait-free. (§14.5.2)
3747. The behavior ABI — how a runtime invokes a behavior by ID with input-cell values and receives its outputs — is defined abstractly in the Behavior IR (§15.4.4). (§14.6)
3748. The runtime reaches a behavior only through the behavior ABI; it never reaches inside. (§14.6)
3749. Behaviors are stateless at the runtime level: pure transformations that read inputs from cells, compute, and write outputs, with all state in reactive cells. (§14.6.1)
3750. A stateful computation (filter with history, oscillator with phase, accumulator) is structured as a record whose attrs hold the state plus a behavior that reads the state-attrs, computes new state, and writes back. (§14.6.1)
3751. Local mutation within a behavior — `mut` bindings, indexed assignment, iterator state — is permitted per §§11–12 but visible only within that invocation; it does not escape. (§14.6.1)
3752. Behaviors follow the two-track failure model uniformly: trap-track failures (overflow under default operators, division by zero, out-of-range indices, explicit `panic`) abort the process. (§14.6.2)
3753. Recoverable conditions in behaviors are expressed as value-track `Option`/`Result` values flowing through the type system. (§14.6.2)
3754. The runtime does not isolate behavior traps: no `catch_unwind` boundary, no errored-cell sentinel, no continuation past a trap. (§14.6.2)
3755. Behavior IDs are assigned at compile time and are content-addressed: a stable hash of the canonicalized typed IR of the behavior body. (§14.6.3)
3756. Canonicalization before hashing alpha-renames locals, sorts declaration order where order is irrelevant, and strips position information. (§14.6.3)
3757. Cosmetic changes — added whitespace, reordered independent declarations, renamed local bindings — do not perturb a behavior's ID. (§14.6.3)
3758. Semantic changes — different operations, different inputs, different output type — produce different behavior IDs. (§14.6.3)
3759. The behavior-ID hash algorithm is fixed per Ductus toolchain version (§14.9). (§14.6.3)
3760. Canonicalization may change across major toolchain versions. (§14.6.3)
3761. Cross-version hot reload is not supported. (§14.6.3)
3762. Each behavior carries a debug name, the qualified source path: `module::path::clip_name::derived_name`. (§14.6.3)
3763. The runtime resolves behaviors by ID; debug names appear only in diagnostic output (diagnostics, profiles, error messages). (§14.6.3)
3764. Which execution context invokes each behavior, and how it maps onto OS threads, is a backend concern; Ductus source does not specify thread roles. (§14.6.4)
3765. Behaviors are thread-safe by construction: no shared mutable state exists outside reactive cells, which the runtime coordinates. (§14.6.4)
3766. The `Drop` trait declares a single method receiving the value by `own`: `trait Drop: fn drop(own value: Subject)`. (§14.7.1)
3767. A type implementing `Drop` provides cleanup logic that runs when a value of the type goes out of scope. (§14.7.1)
3768. The `drop` method is the value's final owner. (§14.7.1)
3769. A `drop` body may move fields out to release them and, for in-place teardown, rebind the value to a `mut` local (§11.7.3) — the ordinary `own`-parameter-to-`mut`-local pattern. (§14.7.1)
3770. `Drop::drop` involves no `mut` parameter; §11.7.2's prohibition on `mut` parameters holds without exception. (§14.7.1)
3771. The compiler emits the consuming `drop` call — a `move` of the value into `drop` — at scope exit. (§14.7.1)
3772. Fields left un-moved by a `drop` body drop afterward via drop glue, in reverse declaration order. (§14.7.1)
3773. Drop glue does not re-invoke `Drop::drop` on the value itself. (§14.7.1)
3774. A drop call is inserted at the end of a value's lexical scope, when its `let` or `mut` binding goes out of scope. (§14.7.2)
3775. A drop call is inserted at the end of a function for un-returned locals. (§14.7.2)
3776. A drop call is inserted at the point of `break` for non-yielded iterator elements (§12.9.3). (§14.7.2)
3777. Compound values (records, enums) drop their fields in reverse declaration order: the last-declared field drops first. (§14.7.2)
3778. When only some fields of a record have been moved out at scope exit, only the un-moved fields drop. (§14.7.3)
3779. The compiler tracks per-binding move flags during semantic analysis. (§14.7.3)
3780. A panic inside a `drop` method aborts the process, per the standard trap behavior of §4.6.1. (§14.7.4)
3781. The runtime manages drop for reactive cells. (§14.7.5)
3782. Signals declared at program startup live for the program's lifetime; their cells drop at program shutdown. (§14.7.5)
3783. The unit of hot reload is the behavior. (§14.8.1)
3784. Hot reload begins when the CLI's watch mode detects a source change and the frontend re-runs on the changed file. (§14.8.1)
3785. Reload diffs behaviors by content-addressed ID per §14.6.3: IDs only in the old program are removed, IDs only in the new program are added, IDs in both carry over unchanged. (§14.8.1)
3786. Reload diffs cells by fully-qualified declaration path per §13.15.2: cells with matching path and type carry forward preserving values, new cells are added, removed cells are dropped per §14.7. (§14.8.1)
3787. Hot reload matches operator instances by (enclosing scope, operator name, argument bindings), tolerating positional moves within the same scope. (§14.8.1)
3788. Reload-matched operator instances preserve their internal cell state; unmatched instances are dropped or added with the corresponding cell churn. (§14.8.1)
3789. An added behavior registers in the behavior table at its content-addressed ID; graph-spec edges and cell allocations referencing that ID become live, and subsequent invocations dispatch through it. (§14.8.1)
3790. An added cell gets storage allocated and initialized per the new source. (§14.8.1)
3791. An added operator instance gets internal cell state allocated and initialized per the new source. (§14.8.1)
3792. A removed behavior is deregistered from the behavior table. (§14.8.1)
3793. A removed cell is dropped per §14.7 in reverse-declaration order. (§14.8.1)
3794. A removed operator instance has its internal cells dropped per §14.7. (§14.8.1)
3795. A re-initialization pass recomputes, from current inputs, the initial value of each derived whose behavior body changed (different content-addressed ID). (§14.8.1)
3796. Deriveds whose behavior body is unchanged retain their values. (§14.8.1)
3797. Reload commits the reloaded state as an explicit step after re-initialization. (§14.8.1)
3798. After commit, the reload lock is released: signal/attr writes resume and any queued writes apply to the new state. (§14.8.1)
3799. Reactive cell values persist across hot reload: signal, attr, and derived cached values change only when the source explicitly changes them. (§14.8.2)
3800. Graph topology persists across hot reload. (§14.8.2)
3801. Matched operator instances preserve their internal cell state across reload by the operator-instance-identity scheme of §13.17.10, via the same cell-identity mechanism (§13.15.2) used for top-level cells. (§14.8.2)
3802. Changing the body of an existing behavior (same signature, different implementation) is reload-safe. (§14.8.3)
3803. Adding new behaviors, signals, attrs, or derived declarations is reload-safe. (§14.8.3)
3804. Reload-unsafe changes fall into two classes per §13.15.4: those requiring full-runtime restart and those needing only per-instance restart. (§14.8.3)
3805. Changes to the reactive state buffer layout that would relocate live cells require a full-runtime restart. (§14.8.3)
3806. Operator-specific changes (operator signature changes, internal cell type changes per §13.17.10) need only per-instance restart: only the affected operator instances are recreated. (§14.8.3)
3807. All other changes — cell removal (compile-gate-verified unreferenced), cell type changes (remove + add per §13.15.2), connection topology changes (remove + add per §13.15.2) — are reload-safe and need no restart. (§14.8.3)
3808. The implementation diagnoses unsafe changes at reload time and either rejects them — the runtime keeps running the old version — or cleanly applies the appropriate restart, full-runtime or per-instance per §13.15.4. (§14.8.3)
3809. Whether an unsafe change is rejected or handled by restart is implementation-defined. (§14.8.3)
3810. If the new source fails to compile (parse, type, or ownership error), the reload is abandoned and the runtime keeps running the previous version. (§14.8.4)
3811. The CLI surfaces reload compilation errors to the user. (§14.8.4)
3812. Hot reload never yields an inconsistent runtime: either the old version continues running or the new version is fully applied, never a mix. (§14.8.4)
3813. Each Ductus release is a matched set of four versions: source format, graph specification schema, behavior ABI, and runtime binary. (§14.9)
3814. Cross-version mixing is not supported: a program produced by version X.Y compiles with and runs against the same X.Y toolchain. (§14.9)
3815. Forward and backward compatibility across major version boundaries are explicit, not implicit. (§14.9)
3816. Version metadata is recorded in the graph specification header (§15.4), where cross-version compatibility checks happen. (§14.9)
3817. There is no source-level version directive; matched-set versioning is carried entirely by the toolchain and the graph-spec header. (§14.9)

## Compilation Model & IR

3818. Compilation transforms Ductus source files into executable form plus the build-time artifacts the runtime consumes at startup. (§15)
3819. The compiler emits exactly two artifact classes: executable code and the reactive graph specification. (§15.1)
3820. Executable code is bytecode in interpreter mode or a native executable in native mode. (§15.1)
3821. The reactive graph specification is a build-time description of the program's reactive shape that the runtime consumes at startup and hot reload diffs against. (§15.1)
3822. Both artifact classes are produced by the same shared frontend. (§15.1)
3823. Backends fork only at the final lowering step, where each consumes the IR to produce its executable artifact. (§15.1)
3824. The spec does not prescribe the compiler's internal IR shape; any phase organization satisfying the §15.2 obligations is permitted. (§15.1)
3825. The required analyses are obligations, not phases; the compiler may organize them into any combination of passes as long as each program is accepted or rejected per the semantics of §§1–13. (§15.2)
3826. Lexical and syntactic parsing per this specification is a required compiler obligation. (§15.2.1)
3827. Name resolution (per §3.4, §10) is a required compiler obligation. (§15.2.1)
3828. Type checking (per §2, §4, §6, §7, §9) is a required compiler obligation. (§15.2.1)
3829. Trait resolution (per §3.4.2, §3.7) is a required compiler obligation. (§15.2.1)
3830. Borrow checking (per §11.9) is a required compiler obligation. (§15.2.1)
3831. Monomorphization (per §2.3) is a required compiler obligation. (§15.2.1)
3832. Reactive-graph extraction (per §13, §15.4) is a required compiler obligation. (§15.2.1)
3833. Failing any required obligation is a compile error; the compiler rejects the program before emitting any artifact. (§15.2.1)
3834. Name resolution depends on parsing. (§15.2.2)
3835. Type checking depends on name resolution. (§15.2.2)
3836. Trait resolution depends on type checking. (§15.2.2)
3837. Borrow checking depends on type checking. (§15.2.2)
3838. Monomorphization depends on trait resolution and type checking. (§15.2.2)
3839. Reactive-graph extraction depends on monomorphization. (§15.2.2)
3840. Cells in generic node types are extracted per-instantiation. (§15.2.2)
3841. The compiler may interleave the obligations across passes as long as their dependency partial order is preserved. (§15.2.2)
3842. The spec prescribes no particular compiler IR layering or pass count. (§15.2.2)
3843. The reference implementation's canonical AST → HIR → MIR → backend layering is non-normative. (§15.2.2)
3844. Diagnostic quality, error-message formatting, and recovery behavior are implementation-defined. (§15.2.3)
3845. The spec requires only that ill-formed programs be rejected and well-formed programs be accepted per the semantics of §§1–13. (§15.2.3)
3846. The graph specification carries all build-time metadata the runtime needs at startup: behavior table, string-pool seed, and schema and format versions. (§15.3)
3847. Both backends produce the same graph specification; only the executable-code form differs. (§15.3)
3848. In interpreter mode both compilation artifacts are held in memory by the running runtime. (§15.3.1)
3849. In interpreter mode hot reload replaces the in-memory artifacts in place. (§15.3.1)
3850. In native mode both artifacts are embedded in the compiled binary. (§15.3.1)
3851. At native-mode program startup the runtime deserializes the graph specification and registers the behavior table. (§15.3.1)
3852. The Ductus IR is the backend-agnostic contract between frontend and runtime and the unit a hot reload diffs. (§15.4)
3853. Every backend — compiled, interpreted, or a future alternative — is implemented against the Ductus IR. (§15.4)
3854. An IR module has three parts over a shared type table: `module <name> { types {…} graph {…} behaviors {…} }`. (§15.4)
3855. The `types` table maps each `%TypeId` to its layout: size, align, and field/variant order. (§15.4)
3856. The graph IR is declarative data — cells and their dependency edges, gates, streams, effects, and scopes — referencing behaviors by id. (§15.4)
3857. The behavior IR is the pure compute that each derived, gate, or effect-`desired` runs. (§15.4)
3858. Graph and behaviors meet only through the behavior ABI: the graph names a behavior by id and the runtime invokes it. (§15.4)
3859. Both IR parts speak one monomorphic type vocabulary; the frontend has specialized all generics before emission. (§15.4)
3860. The IR type vocabulary is the §4.1 primitives plus `str`, tuples `(T,…)`, arrays `[T;N]`, record/enum `%TypeId`s, `pool_index<%PoolId>`, and `closure<(T,…)->R>`. (§15.4)
3861. IR `str` is a pooled-string index. (§15.4)
3862. Records and enums appear in the IR as `%TypeId` references whose layout lives in the `types` table. (§15.4)
3863. `pool_index<%PoolId>` is the IR type for dynamic-size values. (§15.4)
3864. Behaviors are fully typed. (§15.4)
3865. The graph is type-erased to tag plus size/align. (§15.4)
3866. The IR's text form is normative: it is the only serialization this specification defines and what tests assert against. (§15.4)
3867. No binary or wire encoding of the IR is mandated; any such encoding is implementation-defined. (§15.4)
3868. An implementation need not externalize the IR at all; holding it in memory across the frontend→backend boundary is conforming. (§15.4)
3869. The graph is built from exactly six primitives: `cell`, `connection`, `gate`, `stream`, `effect`, and `scope`. (§15.4.1)
3870. Each graph primitive references behaviors by id and is identified by a stable fully-qualified path. (§15.4.1)
3871. All surface reactive constructs desugar into the six graph primitives. (§15.4.1)
3872. Surface `signal`, `attr`, `derived`, `recurrent`, and `const` all lower to `cell`. (§15.4.1)
3873. Surface `node` and `part` lower to `scope`. (§15.4.1)
3874. `repeat` lowers to a dynamic `scope`. (§15.4.1)
3875. `when` and `given` lower to `gate`. (§15.4.1)
3876. Surface connections and `|>` lower to `connection`. (§15.4.1)
3877. An operator lowers to a `scope` with ports. (§15.4.1)
3878. A surface stream lowers to the `stream` primitive. (§15.4.1)
3879. A surface effect lowers to the `effect` primitive. (§15.4.1)
3880. A `scope` is the structural container for a node, part, or operator instance, or a `repeat`'s per-element instance. (§15.4.1)
3881. A scope's `exposes` list holds the ordered placements the runtime traverses — child scopes and references to the scope's connection entries, in exposition order (engagement positions). (§15.4.1)
3882. A scope's hosted effects live in a separate `effects` set and never appear in `exposes`. (§15.4.1)
3883. A `dynamic` scope additionally carries a `keyed_by` identity for `repeat`. (§15.4.1)
3884. `repeat` is the only mount/unmount construct; gates merely freeze. (§15.4.1)
3885. Scope hierarchy is encoded in the entries' fully-qualified paths. (§15.4.1)
3886. A cell entry's `id` is the cell's fully-qualified declaration path. (§15.4.1)
3887. A cell entry's `type` is a primitive type tag per §4.1, extended with the string-pool-index and dynamic-pool-index types. (§15.4.1)
3888. A cell entry's `observability` is one of `cross_thread_snapshot`, `cross_thread_atomic`, or `confined`. (§15.4.1)
3889. A cell entry's optional `cadence_hint` is one of `realtime`, `bounded`, or `lazy`. (§15.4.1)
3890. A cell entry's optional `initial_value` is the compile-time initial value for reactive-safe initializers. (§15.4.1)
3891. A cell entry records `size` and `alignment` explicitly — derivable from `type` but recorded for cross-implementation interop. (§15.4.1)
3892. A connection entry's `from` is the source instance's fully-qualified path. (§15.4.1)
3893. A connection entry's `to` is the destination instance's fully-qualified path, or `null` for sink-side connections. (§15.4.1)
3894. A connection entry records the connection's declared `connection_type`. (§15.4.1)
3895. A connection entry's `attrs` is an ordered list of `(name, value)` pairs. (§15.4.1)
3896. A connection entry's optional `when` is a gate predicate encoded as a behavior ID plus its input-cell list. (§15.4.1)
3897. Derived dependency edges are recorded as `(derived_cell_id, [input_cell_ids])` pairs. (§15.4.1)
3898. The runtime uses derived dependency edges for dirty-set propagation and topological evaluation ordering. (§15.4.1)
3899. Recurrent dependency edges are `(recurrent_cell_id, [input_cell_ids], output_history_N, input_lookback_map)` tuples. (§15.4.1)
3900. A recurrent edge's `input_cell_ids` are the non-self references that drive re-evaluation (implicit triggers). (§15.4.1)
3901. A recurrent edge's `output_history_N` is the declared `[N]` self-history depth, defaulting to 1. (§15.4.1)
3902. A recurrent edge's `input_lookback_map` maps input cell IDs referenced via `.past(k, ...)` to their maximum `k`. (§15.4.1)
3903. A recurrent whose expression is an `observe` block additionally carries the observe's per-arm trigger sets. (§15.4.1)
3904. Each gated instance records its predicate in compiled form: a behavior ID plus the input cell IDs the predicate reads. (§15.4.1)
3905. Each gated instance records a `gate_parent`: the path of the nearest enclosing gated instance, or `null` if none. (§15.4.1)
3906. The runtime composes an instance's own predicate with its `gate_parent` chain to obtain effective activation, which drives transitive freeze and suspend/resume delivery. (§15.4.1)
3907. Block selectors `when` and `given` lower to per-arm gates: each arm becomes a gated subtree. (§15.4.1)
3908. A `when`-block arm's gate predicate is the arm's guard. (§15.4.1)
3909. A `given`-block arm's gate predicate is the arm's variant test against the scrutinee. (§15.4.1)
3910. The compiler encodes declaration-order priority and exhaustiveness into arm predicates so arms are mutually exclusive by construction (arm i's effective predicate conjoins the negations of all earlier arms' guards). (§15.4.1)
3911. Arm predicates still evaluate against runtime cell values each commit. (§15.4.1)
3912. The behavior table is a list of `(behavior_id, debug_name, input_cell_ids, output_cell_id?)` entries. (§15.4.1)
3913. Behavior IDs are content-addressed per §14.6.3. (§15.4.1)
3914. The runtime binds behavior IDs to function pointers at program startup. (§15.4.1)
3915. A stream entry's `id` is the stream's fully-qualified declaration path. (§15.4.1)
3916. A stream entry's `element_type` is the element's type tag, per §15.4.1's cell `type` encoding. (§15.4.1)
3917. A stream entry's `policy` is `ring` or `gate`. (§15.4.1)
3918. A stream entry's `capacity` is an integer literal. (§15.4.1)
3919. A stream entry's `source_dependencies` are the input cells the source expression reads, used for dirty-set propagation when the source is a derived chain. (§15.4.1)
3920. A stream entry's `observation_cell_ids` name the synthesized observation cells: `pending_count`, `pressure`, `is_full`, `dropped_total`, `rejected_total`, `last_overflow_at`. (§15.4.1)
3921. A stream entry's `reset_on_reload` boolean is true exactly when the stream carries `@reset_on_reload`. (§15.4.1)
3922. A stream entry's `output_history_size` is the integer N from `recurrent[N] stream`, or 0 for a non-recurrent stream declaration. (§15.4.1)
3923. `output_history_size` determines the number of past-event slots allocated for `.past(k, ...)` access on the stream's output. (§15.4.1)
3924. A stream entry's `input_lookback_map` maps input cell IDs read via `.past(k, ...)` in a recurrent stream's body to integer max-`k`, determining per-input history allocation. (§15.4.1)
3925. A stream's `input_lookback_map` is empty for non-recurrent streams and for recurrent streams whose body calls no `.past` on inputs. (§15.4.1)
3926. A Sink declared in an effect's `desired:` block shares its cell ID with the corresponding Stream view. (§15.4.1)
3927. A dual-view Sink/Stream cell gets a single stream entry carrying a flag that the cell is exposed in both views. (§15.4.1)
3928. An effect entry's `id` is the instance's fully-qualified path — the binding name or pipe-position site. (§15.4.1)
3929. An effect entry's `effect_type_name` is the effect's declared type name, used to dispatch to the host's reconciler. (§15.4.1)
3930. An effect entry's `parameter_bindings` are `(parameter_name, source_cell_id | value_literal)` pairs. (§15.4.1)
3931. An effect entry's `desired_cell_ids` name the cells declared in the instance's `desired:` block. (§15.4.1)
3932. An effect entry's `observed_cell_ids` name the cells declared in the instance's `observed:` block. (§15.4.1)
3933. An effect entry's `gate_parent` is the path of the nearest enclosing gated instance, or `null` if the effect is never gated. (§15.4.1)
3934. The runtime uses an effect's `gate_parent` to compute effective activation and to time `suspend`/`resume` reconciler-hook delivery on enclosing-subtree gate transitions. (§15.4.1)
3935. Reconciler dependencies are `(effect_type_name, [concrete_type_parameters])` pairs the host must register via `runtime.register_reconciler` before the runtime can enter the live state. (§15.4.1)
3936. A non-generic effect's reconciler-registration key has an empty type-parameter list. (§15.4.1)
3937. Each generic-effect instantiation is a distinct reconciler-registration key. (§15.4.1)
3938. The string pool seed lists the program's string literals, pre-loaded into the pool at startup. (§15.4.1)
3939. The graph specification records a schema version: the Ductus toolchain version that produced it, per §14.9. (§15.4.1)
3940. The graph specification records a format version: the abstract data model's own version, distinct from the toolchain version. (§15.4.1)
3941. A cell ID is a dot-separated identifier path from module root through enclosing instances to the cell name: `audio.synth_a.osc_1.frequency`. (§15.4.1.1)
3942. Cell IDs are derived deterministically from source: lexical nesting plus declared instance/cell names. (§15.4.1.1)
3943. Cell-ID syntax is identical to the hot-reload identity of §13.15.2; the two are one mechanism, stated in source-level versus wire-format terms. (§15.4.1.1)
3944. A cell ID for anonymous or duplicated sibling placements carries the ordinal suffix `:N` in the wire format, with N the zero-based declaration-order index among same-type siblings at the same nesting depth. (§15.4.1.1)
3945. Cell IDs are stable across the same source compiled by any conformant compiler. (§15.4.1.1)
3946. Cross-implementation hot reload at the same source version yields matching cell IDs by construction. (§15.4.1.1)
3947. `observability` declares the concurrency contract the cell's storage must honor. (§15.4.1.2)
3948. `cross_thread_snapshot` guarantees multi-thread readers a snapshot-consistent view, cross-cell consistency within one commit transaction, and no torn reads. (§15.4.1.2)
3949. `cross_thread_atomic` guarantees single-cell atomic reads with no cross-cell consistency guarantee. (§15.4.1.2)
3950. A `cross_thread_atomic` cell's value must fit in one 64-bit atomic slot. (§15.4.1.2)
3951. A `confined` cell is accessed only from one thread; no atomics are required. (§15.4.1.2)
3952. The mapping from observability class to storage mechanism is the runtime's concern; any mechanism satisfying the class's contract conforms. (§15.4.1.2)
3953. `cadence_hint` is informational; the runtime uses it only to bias storage-mechanism selection. (§15.4.1.3)
3954. `realtime` means updates are deadline-bound and readers cannot block. (§15.4.1.3)
3955. `bounded` means updates are committed but not deadline-bound; it pairs with any observability value. (§15.4.1.3)
3956. `lazy` means updates are best-effort and the cell tolerates large staleness. (§15.4.1.3)
3957. A cell without a `cadence_hint` is treated as `bounded`. (§15.4.1.3)
3958. The compiler assigns each cell's observability class from its declaration kind and the access pattern observed during reactive analysis. (§15.4.1.4)
3959. Top-level `signal` cells default to `cross_thread_snapshot`. (§15.4.1.4)
3960. `attr` cells on node/connection instances default to `cross_thread_snapshot`. (§15.4.1.4)
3961. `recurrent` cells on node/connection instances default to `cross_thread_snapshot`. (§15.4.1.4)
3962. `derived` reactive cells default to `cross_thread_snapshot`. (§15.4.1.4)
3963. Stream cells (head pointer plus metadata) default to `cross_thread_snapshot`. (§15.4.1.4)
3964. Stream observation cells default to `cross_thread_snapshot`. (§15.4.1.4)
3965. Host-written effect `observed:` cells default to `cross_thread_snapshot`. (§15.4.1.4)
3966. Program-written effect `desired:` cells default to `cross_thread_snapshot`. (§15.4.1.4)
3967. Stdlib single-cell types per §13.12.4 default to `cross_thread_atomic`. (§15.4.1.4)
3968. Local `let`/`mut` bindings inside function bodies are non-reactive and do not appear in the graph specification. (§15.4.1.4)
3969. Closure captures and function parameters do not appear in the graph specification. (§15.4.1.4)
3970. The compiler may downgrade `cross_thread_snapshot` to `confined` for cells provably accessed from only one thread; this optimization is implementation-defined. (§15.4.1.4)
3971. The compiler may never upgrade a cell's observability class (e.g., `confined` to `cross_thread_snapshot`). (§15.4.1.4)
3972. Cells declared inside placements that participate in the runtime's evaluation cycle default to `cadence_hint` `realtime`. (§15.4.1.4)
3973. Cells on non-realtime paths default to `cadence_hint` `bounded`. (§15.4.1.4)
3974. A conformant runtime accepts any specification whose format version it understands. (§15.4.2)
3975. Format-version mismatches are diagnosed at load time per §14.9. (§15.4.2)
3976. The specification is type-erased at the runtime boundary: it carries primitive type tags and cell layouts but no record definitions, trait conformances, or generic parameters. (§15.4.3)
3977. Record definitions, trait conformances, and generic parameters are compile-time artifacts of the frontend, fully resolved before the specification is emitted. (§15.4.3)
3978. The runtime's view of the program is a graph of cells with primitive types, dependency edges, behavior references, and gate predicates. (§15.4.3)
3979. The runtime manages bits in cells and invokes functions by ID; it never needs to understand records as records or traits as traits. (§15.4.3)
3980. A behavior is a pure function the runtime invokes to compute a derived value, a gate predicate, or an effect's desired state. (§15.4.4)
3981. The behavior IR is typed: every value has a concrete type from the §15.4 vocabulary. (§15.4.4)
3982. The behavior IR is SSA: each value is assigned once and control flow is basic blocks. (§15.4.4)
3983. There are no phi nodes; a block needing a merged value declares it as a block parameter: `bb1(%k: i32):`. (§15.4.4)
3984. The behavior IR is monomorphic; generics are already specialized: `push$Vec_i32`. (§15.4.4)
3985. The behavior IR is ownership-explicit: `move`, `clone`, and `drop` are written out, never inferred. (§15.4.4)
3986. A behavior is pure: a function of its declared inputs only, with no ambient state and no I/O. (§15.4.4)
3987. I/O is the domain of effects, which are graph-level, never of behaviors. (§15.4.4)
3988. A behavior may compute, allocate, call other behaviors, and `trap`. (§15.4.4)
3989. Behaviors are total except `trap`: the only non-`ret` exit is `trap`. (§15.4.4)
3990. A behavior's signature is `behavior <id> (params) -> <type> { block+ }`. (§15.4.4)
3991. A behavior parameter `p: T` is borrow-default: read, not consumed. (§15.4.4)
3992. A behavior parameter `own p: T` is consumed. (§15.4.4)
3993. The entry block's parameters are the behavior's parameters. (§15.4.4)
3994. A branch supplies its successor block's arguments, replacing phi: `cond_br flag, bb1(%0), bb2`. (§15.4.4)
3995. Constant instructions are typed: `const.i32 5`, `const.f64 3.14`, `const.bool true`, `const.str "…"`. (§15.4.4)
3996. The arithmetic/bitwise instructions are `add sub mul div rem neg and or xor shl shr not`, with typed variants implied by `.iN`/`.fN` suffixes. (§15.4.4)
3997. Overflow in behavior-IR arithmetic follows the rules of §4. (§15.4.4)
3998. The comparison instructions `eq ne lt le gt ge` produce `bool`. (§15.4.4)
3999. Conversions are explicit per §7.5: `cast.<from>.<to> %x`. (§15.4.4)
4000. The aggregate instructions are `tuple.make`/`tuple.get`, `record.make %T {…}`/`field.get`, `enum.make %T #V(p)`/`enum.tag`/`enum.payload`, and `array.make […]`. (§15.4.4)
4001. Field/index assignment is a trait `call`, not an IR primitive, so in-place reuse follows the ordinary ownership rules. (§15.4.4)
4002. Direct calls are statically resolved: `call f(move %a, %b)`. (§15.4.4)
4003. `call.dyn %obj #m (…)` is the vtable call on a `dyn` trait object. (§15.4.4)
4004. `call.closure %c (…)` invokes a closure. (§15.4.4)
4005. `closure.make B@id captures {…}` constructs a closure. (§15.4.4)
4006. `dyn` and closure calls are the only indirect call forms; the frontend resolves all other dispatch to direct calls. (§15.4.4)
4007. `move` is an operand modifier at call arguments, stores, and returns. (§15.4.4)
4008. `clone` duplicates a value: `%c = clone %x`. (§15.4.4)
4009. `drop %x` marks explicit end-of-life. (§15.4.4)
4010. The intrinsics are `raw_alloc<T>(%n)`, `raw_free(move %b)`, `raw_read<T>`, `raw_write<T>`, and `trap "msg"`. (§15.4.4)
4011. The intrinsics are the fixed primitive floor: the only operations not expressible in Ductus, available to every program equally. (§15.4.4)
4012. Stdlib collections are ordinary behaviors built on the intrinsics; no privileged operations exist. (§15.4.4)
4013. Every behavior block ends in exactly one terminator. (§15.4.4)
4014. `br bbN(args)` is the unconditional branch terminator. (§15.4.4)
4015. `cond_br %c, bbT(args), bbF(args)` is the conditional branch terminator. (§15.4.4)
4016. `switch %tag [0:bb0, 1:bb1, default:bbd]` is the multi-way terminator. (§15.4.4)
4017. `match` lowers to `enum.tag` plus `switch`. (§15.4.4)
4018. `ret %v` is the value-returning terminator. (§15.4.4)
4019. `trap "msg"` is a terminator. (§15.4.4)
4020. Recoverable failures (`Result`/`Option`, `?`) are ordinary values plus `switch`. (§15.4.4)
4021. Unrecoverable failures — `panic`, overflow, failed index — are `trap`: process abort, not catchable. (§15.4.4)
4022. Surface-construct lowerings introduce no new IR primitives. (§15.4.4)
4023. A `for`/iterator loop lowers to a block calling `next` (tuple-return) plus a `switch` on the `Option`. (§15.4.4)
4024. The linear-ownership in-place loop optimization is the dead-`move`-plus-rebind pattern (§11.11.2, §12.7.2). (§15.4.4)
4025. `?` and string interpolation desugar to plain `switch`/`call` sequences. (§15.4.4)
4026. An `observe` expression lowers to a derived read. (§15.4.4)
4027. A closure's captured environment has a layout recorded as a `%TypeId` in the `types` table. (§15.4.4)
4028. A newtype lowers to a `%TypeId` wrapper, or is transparent to its wrapped tag. (§15.4.4)
4029. The runtime invokes a behavior by id with input values and receives outputs; it never reaches inside. (§15.4.4)
4030. A derived behavior's inputs are its cell's input cells as current snapshot values; its output is the new value. (§15.4.4)
4031. A recurrent behavior's inputs are its input cells, self-history slots (`name.past(k,d)` becomes a `pastK` param), and input-history slots (`input.past(k)` becomes an `<input>_pastK` param); its output is the new value. (§15.4.4)
4032. A gate-predicate behavior's inputs are the gate's input cells; its output is `bool`, or a tag for a `given` selector. (§15.4.4)
4033. An effect desired-builder behavior's inputs are the `desired:` block's input cells; its output is the desired record. (§15.4.4)
4034. ABI inputs are passed per the signature's ownership modes (borrow vs `own`). (§15.4.4)
4035. The ABI call is the entire graph-to-behavior coupling: behaviors hold no wiring, the graph holds no logic. (§15.4.4)
4036. The text grammar's behavior production is: `behavior ::= 'behavior' BID '(' params ')' '->' type '{' block+ '}'`. (§15.4.4)
4037. The text grammar's params production is: `params ::= (('own')? NAME ':' type) (',' …)*`. (§15.4.4)
4038. The text grammar's block production is: `block ::= LABEL ('(' params ')')? ':' instr* terminator`. (§15.4.4)
4039. The text grammar's instruction production is: `instr ::= '%'NAME '=' op operand* (':' type)? | op operand*`. (§15.4.4)
4040. The text grammar's operand production is: `operand ::= '%'NAME | 'move' '%'NAME | literal`. (§15.4.4)
4041. An effect's desired computation has no activation input anywhere; activation is expressed solely through the suspend/resume model (§13.19.12). (§15.4.5)
4042. Interpreter and native modes share the entire frontend and differ only in the executable-code artifact. (§15.5)
4043. The spec does not specify the interpreter bytecode format or the per-mode build pipeline. (§15.5)
4044. Mode selection and toolchain integration are implementation concerns. (§15.5)
4045. Hot reload diffs the graph specifications of two builds of the same program (`old_spec` vs `new_spec`); the diff computes the changes the runtime applies. (§15.6)
4046. The diff classifies the overall change set per §13.15.4 into one of three categories: reload-safe, per-instance restart required, or full-runtime restart required. (§15.6.1)
4047. Reload-safe changes are applied in place per §14.8. (§15.6.1)
4048. Per-instance restart means operator-specific reinit per §13.17.10. (§15.6.1)
4049. Full-runtime restart is required for buffer-layout relocation per §13.15.4. (§15.6.1)
4050. Reload classification is computed from the diff alone; the runtime does not re-parse source. (§15.6.1)
4051. The diff produces a reload plan: a sequenced list of cell add/remove/change, connection add/remove/change, and behavior add/remove operations the runtime applies in topological order. (§15.6.2)
4052. The reload-plan format is implementation-defined. (§15.6.2)
4053. A reload plan must preserve the ordering constraints of §14.7 (drop in reverse-declaration order; connections before endpoint instances) and §14.8. (§15.6.2)
4054. An implementation comprises a compiler and a runtime operating on the same graph specification format; conformance requires both components to meet their criteria. (§15.7)
4055. A conformant compiler accepts every program that §§1–13 define as well-formed. (§15.7.1)
4056. A conformant compiler rejects every ill-formed program, with diagnostics whose format is implementation-defined. (§15.7.1)
4057. A conformant compiler produces an IR conforming to the abstract data model of §15.4.1, with any serialization implementation-defined. (§15.7.1)
4058. A conformant compiler's executable code, run against a conformant runtime with the produced graph specification, exhibits the observable behavior defined by §§1–13. (§15.7.1)
4059. A conformant runtime accepts any IR conforming to the §15.4.1 abstract data model, in whatever serialization the implementations share. (§15.7.2)
4060. A conformant runtime allocates cells per the observability and cadence contracts, using any mechanism that satisfies them. (§15.7.2)
4061. A conformant runtime implements the runtime semantics of §13 and §14: cell evaluation order, drop semantics, hot reload, and thread orchestration. (§15.7.2)
4062. Interoperability is defined against the IR's text form. (§15.7.3)
4063. Two implementations exchanging a serialization must load it at the same format version. (§15.7.3)
4064. Mixing a compiler from one implementation with a runtime from another is permitted at the same schema and format version. (§15.7.3)
4065. The spec prescribes no reference test suite. (§15.7.4)
4066. Implementations may publish conformance suites; passing such a suite is not a normative requirement. (§15.7.4)
