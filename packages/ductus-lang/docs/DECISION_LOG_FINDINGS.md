# Ductus Spec Findings Ledger

142 contradictions, ambiguities, incoherencies, and unsound inferences discovered in SPEC.md during its lossless serialization into DECISION_LOG.md. Per the production protocol, NONE of these was silently resolved: each finding records the competing readings, the impact, and the contested entry wording (quarantined — withheld from DECISION_LOG.md until ruled on). Every finding was independently verified by at least two agents (extractor + skeleton-first reviewer; three for chunks 10/13/14, which received an additional reconstruction audit; items discovered after delivery — final-QA spot-audits and the syntax-completeness sweep — are marked in-row and were verified by the discovering auditor plus the orchestrator against SPEC.md); per-finding assessments live in the production reports (git history of `.declog-work/report/`).

Ruling protocol: for each finding, decide the intended reading, fix SPEC.md accordingly, then add the now-clean decision(s) to DECISION_LOG.md (next free number, placed by topic). Strike the finding from this ledger when resolved.

Counts by kind: 77 ambiguities · 48 incoherencies · 16 contradictions · 1 unsound-inference.

## High-priority digest (orchestrator's adjudication of the strongest items)

1. §4.4.1.2 — `\` is called "truncating" integer division but the spec's own example `-5 \ 2` yields `-3` "(toward negative infinity)", i.e. floor division (truncation gives `-2`).
2. §4.4.3/§4.4.6 vs §4.9.1 — comparison operators attributed to `Ord`, but `Ord` is a methodless umbrella; dispatch targets are `Lt::lt`/`Le::le`/`Gt::gt`/`Ge::ge`. Different inferred generic constraints under each reading.
3. §4.8.2 vs §4.9.2 — `Float` umbrella "requires all" float-op traits, but the canonical §4.9.2 requires-list omits `Log` (two-arg) and the inspection traits (`is_nan` etc.).
4. §4.5/§4.9.4 vs §4.7 — residual references to lossless casts being "trivially equivalent to `as`" although §4.7 defines `as` as not-a-cast (naming only). Apparent residue of an earlier design.
5. §8.4.1 vs §7.9 — the literal `?` desugar `return From::from(failure)` type-checks for `Option` (via `From[()] for Option[T]`) but not for `Result`, whose stated requirement `From[SourceError] for DestError` needs an unstated `Err(...)` re-wrap.
6. §9.3.4 — the `usize`-indexing example (`arr[n]` ✓ "widens to isize") contradicts the stated rule that types whose range exceeds `isize` require an explicit cast; the "same source valid on every platform" claim also conflicts with the platform-dependent cast requirement.
7. §11.3.5 — Rule (P)'s body lists `move x` at a call site among name-only consumes, while the corollary/diagnostic/example make `move` of an alias into an `own` parameter a compile error; §11.8.5 permits `move` only toward `own` parameters, so the success path is unreachable.
8. §11.10.1 vs §11.10.5/§11.9.1 — unconditional Copy-only closure captures vs non-escaping closures "may treat their environment access as alias access": observable legality differs for `fn(): sum(buf)` with non-Copy `buf`.
9. §11.5/§11.3.6 — `Clone`'s declared signature `fn clone(value: Subject) -> Subject` is borrow-default-returning under the spec's own return conventions, contradicting its independent-owned-copy contract (would need `-> own Subject`).
10. §12.3.7 — the fixed-extent paragraph ("a loop over a fixed-extent value unrolls") conflicts with the section's own iff-rule (unroll iff iterable compile-time-known) and the two-admitted-forms list; tuple iteration both "not provided" and in the fixed-extent set.
11. §13.3.3 vs §13.3.7.2 — an open-parts node's children are "walked through its exposition" while the same node with no `parts:` and no `expose:` has an EMPTY exposition.
12. §13.9.7 — the deriveds bullet forward-references a "one-shot effect-desired recompute of the close transition (below)" that the gate-close paragraph explicitly denies ("there is no recompute pass").
13. §13.14.7 vs §13.15.6 — reconciler registrations after the live transition are rejected, yet a renamed effect type must be registered on a live runtime "before the reload reaches the live state".
14. §13.19.2 vs §13.19.3 — "at least one of desired:/observed: must be present" vs the `effect log(...)` example with neither block ("pure fire-and-forget consumer").
15. §13.17.13 vs §3.5.4 — the operator-type examples use `=` named-argument syntax (`apply(source = s, kernel = gain)`) although named value arguments use `name: value`.
16. §13.17.7 — "`|>` may only apply operators or effects" vs the same section's Case 3 (RHS is a `Sink[T]`).
17. §3.6.1/§2.2.4/§13.17.8 vs §5.1 — trait bounds written both `T: Add + Mul` and `T: Numeric & Ord`; only `&` is ever defined as bound conjunction; `+` in bound position is used but never specified.
18. §14.6.3 (and §15.4.4's `B@aa10` ids) — behavior IDs are both "stable u32" and "content-addressed stable hash"; width vs content unreconciled, `BID` lexeme undefined in the IR grammar.
19. §15.4.5 vs §15.4.1 — the worked example's text form diverges from the field lists (standalone `gate ... guards [...]` entries; cell lines carrying `role=`/`uses`/`inputs`/`depth`; omitted observability/size; `App.print:0.text : str` fed by a behavior returning `%TextRec`; `label |> print` producing no `connection` entry despite the desugar map).
20. §13.2.7 — the no-source-write rule enumerates five declaration kinds but claims to apply "to all six declaration kinds uniformly" (the §13.2 intro's six include `stream`, omitted from the enumeration).

## Full ledger (all 142, in spec order by chunk; each line carries kind, sections, competing readings, impact, and quarantined wording)

[chunk 01] - kind: ambiguity | sections: §2.1.2, §2.1.2 | readings: §2.1.2 lists "an explicit type annotation (`let y: i32 = x`)" as a use site that resolves the RHS placeholder, but a later §2.1.2 paragraph states that for a binding whose RHS is "itself an expression with a placeholder", the expression resolves first from its own context and "the binding's annotation status (or absence) does not provide context to the expression". Reading 1: the annotation is a use site only for a bare placeholder-bearing value (literal or transparent alias), while a compound RHS expression resolves internally (e.g. by defaulting) and the annotation never reaches inside it. Reading 2: "an expression with a placeholder" includes bare references, so the no-backward-flow rule contradicts the annotation-as-use-site bullet for `let y: i32 = x`. | impact: determines whether `let y: i64 = x * 2` (x a placeholder) resolves the multiplication at i64 from the annotation, or defaults internally (e.g. to i32) with the annotation then checked against the already-resolved type. | quarantined: "A binding whose RHS is a compound expression with a placeholder resolves the expression first from its own context (operand types, value-fits, defaulting) and adopts the resolved type; the annotation provides no context to the expression. (§2.1.2)" vs "An explicit binding annotation is a use site that resolves the placeholder of the RHS expression, compound or bare. (§2.1.2)"
[chunk 01] - kind: incoherence | sections: §2.5.2, §2.5.3, §2.5.6 | readings: §2.5.2 lists concrete const-generic arithmetic as "including `\` and `%`" while §2.5.3's rejection table ("Integer `/`, `%`") and §2.5.6's predicate vocabulary ("including `/`, `%`") spell integer division `/`. Reading 1: `\` in §2.5.2 is a typo for `/`; the language has a single division token `/`. Reading 2: `\` is a distinct operator (e.g. a dedicated integer-division token) admitted in concrete const-generic position, with its own lexical and symbolic-rejection status unspecified. | impact: determines the arithmetic operator token set a conforming lexer/checker must accept, and which token §2.5.3's symbolic rejection applies to. | quarantined: the token-level claim "concrete const-generic arithmetic includes the operator `\`" (draft 01-161's operator spelling); entry 01-164 retains the semantic commitment using operator names ("division and modulo") instead of the disputed token spelling.
[chunk 01] - kind: ambiguity | sections: §2.4.4, §2.5.2, §4.6.5 vs §11.2, §2.4.1.1 | readings: initializer-less annotated `let` (`let arr: i32[fib(10) + 1]`, `let history: i32[fib(10) + 1]`, `let arr: i32[some_large_compile_time_value]`) is a truncated/elided example — §11.2 defines exactly `let x = expr`/`mut x = expr` and immutability forecloses later first-assignment vs an intended-but-never-defined annotated declaration form | impact: whether annotated `let` without `=` parses; three spec examples and two log entries (§2.4.4, §2.5.2) carry the form; spec fixes at lines ~781/~892/~3365 are HELD pending ruling | quarantined: none (entries transcribe the spec's own examples; flagged independently by two sweep auditors, orchestrator-verified)
[chunk 01] - kind: incoherence | sections: §2.4.1.1 vs §11.2 | readings: "The language has two binding forms" introducing `let`/`const` vs §11.2's "two binding forms for runtime values" (`let`/`mut`) with `const` separate — the unqualified §2.4.1.1 phrasing contradicts the three-form reality | impact: editorial; the log carries §11.2's qualified phrasing | quarantined: none (syntax-sweep discovery, orchestrator-verified)
[chunk 01] - kind: ambiguity | sections: §1.4, §2.5.3, §3.1.7, §13.2.8 | readings: keywords are contextual, so §2.5.3's example legally names an operator `gate` (`operator gate[const N: usize, T](...)`), §3.1.7 uses `const type:` as a member name, and §13.2.8 declares `fn observe[T](signal: Signal[T])` naming a function `observe` and parameter `signal` — all keywords used as identifiers vs keywords are reserved everywhere, making those names illegal for a reason no ✗ flags | impact: reservedness policy per keyword class; validity of the §2.5.3, §3.1.7, and §13.2.8 example names | quarantined: none (syntax-sweep discovery, orchestrator-verified)
[chunk 01] - kind: ambiguity | sections: §2.4.4, §2.5.2 vs §9.3.3, §4.6.5 | readings: `usize`-typed const-generic size parameters (`type Buffer[T, const N: usize = 1024]: data: T[N]`) are bridged per-instantiation to the `isize` array-length type by value-fit vs size parameters were meant to be `isize` | impact: declaration-time well-formedness of `T[N]` with a symbolic `usize` `N`; distinct from the ledgered §9.3.4 indexing items | quarantined: none (syntax-sweep discovery, orchestrator-verified)
[chunk 01] - kind: incoherence | sections: §1.4, §4.7 vs §10.2, §10.4 | readings: `as` is twice asserted to serve "import aliases (§10.2)" vs §10.2/§10.4 defining no import-alias form at all (residue of a removed feature?) | impact: whether `use root::audio::Synthesizer as Synth` parses; the log mirrors both sides verbatim | quarantined: none (syntax-sweep discovery, orchestrator-verified)
[chunk 02] - kind: ambiguity | sections: §3.2, §3.3.5 | readings: auto-satisfaction is conditioned only on satisfied `requires` plus all-default-bodied methods, so a trait with an undefaulted associated type but no abstract methods auto-satisfies with the associated type unbound vs an undefaulted associated type blocks auto-satisfaction and forces explicit `satisfies`+`fulfill` | impact: determines whether traits with undefaulted associated types and no abstract methods need explicit fulfill blocks | quarantined: "A trait with an undefaulted associated type cannot be automatically satisfied; binding it requires a `fulfill` block. (§3.3.5)" vs none (spec-literal rule already serialized as 02-100)
[chunk 02] - kind: incoherence | sections: §3.2.1, §3.4.1, §3.3.5 | readings: the at-most-one-parent-trait-candidate guarantee rests on rejection of conflicting `satisfies` sets, but auto-satisfied traits (§3.3.5) never appear in any `satisfies` set yet are fulfilled and thus collected by the §3.4.1 step-1 trait-impl search; two in-scope auto-satisfied traits (or one auto-satisfied plus one declared) with overlapping method names could yield two parent-trait candidates | impact: dispatch determinism guarantee may not hold for auto-satisfied traits; collision checking scope is unspecified for them | quarantined: "Call-site name resolution finds at most one parent-trait source for any (type, method-name) pair. (§3.2.1)" [drafter 02-76]; "The trait-impl search yields zero or one parent-trait candidate; two is impossible because a `satisfies` set with overlapping method names across parents is rejected. (§3.4.1)" [drafter 02-114]; the step-2 clause "the set of candidates after this collapse contains at most one parent-trait entry" (§3.4.1, trimmed from the collapse entry); the §3.4.1 claim "the algorithm is deterministic" (skeleton-identified, never serialized)
[chunk 02] - kind: incoherence | sections: §3.2, §3.3.5 | readings: §3.2's parenthetical equates "traits with no methods" with pure-requirement traits, but §3.3.5 defines pure-requirement traits as declaring no methods AND no associated types; a no-method trait with associated types is pure-requirement under the first reading and not under the second | impact: classification of no-method traits carrying associated types, and whether §3.2's auto-satisfaction exception covers them; feeds the undefaulted-associated-type ambiguity above | quarantined: none (the parenthetical equation is not serialized; 02-65 and 02-99 each state their section's literal wording)
[chunk 02] - kind: ambiguity | sections: §3.1.7, §13.2.3 | readings: trait-body required cells use "the same syntax as a node or connection body", yet the example declares `derived effective_speed: f32` with no `= expression` (its comment reads "abstract: implementor supplies the body") — a bodiless abstract-cell variant exists in trait bodies vs the example is invalid against §13.2.3's `derived name: Type = expression` | impact: grammar of trait-body required reactive members | quarantined: none (entries transcribe; syntax-sweep discovery, orchestrator-verified)
[chunk 02] - kind: ambiguity | sections: §3.1.7, §1.4, §13.7.5 | readings: `const type: string` uses the keyword `type` as a member name — keywords are contextually permitted as member identifiers vs §3.1.7's central examples are invalid (§13.7.5's doubling allowance covers `from`/`to`/`parts` and friends, not `type`) | impact: identifier grammar; compilability of §3.1.7's examples and the mirroring log entry | quarantined: none (syntax-sweep discovery, orchestrator-verified)
[chunk 02] - kind: ambiguity | sections: §3.2, §3.3.4 | readings: the satisfies/fulfill pairing rule forces a `satisfies Display` clause in generic `Result[T, E]`'s body for §3.3.4's conditional `fulfill Display for Result[T, E] where T: Display, E: Display` — a plain unconditional clause whose availability is conditioned by the fulfill's where-clause vs a conditional-satisfies surface form (no such syntax exists) vs pairing waived for conditional fulfills | impact: what a generic type's body must contain for §3.3.4's canonical example to compile | quarantined: none (syntax-sweep discovery, orchestrator-verified)
[chunk 03] - kind: contradiction | sections: §3.6.1, §3.7.4 | readings: §3.6.1 writes fine-grained bounds as `T: Add + Mul` (operator `+`) while the same section writes `T: Numeric & Ord` and §3.7.4 names `&` composition — `+` as a second bound-composition operator vs a typo for `&` | impact: grammar of trait-bound composition is unclear (one operator or two) | quarantined: entry 03-38 uses the `&` form shared by both sections; no entry serializes `+` as valid syntax.
[chunk 03] - kind: ambiguity | sections: §3.9 | readings: note "the registered type must be a distinct nominal type" read as a normative requirement (registering a suffix on a bare type alias is a compile error) vs as non-normative advice (alias registration is legal but provides no type-level distinction) | impact: whether `@literal_suffix` on `type Frequency = i64` compiles | quarantined: "Registering a @literal_suffix on a bare type alias is a compile error." vs "@literal_suffix on a bare type alias is legal but yields no distinct nominal type."
[chunk 03] - kind: incoherence | sections: §3.9.1, §3.9.3 | readings: suffix grammar admits digit-initial suffixes (identifier-continue includes digits), so `<NumericLiteral><suffix>` tokenization can be ambiguous (e.g. suffix `2k`: is `4402k` 440+`2k` or 4402+`k`?); alternative reading: an unstated constraint forbids digit-initial suffixes | impact: lexer behavior is unspecified for digit-initial suffixes | quarantined: none (entry 03-108 states the character rule exactly as written).
[chunk 03] - kind: contradiction | sections: §3.9, §3.6.1 | readings: §3.9 example comment types `440hz` as "resolved to from_hz(440), i64 literal", but the constructor is `[N: Numeric]`-bound and `Numeric` declares `@default(i32)` (§3.6.1/§4.9.2), which would default the literal to i32 | impact: the inferred type of an integer literal under a custom suffix (i64 vs i32) | quarantined: "An integer literal under a custom suffix is passed as an `i64` literal." (not serialized).
[chunk 03] - kind: ambiguity | sections: §3.9.1, §3.9.4 | readings: §3.9.1 forbids registering reserved duration suffixes unconditionally; §3.9.4 qualifies the prohibition "in the same scope" | impact: likely none — built-in suffixes are globally visible (§3.9.5), making every scope "the same scope" | quarantined: none (entry 03-115 states §3.9.1's unconditional rule; entry 03-126 carries §3.9.4's same-scope qualifier verbatim).
[chunk 03] - kind: incoherence | sections: §3.5.1, §3.5.3 | readings: §3.5.1 states "Both forms are valid for any single-argument call" and "no special rule restricts single-argument calls to one form", which read literally permits named construction of a single-argument newtype and positional construction of a single-field record, contradicting §3.5.3's per-callable constraints; alternative reading: §3.5.1 only denies arity-based restrictions and §3.5.3's per-callable constraints still apply | impact: legality of `Newtype(value: x)` and of positional single-field record construction | quarantined: "Single-argument calls accept both forms: `square(5)` and `square(value: 5)` are equivalent. (§3.5.1)" (draft 03-5).
[chunk 03] - kind: incoherence | sections: §3.9, §3.9.3 | readings: §3.9 names the suffixed-token nonterminal `<NumericLiteral>` while §3.9.3 writes `<NumberLiteral>` — one nonterminal under two names vs two distinct nonterminals | impact: none semantic if a naming slip; the grammar cross-reference is inconsistent | quarantined: none (entry 03-105 uses §3.9's spelling).
[chunk 03] - kind: incoherence | sections: §3.5.1, §3.5.3 vs §3.3.1 | readings: named form is available for trait methods "at every call site" matched "by name", but impls rename non-receiver parameters (trait `Add` declares `fn add(a: Subject, b: Rhs)`; `fulfill Add for Vec3` writes `fn add(left: Vec3, right: Vec3)`) — trait parameter names are the named-form contract vs resolved-impl names govern, breaking named form at generic call sites | impact: legality of `v1.add(b: v2)` vs `v1.add(right: v2)`; whether fulfill matching must enforce parameter-name equality | quarantined: none (syntax-sweep discovery, orchestrator-verified)
[chunk 03] - kind: ambiguity | sections: §3.9, §3.9.1, §3.9.2 | readings: `@literal_suffix` decorates the type declaration (§3.9's example; §3.9.1 "decorate one type") vs the constructor function declaration (§3.9.2's examples place it above `fn from_hz...`) | impact: which declaration site the annotation attaches to; how "the annotated type" of §3.9.2's return-type rule is determined | quarantined: none (syntax-sweep discovery, orchestrator-verified)
[chunk 04] - kind: incoherence | sections: §4.4.1.2 | readings: `\` is "truncating" integer division (toward zero, so `-5 \ 2` is `-2`) vs the same sentence's example and annotation "`-5 \ 2` produces `-3` (toward negative infinity)" (floor division) | impact: result of `\` on negative operands differs, and the derived `\%`/`\|`/`\?` semantics inherit the ambiguity; §4.7.3 elsewhere uses "truncate" to mean toward zero | quarantined: "`\` rounds toward zero: `-5 \ 2` is `-2`." vs "`\` rounds toward negative infinity: `-5 \ 2` is `-3`."
[chunk 04] - kind: ambiguity | sections: §4.4.3, §4.4.6, §4.9.1 | readings: `<`/`<=`/`>`/`>=` constrain operands to `Ord` (whose requires-closure pulls in `Eq`) and dispatch to the fine-grained `Lt::lt`/`Le::le`/`Gt::gt`/`Ge::ge` vs each operator constrains only its own fine-grained trait (`<` → `Lt`, etc.) with the tables' `Ord` as shorthand | impact: inferred generic constraints differ (whether `<` alone requires `Eq`); `Ord` declares no methods, so the tables' stated operand trait has no dispatch target of its own | quarantined: "`<`/`<=`/`>`/`>=` constrain operands to `Ord` and dispatch through `Lt::lt`/`Le::le`/`Gt::gt`/`Ge::ge`." vs "`<` constrains operands to `Lt` only (similarly `<=` → `Le`, `>` → `Gt`, `>=` → `Ge`)."
[chunk 04] - kind: contradiction | sections: §4.8.2, §4.9.2 | readings: the `Float` umbrella requires traits for all float-only operations including two-argument `log` and the inspection methods (§4.8.2: "The `Float` umbrella requires all of them") vs `Float`'s requires list omits `Log` and the `is_nan`/`is_infinite`/`is_finite`/`is_normal` traits (§4.9.2 trait block) | impact: whether generic `T: Float` code can call `log(base, x)` or `is_nan` without extra bounds | quarantined: "`Float` requires `Numeric`, `Neg`, `Div`, `CheckedDiv`, `CheckedNeg`, `Sqrt`, the trig traits, `Ln`/`Log2`/`Log10`, `Exp`/`Exp2`, the rounding traits, and `FloatPow`." vs the same list plus `Log` and the four inspection traits.
[chunk 04] - kind: ambiguity | sections: §4.6.2, §4.6.3 | readings: unary `-%`/`-|` (and by extension `-?`) are prefix like base unary `-` vs postfix as the table examples consistently write them (`(-128_i8) -% is -128_i8`, `(-128_i8) -| is 127_i8`) | impact: surface syntax and parse position of the unary wrapping/saturating/checked negation operators | quarantined: none (entries 04-108 and 04-114 state the semantics without positional syntax); positional candidates: "unary `-%` is prefix: `-% x`" vs "unary `-%` is postfix: `x -%`."
[chunk 04] - kind: incoherence | sections: §4.9.4, §4.7, §4.7.2 | readings: §4.9.4 twice describes lossless float cast cases as "trivially equivalent to `as` per §4.7.2", implying `as` is a conversion form vs §4.7's explicit rule that `as` is not a cast (naming-only keyword: placement names, import aliases) and §4.7.2's equivalence being defined in terms of `T(x)` | impact: referent of §4.9.4's equivalence claims; behavior is unchanged if "`as`" is read as the `T(x)` conversion of §4.7.2, but the wording contradicts the `as`-is-not-a-cast rule | quarantined: none (entries 04-203 and 04-204 state destination coverage without the "`as`" phrasing; the lossless-equivalence rule is captured at §4.7.2 by 04-138 and 04-139).
[chunk 04] - kind: incoherence | sections: §4.6.1 vs §4.6.6, §4.6.2 | readings: "default arithmetic operators (`+`, `-`, `*`, `/`, `\`, `%`, unary `-`) trap on overflow at runtime, in all build modes" read literally over always-Float `/` and float-typed arithmetic vs float overflow produces signed infinity without trapping (§4.6.6; §4.6.2's floats "don't trap-overflow"), the trap rule being implicitly integer-scoped | impact: whether float overflow under default operators traps or yields infinity; the log inherits the unscoped pair verbatim | quarantined: none (entries transcribe; syntax-sweep discovery, orchestrator-verified)
[chunk 04] - kind: ambiguity | sections: §4.6.7 vs §4.4.1.1, §4.6.4 | readings: "integer division by zero traps" makes integer-operand `/` by zero trap vs `/` widens to float first (§4.4.1.1) so `5 / 0` is `5.0 / 0.0` yielding Infinity per §4.6.4, the trap scoped to `\`/`%` | impact: result of `5 / 0` — trap vs `f64` infinity | quarantined: none (entries transcribe each section's wording; syntax-sweep discovery, orchestrator-verified)
[chunk 04] - kind: ambiguity | sections: §4.5.1 | readings: `isize`/`usize` widening targets appear only in the `i8`/`u8` table rows, read as exhaustive (wider sources need casts) vs the same-signedness widening principle generalizing with "wider" judged against platform-dependent `isize`/`usize` width | impact: legality and portability of non-index `i16`/`i32`/`i64` → `isize` (and unsigned analogs) without casts; distinct from the ledgered §9.3.4 indexing items | quarantined: none (syntax-sweep discovery, orchestrator-verified)
[chunk 04] - kind: ambiguity | sections: §4.3.1, §4.3.2 | readings: `0x1_f32` lexes as a hex digit run with separator (`0x1F32`) vs a float-suffixed based literal (cf. `1_f32`); whether based literals admit type suffixes is unstated | impact: lexer maximal-munch policy where `_`-groups in hex literals spell `f32`/`f64`; distinct from the ledgered §3.9.1 digit-initial-suffix item | quarantined: none (syntax-sweep discovery, orchestrator-verified)
[chunk 05] - kind: ambiguity | sections: §5.2.2 | readings: the spec calls rejection of a bare trait at value position a "parse error", yet the condition ("when `Drivable` is a trait rather than a concrete type") requires name resolution unavailable to a parser — reading A: a literal parse-phase diagnostic is mandated (grammar must somehow distinguish traits); reading B: it is a post-resolution compile error loosely labeled | impact: which compilation phase/diagnostic class a conforming implementation must report for `let x: Drivable` | quarantined: the error-class claim only — "The bare form `let x: Drivable` (no `dyn`) is a parse error when `Drivable` is a trait" vs "...is a compile-time (post-name-resolution) error"; entries 05-13/05-14 state the rejection without committing to a phase.
[chunk 05] - kind: ambiguity | sections: §5.3.2 | readings: derive on a record intersection "delegat[es] to the operand types' implementations", but "when both operand records have fulfill blocks for the same trait that would equally apply, derivation is ambiguous" — reading A: derivation succeeds only when exactly one operand implements the trait, and both implementing is always the ambiguity error; reading B: the compiler can compose both operands' impls and errors only under an unspecified "would equally apply" test | impact: whether `@derive(Display, Hash)` on `type InsuredCar = Car & Insured` compiles when both `Car` and `Insured` implement the trait | quarantined: "When both operand records' `fulfill` blocks for a derived trait would equally apply, derivation is ambiguous and a compile error; the user writes the implementation manually."
[chunk 05] - kind: incoherence | sections: §5.7.3 | readings: "a type value ... carries no runtime representation" versus "selecting among type values of the same slot type at runtime is allowed (`match` ... yielding a `Type[C]` picks one)" — runtime selection implies some runtime datum recording the choice; reading A: every arm is compiled and the only runtime artifact is ordinary control flow, so "no runtime representation" holds for the type value itself; reading B: the two rules conflict and a tag-like runtime representation is required | impact: representation obligations for `Type[C]`-yielding `match` expressions | quarantined: the clause "carries no runtime representation" (§5.7.3), removed from the reviewed form of 05-59 pending resolution; the uncontested remainder ("resolved at compile time", "never erased behind a vtable", 05-59) and the selection permission (05-64) remain serialized.
[chunk 05] - kind: ambiguity | sections: §6.1.5, §6.1.3 | readings: override values are "subject to the same widening and conversion rules as direct construction per §6.1.3", but §6.1.3 states no widening or conversion rules — reading A: the rules live elsewhere (e.g. §4 conversions) and the citation is misdirected; reading B: construction admits no widening/conversion at all, making the clause vacuous | impact: which implicit conversions a conforming implementation accepts in `with` overrides and in construction arguments | quarantined: the clause "subject to the same widening and conversion rules as direct construction per §6.1.3"; 05-104 retains only the type-compatibility requirement.
[chunk 05] - kind: unsound inference | sections: §6.3.2, §6.3.1 | readings: the never-collide guarantee for `T(value)` rests on "construction names the newtype ... extraction names the wrapped type — the conversion role (the target is a built-in scalar)", yet §6.3.1 permits wrapping arbitrary types (`type MyVec[T]: wraps Vec[T]`) — reading A: `T(value)` extraction is defined only for built-in wrapped types and extraction for other wrapped types is unspecified; reading B: extraction generalizes to any wrapped type, in which case `T(value)` can collide with the construction role (e.g. a newtype wrapping a user type with a unary constructor) | impact: whether and how a conforming implementation supports extraction for newtypes wrapping non-built-in types, and how it disambiguates `T(value)` there | quarantined: draft entry 05-170 — "Construction and extraction share the `T(value)` form but never collide: construction names the newtype (`Email(x)`); extraction names the wrapped type (`string(email)`)."
[chunk 05] - kind: contradiction | sections: §6.2.3 vs §6.2.2, §6.2.4, §6.3.1, §8.3.1 | readings: every variant reference is path-qualified by default and unqualified names require explicit `use` import (§6.2.3), yet §6.2.2/§6.2.4/§8.3.1 write bare variant patterns (`Red: "red"`, `Circle(radius):`, `Some(x):`) and §6.3.1 writes bare constructions (`Ok(Email(s))`) with no import in scope — (A) patterns and expected-type constructions resolve against the scrutinee's/expected enum via an exemption nowhere stated, (B) the rule is universal and the spec's canonical examples elide required imports, or (C) `Option`/`Result` are prelude-imported (only `panic` is stated prelude), which still leaves the user-enum patterns unaccounted | impact: pattern name resolution; legality of bare variants without imports; compilability of the spec's match examples | quarantined: "Variant patterns in match arms resolve against the scrutinee's enum without imports: `Circle(radius):`. (§6.2.4)" vs "Variant patterns require the same qualification or imports as constructions. (§6.2.4)" (neither serialized; syntax-sweep discovery, orchestrator-verified)
[chunk 05] - kind: ambiguity | sections: §6.1.1 vs §6.3.1, §6.3.3 | readings: `satisfies` "may appear once per record" generalizing once-per-body to all nominal kinds (the newtype plural being loose wording) vs newtype bodies admitting multiple `satisfies` clauses (§6.3.1 "may include `satisfies` clauses"; §6.3.3 adds one beside `@derive`-implied conformances) | impact: whether two `satisfies` lines in one body compile; scope of the once-rule | quarantined: none (syntax-sweep discovery, orchestrator-verified)
[chunk 05] - kind: ambiguity | sections: §5.2.5 vs §11.8.5 | readings: "the `dyn` prefix mirrors `move` (§11.8.5)" as grammatical mirroring confines value-position `dyn` to call-argument positions — contradicting §5.2.5's own let-RHS example `let shape: dyn Drawable = dyn circle` (let-RHS is ✗ for `move`) vs the sentence is rhetorical and fixes no grammar, leaving `dyn`'s admitted positions and operand extent unstated | impact: which positions admit the `dyn` coercion; whether `move`'s positional restrictions carry over | quarantined: none (syntax-sweep discovery, orchestrator-verified)
[chunk 05] - kind: incoherence | sections: §6.2.4, §8.4, §9.1.3, §9.1.9, §9.2.1–§9.2.3, §9.3.2, §10.2.3, §10.3, §10.4 | readings: normative delegations to "grammar §2.5.5", "grammar §3.3/§3.4", "grammar §3.13–§3.15" target a grammar document that does not exist in the repo (GRAMMAR.md is unwritten) and whose numbers collide with unrelated SPEC sections — forward declarations of a planned document vs dangling references | impact: the delegated micro-syntax (string-literal fine structure, `use`/path grammar, match grammar) has no normative source; explains several syntax questions the sweep reports as SPEC-SILENT | quarantined: none (syntax-sweep discovery, orchestrator-verified)
[chunk 06] - kind: incoherence | sections: §8.4.1, §8.4, §7.9 | readings: the desugar is schematic and the compiler re-wraps the From-converted inner error in the function's failure container (`return Err(From::from(failure))` for Result) vs the desugar `Break(failure): return From::from(failure)` is literal, requiring container-targeting impls (`From[E1] for Result[T2, E2]`) — contradicting §7.9's required `From[SourceError] for DestError` and §8.4's inner-failure-value claim; the Option case typechecks only because stdlib `From[()] for Option[T]` targets the whole container | impact: determines which From impls users must write and what code `?` emits at every Result `?` site | quarantined: "`?` propagation applies `From::from` to the inner failure value (`E` for `Result`, `()` for `Option`), not to the `None`/`Err(...)` container (§8.4)"; "`expr?` desugars literally to `match Try::branch(expr): Continue(value): value; Break(failure): return From::from(failure)` (§8.4.1)"; "when failure types are identical the desugared `From::from` is the identity conversion with no special case (§8.4.1)"; "stdlib provides `From[()] for Option[T]`, yielding `None`, to serve Option-to-Option `?` propagation (§8.4.1)"
[chunk 06] - kind: ambiguity | sections: §8.3.1, §8.4.2 | readings: "match plus ? is the complete surface for consuming Option and Result" is a loose pattern-sugar claim (only excluding `if let`), with postfix `!` (§8.4.2) and stdlib methods (§8.7) as additional consumers vs the claim is normatively exhaustive, contradicting the existence of `!` and the consuming stdlib methods | impact: affects whether §8.3.1 constitutes a normative exclusion of further Option/Result-consuming operators | quarantined: "`match` and `?` are the complete surface for consuming `Option` and `Result`. (§8.3.1)"
[chunk 07] - kind: contradiction | sections: §9.3.4 | readings: the stated rule says index types whose range exceeds `isize` require an explicit cast (on 64-bit only types up to and including `i64` widen losslessly, with `u64`/`i128`/`u128` needing casts), yet the worked example shows `let n: usize = compute(); let w = arr[n]` annotated "usize widens to isize for indexing" ✓ — `usize`'s range exceeds `isize`'s on every platform vs `usize` is special-cased to widen implicitly for indexing | impact: whether `arr[n]` with a `usize` index compiles without a cast is undecidable from the spec | quarantined: "A `usize` array index widens implicitly to `isize`: `arr[n]` with `n: usize` ✓." vs "A `usize` array index requires an explicit cast to `isize`, like `u64` on 64-bit."
[chunk 07] - kind: incoherence | sections: §9.3.4 | readings: "the same source code is valid on every platform" vs the immediately following clause and example ("may also fail on 32-bit") under which code lacking a cast that a narrower-`isize` platform requires is invalid there — alternatively the sentence means only that the widening rule itself is uniform while its outcome is platform-dependent | impact: the portability guarantee for index expressions is unclear; a conforming 32-bit implementation cannot tell whether it must accept cast-free code that 64-bit accepts | quarantined: "Index code is valid on every platform unchanged." vs "Index-widening legality is platform-dependent; a cast required on a narrower-`isize` platform may be unnecessary on a wider one."
[chunk 07] - kind: incoherence | sections: §9.4.2.2, §9.4.2.3 | readings: §9.4.2.2's stdlib bullet "No conversion to absolute values (would imply wall-clock semantics the language does not commit to)" plus "no other introspection is provided" scope only the construction/observation API, leaving §9.4.2.3's persistence path lawful vs they are unscoped constraints on stdlib, contradicting §9.4.2.3's "must use stdlib serialization that converts instants to and from a stable absolute representation (e.g., Unix nanoseconds)" — a saved-instant round-trip is exactly an instant→absolute→instant conversion | impact: whether a conforming stdlib may (or must) expose instant↔absolute-time conversion | quarantined: none (entries under §9.4.2.2 and §9.4.2.3 transcribe each section's own formulation; discovered in final-QA spot-audit, verified by the orchestrator against SPEC.md lines 6530–6554)
[chunk 07] - kind: ambiguity | sections: §10.4.3 | readings: the prescribed alternative to function-scope `use` is "binding the desired value to a `let` or `mut` (e.g., `let synth = root::audio::Synthesizer`)" — this `let`-binds a type/constructor name as a value, which presupposes type names (or constructors) are first-class bindable values vs the example is loose and only ordinary values (functions, constants, instances) can be locally bound | impact: whether `let X = path::TypeName` is legal, and what such a binding denotes, is not established by the cited subsection | quarantined: "Local short names inside a function body are achieved by `let`-binding the desired value: `let synth = root::audio::Synthesizer`."
[chunk 07] - kind: ambiguity | sections: §10.2 vs §10.4, §10.4.1 | readings: `use root::audio::effects` ✗ annotated "audio/effects/ is not a module" implies module-targeting `use` is legal when the path IS a module vs §10.4's `use` "imports a name from a module", forbidding module targets outright | impact: legality of `use root::audio::synth` followed by `synth::Oscillator` | quarantined: none (syntax-sweep discovery, orchestrator-verified)
[chunk 07] - kind: incoherence | sections: §9.1.2 vs §9.1.3 | readings: the char example `'\''` plus "the same escape conventions as string literals (§9.1.3) apply" vs §9.1.3's closed escape list (`\n`, `\t`, `\\`, `\"`, `\xHH`, `\u{HHHHHH}`) lacking `\'` — delimiter-adaptive escape set vs closed set | impact: lexer validity of `\'` in char literals (and `\"` in chars) | quarantined: "`\'` is a valid character-literal escape: `'\''`. (§9.1.2)" (withheld pending ruling; syntax-sweep discovery, orchestrator-verified)
[chunk 07] - kind: incoherence | sections: §9.4.1.4, §9.4.2.2 vs §8.7, §10.2.3 | readings: `duration::from_nanos(n)` and `instant::now()` qualify free functions on primitive type keywords — loose module-style shorthand the spec elsewhere denies ("no type-qualified free-function form"; the closed path-base set of §10.2.3) vs an intended type-qualified call form | impact: surface validity of those stdlib calls and of the log's verbatim §9.4.x entries | quarantined: none (entries transcribe; syntax-sweep discovery, orchestrator-verified)
[chunk 07] - kind: ambiguity | sections: §9.3.6 vs §13.3.3.4, §13.5.4.3, §13.5.4.9 | readings: dynamic-collection (vector) syntax is out-of-spec stdlib territory (§9.3.6) and §9.3 never defines an array-literal form, yet §13 examples initialize `Vec[…]` cells with `[]` — array→Vec conversion, a polymorphic empty-collection literal, or loose examples | impact: compilability of `attr posts: Vec[PostData] = []`; compounds the undefined array-literal construction grammar | quarantined: none (syntax-sweep discovery, orchestrator-verified)
[chunk 08] - kind: ambiguity | sections: §11.3.5 (interacting with §11.8.5) | readings: the Rule (P) body lists `move x` at a call site among the consume forms whose result is a name-only role transfer (it succeeds), vs the corollary, diagnostic, and worked example which make passing an alias via `move` to an `own` parameter a compile error — and since §11.8.5 permits `move` only toward `own` parameters, `move` on an alias can never take the success path | impact: whether `move alias` at a call site compiles as a name-only transfer or is rejected | quarantined: "The Rule (P) body lists `move x` at a call site among the consume forms applied to a borrow-equivalent alias, under which such a `move` is a name-only role transfer leaving the source value untouched. (§11.3.5)" and "Passing a borrow-equivalent alias to a parameter declared `own T` is a compile error: `consume(move y)` ✗ when `y` is a cluster member. (§11.3.5)" (the two contested readings, withheld from the list; the uncontested let/mut name-only-transfer and source-untouched clauses remain serialized)
[chunk 08] - kind: incoherence | sections: §11.5.3 vs §11.7.4/§11.8.5 | readings: the §11.5.3 example writes `process(buf)` with the comment "buf consumed" and no `move`, vs the own⇔move symmetry rule making an `own`-parameter call without `move` a compile error (alternatively, if `process` takes a default parameter, the "buf consumed" comment is false) | impact: whether call-site `move` is strictly mandatory for every category A consumption | quarantined: 08-98
[chunk 08] - kind: ambiguity | sections: §11.5 vs §11.3.6/§11.3.4 | readings: §11.5 declares `fn clone(value: Subject) -> Subject` under the default convention yet asserts the result is an independent owned copy, vs §11.3.6's borrow-default return elaboration and §11.3.4's borrow-default trait-slot rule under which a trait-level `-> Subject` return rooted in the argument would elaborate as a borrow-equivalent alias in the receiver's cluster absent `-> own Subject` | impact: whether `.clone()` results are independent real owners or cluster members that lock the source against move/mutation (also load-bearing for §11.5.4 anchoring) | quarantined: 08-91, 08-98, 08-99
[chunk 09] - kind: contradiction | sections: §11.10.1, §11.9.1, §11.10.5 | readings: (A) the Copy-snapshot rule governs only escaping/snapshot captures, and a non-escaping closure may additionally access non-Copy enclosing values as cluster-member borrows ("Non-escaping closures may treat captures as cluster-member borrows", "may treat their environment access as alias access") vs (B) every capture must be Copy without exception (§11.10.1 "Every value captured by a closure must have a Copy type") and the non-escaping alias treatment is only an unobservable compiler-internal strategy | impact: observable legality differs — e.g. passing fn(): sum(buf) (buf non-Copy) directly to a higher-order function is legal under A, a compile error under B | quarantined: "A non-escaping closure may treat its captures as cluster-member borrows; values that escape the cluster cannot be captured by alias. (§11.9.1)" and "A non-escaping closure invoked synchronously within the cluster's lifetime may treat its environment access as alias access. (§11.10.5)" (reading-A allowance entries, withheld from the list; 09-32/09-33 state reading B's unconditional rule as §11.10.1 declares it)
[chunk 09] - kind: ambiguity | sections: §11.9, §11.9.1 | readings: §11.9 states the surface has no `&T` syntax, yet §11.9.1 types a handle's resolution as `Option[&T]`; either `&T` is spec/elaborated-form notation never writable in source, or `Option[&T]` is a nameable surface type contradicting §11.9 | impact: determines whether a conforming parser/typechecker must accept `&T` in type positions | quarantined: none (09-1 and 09-13 each quote their section without committing to surface writability)
[chunk 09] - kind: incoherence | sections: §11.10.2, §11.12 | readings: §11.10.2 says a non-Copy captured subvalue fails "regardless of whether the root binding is Copy", but per §11.12 a Copy compound requires all fields/payloads Copy, so a Copy root with a non-Copy subvalue cannot exist; the qualifier is vacuous (alternatively it anticipates some Copy type with non-Copy components not expressible in §11.12's model) | impact: none on conformance — no program distinguishes the readings; flagged for spec cleanup | quarantined: none
[chunk 09] - kind: contradiction | sections: §11.9.4 vs §11.3.6, §11.3.4 | readings: `return v` is annotated "✓ if Vec[i32] is Clone: §11.3.6 auto-anchoring applies" for a default-convention return of a borrow-equivalent parameter vs default returns are cluster propagation with no Clone gate (anchoring is opt-in via `-> own T`); the host signature `-> isize` additionally makes the annotated ✓ line ill-typed | impact: whether `return param` compiles for non-`Clone` types; alias-vs-owner status of the caller's binding | quarantined: none (entries transcribe each section's wording; syntax-sweep discovery, orchestrator-verified)
[chunk 09] - kind: ambiguity | sections: §11.11.2 vs §11.8.5, §11.7.4 | readings: `let b = a.clone().push(move x)` feeds `push`'s `own` receiver a temporary with no `move` — rvalues feeding `own` parameters are implicitly consumed vs the chain is ill-formed under the own⇔move symmetry and identifier-only `move` grammar; the sibling line `v = (move v).push(move x)` further shows `move` on a parenthesized receiver, outside §11.8.5's call-argument position | impact: legality of chaining into consuming methods; `move`'s admitted positions and operands | quarantined: none (entries transcribe; syntax-sweep discovery, orchestrator-verified)
[chunk 09] - kind: incoherence | sections: §11.10.4 vs §11.7.2, §11.11.2, §11.1 | readings: the canonical capture example calls `apply_scale_in_place(local, scale)` and then treats `local` as scaled — a borrow call cannot mutate `local` (no mutable-reference parameters; callee mutation is globally invisible) and an `own` call would require `move local` plus rebinding | impact: example-level; the illustration demonstrates a forbidden-or-no-op call shape | quarantined: none (syntax-sweep discovery, orchestrator-verified)
[chunk 09] - kind: incoherence | sections: §11.12 vs §11.8.3, §6.3, §9.2 | readings: enum payloads and newtype wrapped values "may all be assigned through a `mut` binding to the containing value", but no place syntax names them — whole-value reassignment only vs an unspecified place grammar vs match-bound assignable places | impact: the asserted permission has no expressible surface form | quarantined: none (syntax-sweep discovery, orchestrator-verified)
[chunk 10] - kind: contradiction | sections: §12.3.7, §12.3.7 | readings: "Admitted iterables" states tuple-element iteration is not provided, vs the "Fixed-extent vs. variable-extent" paragraph lists tuples (and records) in the fixed-extent (unroll) set, implying loops over them are legal and unroll | impact: whether `for x in some_tuple:` (or over a record) is legal at all | quarantined: "A loop over a value of a fixed-extent type (`T[N]`, tuples, records) is compile-time-unrolled. (§12.3.7)"
[chunk 10] - kind: incoherence | sections: §12.3.7, §12.3.7 | readings: the governing rule "unrolled iff the iterable is compile-time known" plus mechanics ("binding the iteration variable to the corresponding compile-time value") and the closed two-form admitted list, vs "a loop over a value of a fixed-extent type unrolls" and "if a type can carry a const value, a for over it unrolls" — a runtime-valued `f32[N]` has compile-time extent but runtime elements, so it cannot satisfy the mechanics; reading r1: only the two admitted forms unroll and runtime-valued fixed arrays run at runtime; reading r2: fixed-extent loops unroll by count with runtime element accesses | impact: which loops a conforming compiler is required to unroll | quarantined: "A `for` over a runtime-valued fixed-size array `T[N]` is compile-time-unrolled by element count. (§12.3.7)" vs "A `for` over a runtime-valued fixed-size array `T[N]` runs at runtime. (§12.3.7)"
[chunk 10] - kind: ambiguity | sections: §12.6.2, §12.6.4 | readings: §12.6.2's note states declaratively that when the body provably never completes naturally the loop type "collapses to T regardless of the else: clause's presence" (required, deterministic), vs §12.6.4's "the compiler MAY use the never type" (permissive, implementation-discretionary) | impact: whether programs relying on the collapse (break-value loop, no else:, all paths diverging) are portably well-typed across conforming implementations | quarantined: "If the body provably never completes naturally, the loop expression's type collapses to `T` regardless of the `else:` clause's presence. (§12.6.2)" vs "An implementation may, but need not, use `never` (§8.2.2) to unify the unreachable natural-completion case. (§12.6.4)"
[chunk 10] - kind: ambiguity | sections: §12.3.3, §12.3.3 | readings: the `for own` Copy-element case says `buf` "is consumed at loop entry" and the example comments "buf is consumed by `for own`; cannot be used after the loop", yet the same sentence calls the case "functionally indistinguishable from the default form for Copy element types" (under which the source survives); note §12.2/§12.10.2 make the indistinguishability claim only for `Range[T]`, which is itself Copy | impact: whether the source binding of a `for own` loop over a Copy-element (but possibly non-Copy) aggregate is usable after the loop | quarantined: "Under `for own` with a Copy element type, the source binding is consumed at loop entry and unusable afterward. (§12.3.3)" vs "Under `for own` with a Copy element type, the source remains usable after the loop, indistinguishable from the default form. (§12.3.3)"
[chunk 10] - kind: ambiguity | sections: §12.1, §12.5.1, §12.6.2 | readings: §12.5.1 permits `break expr` "in expression context (§12.6)" while §12.1 says loops not used in expression context produce unit; r1: `break value` is permitted only in expression context, so a statement-position loop containing `break 42` is ill-formed; r2: `break value` is always permitted and a statement-position loop discards the value, producing unit per §12.1 | impact: whether `for i in 0..N: break 42` in statement position compiles | quarantined: "A `break value` outside expression context is an error. (§12.5.1)" vs "A statement-position loop discards any `break value` and produces unit. (§12.1)"
[chunk 10] - kind: incoherence | sections: §12.3.1, §12.7.4 | readings: §12.3.1 step 3 (default form) admits an `own Item` opt-in ("unless the implementation opts in to `own Item`") while step 7 guarantees the source "is unchanged and remains owned", and §11.9.2/§12.8.2 forbid moving from the source while the iterator is live; §12.7.4 likewise says a source-bearing iterator "opts into owned items (consuming each element out of the collection)" via `type Item = own Record` with survival decided by the Iterable/IntoIterable choice; r1: `type Item = own T` is only well-formed on consuming (`Source = ()`) iterators reached through `IntoIterable`, and an `Iterable`-dispatched iterator declaring it is rejected; r2: the combination is well-formed and default-form iteration may yield owned elements moved out of the source, contradicting the source-unchanged guarantee | impact: whether a `fulfill Iterable` whose `Iter` declares `type Item = own T` compiles, and what the default form yields if it does | quarantined: "Under the default form, a yielded `Item` is owned when the iterator implementation opts in to `own Item`. (§12.3.1)" vs "A source-bearing iterator opts into consuming elements out of the collection by declaring `type Item = own Record`. (§12.7.4)" vs "An iterator reached through `Iterable` dispatch may not declare `type Item = own T`. (§12.8)"
[chunk 11] - kind: ambiguity | sections: §13.2.2 | readings: the final list item "or any compile-time-evaluable expression" is a permissive catch-all alongside the attr- and signal-referencing forms (defaults evaluated at instance construction) vs a governing constraint requiring every attr default to be compile-time-evaluable (under which signal-referencing defaults, explicitly listed as permitted, are incoherent) | impact: legality and evaluation time of attr defaults referencing signals, e.g. `attr x: f32 = some_signal` | quarantined: "Any compile-time-evaluable expression is a permitted attr default." vs "Every attr default must be compile-time-evaluable."
[chunk 11] - kind: ambiguity | sections: §13.2.2 | readings: "Defaults may reference previously-declared attrs of the same node" permits earlier-attr references in defaults only for node attrs (connection attr defaults may not reference earlier attrs) vs "node" is loose wording for the section's own "enclosing node or connection type" and the rule applies to both | impact: legality of a connection attr default referencing a previously-declared attr of the same connection type | quarantined: none — entry 11-63 mirrors the spec's literal "same node" wording; the connection extension is not serialized.
[chunk 11] - kind: incoherence | sections: §13.2.3 vs §13.2.9.1, §13.3.3.4, §13.3.4.1 | readings: §13.2.3's grammar shows only `derived name: Type = expression` and all its own examples annotate, while §13.2.4 (recurrent) and §13.18.2 (stream) explicitly state the annotation is optional — reading 1: a `derived`'s type annotation is likewise optional when inferable, so the untyped examples (`derived A = …`, `derived t = …`) are valid vs reading 2: the grammar is exhaustive and untyped `derived x = expr` is ill-formed | impact: whether untyped `derived` parses; the log carries untyped-derived examples with no rule sanctioning the omission | quarantined: "A `derived`'s type annotation is optional when inferable from its expression: `derived t = (signal_a, 15, signal_b)`. (§13.2.9.1)" (withheld; independently flagged by two sweep auditors, orchestrator-verified against the §13.2.3 grammar block)
[chunk 11] - kind: ambiguity | sections: §13.2.4, §13.2.4.6 | readings: the scalar recurrent schema marks the type optional (`recurrent[N]? name: Type? = …`, "optional when inferable"), but the tuple-coupled schema `recurrent[N]? (name1, …): (Type1, …) = …` shows the tuple type unconditionally and never states optionality — reading 1: the tuple type is required vs reading 2: optional-when-inferable like the scalar form | impact: whether `recurrent (a, b) = expr` (no type) is well-formed | quarantined: none (entries transcribe each schema; syntax-sweep discovery, orchestrator-verified)
[chunk 12] - kind: ambiguity | sections: §13.2.11.5, §13.2.11.8 | readings: a where-filtered signal trigger (`sig where C`) counts as "a signal in the trigger set", so `default:` stays optional even though `C` may be false at startup, leaving the observe valueless in a signal context vs a filtered trigger is a stream-like cell per §13.18.10, so an arm set whose only signals are where-filtered consists "entirely of stream cells" and requires `default:` | impact: determines whether certain observes without `default:` compile and whether the signal no-value invariant can be violated at commit zero | quarantined: none (entries 12-143/12-144 restate the spec's literal rule without resolving the filtered-trigger classification)
[chunk 12] - kind: incoherence | sections: §13.2.7 | detail: the no-assignment sentence enumerates five kinds (signal, attr, recurrent, derived, const) while the closing sentence asserts the rule "applies to all six declaration kinds uniformly"; §13.2 defines the six kinds as including `stream`, which the enumeration omits, so the rule's coverage of stream is asserted but never stated | impact: whether stream cells are formally covered by the no-source-write enumeration rests on the unstated sixth kind | quarantined: none (12-43 reproduces the five-kind enumeration; 12-49 reproduces the six-kind claim)
[chunk 12] - kind: incoherence | sections: §13.2.9.8 | detail: the running example aliases `some_regular_property` (declared `i32` in `PeakResult`, §13.2.9.1) to `signal_c`, which the same running example establishes as `f32` (element of `derived arr: f32[4]`, §13.2.9.1); read as one continuous example, `derived A2 = A with some_regular_property: signal_c` is ill-typed | impact: example-level only; no normative rule is contradicted, but the canonical `with`-aliasing illustration does not type-check | quarantined: none (12-98 carries the example as written in the spec)
[chunk 12] - kind: ambiguity | sections: §13.2.8, §13.2.2 | readings: §13.2.8 makes `Signal[T]` the umbrella over exactly signal/derived/recurrent and names no reactive-reference type for an `attr`, while stating `Cell[T]` is "the broader abstraction over all reactive cells" and §13.2.2 calls attrs reactive cells — reading 1: an attr is a `Cell[T]` and may flow into a `Cell[T]` reference slot vs reading 2: an attr has no first-class cell-reference type and only its value flows via a `T` parameter (the §13.2.8 reference-passing examples cover only `Signal[T]`) | impact: whether a function/operator may take an attr by reference as `Cell[T]`, or only by value as `T`; there is no `Attr[T]` type | quarantined: none (entries 1816–1817 transcribe the membership rules without resolving attr reference-passing; syntax-sweep discovery, orchestrator-verified)
[chunk 13] - kind: contradiction | sections: §13.3.3, §13.3.7.2 | readings: §13.3.3 states an open-parts node's children are walked by the host "through its exposition" (its Processor example writes no expose: clause) vs §13.3.7.2 states a node with no parts: clause and no expose: clause has an empty exposition (no structural output) — either the expose: parts default also covers open-parts nodes' supplied children, or the empty rule stands and such children are unreachable via exposition. | impact: whether caller-supplied children of a no-parts:-clause node without an explicit expose: appear in the exposition the host and runtime traverse. | quarantined: "A node with no parts: clause and no expose: clause has an empty exposition; it has no structural output and exists only for its state and connections. (§13.3.7.2)" vs "A node without a parts: clause still exposes its supplied children by default; the host walks them through the exposition. (§13.3.3)"
[chunk 13] - kind: ambiguity | sections: §13.3.7.4 | readings: "There is no part that is never exposed" read as requiring an explicit expose: clause to cover every supplied part (omission illegal) vs "an explicit expose: arranges or selects among the parts" read as permitting exposure of a subset (unselected parts have no structural output). | impact: whether a conforming compiler must reject an explicit expose: clause that gives some supplied part no exposition entry. | quarantined: "An explicit expose: must cover every supplied part; a supplied part with no exposition entry is a compile error. (§13.3.7.4)" vs "An explicit expose: may select a subset of the supplied parts. (§13.3.7.4)"
[chunk 13] - kind: ambiguity | sections: §13.3.7.5, §13.3.4 | readings: §13.3.7.5 states connections are "always present on the schema" and that what may change at runtime is a dynamic destination's target, "never whether the connection exists," while §13.3.4 states a connection materialized per repeat key has varying existence ("its existence varies") and §13.3.4.1's fan-in example mounts one Ack per current member, "mounting and dismounting with it" — reading 1 scopes §13.3.7.5's claim to directly written connection entries only (repeat-scoped connection placements mount and dismount with their keyed scopes); reading 2 takes the claim as universal over all connection placements, contradicting §13.3.4. | impact: whether an implementation may treat every connection placement as constructed once and permanent, or must support runtime mount/dismount of repeat-materialized connection instances, and what .exposition reports for them. | quarantined: "Connection entries are always present on the schema: each exists from construction; a dynamic destination may re-point at runtime, but the connection's existence never changes. (§13.3.7.5)" vs "A directly written connection entry exists from construction and only its dynamic destination may re-point; a repeat-materialized connection placement mounts and dismounts with its keyed scope. (§13.3.7.5)"
[chunk 14] - kind: ambiguity | sections: §13.5, §13.5.4 | readings: the primitive permits "zero, one, or many" instantiations *per source element* (multi-scope-per-element capability runtimes must support) vs loose phrasing for "zero-to-many scopes overall, exactly one per unique key" (as `repeat`'s one-scope-per-element behavior implies) | impact: whether conformant runtimes must support multiple scopes per element for future constructs | quarantined: "The keyed-scope primitive permits a template to be instantiated zero, one, or many times per source element. (§13.5)" (entry 14-39 was written without the per-element multiplicity claim)
[chunk 14] - kind: incoherence | sections: §13.5.4.1, §13.5.4.2 | readings: §13.5.4.2 step 2's parenthetical gloss ("explicit `keyed by`, then `Keyed` trait, then stringifiable element, else compile error") is an incomplete restatement and the full 5-path order of §13.5.4.1 (including carried keys) applies at iteration time vs §13.5.4.2 normatively excludes the carried-key path from per-commit re-keying (contradicting §13.5.4.1 path 2 and §13.5.4.8's carried-key diffing) | impact: whether `dynamic` namespace sources use carried keys during per-commit key derivation | quarantined: "Key derivation at iteration time proceeds: explicit `keyed by`, then `Keyed` trait, then stringifiable element, else compile error. (§13.5.4.2)" (entry 14-110 cites §13.5.4.1's ordered selection without enumerating the paths)
[chunk 14] - kind: contradiction | sections: §13.5.4.8 | readings: "the `<key-expr>` / `Keyed::key` body that those paths use must not read it" bars all key-exprs from reading the `at` index, with bare `keyed by <index>` as the sole sanctioned exception vs the must-not-read clause binds only implicit-path derivation (`Keyed::key`), and explicit `keyed by` expressions may freely read the index (as `keyed by <index>` in the same subsection demonstrates) | impact: legality of explicit key expressions that read the index (e.g. arithmetic over it) | quarantined: "The `<key-expr>` and the `Keyed::key` body must not read the `at` index bind. (§13.5.4.8)"
[chunk 14] - kind: ambiguity | sections: §13.5.4.1, §13.5.4.2 | readings: two elements deriving equal keys in one evaluation collapse to one scope (with an unspecified element winning the bind) vs duplicate keys are a runtime or compile-time error; the spec defines key-set diffing but never duplicate-key behavior | impact: divergent implementations on sources yielding duplicate keys | quarantined: none
[chunk 14] - kind: ambiguity | sections: §13.5, §13.5.4.1 | readings: §13.5's intro glosses the `repeat` source as "a reactive iterable source (`Signal[I]` where `I: Iterable`)", subsuming `dynamic` namespace cells because such cells are themselves `Signal[I]`-typed vs the intro omits the second source kind §13.5.4.1 admits (`dynamic` namespace cells as a distinct, non-`Signal[I]` source) | impact: whether conformance requires `dynamic` namespace cells to be representable as `Signal[I]` | quarantined: none
[chunk 14] - kind: ambiguity | sections: §13.5.2, §13.5.4.1, §13.5.4.3 | readings: state-shape counts only direct `signal`/`attr`/`recurrent` declarations in the template body — but a `repeat` body is a placement-body block of placements and connections, which may make every `repeat` template trivially stateless — vs state-shape encompasses the cells of the template's placed child instances, as §13.5.4.3's per-key persistence of `Voice` recurrents and §13.5.3-style paths (`<effect-instance>.<row.id>.<cell>`) imply | impact: whether `repeat` scopes whose placed children hold state take the no-pool fast path of §13.5.2 | quarantined: none
[chunk 15] - kind: ambiguity | sections: §13.6.0, §13.6.2 | readings: (a) "always reactively live" is engagement-scoped — live regardless of traversal/engagement position, with §13.6.2’s two switches (gates, unresolved destination) as the only liveness switches vs (b) literal unconditional always-live, contradicting §13.6.2’s freeze and the §13.9.7 semantics it cites | impact: whether a frozen connection is an exception to §13.6.0 or a contradiction of it; the drafter adopted reading (a) silently with zero recorded findings | quarantined: "A statically placed connection is reactively live regardless of traversal/engagement position; its exposition position orders only its engagement. (§13.6.0)" — reinstate under reading (a); under reading (b) it conflicts with 15-51 and 15-53
[chunk 15] - kind: incoherence | sections: §13.7.1, §13.7.5 | readings: §13.7.1 lists `parts` separately from "the reserved endpoint/structure fields" (`from`, `to`, `incoming`, `outgoing`, `pair`, `exposition`) vs §13.7.5 including `parts` inside its reserved-fields roster | impact: whether `parts` belongs to the named "reserved fields" set differs between the two enumerations; the union and normative effects are consistent either way | quarantined: none (15-83 and 15-95 each faithfully reproduce their own section’s roster and both are retained)
[chunk 15] - kind: ambiguity | sections: §13.7.2 | readings: the free-function example reads `d.risk_tolerance` on a `Driver` whose shown body declares only `attr expertise_level` — reading 1: `Driver` is a running example carrying `risk_tolerance` (consistent with §13.8.1's `risk_tolerance=0.8`) and the snippet is a partial view vs reading 2: the snippet is self-contained and references an undeclared field | impact: example-level only; `d.risk_tolerance > 0.7` is well-formed surface syntax and the illustrated name-resolution rule is unaffected (recorded for disclosure; low severity) | quarantined: none (syntax-sweep discovery, orchestrator-verified)
[chunk 16] - kind: contradiction | sections: §13.8.2.2, §13.8.8.3 | readings: §13.8.2.2's parenthetical "a flag always sets a literal boolean (true for `'name`, false for `!name`)" either (r1) loosely refers to the inline bare forms of §13.8.7 so flags remain true-only, or (r2) defines a prefix flag notation in which `!name` sets false, contradicting §13.8.8.3's "there is no flag form for setting false" and §13.8.8's suffix-on-TypeRef flag syntax | impact: whether a conforming implementation must accept a false-setting flag form | quarantined: "A flag sets only the literal `true`; the inline `!name` form sets `false`. (§13.8.2.2)" vs "Flag notation `'name` sets `true` and `!name` sets `false`. (§13.8.2.2)"
[chunk 16] - kind: ambiguity | sections: §13.8.10 | readings: "must be parenthesized or placed on its own line. This is enforced, not advised: an unparenthesized open expression silently mis-parses" either (r1) requires implementations to diagnose an unparenthesized open-expression placement sharing a line as a compile error, or (r2) means the greedy grammar simply yields a different parse with no diagnostic ("enforced by the grammar") | impact: whether `C/x - G` on one line is rejected or silently parses as a subtraction | quarantined: "An unparenthesized open-expression placement sharing a line with another placement is a compile error. (§13.8.10)" vs "An unparenthesized open-expression placement sharing a line silently parses differently, with no diagnostic. (§13.8.10)"
[chunk 16] - kind: incoherence | sections: §13.8.2.1 | readings: the reactive-expression bullet lists "top-level signals or consts" among referencable reactive cells although consts are non-reactive; either (r1) consts are merely visible names usable inside a reactive expression without contributing reactivity (the provenance rule governs), or (r2) const references count as reactive-cell references, making a const-only RHS category C and contradicting the category-B bullet | impact: binding category (consume vs synthesized derived) for const-referencing RHS expressions | quarantined: "A reactive placement RHS may reference sibling part instances by name, top-level signals or consts, or any cell reachable through visible names. (§13.8.2.1)"
[chunk 16] - kind: ambiguity | sections: §13.8.7, §13.8.2.2 | readings: "All three forms (value, bare, negated-bare) and the flag form handle attr binding uniformly" either (r1) means all forms write the same underlying attr cell through the same binding machinery, flags being literal-true value bindings, or (r2) means all forms admit the same binding categories including reactive, contradicting §13.8.2.2's rule that flags have no expression slot and take no reactive bindings | impact: whether flags can participate in reactive binding | quarantined: "All three inline attribute forms and the flag form handle attr binding uniformly. (§13.8.7)"
[chunk 16] - kind: ambiguity | sections: §13.8.10 | readings: the self-delimiting taxonomy's first row, "a bare type, with flags and octave/duration sigils: `C`, `G'`, `Pin'!`", uses the term "octave/duration sigils", which occurs nowhere else in the spec and is never defined; either (r1) it is a loose gloss for flag characters as employed by music-notation types, so the row reduces to "bare type with flag run" and admits no additional syntax, or (r2) it names a distinct placement-syntax element (octave/duration markers) whose form is unspecified, leaving the self-delimiting token set underspecified | impact: which token sequences a conforming parser must accept as a self-delimiting bare-type placement in same-line multi-placement | quarantined: "A bare type with flags and octave/duration sigils is self-delimiting. (§13.8.10)"
[chunk 16] - kind: incoherence | sections: §13.8.3 vs §13.8.4, §13.8.4.2 | readings: §13.8.3 grants placement-name visibility only to "subsequent placements inside the same placement body", yet the canonical §13.8.4 example (`Cascade: next_filter`) and §13.8.4.2 (`link_kind: the_car`) reference siblings placed LATER in the scope — reading 1: placement-scope names are whole-scope and order-independent (the examples are authoritative; "subsequent" is loose) so forward references are legal vs reading 2: visibility is genuinely subsequent-only and the spec's first connection examples are ill-formed (unresolved destinations) | impact: whether a connection destination or placement may name a not-yet-placed sibling; compilability of the spec's canonical connection examples | quarantined: "Placement-scope instance names are visible across the whole enclosing placement scope regardless of order; a placement may reference a sibling placed later: `Cascade: next_filter` with `next_filter` placed afterward. (§13.8.4)" (withheld; syntax-sweep discovery, orchestrator-verified)
[chunk 17] - kind: contradiction | sections: §13.9.7, §13.9.8 | readings: gate-close performs no recompute pass at all ("There is no recompute pass"; "Gate-close adds no DAG work") vs the deriveds bullet referencing "the one-shot effect-desired recompute of the close transition (below)" at the false-flip commit, a recompute the close paragraph never defines | impact: whether effect desired state recomputes once at suspend time | quarantined: "Gate-close triggers no recompute pass; freezing plus effect `suspend` is the entire close behavior. (§13.9.7)" vs "At the false-flip commit a one-shot effect-desired recompute runs before the subtree settles frozen. (§13.9.7)"; also "Gate-close adds no per-commit DAG work; its only consequence is firing `suspend` for contained effects. (§13.9.8)"
[chunk 17] - kind: ambiguity | sections: §13.9.8, §13.9.7 | readings: "A subtree that is gated off is excluded from the DAG; nothing in it recomputes" read as absolute vs as implicitly excepting the predicate-support subgraph that §13.9.7 requires to stay current | impact: whether predicate-read deriveds in a gated subtree recompute via the per-commit DAG or outside it | quarantined: "A gated-off subtree is excluded from the per-commit DAG; nothing in it recomputes. (§13.9.8)" vs "A gated-off subtree is excluded from the per-commit DAG except the minimum subgraph supporting its `when` predicate. (§13.9.8)"
[chunk 17] - kind: ambiguity | sections: §13.9.6, §13.9.7, §13.9.8 | readings: the open snap is "scheduled within the commit that flips the predicate" (§13.9.6, §13.9.8) vs §13.9.7's "transitions from false to true between commits", readable as the snap landing in the following commit | impact: same-commit cascade vs one-commit latency at gate-open | quarantined: "The gate-open transition is a propagation event scheduled within the commit that flips the predicate. (§13.9.6)" vs "A gate-open detected across a commit boundary is processed as a propagation event in the next commit. (§13.9.6)"
[chunk 17] - kind: incoherence | sections: §13.9.5 | readings: the explicit-combination example reads `my_app.is_emitting` although the type-level gate reads `from.is_emitting` and the example `App` declares no `is_emitting`; likely intended `d1.is_emitting` or `from.is_emitting` | impact: non-normative example only; the total-replacement rule is unaffected (reviewer note: entry 17-33 now transcribes the spec example verbatim, per the no-invented-examples rule; this finding flags its incoherence) | quarantined: none
[chunk 17] - kind: ambiguity | sections: §13.9.3, §13.9.4, §13.9.5 | readings: the §13.9.3 example `ShowsCount when from.count > 0: d1` has a placement-level predicate reading the connection's `from`, but §13.9.4's placement-level scope enumeration (siblings, parent attrs, top-level cells, identifiers resolvable at that point) never grants `from`/`to`/`pair`, which the type-level bullet grants explicitly "for connections" — endpoint access at placement level is either implied by the example or not granted by the rule | impact: whether a connection placement's `when` may reference endpoint cells; if not, a type-level endpoint predicate such as `from.is_emitting` cannot be reproduced in the explicit placement-level combination §13.9.5 requires under total replacement (plausibly why the §13.9.5 example swaps to `my_app.is_emitting`, see the incoherence finding above) | quarantined: none (entries 17-23 and 17-31 transcribe the spec verbatim)
[chunk 17] - kind: ambiguity | sections: §13.9.12 | readings: `default:` is required "unless some guard is provably total" — "provably total" occurs nowhere else in the spec and no proof criterion is defined (const-evaluable literal `true`? type-derived totality? implementation-chosen analysis?) | impact: whether a multi-way `when:` block without `default:` is accepted varies by implementation; the boundary of the missing-default diagnostic class is unspecified | quarantined: none (entry 17-99 transcribes the rule verbatim)
[chunk 17] - kind: ambiguity | sections: §13.9.12, §13.9.13 vs §13.2.11.5, §6.2.4 | readings: §13.2.11.5 makes the `observe` `default:` arm "the last arm in declaration order" with a non-last `default:` a compile error, and §13.9.12 says the block `default:` "reuses the reactive-fallback word of `observe`" while §13.9.13 says it behaves "exactly as a catch-all does in value `match`" — reading 1: the last-position constraint is inherited by the `when:` and `given` block forms vs reading 2: no position constraint applies, since neither block section nor §6.2.4 restates one | impact: whether a `when:`/`given` block with a non-last `default:` compiles, and the boundary of any misplaced-default diagnostic; distinct from the ledgered §13.9.12 "provably total" item (that concerns when `default:` is required, not its position) | quarantined: none (entries state default's role without asserting a position rule; syntax-sweep discovery, orchestrator-verified)
[chunk 18] - kind: ambiguity | sections: §13.10.4, §13.10.1, §13.10.2, §13.14.5 | readings: transaction close marks written cells dirty and they persist to commit ("Dirty cells remain dirty until the next runtime.commit()") vs "dirty" is informal for pending staged changes, with dirtiness computed only at commit by staged-vs-previous-committed comparison (supported by "Outside or inside a transaction, identical semantics" and step 1's revert clause) | impact: whether a write-then-revert spanning a transaction boundary fires triggers at the next commit | quarantined: "After transaction close, written cells remain dirty until the next `runtime.commit()`. (§13.10.4)" and "The staged cells of a transaction remain dirty until the next `runtime.commit()`, which settles and publishes them. (§13.14.5)" (the §13.14.5 sentence restates the contested claim verbatim; the log's §13.14.5 entry retains only the uncontested atomicity-of-grouped-writes-only clause)
[chunk 18] - kind: ambiguity | sections: §13.11.3, §13.10.2 | readings: `where` guards evaluate during DAG construction (step 2, per §13.11.3's "per §13.10.2 step 2" cross-reference) vs during topological evaluation (step 3, where §13.10.2 places observe arm selection "during evaluation") | impact: a guard reading a dirty derived sees its previous-commit value under step-2 timing but the freshly recomputed value under step-3 topo-ordered timing | quarantined: none (entries state only that guards evaluate within the current commit, common to both readings)
[chunk 18] - kind: incoherence | sections: §13.12.4 | readings: types above the atomic word always use pool indices, with the sole exception of the backend two-cell option for 9–16 bytes vs the cell-fit table footnote "larger values span multiple consecutive cells per §14.3.2 or use pool storage per §14.3.3", which permits direct multi-cell spanning beyond 16 bytes | impact: whether a >16-byte fixed-size type may be stored directly across consecutive cells or must be pool-indexed (the five-i64-record table row says pool) | quarantined: "A value larger than 8 bytes spans multiple consecutive cells or uses pool storage. (§13.12.4)"
[chunk 18] - kind: ambiguity | sections: §13.12.4 | readings: reactive cell storage is always in 64-bit (8-byte) cells per the cell-fit table footnote vs cell width is the platform's atomic word width with 8 bytes only typical per the direct-storage rule ("≤ the platform's atomic word width (typically one i64 = 8 bytes)") | impact: direct-storage eligibility threshold and cell granularity on platforms whose atomic word is not 64 bits | quarantined: "Reactive cell storage is in 64-bit (8-byte) cells. (§13.12.4)"
[chunk 18] - kind: incoherence | sections: §13.12.1, §13.10.1, §13.10.2 | readings: §13.12.1's "used by the dirty-bit propagation in §13.10.1" intends §13.10.2 step 1, where dirty propagation is actually defined vs intends §13.10.1's commit-time dirty-bit determination, which states no propagation rule | impact: editorial wrong internal cross-reference; no conformance divergence, both targets describe the same commit-time model | quarantined: none (entries omit the cross-reference)
[chunk 18] - kind: ambiguity | sections: §13.12.4, §13.10.2 | readings: a fixed-extent cell's pre-allocated slot is reserved capacity (e.g. double-buffered or index-flipped) so the previous-committed value stays readable until the step-5 atomic publish vs a literal single slot written in place during staging, which would expose mid-commit mutation to readers and reference-holders, conflicting with the pool refcount lifetime rule and the staged-vs-previously-committed comparison of step 1 | impact: whether implementations may write fixed-extent pool values in place before publish without violating snapshot consistency | quarantined: none (the entry retains the spec's underdetermined "slot is pre-allocated ... the runtime writes the value into it" wording, common to both readings)
[chunk 19] - kind: contradiction | sections: §13.14.7, §13.15.6 | readings: registrations after the runtime is live are always rejected (making an effect-type rename un-hot-reloadable) vs hot reload re-opens a pre-live registration window in which the host may register the renamed type "before the reload reaches the live state" | impact: whether an effect-type rename is hot-reloadable at all, and when register_reconciler is legal | quarantined: "After an effect-type rename, the host must register a reconciler for the new type via `runtime.register_reconciler` before the reload reaches the live state. (§13.15.6)" vs "An effect-type rename is reloadable only if the new type's reconciler was registered before the original live transition, since registrations after live are rejected. (§13.15.6)"
[chunk 19] - kind: ambiguity | sections: §13.14.1, §13.14.4, §13.14.9 | readings: the commit summaries list "run reconcilers, and publish a new snapshot" in that order, implying hooks run before snapshot publication vs §13.14.9's explicit rule that hooks fire after the commit publishes its snapshot | impact: whether reconciler hooks observe the just-published snapshot version within the same commit | quarantined: none (entries 19-32, 19-51–19-53, and 19-98 reproduce each section's own wording without asserting a cross-section ordering)
[chunk 19] - kind: ambiguity | sections: §13.14.11, §13.15.1, §13.15.3 | readings: the reload verb accepts a precompiled IR diff (`runtime.reload(diff)`) with compilation a host-side precondition occurring "before any runtime-side action" vs reload step 1 has the runtime itself compile the new source as part of its atomic driving-context sequence | impact: which artifact (source vs IR diff) a conforming runtime's reload entry point must accept, and where compilation runs | quarantined: none (entries 19-119, 19-124, and 19-135 quote each section's own formulation)
[chunk 19] - kind: incoherence | sections: §13.14.10, §13.16 | readings: §13.14.10's enumeration of behaviors ("the pure compute of each derived, gate predicate, and effect `desired:` builder") is exhaustive, so recurrent expression bodies are not invoked via the behavior ABI vs §13.16's clause ("derived, recurrent, gate, and effect-`desired` bodies" conform to the behavior ABI) is authoritative and §13.14.10's list is illustrative and under-inclusive | impact: whether recurrent expression bodies are invoked by content-addressed behavior id, which determines what the compiler must emit as behaviors and what a runtime calls back during commit | quarantined: "The behaviors the runtime calls are the pure compute of each derived, gate predicate, and effect `desired:` builder. (§13.14.10)" (role enumeration kept out of the entry list; 19-193 reproduces §13.16's own list)
[chunk 19] - kind: ambiguity | sections: §13.15.4 | readings: any change to a cell's `= initial` expression triggers per-instance operator restart (conservative textual criterion) vs only changes that "would alter its current state semantics" do, requiring a semantic-equivalence judgment this section does not operationalize (full operator-reload rules are delegated to §13.17.10, outside this chunk) | impact: which operator-cell initial-expression edits force per-instance restart rather than in-place reload; contrasts with §13.15.6 where effect-cell initial-value expression changes are categorically reload-safe | quarantined: none (entry 19-151 reproduces the spec qualifier verbatim)
[chunk 20] - kind: ambiguity | sections: §13.17.3 | readings: "Inside the body, the parameter is a compile-time-fixed value" reads as value parameters requiring compile-time-constant arguments vs as runtime-evaluated snapshots immutable per instance (the call-site bullet says arguments are "evaluated and snapshotted", and the category-B implicit-move flow implies runtime data movement) | impact: legality of passing a runtime-computed expression to a `T` parameter | quarantined: "Inside the body, a value parameter is a compile-time-fixed value." vs "Inside the body, a value parameter is an immutable runtime snapshot fixed per instance."
[chunk 20] - kind: incoherence | sections: §13.17.5 | readings: "return a record or tuple containing reactive cells" says multi-output records have cell-typed fields vs the example where `PeakResult` declares plain `f32`/`u32` fields, the operator returns `Signal[PeakResult]`, and consumers project fields reactively per §13.2.8 | impact: type of `result.peak` (a `Signal[f32]`-typed field vs a reactive value projection from one record-valued cell) | quarantined: "A multi-output record or tuple contains reactive cells as fields." vs "A multi-output record is a plain value carried by one output cell whose fields are projected reactively."
[chunk 20] - kind: incoherence | sections: §13.17.7 | readings: "`|>` may only apply operators or effects" taken as exhaustive over RHS kinds (excluding sinks, contradicting Case 3) vs as governing only application-form RHS, with `Sink[T]` forwarding a permitted non-application case | impact: legality of `stream |> sink` under the applicability sentence; Case 3 explicitly permits it | quarantined: "`|>` may only apply operators or effects." vs "`|>` applies only operators or effects when the RHS is a call; a `Sink[T]` RHS is forwarding, not application."
[chunk 20] - kind: ambiguity | sections: §13.17.13 | readings: the examples bind named arguments with `=` (`apply(source = some_signal, kernel = gain)`) while operator call sites elsewhere use `:` (`smooth(rate: 0.1, clock: tick)`) — either both named-argument spellings are valid or the `=` form is an error for `:` | impact: which named-argument spelling conforming parsers must accept at operator call sites | quarantined: "Operator-typed arguments are bound by name at the call site: `apply(source = some_signal, kernel = gain)`."
[chunk 20] - kind: ambiguity | sections: §13.17.3, §13.17.13 | readings: "Operator parameters are of two kinds" (cell-bound `Signal[T]`, value `T`) read as an exhaustive taxonomy vs §13.17.13 admitting operator-typed parameters (`kernel: operator(Signal[f32]) -> Signal[f32]`) as either a third kind or a compile-time-resolved value parameter | impact: which parameter rules (snapshotting, defaults, call-site forms, the §13.17.12 non-cell diagnostic) govern operator-typed parameters | quarantined: "Operator parameters are of exactly two kinds: cell-bound and value."
[chunk 20] - kind: incoherence | sections: §13.17.6 | readings: "The result of each step is a `Signal[T]` consumed by the next" read as a normative restriction on pipe-chain intermediates vs descriptive of the common case, since §13.17.5 allows `Stream[T]`/`Sink[T]` outputs and §13.17.7 Case 1 yields whatever output cell the operator declares | impact: legality of `|>` chains with `Stream[T]`-typed intermediate results (e.g. composing the `to_stream`/`filter`/`merge` operators named in §13.17.5) | quarantined: "The result of each `|>` step is a `Signal[T]` consumed by the next."
[chunk 20] - kind: incoherence | sections: §13.17.5 | readings: "For multiple outputs, return a record or tuple containing reactive cells:" ends in a colon followed directly by the consumer-projection prose, read as a missing example block vs a colon typo for a period | impact: editorial — a multi-output example may have been dropped from the spec; no additional rule is recoverable from the surrounding text | quarantined: none
[chunk 21] - kind: ambiguity | sections: §13.18.5 | readings: `Signal[T]` implements `Writable[T]` in the type system with writes host-restricted vs `Signal[T]` is not program-visibly `Writable[T]` and the "host's perspective" parenthetical is conceptual only | impact: whether a `Signal` satisfies a `Writable[T]` bound in program code | quarantined: "`Writable[T]` is additionally implemented by `Signal[T]` from the host's perspective, though program code cannot write to signals."
[chunk 21] - kind: incoherence | sections: §13.18.3 | readings: capacity may be elided in signatures (`GateStream[T]`) to constrain policy alone vs concrete stream types always take `[T, N]` and the `persist` example is abbreviated | impact: whether `GateStream[T]` is valid type syntax in operator signatures | quarantined: "An operator may constrain policy without capacity by writing `GateStream[T]`."
[chunk 21] - kind: ambiguity | sections: §13.18.7.3, §13.18.7.4 | readings: a zero-stream expression may be bound only in Signal-producing contexts (`derived`, `attr` default, `recurrent` expression) vs `stream X = expr` additionally accepts it via implicit `to_stream` per assignment table row 4 and §13.18.2 source forms | impact: whether `stream x = sig_a * sig_b` compiles without explicit `to_stream` | quarantined: "A zero-stream reactive expression must be bound in a Signal-producing binding context (`derived`, `attr` default expression, `recurrent`'s expression)."
[chunk 21] - kind: ambiguity | sections: §13.18.8.1, §13.18.8.4 | readings: the `N` self-history slots are an allocation separate from the consumer buffer, the default capacity `sum_of_input_capacities + N` merely adding headroom on top of the §13.18.8.4 history ring vs the `+ N` capacity slots are themselves the self-history region, so the `N * sizeof(T)` output-history allocation lives inside the buffer and an explicit `[capacity]` must absorb the history slots | impact: total memory footprint and pool sizing of a recurrent stream declaration, and whether an explicit capacity includes or excludes the self-history slots | quarantined: none — entries 21-24, 21-125, 21-128 retained verbatim; the ambiguity lies in their interaction ("the recurrent's self-history allocation adds to the inferred consumer-buffer capacity" vs a distinct output-history ring)
[chunk 22] - kind: contradiction | sections: §13.18.10.5, §13.18.15, §13.18.16 | readings: §13.18.15's ".past/.previous only valid inside recurrent[N] stream bodies; use in signal arms is a compile error" read literally (banning recurrent-signal history) vs scoped to non-recurrent contexts only, since §13.18.10.5 (and §13.2.4.3) allow arm expressions in a recurrent's observe body to use .previous/.past | impact: legality of history access in recurrent-signal observe arms (e.g. `on tick where active_mode: counter.previous(0) + 1`) differs | quarantined: "When an observe block is a recurrent's body, arm expressions may reference the recurrent's self-history via `.previous(fallback)` and `.past(k, fallback)`. (§13.18.10.5)" and "`.past(n, fallback)` and `.previous(init)` are valid only inside the expression body of a `recurrent[N] stream` declaration; use in plain `stream` declarations, `derived` expressions, or signal arms is a compile error. (§13.18.15)"
[chunk 22] - kind: ambiguity | sections: §13.18.12, §13.18.13 | readings: `dropped_total` is a per-stream cumulative displacement counter (per the §13.18.6 surface table and §13.18.13 per-stream metadata), incrementing at overwrite time vs §13.18.12's "the dropped_total signal increments by the number of skipped events" implying per-consumer gap accounting at cursor-jump time | impact: observable counter value differs when multiple consumers lag by different amounts | quarantined: "When a ring overwrite forces a cursor jump, `dropped_total` increments by the number of events the cursor skipped. (§13.18.12)"
[chunk 22] - kind: ambiguity | sections: §13.18.14 | readings: decreasing an input's max `k` "reload-safely truncates allocation" retaining the most recent `k` values vs the same bullet's "preserved iff ... max k does not decrease" implying the history is not preserved (contents reset to fallbacks, allocation shrunk) | impact: post-reload `.past` reads return retained pre-reload values vs fallbacks | quarantined: "Decreasing an input's max `k` is reload-safe and truncates its history allocation. (§13.18.14)"
[chunk 22] - kind: ambiguity | sections: §13.18.9 | readings: `throttle`'s `clock: Signal[u64]` measures `window` in ticks aligned with `duration`'s nanosecond representation (§9.4.1) vs the tick unit and the clock-to-window relation are implementation-/caller-defined — the catalog never states how `clock` and `window: duration` interact | impact: which events a conforming `throttle` emits vs suppresses | quarantined: none (22-27 states the contract as listed; no entry removed)
[chunk 22] - kind: ambiguity | sections: §13.18.9, §13.18.11 | readings: all Stream-to-Stream catalog operators (including `throttle`, `scan`, `pairwise`) preserve source policy and capacity by construction, §13.18.11's "(map, filter, skip)" list being exemplary vs preservation holds only where expressly stated (`map`, `filter`, `skip`, `take`, `merge`), leaving `throttle`/`scan`/`pairwise` output policy and capacity unspecified | impact: output buffer configuration (overflow policy and capacity) of `throttle`/`scan`/`pairwise` results | quarantined: none (no entry asserts preservation for these operators; no entry removed)
[chunk 23] - kind: contradiction | sections: §13.19.2, §13.19.3 | readings: an effect must declare at least one of `desired:`/`observed:` (an effect with neither has no surface) vs the `log` example declares an effect with neither block ("// no desired, no observed — pure fire-and-forget consumer"), implying stream-parameter-only effects may omit both | impact: whether `effect log(entries: GateStream[LogEntry, 4096])` with an empty body is legal | quarantined: "At least one of `desired:` or `observed:` must be present in an effect declaration. (§13.19.2)" or "An effect taking only stream parameters may declare neither block, acting as a fire-and-forget consumer: `effect log(entries: GateStream[LogEntry, 4096])`. (§13.19.3)"
[chunk 23] - kind: incoherence | sections: §13.19.15, §13.19.16 | readings: "an effect-using function would need to be promoted to an operator" means promotion enables consuming/transforming effect cells only (instantiation still belongs in `effects:`) vs it implies operators may host effect instantiations, contradicting the same restrictions list and the operator-body diagnostic | impact: whether promotion to operator grants any effect-hosting capability | quarantined: "An effect-using function must be promoted to an operator. (§13.19.15)"
[chunk 23] - kind: incoherence | sections: §13.19.2, §13.19.4, §13.19.16 | readings: §13.19.2's "no other declaration kinds are permitted inside the blocks" and the §13.19.16 hint "effect blocks accept only `derived`, `sink` (in `desired:`) and `signal`, `stream` (in `observed:`)" read literally ban `repeat`/`when`/`given` inside `desired:` vs §13.19.4 expressly admits `repeat` declarations and `when`/`given` selection blocks in `desired:` | impact: whether the disallowed-declaration diagnostic fires on `repeat`/`when`/`given` in a `desired:` block | quarantined: "Effect blocks accept only `derived`, `sink` (in `desired:`) and `signal`, `stream` (in `observed:`). (§13.19.16)"
[chunk 23] - kind: ambiguity | sections: §13.19.4, §13.19.5 | readings: §13.19.5's direction to place a `repeat`/`when`/`given` in "the same effect's `desired:` block, consuming the observed cell as the source" implies sibling `observed:` cells are in scope as inputs inside `desired:` vs §13.19.4 defines `derived` inputs only as "parameters and any in-scope cells" and never states that sibling observed cells are in scope, nor how the resulting host-write to desired-recompute to reconciler-update loop is ordered | impact: whether a desired-block `repeat` or `derived` may consume the same instance's observed cells, and the commit ordering of the resulting feedback loop | quarantined: none
[chunk 23] - kind: incoherence | sections: §13.19.5, §13.19.7, §13.19.16 | readings: the "cannot assign to cell `response`" diagnostic's hint in §13.19.5 ("the host's reconciler is the sole writer ... to inject test data, construct a different effect instance or use a stub effect") vs the hint in §13.19.7/§13.19.16 ("change the upstream signal(s) ... or pipe a stream into the effect's sink(s)") | impact: low — one diagnostic class shown with divergent exemplar hint texts; class trigger and behavior identical | quarantined: none
[chunk 24] - kind: ambiguity | sections: §14.6.3 | readings: the behavior ID is literally a 32-bit value (content hash computed/truncated at u32 width, collisions possible) vs the ID is a full-width content-addressed hash and the stated u32 width conflicts with content-addressing | impact: determines ID width, collision exposure, and what the behavior table and hot-reload matching key on | quarantined: "Each behavior is identified by a stable u32 ID assigned at compile time, computed as a 32-bit content hash of the canonicalized typed IR." vs "Each behavior is identified by a stable content-addressed hash of its canonicalized typed IR; the u32 designation names the runtime handle, not the hash width."
[chunk 24] - kind: ambiguity | sections: §14.7.2 | readings: the compiler literally inserts a drop call at a value's point of consumption (coherent only as dropping an assignment target's previous value there; incoherent for moves into function parameters) vs consumption merely transfers drop responsibility with the move and empties the source's drop slot, with no call inserted at that site | impact: whether the drop of an overwritten assignment target's previous value at the assignment point is specified by the four drop points or left unspecified | quarantined: "The compiler inserts a drop call at a value's point of consumption (move into a function parameter or assignment); the moved-out source's drop slot is empty thereafter." vs "A value's point of consumption (move into a function parameter or assignment) transfers drop responsibility with the value; the moved-out source's drop slot is empty thereafter."
[chunk 24] - kind: incoherence | sections: §14.7.5, §14.4, §14.8.1 | readings: on node/connection instance removal only attr and derived cells receive Drop, leaving teardown of recurrent cells (and stream metadata cells) unspecified vs "attr and derived" abbreviates all of the instance's reactive cells, all dropped per §14.7, consistent with §14.4 listing recurrent/stream as instance reactive state and with §14.8.1 step 5's unqualified "for each removed cell: invoke drop per §14.7" | impact: whether Drop impls run for values held in a removed instance's recurrent cells — resource-leak exposure under the literal reading | quarantined: "When a node or connection instance is removed (removal mechanics per §13.15), its attr and derived cells are dropped per their type's Drop impl." vs "When a node or connection instance is removed (removal mechanics per §13.15), all of its cells — attr, recurrent, derived, and stream metadata — are dropped per their types' Drop impls."
[chunk 24] - kind: ambiguity | sections: §14.2.1, §14.1.1 | readings: ductus check runs the full five-pass frontend of §14.1.1 including monomorphization ("runs the frontend" governs and the parenthetical is illustrative) vs check runs only the four enumerated passes and skips monomorphization (the parenthetical is exhaustive, listing exactly passes 1–4 of §14.1.1) | impact: whether monomorphization-stage processing and any diagnostics it yields are part of ductus check and LSP editor integrations | quarantined: "ductus check runs the frontend — all five passes of §14.1.1, including monomorphization — without invoking either backend." vs "ductus check runs lexing, parsing, type checking, ownership checking, and reactive analysis (the frontend minus monomorphization) without invoking either backend."
[chunk 24] - kind: incoherence | sections: §14.7.1, §14.7.3 vs §11.8.5, §11.8.3, §11.3.1 | readings: drop bodies "may move fields out" and drop glue drops "fields left un-moved", but `move` accepts only bare l-value identifiers and field access never consumes — `move x.field` is admitted after all vs an unspecified construct vs dead spec text | impact: `move`'s operand grammar; whether `Drop` bodies can release fields; distinct from the ledgered §14.7.2 item | quarantined: none (syntax-sweep discovery, orchestrator-verified)
[chunk 25] - kind: contradiction | sections: §15.4.4, §14.6.3 | readings: behavior IDs are u32 integers with `B@aa10` a 32-bit hex rendering vs IDs are wider content-addressed hashes rendered abbreviated in the text form | impact: ID width fixes collision and text-form interop semantics across implementations | quarantined: none
[chunk 25] - kind: ambiguity | sections: §15.4.1, §15.4.5 | readings: gating is encoded per gated instance (compiled predicate plus `gate_parent` instance path) vs the text form uses named gate entries with `guards` lists that guarded entries reference by gate id (`gate App.g0 ... guards [App.print:0]`) | impact: the normative text-form shape for gates differs between readings | quarantined: A graph text-form gate entry carries `pred`, `inputs`, and a `guards` list of guarded instance paths: `gate App.g0 pred B@d4 inputs [App.show] guards [App.print:0]`. (§15.4.5)
[chunk 25] - kind: incoherence | sections: §15.4.4, §15.4.5 | readings: a desired-builder returns the whole desired record that the runtime scatters into per-field desired cells vs each desired cell's behavior returns that cell's own field-typed value | impact: the worked example types `App.print:0.text` as `str` while its behavior `B@d5` returns `%TextRec`, a mismatch under either strict reading | quarantined: none (the §15.4.4 ABI row "output: the desired record" is serialized as stated)
[chunk 25] - kind: ambiguity | sections: §15.4, §15.4.1 | readings: a cell entry's `type` admits only §4.1 primitives plus the two pool-index types (aggregates live behind `pool_index`) vs cells may carry any §15.4 vocabulary type as tag plus size/align | impact: representability of record/tuple-typed cells in the graph, including effect desired records, differs | quarantined: none
[chunk 25] - kind: ambiguity | sections: §15.4, §15.4.1, §15.4.5 | readings: the §15.4.1 abstract record (cell entries with observability/cadence/size fields plus separate dependency-edge lists) determines the normative text form, the §15.4.5 module text being illustrative vs the §15.4.5 text form (cell lines carrying role=/uses/inputs/init/depth inline, omitting observability/size/alignment, with no grammar given for the module/types/graph sections — only behaviors have one, §15.4.4) is itself the normative serialization | impact: §15.4 makes the text form normative and §15.7.3 defines interop against it, yet the graph section's concrete syntax is underdetermined | quarantined: A graph text-form cell line reads `cell App.total : i32 role=recurrent uses B@d2 inputs [App.count] depth 1 init 0`. (§15.4.5)
[chunk 25] - kind: incoherence | sections: §15.4.1, §15.4.5 | readings: every surface connection/`|>` lowers to a `connection` entry per the desugar map vs a `|>` at an effect pipe-position lowers only to effect `parameter_bindings` (the worked example emits no connection entry for `label |> print` and the scope's `exposes` is empty) | impact: whether effect pipes produce connection entries affects graph contents, `exposes` traversal, and hot-reload diffing | quarantined: none
