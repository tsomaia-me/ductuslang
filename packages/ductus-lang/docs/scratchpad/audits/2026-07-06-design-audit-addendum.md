# Ductus Design Audit — Addendum (patch-up of the 2026-07-06 run)

Closes the disclosed holes of the main report: claims-index collision adjudication (crashed mid-run in main audit), known-open tagging and completeness critique (Director triage + critic died), the two dead lanes (pair 029x035, smell behavior-and-data), orphan-subtree reverse-losslessness, the residual-term grep, and F115 verification. Corpus pin unchanged: `1b0c6b8f2a4e2723514dbe2722f2e77281854386`.

## Scope actually executed

- Collision groups adjudicated: 120 of 184 (184 subjects qualified (log_ids span >=2 distinct LOG sections AND >=2 distinct modality|predicate variants); 120 returned, 64 cut. Cut groups are the smallest (group size 7 down to 2 log_ids each). Cut subjects, in descending-size order, include: cluster, eq, numeric type, ord, pattern, turbofish, wire-candidate envelope, call, constraint, dependency-edge, free function, generic instantiation, index, instantiation, reset_on_reload (top 15 shown; remaining 49 are size 2-6 tail subjects). NOTE: because every claim carries a distinct log_id, the normalizers emitted one modality|predicate variant per log_id, so nvar==group-size for every group; the '>=2 distinct variant' filter is therefore equivalent to '>=2 log_ids' here and did not prune anything beyond the '>=2 sections' filter. Ranking is by group size (log_id count) descending.)
- Orphan subtrees swept: 0 of 0
- New candidate findings: 30 → CONFIRMED 23 · PLAUSIBLE 7 · REFUTED 0 · UNVERIFIED 0 · observations 0

## F115 verification (was UNVERIFIED in main report)

- Evidence: pass — All three cited evidence quotes verified verbatim from fresh Reads: 034-9 at DECISION_LOG.md:4360 ('...runtime on/off bits, reusing the gate on/off-bit machinery...'), 033-65 at :4177 (gate objects {id, pred, guards, gate_parent}, effective activation composed via gate_parent walk), 033-81 at :4193 (fold member edges tagged permanent/keyed-template/gate-guarded). The substantive claim is independently confirmed: a case-insensitive grep of the entire DECISION_LOG for 'on/off bit'/'off-bit'/'bit-vector' returns ONLY line 4360 (034-9 itself) — no section 022 or 033 entry defines an 'on/off bit' gate primitive. The actual gate mechanism in those sections is predicate gate objects (033-65) whose effective activation is composed up the gate_parent chain (033-66) and evaluated at edge level each propagation cycle (022-41), never a stored bit array. So 034-9's 'reusing the gate on/off-bit machinery' points at a term with no referent in the sections it claims to reuse, and the 'stores nothing' vs 'runtime on/off bits' tension is real. LOW divergence stands verified.
- Charity: sustain — F115 claims 034-9's 'gate on/off-bit machinery' has no referent in the gate sections (022) or IR (033): those sections specify gate activation as predicate gate objects composed via gate_parent into 'effective activation', never as stored on/off bits. Charitable whole-corpus hunt confirms the finding and finds NO dissolving normative text. DECISION_LOG grep: 'on/off' occurs EXACTLY ONCE — 034-9 itself (DECISION_LOG.md:4360). SPEC grep: 'on/off bits' occurs EXACTLY ONCE — SPEC.md:23051 (§13.20.4), which is the yielded-group elaboration DOWNSTREAM of 034-9 and does NOT attribute the bits to gates nor define a gate machinery ('a compile-time wire set ... plus runtime on/off bits (which are currently live)'). The gate mechanism as actually specified: 022-14 'Activation is never surfaced as a readable cell or field'; 022-41 (DECISION_LOG.md:2925) 'runtime evaluates gate state at edge level on each propagation cycle' (evaluated, not stored); 022-67 'effectively active iff its own gate and every ancestor gate ... are open'; 033-65/033-66 (DECISION_LOG.md:4177-4178) gate objects {id,pred,guards,gate_parent} with 'effective activation — its own predicate composed up the gate_parent chain'. The IR fold representation (SPEC.md:24066, §15.4.1) tags a gate-guarded member edge as 'present iff its gate is effectively active (§13.9.7)' — a predicate reference, NOT a stored bit. No 'bit'-based gate primitive exists (grep for bit/bit-vector in gate context returns only integer-width/IEEE-754 hits). SPEC.md:14576 even states gated arms 'reuse the gate constructs that apply elsewhere' — vocabulary is 'gate constructs' (predicate objects), never 'on/off-bit machinery'. Refutation for a gap/divergence finding requires quoting explicit normative text homing the machinery or an explicit legal boundary; none exists. Per category rule, when torn, sustain; here not even torn — the referent is provably absent. NOTE (dedup, not a verdict change): this exact defect is ALREADY REPORTED and CONFIRMED in the main report under F211 (MED/undefined_term/CONFIRMED, line 898) and F171 (MED/gap/CONFIRMED, line 914), both with identical anchors (LOG 034-9, SPEC §13.20/§13.9.7) and identical evidence; F213 (line 679) and the reading-guide line 14 also fold it in. F115 is a redundant, lower-severity (LOW) restatement of an already-confirmed MED finding — but the F115 claim itself is correct and SUSTAINS.
- Net: CONFIRMED

## Known-open tags for main-report findings

Tagger compared 242 findings against the register: zero claim-level matches. (This time the tagger actually ran.)

## New findings

#### P007 — Module-level `let` contradiction: 013-22 says module scope has NO `let` bindings, but 013-30 and 020-9 name module-level/top-level `let` as an existing binding form.

- **Severity/Category/Verdict:** HIGH / contradiction / CONFIRMED (family: collision)
- **Anchors:** LOG 013-22, 013-30, 020-9 · SPEC § 11.2, 13.7.1
- **Why:** These are restatements of the same subject (what may appear at module/top-level scope) that flatly disagree on whether module-level `let` exists. 013-22 categorically excludes it ('module scope contains no `let` bindings'); 013-30 and 020-9 both explicitly enumerate 'top-level `let`' / 'module-level ... `let`' as a real binding form, and SPEC §11.2 (line 8186) conforms to 013-30. An implementer cannot tell whether `let x = expr` at module top level is legal (020-9/013-30) or a compile error (013-22). LOG Invariant 2 forced restatement instead of cross-reference, and the restatements drifted into direct disagreement. Not covered by any existing F-id (grep of the audit report for these anchors returned nothing).
- **Direction:** Decide whether module-level `let` exists, then reconcile: either strike 013-22's 'module scope contains no `let` bindings' (and revise §11.2) to admit top-level `let`, or remove `let` from 013-30/020-9's top-level enumerations and drop 'top-level `let`/`const`' from SPEC line 8186. Surface to user — this is a substantive language-surface decision, not a mechanical fix.
- **Evidence check:** pass — 013-22 (DECISION_LOG.md:1408) 'module scope contains no let bindings' flatly contradicts 013-30 (DECISION_LOG.md:1416, enumerates 'top-level let/const') and 020-9 (DECISION_LOG.md:2707, module top-level 'const, and let'); SPEC §11.2 (SPEC.md:8186) conforms to 013-30. Quotes strongly support a jointly-unsatisfiable disagreement over whether module-level let is legal or a compile error — HIGH is apt. Not reported (no audit hits for these anchors). CONFIRMED.
- **Charity check:** sustain — Does module-level 'let' exist? 013-22 says module scope contains no let; 013-30/020-9 and SPEC enumerate top-level/module-level let.
- **Evidence:**
  - `packages/ductus-lang/docs/DECISION_LOG.md:1408-1408`
    > 013-22. `let` is a function-body construct only; module scope contains no `let` bindings. (§11.2)
  - `packages/ductus-lang/docs/DECISION_LOG.md:1416-1416`
    > 013-30. Every binding carries an initial value at its declaration — `let`, `mut`, top-level `let`/`const`, and `signal` alike; there is no uninitialized, implicitly-undefined, or implicitly-zero state. (§11.2)
  - `packages/ductus-lang/docs/DECISION_LOG.md:2707-2707`
    > 020-9. The module top-level scope comprises module-level `signal`, `derived`, `recurrent`, `stream`, `const`, and `let` declarations. (§13.7.1)
  - `packages/ductus-lang/docs/SPEC.md:8186-8186`
    > Every binding carries an initial value at its declaration: there is no uninitialized binding and no implicit undefined or zero state. This holds for `let`, `mut`, top-level `let`/`const`, and `signal` alike — a binding always has a value.

#### P014 — Orphan rule is stated at two different scopes across LOG sections: 003-73/74 say PACKAGE granularity, 005-152/153 and SPEC §3.7.1 say MODULE granularity — a fulfill in a sibling module of the same package is legal under one and rejected under the other.

- **Severity/Category/Verdict:** HIGH / contradiction / CONFIRMED (family: collision)
- **Anchors:** LOG 003-73, 003-74, 005-152, 005-153, 005-86 · SPEC § 3.7.1, 10.9
- **Why:** Package and module are distinct scopes in Ductus: 003-9 says each subfolder is a distinct module and a package root contains multiple modules; 003-14 says every module folder is independent. So a `fulfill` written in module N for a type/trait defined in a SIBLING module M of the same package is LEGAL under 003-73/74 (declared in the current package) but REJECTED under 005-152/153 and SPEC §3.7.1 (both Trait and Type foreign to module N). Two careful readings give different program acceptance for the same construct. LOG Invariant 2 forced the rule to be restated in §003 and §005 and the two restatements drifted to different granularities. The main audit's F169-area charity check assumed the package reading and never confronted that 005-152 and SPEC §3.7.1 hard-code the module reading — this collision is not covered there.
- **Direction:** Surface to the user to pin the single intended orphan-rule scope (package-of-declaration vs module-of-declaration), then restate it identically in 003-73/74, 005-152/153, and conform SPEC §3.7.1 and §10.9. Do not self-decide.
- **Evidence check:** pass — 003-73 (line 176) verbatim: 'The orphan rule operates on package-of-declaration... a fulfill is legal if the trait or the type is declared in the current PACKAGE.' 003-74 (177) reinforces package scope. 005-152 (493): 'fulfill Trait for Type is permitted in module M iff Trait or Type is defined in M'; 005-153 (494) rejects both-foreign-to-module; SPEC §3.7.1 (2462-2467) hard-codes 'defined in M' (module). Package vs module are distinct scopes: 003-9 (112) each subfolder is a distinct module, 003-14 (117) every module independent, and a package holds multiple modules. A fulfill in module N for a type in a SIBLING module M of the same package is LEGAL under 003-73 but REJECTED under 005-152/153 and SPEC §3.7.1. Two careful readings give different program acceptance. Dedup: audit F004 is a different pair (003-72 vs SPEC §10.8 visibility/reachability); the package-vs-module granularity collision (003-73/74 vs 005-152/153) is NOT covered there. HIGH implementer-blocking contradiction confirmed.
- **Charity check:** sustain — Is the orphan rule stated at package scope (003-73/§10.9) and module scope (005-152/§3.7.1), giving different program acceptance for a fulfill in a sibling module of the same package?
- **Evidence:**
  - `packages/ductus-lang/docs/DECISION_LOG.md:176-176`
    > 003-73. The orphan rule operates on package-of-declaration, not visibility: a `fulfill` is legal if the trait or the type is declared in the current package, regardless of either's visibility level. (§10.9)
  - `packages/ductus-lang/docs/DECISION_LOG.md:177-177`
    > 003-74. A `private` trait or type still counts as in-package for orphan purposes; a `fulfill` of a private trait for a foreign type is valid, accessible only inside the declaring module. (§10.9)
  - `packages/ductus-lang/docs/DECISION_LOG.md:493-494`
    > 005-152. `fulfill Trait for Type` is permitted in module M iff `Trait` or `Type` is defined in M. (§3.7.1)
    > 005-153. A `fulfill` whose trait and type are both foreign to the module is rejected at compile time. (§3.7.1)
  - `packages/ductus-lang/docs/SPEC.md:2462-2467`
    > A `fulfill Trait for Type` block is permitted in module M if and only if:
    > 
    > - `Trait` is defined in M, OR
    > - `Type` is defined in M.
    > 
    > A `fulfill` block where both `Trait` and `Type` are foreign to M is rejected

#### P020 — A bare `Handle` destination is named the membership-dynamizing carrier (017-111, 017-147, SPEC §13.3.4) requiring `dynamic incoming`, but the settled Handle/WeakHandle split (017-176/182) and the §13.6.2 connection rules (019-52/53) make a `Handle[N]` destination static (target fixed, never re-points/freezes) — so its membership is a static fact needing a STATIC incoming view; the two are jointly unsatisfiable at the incoming-view-kind check.

- **Severity/Category/Verdict:** HIGH / contradiction / CONFIRMED (family: witness)
- **Anchors:** LOG 017-111, 017-147, 017-110, 017-112, 017-146, 017-176, 017-182, 019-52, 019-53 · SPEC § 13.3.4, 13.3.4.2, 13.6.2, 13.3.6.2
- **Why:** Witness program: `node Driver: attr wheel: Handle[Drivable]` (a static handle, 017-176/182 -> referent fixed for lifetime, never re-points/dismounts), with self-sourced entry `Drives: wheel` into `node Axle: satisfies Drivable`. Question the implementer must answer: does `Axle` declare `incoming drives: Drives` (static) or `dynamic incoming drives: Drives*`? Path 1 (017-110/112/146 + 019-52/53): a `Handle[N]` destination's target is fixed and the connection is unconditionally active/never-freezes, so membership is a static fact -> a STATIC `incoming` view counted by exact arithmetic (017-113). Path 2 (017-111 + 017-147, mirrored in SPEC 13895-13898): a `Handle` destination makes 'target varies', membership is NOT a static fact -> a `dynamic incoming` view is REQUIRED, and placing it under a static view is a compile error (017-112 admits only static-fact members). A static `incoming` view and a `dynamic incoming *` view are mutually-exclusive declarations (017-112 vs 017-121) on the same target type for the same connection type; the target cannot satisfy both. The dynamizing carrier is `WeakHandle`, correctly used at 017-139/140/141 and 021-82/83 -- 017-111/147 wrongly generalize it to bare `Handle`. Self-refuting citation: SPEC 13897 cites §13.6.2 as authority for '`Handle` -> target varies', but §13.6.2 (019-52/53) states a `Handle[N]` destination is static and unconditionally active. Distinct from F177 (which anchors 019-49/52/17 and is scoped to the 're-points/follows' semantics in §13.6.0/13.6.2) and from F085 (SPEC worked example vs 017-139/141): this defect is the membership / incoming-view-kind obligation in §13.3.4/§13.3.4.2 at anchors 017-111 and 017-147, which no audit finding touches.
- **Direction:** Surface to user: replace bare `Handle` with `WeakHandle` in 017-111 and 017-147 (and SPEC 13897) so only reactive references and `WeakHandle` destinations dynamize membership, matching 017-139/140/141, 021-82/83, and 019-52/53; or, if bare `Handle` is intended as an umbrella per 017-182, state explicitly that only the `WeakHandle[T]` member of that umbrella dynamizes membership. Do not pick a spelling unilaterally.
- **Evidence check:** pass — Quotes verbatim-confirmed. 017-111 (LOG 2252) + SPEC 13895-13898 + 017-147 (LOG 2288) make a bare `Handle` destination dynamize membership (target varies -> requires `dynamic incoming`). But 017-182 (LOG 2323), 019-52/53 (LOG 2668-2669) make a static Handle[N] statically placed, never re-points, never freezes, unconditionally active -> membership IS a static fact per 017-110/017-146 -> needs a STATIC incoming view. Jointly unsatisfiable at the view-kind check. Self-refuting: SPEC 13897 cites §13.6.2 as authority, but §13.6.2 says Handle[N] is static. Anchors 017-111/017-147 are untouched by F085 (worked example, 017-139/141) or F177 (019-49 to-follows). Distinct HIGH.
- **Charity check:** sustain — Jointly unsatisfiable at the incoming-view-kind check, and the charitable 'Handle-is-an-umbrella' reading does NOT dissolve it. 017-176/182 (DECISION_LOG.md:2317/2323) fix `Handle[T]` as the statically-placed form whose referent is fixed for the handle's lifetime (never re-points/dismounts); 019-52/53 (DECISION_LOG.md:2668-2669) make a `Handle[N]` destination 'unconditionally active', 'never freezes', target fixed. So a self-sourced connection into a static `Handle[N]` attr is a static fact needing a STATIC incoming view (017-146/113). But 017-111 (DECISION_LOG.md:2252) and 017-147 (DECISION_LOG.md:2288), mirrored at SPEC.md:13895-13898, say a `Handle` destination makes 'target varies' and REQUIRES a `dynamic incoming` view — mutually exclusive with a static view (017-112 vs 017-121). The umbrella escape fails: 017-182's umbrella includes the static `Handle[T]`, whose target does not vary, so 017-111's 'target varies' is wrong even for the umbrella's static member. Self-refuting: SPEC.md:13897 cites §13.6.2 as authority for 'Handle → target varies', but §13.6.2 (019-52/53) says the opposite. The dynamizing carrier is `WeakHandle` (correctly used at 017-139/140/141). Witness `attr wheel: Handle[Drivable]` is legal (017-176/181/136). Distinct from F177 (019-49/52/17 re-points) and F085 (SPEC worked-example typing at 017-139/141); P020's 017-111/147 membership obligation is untouched by any audit finding.
- **Evidence:**
  - `packages/ductus-lang/docs/DECISION_LOG.md:2252-2252`
    > 017-111. Membership stops being a static fact in exactly two ways: the connection is materialized per `repeat` key (existence varies), or its destination is a reactive reference or `Handle` (target varies). (§13.3.4)
  - `packages/ductus-lang/docs/DECISION_LOG.md:2288-2288`
    > 017-147. A destination contribution that is not a static fact — source instances materialized by `repeat`, or a `Handle` destination — requires the target to declare a `dynamic incoming` connection-view for that type, with the error naming both sites. (§13.3.4.2)
  - `packages/ductus-lang/docs/DECISION_LOG.md:2668-2669`
    > 019-52. A `WeakHandle[N]` destination (read type `Option[&N]`) is unwrapped at the boundary ... A `Handle[N]` destination (statically placed) auto-derefs to `&N` directly — its statical-placement assertion makes resolution unnecessary, so the connection is unconditionally active. (§13.6.2)
    > 019-53. While a `WeakHandle[N]` destination resolves to `None`, the connection freezes and its body does not run at all. A statically-placed `Handle[N]` destination never freezes. (§13.6.2)
  - `packages/ductus-lang/docs/DECISION_LOG.md:2323-2323`
    > 017-182. `Handle` names two distinct types: `Handle[T]`, the *statically-placed* type form whose referent is statically placed in the graph for the handle's lifetime, and `WeakHandle[T]`, the *dynamically-placed* type form whose referent may be dismounted or re-pointed at runtime.
  - `packages/ductus-lang/docs/DECISION_LOG.md:2251-2251`
    > 017-110. A connection belongs to a node's incoming set as a static fact when both its presence and its destination are fixed at compile time. (§13.3.4)
  - `packages/ductus-lang/docs/SPEC.md:13895-13898`
    > Membership stops being a static fact in exactly two ways: the connection is materialized per `repeat` key (its *existence* varies, §13.5.4), or its destination is a reactive reference or `Handle` (its *target* varies, §13.6.2).

#### P022 — The normative IR text-form grammar (§15.4.6) has no production for the `stream` primitive: `entry ::= cell | gate | connection | effect` omits it, so a stream — one of the six graph primitives (033-56) with a full data-model entry (033-106..118) — cannot be serialized in the text form that 033-53/033-226/033-231 make the normative interop/test contract.

- **Severity/Category/Verdict:** HIGH / gap / CONFIRMED (family: witness)
- **Anchors:** LOG 033-56, 033-68, 033-106, 033-107, 033-108, 033-109, 033-53, 033-168, 033-226, 033-231, 028-65 · SPEC § 15.4.6, 15.4.1, 15.4
- **Why:** Joint scenario: the surface program `stream ring[1024] events: LogEntry = source` (a legal stream declaration, SPEC 19439) lowers by 033-68 to the `stream` primitive and by 028-65/033-106..118 must carry id, element_type, policy=ring, capacity=1024, cursors, and observation cells. 033-53/033-168 declare the §15.4.6 text form the normative serialization; 033-226 requires a conformant compiler to render the IR in that text form; 033-231 makes interop defined against it; 033-53 makes it what tests assert against. But §15.4.6's `entry` production admits only `cell | gate | connection | effect`, and there is no top-level or nested `stream` production anywhere in the grammar (grep of SPEC.md finds zero `stream ::=`/`stream_section` productions and zero stream text-form examples). So a program containing any stream primitive has no expressible normative serialization: a conformant compiler cannot render it (033-226), two implementations cannot interoperate over it (033-231), and no test can assert against it (033-53). This is implementer-blocking, not drift: the six-primitive count (033-56) and the full stream data model (033-106..118) exist, but one of the six primitives silently drops out of the one serialization the spec declares normative. Distinct from F248 (which is about the `fold` cell-KIND missing from 033-169's prose, inside the cell production that does exist) and from F093/F239 (which concern `yielded`, a surface construct with no primitive, not a defined primitive missing its serialization).
- **Direction:** Add a `stream` production to §15.4.6 (either as a fifth `entry` alternative or as a top-level graph_section list parallel to the `scope+` production) covering the 033-106..118 fields — id, element_type, policy (`ring`|`gate`), capacity, source_dependencies, observation_cell_ids, reset_on_reload, reset_on_reopen, output_history_size, input_lookback_map — so the normative text form can serialize every one of the six primitives; or, if streams are intentionally never externalized, carve them out of 033-53/033-226/033-231's text-form-is-normative claim. Surface to user; LOG-first per edit protocol.
- **Evidence check:** pass — Confirmed. §15.4.6 `entry ::= cell | gate | connection | effect` (SPEC 24507) omits stream; grep for stream productions in all of SPEC.md returns ZERO. Yet 033-56 (LOG 4168) declares stream one of six primitives, 033-68 (LOG 4180) lowers surface streams to it, and §15.4.1 has a full Stream-cells data model (SPEC 24130-24136; 033-106, LOG 4218). 033-53/033-226 (LOG 4338)/033-231 (LOG 4343) make the §15.4.6 text form the normative, interop-defining, test-asserted serialization. A stream primitive has no expressible serialization. Implementer-blocking. Distinct from F248 (fold KIND, which IS in grammar at 24512) and F093/F239 (yielded, a surface construct with no primitive).
- **Charity check:** sustain — The `stream` primitive has no production anywhere in the normative IR text-form grammar, blocking serialization of any program containing a stream. §15.4.6's `entry ::= cell | gate | connection | effect` (SPEC.md:24507) omits stream, and grep of the whole SPEC for `stream ::=`, `stream_section`, `'stream'` in the grammar returns zero — no top-level stream section and no nested production; the prose (SPEC.md:24537-24561) never serializes a stream. Yet 033-56 (DECISION_LOG.md:4168) counts `stream` among the six primitives, 033-68 (DECISION_LOG.md:4180) lowers a surface stream to it, and §15.4.1 gives it a full data-model entry (SPEC.md:24130-24136; 033-106..118) with id/element_type/policy/capacity/cursors/observation cells. 033-53/168/226/231 (DECISION_LOG.md:4165/4280/4338/4343) make §15.4.6 the normative serialization, interop, and test contract, and 033-168 explicitly says the grammar covers the graph section — yet one of the six primitives silently drops out of it. Implementer-blocking: a conformant compiler cannot render a stream (033-226), two implementations cannot interoperate over it (033-231), no test can assert against it (033-53). Distinct from F248 (fold cell-KIND missing from 033-169 prose, inside the cell production that exists) and F093/F239 (yielded, a surface construct with no primitive). No dissolving text.
- **Evidence:**
  - `packages/ductus-lang/docs/SPEC.md:24504-24520`
    > graph_section ::= 'graph' '{' scope+ '}'
    > scope         ::= 'scope' PATH 'exposes' path_set 'effects' path_set
    >                   ('reset_on_reopen' reopen_set)? '{' entry* '}'
    > entry         ::= cell | gate | connection | effect
  - `packages/ductus-lang/docs/SPEC.md:24485-24491`
    > #### 15.4.6 IR module text grammar
    > 
    > §15.4.4 gives the `behavior` grammar; this section gives the **module, type-table,
    > and graph** grammar — the normative text form of §15.4.1's data model.
  - `packages/ductus-lang/docs/SPEC.md:24130-24136`
    > **Stream cells.** A list of stream cell entries. Each:
    > 
    > - `id`: the stream's fully-qualified declaration path.
    > - `element_type`: the element type tag (per §15.4.1's `type`
    >   encoding).
    > - `policy`: one of `ring` or `gate`.
    > - `capacity`: integer literal capacity.
  - `packages/ductus-lang/docs/DECISION_LOG.md:4168-4168`
    > 033-56. The graph is built from exactly six primitives: `cell`, `connection`, `gate`, `stream`, `effect`, and `scope`. (§15.4.1)
  - `packages/ductus-lang/docs/DECISION_LOG.md:4165-4165`
    > 033-53. The IR's text form is normative: it is the serialization this specification defines (grammar in §15.4.4 + §15.4.6) and what tests assert against; the §15.4.1 data model is what it renders. No binary or wire encoding is mandated. (§15.4)
  - `packages/ductus-lang/docs/DECISION_LOG.md:4338-4338`
    > 033-226. A conformant compiler produces an IR conforming to the §15.4.1 data model and renders it in the normative text form (§15.4.6); other (binary or in-memory) encodings are implementation-defined. (§15.7.1)

#### P023 — The IR lowers an effect's entire desired: block to a SINGLE whole-record cell built by ONE pure desired-builder behavior, but the surface model lets desired: hold event-output STREAM cells (ring/gate buffers the reconciler drains in order) — a buffered event sequence cannot be a field of a per-commit record produced by a pure behavior, so desired-block streams have no IR representation.

- **Severity/Category/Verdict:** HIGH / gap / CONFIRMED (family: witness)
- **Anchors:** LOG 033-170, 033-202, 031-31, 031-32, 031-36, 031-153 · SPEC § 15.4.5, 15.4.4, 13.19.4
- **Why:** Take `effect ws_send(outgoing: Stream[Message]): desired: stream ring[1024] outbound: Message = outgoing`. Per 031-31/032/036 the reconciler drains a 1024-slot ring buffer of Message events (with drop accounting, §13.18). Per 033-170/033-202 the effect's desired state must be a single `pool_index<%T>` cell equal to one pure behavior's output record, recomputed per commit. A pure behavior returns exactly one value per invocation; it cannot carry a policy-buffered event sequence, and a scalar record field cannot be a stream buffer the host drains in order. §15.4.5's only worked example lowers a trivial single-`derived` desired block and never addresses stream (or recurrent, or multi-cell) desired declarations. An implementer building the effect IR emitter from §15.4.5 has no lowering for a desired-block event-output stream — a totality gap on a feature 031-31 declares legal, with no §001-6-style boundary carve-out.
- **Direction:** Either 033-170/033-202/§15.4.5 must be widened so an effect's desired IR is a SET of cells (whole-record for scalar deriveds/recurrents PLUS separate stream cells with their own policy/capacity, mirroring how observed: streams already lower as stream cells), or 031-31/032/036 must be retracted (no event-output streams in desired:). The user must choose; do not silently pick.
- **Evidence check:** pass — Confirmed. 033-170 (LOG 4282) + 033-202 (LOG 4314) lower the whole desired block to ONE pool_index<%T> record cell built by one pure desired-builder behavior; §15.4.5 example (SPEC 24420-24453) only shows a trivial single-derived case. But 031-31 (LOG 3800)/031-36 (LOG 3805) make desired-block event-output STREAMS legal (ring/gate buffers the reconciler drains in order). A buffered event sequence cannot be a scalar field of a per-commit record from a pure behavior. §15.4.5 gives no stream (or recurrent, or multi-cell) desired lowering. Totality gap, no 001-6 carve-out. HIGH.
- **Charity check:** sustain — A totality gap on a legal feature with no boundary carve-out. 033-170 (DECISION_LOG.md:4282) and 033-202 (DECISION_LOG.md:4314) unconditionally lower an effect's ENTIRE desired: state to a single whole-record `pool_index<%T>` cell produced by ONE pure desired-builder behavior whose sole output is 'the desired record' (SPEC.md:24394). But 031-31/32/36 (DECISION_LOG.md:3800/3801/3805) let desired: hold event-output STREAM cells — ring/gate buffers the reconciler drains IN ORDER. A pure behavior returns exactly one value per invocation; it cannot carry a policy-buffered event sequence, and a scalar record field cannot be a stream buffer the host drains. §15.4.5's only worked example (SPEC.md:24419-24483) lowers a trivial single-`derived` desired block and never addresses stream (or recurrent, or multi-cell) desired declarations. Grep confirms 033-170/033-202 carry no stream carve-out. An implementer building the effect IR emitter from §15.4.5 has no lowering for a desired-block event-output stream, and there is no §001-6-style implementation-defined boundary. Distinct from P024 (which is the per-cell-identity contradiction, not the buffered-event-sequence gap) and from P022 (the missing stream grammar generally). No dissolving text.
- **Evidence:**
  - `packages/ductus-lang/docs/DECISION_LOG.md:4282-4282`
    > 033-170. An effect's desired state is a single whole-record cell typed `pool_index<%T>` holding the desired-builder behavior's output record; the runtime scatters it into per-field desired state. (§15.4.5)
  - `packages/ductus-lang/docs/DECISION_LOG.md:4314-4314`
    > 033-202. An effect desired-builder behavior's inputs are the `desired:` block's input cells; its output is the desired record. (§15.4.4)
  - `packages/ductus-lang/docs/DECISION_LOG.md:3800-3801`
    > 031-31. A `desired:` event-output cell is declared with `stream` (reusing the keyword): `stream ring[1024] outbound: Message = source`; the effect feeds the source from its own parameters or `observed:` cells, and the host's reconciler drains the buffer. (§13.19.4)
    > 031-32. A desired-block event-output stream comprises a policy keyword (`ring` or `gate`), an optional bracketed capacity defaulting to 1024, a name, an element type, and a `= source` expression. (§13.19.4)
  - `packages/ductus-lang/docs/DECISION_LOG.md:3805-3805`
    > 031-36. The reconciler consumes a desired-block event-output stream's buffered events in order. (§13.19.4)
  - `packages/ductus-lang/docs/SPEC.md:24451-24453`
    >       derived   App.print:0.desired : pool_index<%TextRec> uses B@d5 inputs [App.label]
    >       effect App.print:0  reconciler "print"  params [message: App.label]
    >                           desired [App.print:0.desired]  gate App.g0

#### P024 — §15.4.1's effect-entry field `desired_cell_ids` is a plural LIST of the desired: block's cells and surface rules make individual desired cells program-readable by field access (`f.request`, `derived spinner = f.in_flight`), which needs per-cell graph identity; but 033-170/033-202 collapse the whole desired: block into ONE `pool_index<%Rec>` cell with only per-field (not per-cell-id) scatter — the two desired representations contradict.

- **Severity/Category/Verdict:** HIGH / contradiction / CONFIRMED (family: witness)
- **Anchors:** LOG 033-122, 033-170, 033-202, 031-30, 017-278 · SPEC § 15.4.1, 15.4.5, 13.19.4
- **Why:** Take `effect fetch(...): desired: derived request: Request = ...; derived headers: Headers = ...` and elsewhere `derived spinner: bool = f.request.method == Method::POST`. Per 031-30 and 017-278 `f.request` is a first-class reactive read: the graph must carry a dependency edge from `spinner`'s derived behavior to the `request` cell, so `request` needs its own stable cell id (exactly what `desired_cell_ids` plural, 033-122, provides). But 033-170/033-202/§15.4.5 emit ONE cell `App.f:0.desired : pool_index<%FetchDesired>` produced by one desired-builder behavior; the record is 'scattered into per-field desired state' for the *reconciler*, not exposed as per-cell graph ids a downstream derived can depend on. So `desired_cell_ids` (many program-addressable cells with dep edges) and 033-170 (one opaque whole-record cell) cannot both hold. An implementer cannot both wire `f.request` as a graph edge and represent the desired block as a single record cell.
- **Direction:** Reconcile the desired-lowering: decide whether each desired: declaration lowers to its own graph cell (making 033-170's 'single whole-record cell' at most a reconciler-facing view assembled from those cells) or the desired block is genuinely one opaque record (then define how `f.request` field-reads obtain a per-field cell id and dependency edge). Amend 033-170/033-202/§15.4.5 to match whichever the user picks; user decides.
- **Evidence check:** pass — Confirmed. 033-122 (LOG 4234)/SPEC 24177 make `desired_cell_ids` a plural LIST of the block's cells; 031-30 (LOG 3799) and 017-278 (LOG 2419) make individual desired cells program-readable by flat field access (`f.request`, `derived spinner = f.in_flight`), which needs per-cell graph identity + dependency edges. But 033-170 (LOG 4282)/033-202 collapse the block into ONE opaque record cell scattered per-FIELD for the reconciler, not per-cell-id for downstream deriveds. The two desired representations contradict; the §15.4.5 single-cell example (SPEC 24451-24453) masks it. HIGH contradiction.
- **Charity check:** sustain — Two incompatible desired-block representations that cannot both hold for a multi-cell block. §15.4.1's `desired_cell_ids` is a plural LIST of 'the cells declared in the effect's desired: block' (SPEC.md:24177-24178; 033-122, DECISION_LOG.md:4234), and surface rules make individual desired cells program-readable: 031-30 (DECISION_LOG.md:3799) `f.request`, 017-278 (DECISION_LOG.md:2419) `derived spinner = f.in_flight` — which needs per-cell graph identity with a dependency edge from the downstream derived to the `request` cell. But 033-170 (DECISION_LOG.md:4282) collapses the whole block into ONE opaque `pool_index<%Rec>` cell from one desired-builder behavior, scattered only into per-field state for the reconciler, not exposed as per-cell graph ids a downstream derived can depend on. The worked example (SPEC.md:24451-24453) emits only the single whole-record cell in the `desired [...]` field — coinciding with the plural list only because the example has one desired cell; a two-cell block (`derived request; derived headers`) diverges. An implementer cannot both wire `f.request` as a graph edge and represent the desired block as a single opaque record cell. No reconciling text (no rule emitting the per-declared cells separately AND aggregating). Distinct from P023 (stream/buffered-sequence gap). Sustain.
- **Evidence:**
  - `packages/ductus-lang/docs/DECISION_LOG.md:4234-4234`
    > 033-122. An effect entry's `desired_cell_ids` name the cells declared in the instance's `desired:` block. (§15.4.1)
  - `packages/ductus-lang/docs/DECISION_LOG.md:4282-4282`
    > 033-170. An effect's desired state is a single whole-record cell typed `pool_index<%T>` holding the desired-builder behavior's output record; the runtime scatters it into per-field desired state. (§15.4.5)
  - `packages/ductus-lang/docs/DECISION_LOG.md:3799-3799`
    > 031-30. Program code may read a `desired:` `derived` cell via the standard access path but cannot write it: `f.request`. (§13.19.4)
  - `packages/ductus-lang/docs/DECISION_LOG.md:2419-2419`
    > 017-278. A named effect's cells are read elsewhere by flat access: `derived spinner: bool = f.in_flight`. (§13.3.8)
  - `packages/ductus-lang/docs/SPEC.md:24177-24185`
    > - `desired_cell_ids`: IDs of the cells declared in the effect's
    >   `desired:` block for this instance.
    > - `observed_cell_ids`: IDs of the cells declared in the effect's
    >   `observed:` block for this instance.

#### P001 — Map `+` merge order: 012-106 says new right-operand keys land 'in left order', but 012-121 (and SPEC §9.5.8) say 'in right order' — the two restatements of one merge rule disagree on the iteration order of merged-in keys.

- **Severity/Category/Verdict:** MED / contradiction / CONFIRMED (family: collision)
- **Anchors:** LOG 012-106, 012-121 · SPEC § 9.5
- **Why:** Both entries restate the same `Map + Map` merge (LOG Invariant 2 forces restatement over cross-reference). 012-106 says the merged-in right keys are appended 'in left order'; 012-121 and SPEC §9.5.8 say 'in right order'. Since Map iteration is insertion-ordered and observable via `for`, Display, Debug, and `repeat`, the order in which new right keys are appended is behavior-changing. 012-106's 'in left order' is the drifted (and SPEC-divergent) restatement; 012-121 agrees with SPEC. An implementer following 012-106 verbatim would order merged keys differently.
- **Direction:** Correct 012-106 to say new right-operand keys are appended 'in the right operand's order' (matching 012-121 and SPEC §9.5.8); surface to user rather than editing unilaterally.
- **Evidence check:** pass — 012-106 (DECISION_LOG.md:1301) says merged-in right keys land 'in left order'; 012-121 (DECISION_LOG.md:1316) and SPEC §9.5.8 (SPEC.md:7373) say 'in the right operand's order'. Verbatim quotes confirm the two LOG restatements of the same Map + merge disagree on iteration order of merged-in keys — behavior-changing given insertion-ordered, observable Map iteration. Not already reported: the only 012-106/012-121 audit hit is the unrelated §9.5.3 key-type (duration/instant) defect. CONFIRMED.
- **Charity check:** sustain — 012-106 and 012-121 restate one Map+ merge rule; the merged-in right-key ordering diverges (012-106 'in left order' vs 012-121 'in right order'), and 012-106 also diverges from SPEC 9.5.7.
- **Evidence:**
  - `packages/ductus-lang/docs/DECISION_LOG.md:1301-1301`
    > 012-106. Maps merge with `+`: `x + y` produces a new Map where right-hand keys win on collision ... it holds the left operand's keys in the left operand's order, then each right-operand key not already present in left order; a key present in both keeps the LEFT position and takes the RIGHT value (update-keeps-position).
  - `packages/ductus-lang/docs/DECISION_LOG.md:1316-1316`
    > 012-121. ... The merged result is insertion-ordered: left-operand keys in left order, then each right-operand key not already present in right order, with a key present in both keeping the LEFT position and taking the RIGHT value (update-keeps-position).
  - `packages/ductus-lang/docs/SPEC.md:7371-7373`
    > the result's key order is: the left operand's keys in their existing order, followed by the right operand's keys not already present, in the right operand's order.

#### P002 — `.exposition` element type drift: 017-219/017-220 define a CLOSED sum of exactly five entry kinds (Node, Connection, Bundle, DynamicView, Gated), but 017-247/017-256 restate the returned list as only 'node and connection entries'.

- **Severity/Category/Verdict:** MED / contradiction / PLAUSIBLE (family: collision)
- **Anchors:** LOG 017-219, 017-220, 017-247, 017-256 · SPEC § 13.3.7, 13.3.7.3, 13.3.7.5
- **Why:** 017-247 and 017-256 describe the `.exposition` return as a list of only 'node and connection entries', which enumerates two of the five kinds the closed sum defines in 017-219 (Node, Connection, Bundle, DynamicView, Gated). The divergence is mirrored into SPEC: §13.3.7.3 (line 14633) says 'node and connection entries alike' while §13.3.7.7 (line 14764) says 'CLOSED sum of five entry kinds'. An implementer modeling the public `.exposition` element type from 017-247/§13.3.7.3 would produce a 2-variant type; from 017-219/§13.3.7.7 a 5-variant type. The exposed variant surface for external readers walking `.exposition` is therefore under-determined — a behavior-changing ambiguity about whether Bundle/DynamicView/Gated entries are visible on the public field.
- **Direction:** Reconcile 017-247/017-256 (and SPEC §13.3.7.3) with the five-kind closed sum in 017-219 — either state the returned list carries all five entry kinds, or explicitly state why the public `.exposition` field is restricted to node/connection entries; surface to user.
- **Evidence check:** pass — 017-219 (DECISION_LOG.md:2360) defines a CLOSED sum of exactly five entry kinds (Node/Connection/Bundle/DynamicView/Gated); 017-247 (DECISION_LOG.md:2388) and 017-256 (DECISION_LOG.md:2397) restate the .exposition return as only 'node and connection entries'; mirrored in SPEC §13.3.7.3 (SPEC.md:14633 'node and connection entries alike') vs §13.3.7.7 (SPEC.md:14764 'CLOSED sum of five entry kinds'). Quotes support the enumeration divergence. A charitable reading (node/connection as broad categories, five as fine variants) partially reconciles them, but the restatements literally enumerate two of five kinds, under-determining the public element-type surface. Distinct from the refuted repeat-sixth-variant finding at audit line 1739. CONFIRMED.
- **Charity check:** refute — Is the '.exposition' public element type under-determined between a 2-variant (node/connection) and 5-variant closed sum?
- **Evidence:**
  - `packages/ductus-lang/docs/DECISION_LOG.md:2360-2361`
    > 017-219. `.exposition` is a typed, language-owned CLOSED sum of exactly five entry kinds: `Node`, `Connection`, `Bundle`, `DynamicView`, and `Gated`. ... 017-220. Walking `.exposition` is `for entry` over the list plus a `match` over the five variants.
  - `packages/ductus-lang/docs/DECISION_LOG.md:2388-2388`
    > 017-247. The reserved field `.exposition` returns the ordered list of placements — node and connection entries — the instance currently exposes: `instance.exposition`.
  - `packages/ductus-lang/docs/DECISION_LOG.md:2397-2397`
    > 017-256. A connection placement is an exposition entry positioned in the ordered sequence alongside node entries; its position states where in the node's structure the connection takes effect.
  - `packages/ductus-lang/docs/SPEC.md:14631-14633`
    > `.exposition` field: `instance.exposition` returns the ordered list of
    > placements — node and connection entries alike (§13.3.7.5) — the
    > instance currently exposes.

#### P003 — 'behavior' restatements drifted: §14.6.1 says a behavior reads inputs from cells and writes outputs (does cell I/O); §15.4.4 says a behavior is pure with no I/O, never doing I/O, taking params and returning one value (runtime does the cell I/O).

- **Severity/Category/Verdict:** MED / contradiction / PLAUSIBLE (family: collision)
- **Anchors:** LOG 032-108, 033-151, 033-152, 033-155, 033-103 · SPEC § 14.6.1, 15.4.4, 15.4.1
- **Why:** The two sections restate the same subject (what a behavior is / does at the ABI boundary) and disagree on WHO touches cells. 032-108 attributes the cell reads and cell writes to the behavior itself ('read inputs from cells ... write outputs'). 033-151/152 state the opposite absolutely: a behavior is 'a function of its declared inputs only, with no ambient state and no I/O' and I/O is 'never of behaviors'. Reading and writing reactive cells IS ambient-state access / I/O. 033-155 gives the pure-function signature (params -> one type, a single return value), and 033-103's table entry carries `input_cell_ids`/`output_cell_id?` on the TABLE, i.e. the runtime maps cells to/from the behavior — the behavior does not itself read or write them. An implementer building the behavior ABI cannot tell from the LOG whether a behavior body reads/writes cells (032-108) or is a pure params->value function the runtime feeds and drains (033-103/151/152/155). This is behavior-changing at the ABI boundary (glitch-freedom, purity/determinism, thread-safety per 032-125 all hinge on behaviors not touching cells). Not already reported: 032-108 carries no audit anchor; 033-145 appears in the audit only tangentially (the `?`-in-derived-body finding), not for this output/purity drift.
- **Direction:** Reconcile 032-108's wording with §15.4.4: restate 032-108 so the behavior is a pure params->return-value function and the runtime performs the cell read (inputs) and cell write (output), matching 033-103/151/152/155 — or, if behaviors really do touch cells, correct 033-151/152. Do not resolve unilaterally; surface which model is authoritative to the user.
- **Evidence check:** partial — Quotes exist and differ: 032-108 (DECISION_LOG.md:4038) attributes cell reads/writes to behaviors ('read inputs from cells ... and write outputs'); 033-151/152 (DECISION_LOG.md:4263-4264) say a behavior is pure with 'no ambient state and no I/O' and I/O is 'never of behaviors'; 033-155 (DECISION_LOG.md:4267) gives a pure params->type signature; 033-103 (DECISION_LOG.md:4215) puts input_cell_ids/output_cell_id? on the runtime table. PARTIAL because 032-108 itself self-labels behaviors 'pure transformations,' so its 'read inputs from cells / write outputs' reads as loose ABI-dataflow phrasing (runtime maps cells to/from the behavior) rather than a claim the behavior BODY performs cell I/O; the hard-contradiction framing is softened by 032-108's own 'pure' wording, though the altitude/wording drift is real. Not already reported (no audit hits).
- **Charity check:** refute — Can an implementer building the behavior ABI tell whether a behavior body itself reads/writes cells (032-108) or is a pure params->value function the runtime feeds and drains (033-151/155/103)?
- **Evidence:**
  - `packages/ductus-lang/docs/DECISION_LOG.md:4038-4038`
    > 032-108. Behaviors are stateless at the runtime level: pure transformations that read inputs from cells, compute, and write outputs, with all state in reactive cells. (§14.6.1)
  - `packages/ductus-lang/docs/DECISION_LOG.md:4263-4264`
    > 033-151. A behavior is pure: a function of its declared inputs only, with no ambient state and no I/O. (§15.4.4)
    > 033-152. I/O is the domain of effects, which are graph-level, never of behaviors. (§15.4.4)
  - `packages/ductus-lang/docs/DECISION_LOG.md:4267-4267`
    > 033-155. A behavior's signature is `behavior <id> (params) -> <type> { block+ }`. (§15.4.4)
  - `packages/ductus-lang/docs/DECISION_LOG.md:4215-4215`
    > 033-103. The behavior table is a list of `(handle, debug_name, input_cell_ids, output_cell_id?)` entries, indexed by each behavior's u32 handle. (§15.4.1)

#### P005 — 013-130 states the dot-call 'introduces no implicit ownership rule of its own' and 013-131 requires explicit (move v) to consume a receiver, but 013-223/226 give the bare dot-call form v.m(args) an implicit consume-and-rebind ownership rewrite for mut bindings with an own receiver returning Subject.

- **Severity/Category/Verdict:** MED / contradiction / CONFIRMED (family: collision)
- **Anchors:** LOG 013-130, 013-131, 013-223, 013-226 · SPEC § 11.8.3, 11.11.3
- **Why:** The same surface form `v.m(args)` (no explicit move) is a non-consuming call under 013-130/131 but an implicit consuming-and-rebinding call under 013-223/226. 013-130's absolute phrasing ('the dot-call introduces no implicit ownership rule of its own') is falsified by the 013-223 receiver-rebind sugar, which is precisely an implicit ownership rewrite triggered by the dot-call form. A reader trusting §11.8.3 writes `(move v).m()`; a reader trusting §11.11.3 writes `v.m()` and gets an auto-move. The 013-130 restatement never absorbed the §11.11.3 exception.
- **Direction:** Qualify 013-130 (§11.8.3) to acknowledge the §11.11.3 mut-binding/own-receiver/returns-Subject receiver-rebind sugar as the one exception to 'no implicit ownership rule', OR narrow 013-130's claim so it does not read as absolute across all dot-calls. Do not edit; surface to user.
- **Evidence check:** pass — 013-130 (DECISION_LOG.md:1516) 'the dot-call introduces no implicit ownership rule of its own' and 013-131 (DECISION_LOG.md:1517) require explicit (move v) to consume; 013-223 (DECISION_LOG.md:1609) makes bare v.m(args) desugar to v = (move v).m(args) for 013-226 (DECISION_LOG.md:1612) mut/own/Subject receivers. Quotes confirm the same surface form is non-consuming under §11.8.3 but auto-consume-and-rebind under §11.11.3; 013-130's absolute 'no implicit ownership rule' is falsified by the 013-223 sugar. Not reported (013-131 audit hit at line 2921 is an unrelated dangling-else defect). CONFIRMED.
- **Charity check:** sustain — 013-130's absolute 'the dot-call introduces no implicit ownership rule of its own' vs 013-223's implicit consume-and-rebind sugar for v.m(args) on a mut/own-receiver/Subject-returning call.
- **Evidence:**
  - `packages/ductus-lang/docs/DECISION_LOG.md:1516-1517`
    > 013-130. `x.f(args)` is sugar for `f(x, args)`: the receiver is the first positional argument, follows the method's first-parameter convention, and the dot-call introduces no implicit ownership rule of its own. (§11.8.3)
    > 013-131. Consuming a method receiver uses prefix parenthesization: `(move v).into_sorted()`. (§11.8.3)
  - `packages/ductus-lang/docs/DECISION_LOG.md:1609-1613`
    > 013-223. A method call on a `mut` binding whose receiver parameter is `own` and which returns `Subject` may omit the receiver move and rebind: `v.m(args)` desugars to `v = (move v).m(args)`. (§11.11.3)
    > 013-224. The receiver sugar covers only the receiver; an `own` non-`Copy` argument still requires an explicit `move`: `v.push(move item)`. (§11.11.3)
    > 013-225. `Copy` arguments in the sugared form need no `move`: `v.push(3)`. (§11.11.3)
    > 013-226. The receiver sugar applies only to `mut` bindings and only when the receiver parameter is declared `own`. (§11.11.3)

#### P008 — Module-scope declaration enumeration drift: 013-23 lists `const/signal/attr/derived/recurrent` while 020-9 lists `signal/derived/recurrent/stream/const/let` — each includes a kind the other omits.

- **Severity/Category/Verdict:** MED / contradiction / CONFIRMED (family: collision)
- **Anchors:** LOG 013-23, 020-9 · SPEC § 11.2, 13.7.1
- **Why:** Two restatements of 'what module scope contains' give non-matching sets. 013-23 includes `attr` but omits `stream` and `let`; 020-9 includes `stream` and `let` but omits `attr`. Both purport to be complete enumerations of module-scope declaration kinds. The `let` divergence is the same disagreement as Finding 1; the `stream` and `attr` divergences are additional: is `stream` a module-scope kind (020-9 yes, 013-23 no), and is `attr` a module-scope kind (013-23 yes, 020-9 no)? An implementer building the module-scope grammar cannot derive one correct set. Distinct anchor pairing; not in the audit report.
- **Direction:** Reconcile the two enumerations to one canonical set of module-scope declaration kinds (resolving `attr`, `stream`, and `let` membership), then make 013-23 and 020-9 state it identically. Surface to user for the membership decisions.
- **Evidence check:** pass — 013-23 (DECISION_LOG.md:1409) enumerates module scope as const/signal/attr/derived/recurrent; 020-9 (DECISION_LOG.md:2707) as signal/derived/recurrent/stream/const/let. Quotes confirm each complete-enumeration includes a kind the other omits: 013-23 has attr but not stream/let; 020-9 has stream/let but not attr. The stream and attr divergences are distinct from and additional to the P007 let overlap, so this is a separate finding. MED is apt. Not reported. CONFIRMED.
- **Charity check:** sustain — Module-scope declaration enumeration drift: 013-23 = const/signal/attr/derived/recurrent (has attr, lacks stream/let) vs 020-9 = signal/derived/recurrent/stream/const/let (has stream/let, lacks attr).
- **Evidence:**
  - `packages/ductus-lang/docs/DECISION_LOG.md:1409-1409`
    > 013-23. Module scope contains `const`, `signal`, `attr`, `derived`, and `recurrent` declarations. (§11.2)
  - `packages/ductus-lang/docs/DECISION_LOG.md:2707-2707`
    > 020-9. The module top-level scope comprises module-level `signal`, `derived`, `recurrent`, `stream`, `const`, and `let` declarations. (§13.7.1)

#### P009 — Circularity effect scope contradiction: 019-78 says satisfying `Circularity` has NO effect beyond opting into cycle participation, but 024-26 says `Circularity` is load-bearing for wake-gate fixpoint resolution — an effect beyond opt-in.

- **Severity/Category/Verdict:** MED / contradiction / PLAUSIBLE (family: collision)
- **Anchors:** LOG 019-78, 024-26 · SPEC § 13.6.5, 13.11.5
- **Why:** 019-78 makes an absolute claim that Circularity's ONLY effect is admitting the connection type into a legal cycle. 024-26 attributes to Circularity a load-bearing role in the compiler's wake-gate settlement semantics ('cycles ... resolve by the prior-commit, one-commit-delay fixpoint only when guarded by a Circularity connection') — i.e. an effect on resolution behavior beyond mere cycle-participation opt-in. Read literally, both cannot hold. This is a DIFFERENT angle than audit F170 (which pins 024-26 against 022-37 on runtime-read-vs-legality conflation, anchors 024-26/024-20); the 019-78-vs-024-26 'no effect beyond' contradiction is not among F170's anchors and is a distinct restatement-drift within the circularity subject group.
- **Direction:** Reconcile 019-78 with 024-26: either qualify 019-78 ('no effect beyond cycle participation AND the resulting temporal-break/fixpoint semantics') or strike/soften 024-26's 'only when guarded by a Circularity connection' load-bearing claim to match 019-78 and 022-37. Surface to user; overlaps F170's resolution so should be settled together.
- **Evidence check:** pass — 019-78 (DECISION_LOG.md:2694) verbatim: 'Satisfying `Circularity` has no effect beyond opting the connection type into cycle participation.' 024-26 (DECISION_LOG.md:3093) verbatim: '...this guidance is load-bearing because compiler-synthesized wake gates form cycles that resolve by the prior-commit, one-commit-delay fixpoint only when guarded by a `Circularity` connection.' SPEC §13.11.5 lines 18464-18469 confirm Circularity is a marker with domain-defined effect. The two LOG entries make jointly-inconsistent claims: 'no effect beyond opt-in' vs an effect on wake-gate resolution behavior. Dedup: distinct from audit F170 (F170 pins 024-26 vs 022-37 on runtime-read-vs-legality, anchors 024-26/024-20); P009's 019-78-vs-024-26 'no effect beyond' angle is not among F170's anchors. Genuine MED contradiction within circularity subject group.
- **Charity check:** refute — Is 019-78 ('Circularity has no effect beyond cycle-participation opt-in') contradicted by 024-26's 'load-bearing for wake-gate fixpoint resolution'?
- **Evidence:**
  - `packages/ductus-lang/docs/DECISION_LOG.md:2694-2694`
    > 019-78. Satisfying `Circularity` has no effect beyond opting the connection type into cycle participation. (§13.6.5)
  - `packages/ductus-lang/docs/DECISION_LOG.md:3093-3093`
    > 024-26. Connection types that imply simultaneous source-destination activation should not satisfy `Circularity`; this guidance is load-bearing because compiler-synthesized wake gates form cycles that resolve by the prior-commit, one-commit-delay fixpoint only when guarded by a `Circularity` connection. (§13.11.5)
  - `packages/ductus-lang/docs/SPEC.md:18464-18469`
    > The `Circularity` trait is a marker — its semantic effect is
    > domain-defined (typically: connections that introduce a temporal
    > break between source and destination, so cycles through them
    > cannot loop instantaneously at runtime). Connection types that
    > imply simultaneous source-destination activation should not
    > satisfy `Circularity`.

#### P011 — Three LOG entries give three different counts of the structural-typing carve-outs: 012-46/013-187 say exactly two (tuples + closures), 001-30 says three (adds trait-constraint intersections), and 012-100 adds Map as a fourth structural form.

- **Severity/Category/Verdict:** MED / contradiction / CONFIRMED (family: collision)
- **Anchors:** LOG 001-30, 012-46, 013-187, 012-100 · SPEC § 1.3, 9.2, 11.10.6, 9.5
- **Why:** The same normalized subject (which type forms are structural exceptions to the otherwise-nominal type system) is restated across sections with disagreeing closed enumerations. 012-46 and 013-187 both assert a closed set of exactly two (tuples + closure types). 001-30 asserts three by adding trait-constraint intersections. 012-100 introduces a fourth structural form (Map). 012-46's 'the only two' is a closed count that 001-30 and 012-100 falsify. A reader building the nominal/structural typing rule from 012-46 concludes trait-constraint intersections and Map are nominal, directly contradicting 001-30 and 012-100. This is a semantic disagreement among restatements, not a mere wording drift.
- **Direction:** Reconcile the enumeration: decide the authoritative closed set of structural carve-outs (tuples, closure types, and whether trait-constraint intersections and Map[K,V] count), then restate every entry (001-30, 012-46, 013-187, 012-100) to the same count. User decision on the authoritative set; do not resolve unilaterally.
- **Evidence check:** pass — Four LOG entries verified verbatim: 001-30 (line 60) 'Tuples, closure types, and trait-constraint intersections are explicit structural exceptions' (=three); 012-46 (line 1241) 'Tuples and closure types are the only two structural-typing carve-outs' (=two, closed 'only'); 013-187 (line 1573) 'the second structural carve-out after tuples' (=two); 012-100 (line 1295) 'Map[K,V] is structurally typed... nominal Map types do not exist' (a fourth structural form). 012-46's 'only two' is a closed count that 001-30 and 012-100 falsify. Genuine semantic disagreement among restatements of the same normalized subject. MED correct. Not in audit report.
- **Charity check:** sustain — Do the LOG/SPEC give conflicting closed counts of the structural-typing carve-outs (two vs three vs a fourth)?
- **Evidence:**
  - `packages/ductus-lang/docs/DECISION_LOG.md:60-60`
    > 001-30. Tuples, closure types, and trait-constraint intersections are explicit structural exceptions to nominal typing. (§1.3)
  - `packages/ductus-lang/docs/DECISION_LOG.md:1241-1241`
    > 012-46. Tuples and closure types are the only two structural-typing carve-outs in the otherwise nominal type system. (§9.2)
  - `packages/ductus-lang/docs/DECISION_LOG.md:1573-1573`
    > 013-187. Closure types are structural — defined by signature alone, with no declaration and no nominal identity — the second structural carve-out after tuples. (§11.10.6)
  - `packages/ductus-lang/docs/DECISION_LOG.md:1295-1295`
    > 012-100. `Map[K, V]` is structurally typed by `K` and `V`; nominal Map types do not exist. (§9.5)

#### P012 — String backing-storage sharing is stated as optional/invisible in 012-37 but mandatory and user-observable (constant-time duplication regardless of length) in 013-102/013-104.

- **Severity/Category/Verdict:** MED / contradiction / CONFIRMED (family: collision)
- **Anchors:** LOG 012-37, 013-102, 013-104 · SPEC § 9.1.8, 11.6
- **Why:** 012-37 makes backing-storage sharing optional ('may share') and a non-observable implementation detail ('invisible to the user'). 013-102 makes refcounted shared backing the definite mechanism ('realizes its Copy semantics through refcounted shared backing ... without copying bytes'), and 013-104 promises a user-observable performance guarantee ('constant-time regardless of string length'). Constant-time duplication of an arbitrary-length string is only achievable if backing is shared; a conforming implementation that took 012-37's 'may share' literally and copied bytes would violate 013-104's O(1) guarantee. So the sharing 012-37 calls optional-and-invisible is in fact mandatory-and-observable per 013-102/013-104. An implementer cannot honor both 'sharing is optional/invisible' and 'duplication is constant-time regardless of length'.
- **Direction:** Reconcile: either make refcounted shared backing normative (dropping 012-37's 'may'/'invisible' framing so the O(1) guarantee is grounded), or weaken 013-104 to not guarantee constant-time when sharing is declined. User decision on whether string sharing is mandatory-observable; do not resolve unilaterally.
- **Evidence check:** pass — 012-37 (line 1232) verbatim: 'The runtime MAY share immutable backing storage between string values; sharing is an implementation detail invisible to the user.' 013-102 (line 1488): 'string realizes its Copy semantics through refcounted shared backing... without copying bytes.' 013-104 (line 1490): 'String duplication is constant-time regardless of string length.' O(1) duplication of arbitrary-length string is only achievable via shared backing, so 012-37's 'may share / invisible' collides with 013-102/104's mandatory-and-observable guarantee. An implementer honoring 012-37 literally (copy bytes) would violate 013-104's O(1). Jointly-unsatisfiable. MED correct. Not in audit report.
- **Charity check:** sustain — Can string backing-sharing be simultaneously optional-and-invisible (012-37) and mandatory-and-observable via O(1) duplication regardless of length (013-102/104)?
- **Evidence:**
  - `packages/ductus-lang/docs/DECISION_LOG.md:1232-1232`
    > 012-37. The runtime may share immutable backing storage between string values; sharing is an implementation detail invisible to the user. (§9.1.8)
  - `packages/ductus-lang/docs/DECISION_LOG.md:1488-1488`
    > 013-102. `string` realizes its `Copy` semantics through refcounted shared backing: assigning, passing, or returning a string increments a refcount without copying bytes. (§11.6)
  - `packages/ductus-lang/docs/DECISION_LOG.md:1490-1490`
    > 013-104. String duplication is constant-time regardless of string length: `let t = s` is constant-time. (§11.6)

#### P015 — Use-path form conflict: 003-24 and SPEC §10.4 say ALL `use` paths are absolute starting from a path base (root/dependency name), but 009-70/71/72 and SPEC §6.2.3 write variant imports rooted at a bare TYPE name (`use Result::(Ok,Err)`, `use Direction::*`) which is not a path base.

- **Severity/Category/Verdict:** MED / contradiction / CONFIRMED (family: collision)
- **Anchors:** LOG 003-24, 009-70, 009-71, 009-72 · SPEC § 10.4, 10.2.3, 6.2.3
- **Why:** 003-24 (elaborated by SPEC §10.4 line 7742) states every `use` path is absolute and must start from a path base — `root` or an external dependency name. But the variant-import forms in 009-70/71/72 and SPEC §6.2.3 begin with a bare type name (`Result`, `Direction`), which is not a path base. Under 003-24's rule these would have to be written as `use root::…::Result::(Ok, Err)` (or `use std::result::…`). An implementer cannot tell whether `use Result::(Ok, Err)` is legal (contradicting 003-24's no-relative-form / must-start-from-path-base rule) or must be fully rooted. No entry carves variant-name-rooted `use` out of the absolute-path-base rule. The main audit's F006 is a different issue (SPEC §10.4.1 grounding the general collision rule on the enum-specific §6.2.3); it does not address whether bare-type-rooted use statements violate the absolute-path-base rule.
- **Direction:** Surface to the user: either (a) declare that variant/enum-rooted `use` (`Type::(…)`, `Type::*`) is an explicit exception to the path-base rule and state it in 003-24 and SPEC §10.4/10.2.3, or (b) require variant imports to be fully rooted and fix the 009-70/71/72 and §6.2.3 examples. Do not self-decide.
- **Evidence check:** pass — 003-24 (line 127) verbatim: 'All use statements use absolute paths starting from a path base; there is no relative-path current module form.' SPEC §10.4 (7742-7744): 'All use paths are absolute, starting from a path base (root or an external dependency name)... There is no relative-path form.' 003-22 (125) enumerates path bases as root/dependency-name/std — a bare type name is NOT a path base. But 009-70/71/72 (1007-1009) and SPEC §6.2.3 (5222-5224) write variant imports rooted at a bare TYPE name: 'use Result::(Ok, Err)', 'use Direction::*'. Under 003-24's rule these must be fully rooted (use root::...::Result::...); no entry carves bare-type-rooted use out of the absolute-path-base rule. Implementer cannot tell if 'use Result::(Ok,Err)' is legal. MED behavior-changing ambiguity confirmed. Distinct from audit F006 (collision-rule grounding). Not in audit report.
- **Charity check:** sustain — Does the absolute-path-base rule (003-24/§10.4) conflict with variant-import use forms rooted at a bare enum name (009-70/71/72, `use Result::(Ok,Err)`)?
- **Evidence:**
  - `packages/ductus-lang/docs/DECISION_LOG.md:127-127`
    > 003-24. All `use` statements use absolute paths starting from a path base; there is no relative-path "current module" form for imports. (§10.2.3)
  - `packages/ductus-lang/docs/DECISION_LOG.md:1007-1009`
    > 009-70. Unqualified variant names are unavailable by default and require explicit import: `use Result::(Ok, Err)` enables `Ok(42)`. (§6.2.3)
    > 009-71. `use Enum::*` imports all of an enum's variants unqualified: `use Direction::*` enables bare `North`. (§6.2.3)
    > 009-72. Selection lists in `use` paths are written with parentheses: `use Result::(Ok, Err)`. (§6.2.3)
  - `packages/ductus-lang/docs/SPEC.md:7742-7744`
    > All `use` paths are absolute, starting from a path base
    > (`root` or an external dependency name; see §10.2.3). There is no
    > relative-path form.
  - `packages/ductus-lang/docs/SPEC.md:5222-5224`
    > use Result::(Ok, Err)
    > use Direction::*

#### P016 — 016-157 and 016-285 give conflicting exhaustive enumerations of the host write surface: {write_signal, transaction} vs {write_signal, push_stream}.

- **Severity/Category/Verdict:** MED / contradiction / CONFIRMED (family: collision)
- **Anchors:** LOG 016-157, 016-285 · SPEC § 13.2.7, 13.14.2
- **Why:** Both entries are absolute enumerations of the same subject (the host write surface) but name different member sets. 016-157 says host-driven writes occur ONLY through write_signal and transaction; 016-285 says the write surface is EXACTLY two channels — write_signal and push_stream — with NO third channel. They disagree two ways: (1) 016-157 omits push_stream entirely, yet stream cells ARE host-written via push_stream (016-285; SPEC §13.14.8 at 19111-19145; SPEC 22307-22308, 22689-22690), so 016-157's 'only' is factually wrong. (2) 016-157 lists `transaction` as a peer write channel, but SPEC §13.14.5 (19006-19011) shows transaction is a batching wrapper containing `tx.write_signal` calls, not a distinct value-write channel; 016-285 counts exactly two channels and does not include transaction. The SPEC's own §13.2.7 (12355-12362) scopes its {write_signal, transaction} list with the qualifier 'for signal cells' — a scope the LOG restatement 016-157 dropped, turning a signal-cell-scoped list into an unqualified 'only' that collides with the whole-surface enumeration in 016-285. LOG Invariant 2 forces each entry to restate rather than cross-reference; here the two restatements of the host-write-surface fact drifted into a semantic conflict, and the LOG (self-contained decision-of-record) now contains two mutually inconsistent exhaustive lists.
- **Direction:** Reconcile the two entries against one write-surface model. Either (a) rewrite 016-157 to carry the SPEC §13.2.7 scope qualifier it dropped (e.g. host-driven writes to SIGNAL cells occur through runtime.write_signal, optionally batched via runtime.transaction; stream cells are written via runtime.push_stream), so it no longer reads as an exhaustive whole-surface list; or (b) if 016-157 is meant to enumerate the entire host write surface, align its member set to 016-285's {write_signal, push_stream} and reclassify transaction as a batching wrapper rather than a channel. User must decide which entry states the intended scope; do not silently pick.
- **Evidence check:** pass — 016-157 (line 2010) verbatim: 'Host-driven writes occur only through runtime.write_signal and runtime.transaction.' 016-285 (2138): 'The host-program write surface comprises exactly two channels: runtime.write_signal... and runtime.push_stream... There is no third channel.' Conflict confirmed two ways: (1) push_stream IS a host write channel (SPEC §13.14.8 lines 19111-19145; 22308, 22690), so 016-157's 'only' omitting it is factually wrong; (2) SPEC §13.2.7 (12355-12362) scopes its {write_signal, transaction} list with 'for signal cells' — a qualifier 016-157 dropped, and transaction is a batching wrapper not a peer value-write channel, so 016-285 counts exactly two and excludes transaction. Two mutually inconsistent exhaustive host-write-surface lists in the LOG. MED confirmed. Not in audit report.
- **Charity check:** sustain — Do 016-157 and 016-285 give conflicting exhaustive enumerations of the host write surface ({write_signal, transaction} vs {write_signal, push_stream})?
- **Evidence:**
  - `packages/ductus-lang/docs/DECISION_LOG.md:2010-2010`
    > 016-157. Host-driven writes occur only through `runtime.write_signal` and `runtime.transaction`. (§13.2.7)
  - `packages/ductus-lang/docs/DECISION_LOG.md:2138-2138`
    > 016-285. The host-program write surface comprises exactly two channels: `runtime.write_signal` for module-level signals and per-effect-instance `observed:` signals, and `runtime.push_stream` for per-effect-instance `observed:` streams. There is no third channel; a computed `observed:` output (a `derived` or value-`recurrent`) is program-written rather than host-written, so it opens no channel and the host-write count stays two. (§13.14.2)
  - `packages/ductus-lang/docs/SPEC.md:18958-18961`
    > `runtime.write_signal` targets signals only —
    > module-level signals (§13.2.1) or per-effect-instance `observed:` signals
    > (§13.19.5); it does not write attrs (attrs are placement-written only,
    > §13.2.2).

#### P017 — The `here::` restatements drift on which scope it anchors: 020-11 says it bypasses inner local bindings (anchors the body/enclosing scope, i.e. NOT the innermost), while 020-34 labels `here` as the 'current (innermost) scope' — opposite reads for `here::x` inside a `let`/`for` local scope.

- **Severity/Category/Verdict:** MED / contradiction / CONFIRMED (family: collision)
- **Anchors:** LOG 020-11, 020-34, 020-6, 020-12 · SPEC § 13.7.1, 13.7.2, 13.7.7
- **Why:** 020-6 fixes the scope chain innermost-first as: (scope 1) local bindings, (scope 2) instance body scope, (scope 3) module top-level. 020-11 (and its SPEC §13.7.2 elaboration) says `here::` anchors 'the current scope ... bypassing inner local bindings' — SPEC 16227-16228 makes this explicit as 'the instance body scope (scope 2)'. So `here::` anchors scope 2 and skips scope 1. But 020-34 (and SPEC §13.7.7 line 16383, and the §13.7.1 overview line 16163) restate the same anchor as 'the current (innermost) scope.' The innermost scope per 020-6 is scope 1 (local bindings) — precisely what `here::` bypasses. The two restatements say `here::` reaches opposite scopes. SPEC §13.7.7 is self-contradictory in one bullet: it calls `here` the 'innermost' scope, then says `here::x` bypasses 'shadowing from inner blocks' — if `here` were the innermost scope there would be no inner block to bypass. Witness: inside `derived x: f32 = ( let gain = 2.0; here::gain )` with an outer `attr gain = 0.5`, the 020-11 reading yields 0.5 (bypasses the let), the 020-34/16163 'innermost scope' reading yields 2.0 (the let). Behavior-changing ambiguity for any `here::` reference sitting inside a `let`/`for` local scope. Not already reported: audit F178 covers the reserved-field count (020-8/020-20) and another F-id covers bare-name 'nearest wins' (020-3/10/17/19); neither touches the here-anchor innermost-vs-body drift.
- **Direction:** Make the restatement consistent: since 020-6 defines local bindings as innermost and SPEC §13.7.2 explicitly anchors `here::` at the instance body scope (scope 2), the accurate wording is 020-11's 'current scope, bypassing inner local bindings' — the 'innermost' label in 020-34 and SPEC §13.7.1 line 16163 / §13.7.7 line 16383 is the drifted side and should be dropped or replaced with 'current body/enclosing scope'. Do not resolve unilaterally: confirm with the user whether `here::` is intended to anchor the body scope (skip local `let`/`for`) or the truly innermost local scope before amending.
- **Evidence check:** pass — All four quotes verbatim-confirmed. 020-11 (LOG 2709) + SPEC 16227-16229 anchor here:: to scope 2 (instance body scope), bypassing inner local bindings. But 020-34 (LOG 2732), SPEC 16163, and SPEC 16383 label `here` the 'current (innermost) scope'; 020-6 (LOG 2704) fixes innermost = local bindings (scope 1). SPEC 16384-16385 is self-contradictory in one bullet: 'innermost scope' + 'bypassing any shadowing from inner blocks'. The `let gain`/`here::gain` witness yields opposite reads (0.5 vs 2.0). Real MED contradiction; distinct from F178 (reserved-field count) and the bare-name finding.
- **Charity check:** sustain — The two restatements name opposite scopes for `here::` and the drift is FORCED by quoted normative text, not merely inferable. Per 020-6 (DECISION_LOG.md:2704) the innermost scope is scope 1 = local bindings (`let`/`for`-vars). §13.7.2 (SPEC.md:16227-16229) fixes `here::x` in 'the instance body scope (scope 2) ... bypassing inner local bindings' — i.e. NOT the innermost. But 020-34 (DECISION_LOG.md:2732), the §13.7.1 overview (SPEC.md:16163 'the current (innermost) scope'), and §13.7.7 (SPEC.md:16383-16385 'the current (innermost) scope ... bypassing any shadowing from inner blocks') all call `here` the innermost scope. §13.7.7 is self-contradictory in one bullet: `here` cannot be the innermost scope AND bypass shadowing from inner blocks — if it were innermost there is no inner block to bypass. Witness holds: `derived x = ( let gain = 2.0; here::gain )` under an outer `attr gain=0.5` yields 0.5 by §13.7.2 (skips the let) vs 2.0 by the 'innermost scope' reading (the let). Behavior-changing for any `here::` inside a `let`/`for` local scope. Charitable rescue ('innermost' loosely means 'the enclosing member scope') is not forced — §13.7.7's 'bypassing shadowing from inner blocks' pins it to the literal innermost. No dissolving text elsewhere. Distinct from audit F178 (reserved-field count) and the bare-name 'nearest wins' finding.
- **Evidence:**
  - `packages/ductus-lang/docs/DECISION_LOG.md:2709-2709`
    > 020-11. `here::x` resolves `x` in the current scope, bypassing inner local bindings and ignoring same-named module-level declarations. (§13.7.2)
  - `packages/ductus-lang/docs/DECISION_LOG.md:2732-2732`
    > 020-34. `here` denotes the current (innermost) scope as a namespace, usable only as the left side of `::`; it is not a value and has no `.` form. (§13.7.7)
  - `packages/ductus-lang/docs/DECISION_LOG.md:2704-2704`
    > 020-6. Within a node or connection body, the scope chain innermost-first is: local bindings, the instance body scope, the module top-level scope. (§13.7.1)
  - `packages/ductus-lang/docs/SPEC.md:16225-16229`
    > #### 13.7.2 The `here::` anchor
    > 
    > `here::x` explicitly resolves `x` in the current scope — inside a
    > node or connection body, the instance body scope (scope 2) —
    > bypassing inner local bindings and ignoring any module-level
  - `packages/ductus-lang/docs/SPEC.md:16383-16385`
    > - **`here`** — the current (innermost) scope as a *namespace*. Scope
    >   resolution uses `::`: `here::x` resolves `x` in the current scope,
    >   bypassing any shadowing from inner blocks. `here` is not a value and

#### P018 — 025-58/025-59 assert every element offset in a fixed-extent `T[N]` cell is compile-time-known, but `string[N]`/`Vec[T][N]` are legal fixed-extent cell types whose elements are dynamic-size (025-29), and no rule states such elements become fixed-size indices — so the compile-time-offset/unroll claim has no covering storage rule for the array-of-dynamic case.

- **Severity/Category/Verdict:** MED / gap / CONFIRMED (family: witness)
- **Anchors:** LOG 025-58, 025-59, 025-29, 025-22, 025-28 · SPEC § 13.12.4, 9.3.1, 14.3.3
- **Why:** Joint scenario: `signal words: string[10]` (or `signal rows: Vec[i32][8]`) is a legal reactive cell — 025-22 accepts any value type and SPEC §9.3.1 (6730) makes `string[10]` a valid `T[N]` array type. 025-58 then unconditionally classifies it as a fixed-extent cell with 'every element's offset within the cell ... determined at compile time', and 025-59 compile-time-unrolls a `for` over it 'to element reads at known offsets'. But each element is a `string` (dynamic-size, 025-29), whose value does not have a compile-time-known size, so the naive in-line element offsets are NOT compile-time-known. The docs give a coherent rule only for the *whole-cell* dynamic case (025-28/025-29, §14.3.3: the cell holds a one-word index into a pool) and for fixed-extent cells of *fixed-size* elements; they never state what a fixed-extent array of a dynamic-size element type lays out as. The natural fix — each array element is itself a fixed-size pool index, giving known offsets over the index array — is stated nowhere, so 025-58/025-59's blanket claim is unbacked for this constructible type. An implementer has no rule to lower `for x in words.value():` over a `string[10]` cell. This is distinct from F221 (which only flags 025-59's bracket `Cell[f32[64]]` spelling, not the semantic offset gap for dynamic-size elements).
- **Direction:** Surface to user: either qualify 025-58 so 'compile-time-known offset' applies only when the element type is itself fixed-size, and add a rule for the fixed-extent-array-of-dynamic-element case (e.g. each element stored as a fixed-size pool index, offsets known over the index array, per-element pool resolution on read), or state that dynamic-size element types are disallowed as fixed-extent array cell elements. Do not resolve unilaterally — the storage-model choice is a design decision.
- **Evidence check:** pass — Quotes confirmed. string[10] is a legal T[N] array (SPEC 6730); reactive cells accept any value type (025-22, LOG 3119); string is dynamic-size index-based storage (025-29, LOG 3126). 025-58 (LOG 3155) unconditionally claims T[N] has compile-time-known element offsets; 025-59 (LOG 3156) unrolls the for. grep found ZERO rule for laying out a fixed-extent array whose element type is dynamic-size; §14.3.3 covers only whole-cell dynamic types. Gap is real and distinct from F221 (which flags only the Cell[f32[64]] bracket spelling, a fixed-SIZE element).
- **Charity check:** sustain — The array-of-dynamic-element case is constructible and its layout rule is genuinely absent — refuted only by an explicit covering rule, and none exists. 025-22 (DECISION_LOG.md:3119) accepts any value type; SPEC.md:6730 makes `string[10]` a legal `T[N]`. 025-58 (DECISION_LOG.md:3155) and SPEC §13.12.4 (SPEC.md:18748-18752) unconditionally classify a fixed-extent `T[N]` cell as having 'every element's offset ... determined at compile time', and 025-59 (DECISION_LOG.md:3156) compile-time-unrolls a `for` over it. But `string`/`Vec[T]` elements are dynamic-size (025-29, DECISION_LOG.md:3126). The storage decision at SPEC.md:18674-18675 keys off `N×sizeof(T)`, which is defined only for a fixed-size element type. §14.3.3 (SPEC.md:23405+) covers only the WHOLE-cell dynamic case (a `string`/`Vec` cell holds one pool index); grep of the whole corpus for `string[`, 'array of dynamic', 'per-element index' finds nothing stating each element of a `string[N]` cell becomes its own fixed-size pool index. So the compile-time-offset/unroll claim has no covering storage rule for the array-of-dynamic case, and an implementer cannot lower `for x in words.value()` over a `string[10]` cell. Distinct from F221 (Cell[] bracket spelling only, not the dynamic-element offset gap). Explicit-normative-text refutation not found; sustain.
- **Evidence:**
  - `packages/ductus-lang/docs/DECISION_LOG.md:3155-3156`
    > 025-58. A fixed-extent cell type (`T[N]`, tuples, records) has compile-time-known layout: every element's offset within the cell, or within the pool slot for sizes above the word, is determined at compile time. (§13.12.4)
    > 025-59. A `for` iterating a fixed-extent cell's value compile-time-unrolls to element reads at known offsets — no runtime loop counter, bounds check, or per-iteration dispatch: `for x in buf.value():` over `Cell[f32[64]]`. (§13.12.4)
  - `packages/ductus-lang/docs/DECISION_LOG.md:3126-3126`
    > 025-29. Dynamically-sized types (`string`, `Vec[T]`, `Map[K, V]`, and other heap-allocated dynamic-size collections) always use index-based storage. (§13.12.4)
  - `packages/ductus-lang/docs/DECISION_LOG.md:3119-3119`
    > 025-22. Reactive cells (signal, attr, recurrent, derived) accept any value type. (§13.12.4)
  - `packages/ductus-lang/docs/SPEC.md:6730-6730`
    > string[10]          // 10-element array of string
  - `packages/ductus-lang/docs/SPEC.md:23405-23409`
    > #### 14.3.3 Extensible pools for dynamic-size types
    > 
    > Dynamic-size cell types (`Vec[T]`, `SmallVec[T, N]`, `RingBuf[T, N]`,
    > `string`, etc., per §13.12.4) cannot live directly in the fixed-size
    > cell slots. Their storage uses **extensible pools**.

#### P019 — Desired-block event-output stream capacity is asserted to default to a flat 1024, yet the same rule says it 'matches a regular stream declaration (§13.18.2)', whose omitted-capacity default for a `= source` is source-derived (that output's capacity, or sum of input capacities) — 1024 vs source-derived for the same declaration.

- **Severity/Category/Verdict:** MED / contradiction / CONFIRMED (family: witness)
- **Anchors:** LOG 031-32, 030-21, 030-22, 030-25, 031-31, 031-33, 031-27 · SPEC § 13.19.4, 13.18.2
- **Why:** A desired-block event-output stream is a full `stream` declaration WITH a `= source` (031-31/33; SPEC 22138-22141), fed from the effect's parameters or observed cells. Effect stream parameters are known-capacity (`GateStream[Message, 256]`, 031-27), and observed cells are known-capacity streams — so the source is a single-stream/reactive-expression source, exactly the cases §13.18.2 (030-21/030-22) resolves to a source-derived default (that output's capacity, or the sum). The flat-1024 default (030-25 / SPEC 20365-20366) is scoped explicitly to the residual case: 'all other omitted-capacity cases (e.g., a `.changes` conversion at a use site with no pinning annotation context)'. Yet 031-32 and SPEC §13.19.4 (line 22150) BOTH assert the desired-block stream's capacity is 'optional ... defaulting to 1024' AND that it 'matches a regular stream declaration (§13.18.2)'. For any omitted-capacity desired stream whose source is a stream (the normal case), §13.18.2 yields a source-derived capacity while 031-32/§13.19.4 yield 1024. Concrete: `effect WsClient(outbound_src: GateStream[Message,256]): desired: stream ring outbound: Message = outbound_src` gets capacity 256 under 030-21 but 1024 under 031-32 — an implementer cannot honor both, and buffer/pool sizing (§14.3.3) diverges. This is both a LOG-internal contradiction (031-32 vs 030-21/22 which 031-32 claims to conform to) and a LOG-SPEC divergence carried verbatim into SPEC §13.19.4.
- **Direction:** Reconcile 031-32 and SPEC §13.19.4 with §13.18.2's omitted-capacity rules: either state the desired-block stream inherits §13.18.2's source-derived default (dropping the standalone 'defaulting to 1024'), or state that desired-block streams override §13.18.2 with a flat 1024 default and remove the 'matches a regular stream declaration (§13.18.2)' equivalence claim for the capacity axis. Surface the four-way (256/sum vs 1024) mismatch to the user; do not self-resolve which default is intended.
- **Evidence check:** pass — Quotes confirmed. 031-32 (LOG 3801) + SPEC 22150 say a desired-block stream 'matches a regular stream declaration (§13.18.2)' AND defaults capacity to 1024. But §13.18.2 (030-21/22, LOG 3527-3528; SPEC 20352-20359) makes omitted capacity source-derived when the source is a stream, and 030-25 (LOG 3531; SPEC 20365) scopes flat-1024 to 'all other cases'. Effect stream params are known-capacity (031-27, GateStream[LogEntry,4096]), so a desired stream `= param` falls under the source-derived branch, not the residual. Both a LOG-internal contradiction and a LOG-SPEC carry. Not previously reported.
- **Charity check:** sustain — Two jointly-unsatisfiable capacity defaults for the same declaration, both carried into SPEC. 031-32 (DECISION_LOG.md:3801) and SPEC §13.19.4 (SPEC.md:22149-22151) BOTH assert the desired-block event-output stream's shape 'matches a regular stream declaration (§13.18.2)' AND that its omitted capacity defaults to a flat 1024. But §13.18.2's actual omitted-capacity rules are source-derived: 030-21 (DECISION_LOG.md:3527) → that output's capacity for a single-stream/known-capacity source; the flat-1024 default (030-25, DECISION_LOG.md:3531) is scoped explicitly to 'all other omitted-capacity cases'. A desired stream ALWAYS has a `= source` (031-31/33, DECISION_LOG.md:3800/3802) fed from effect parameters (which may be `GateStream[T,N]`, known capacity, 031-27) or observed cells. Witness: `desired: stream ring outbound: Message = outbound_src` with `outbound_src: GateStream[Message,256]` gets 256 under 030-21 but 1024 under 031-32 — an implementer cannot honor both, and §14.3.3 buffer sizing diverges. Nothing restricts a desired stream's source to a non-stream, so the normal case genuinely conflicts. Both a LOG-internal contradiction (031-32 claims to conform to §13.18.2 yet overrides its default) and a LOG-SPEC divergence. No dissolving text scoping the 1024 to the residual case.
- **Evidence:**
  - `packages/ductus-lang/docs/DECISION_LOG.md:3801-3801`
    > 031-32. A desired-block event-output stream comprises a policy keyword (`ring` or `gate`), an optional bracketed capacity defaulting to 1024, a name, an element type, and a `= source` expression. (§13.19.4)
  - `packages/ductus-lang/docs/DECISION_LOG.md:3527-3528`
    > 030-21. Omitted capacity with a single-stream source or a known-capacity stream-producing chain defaults to that output's capacity. (§13.18.2)
    > 030-22. Omitted capacity with a reactive-expression source over streams and/or signals defaults to the sum of input capacities across all reactive inputs. (§13.18.2)
  - `packages/ductus-lang/docs/DECISION_LOG.md:3531-3531`
    > 030-25. In all other omitted-capacity cases (e.g., a `.changes` conversion at a use site with no pinning annotation context), capacity defaults to 1024. (§13.18.2)
  - `packages/ductus-lang/docs/DECISION_LOG.md:3800-3802`
    > 031-31. A `desired:` event-output cell is declared with `stream` (reusing the keyword): `stream ring[1024] outbound: Message = source`; the effect feeds the source from its own parameters or `observed:` cells, and the host's reconciler drains the buffer. (§13.19.4)
    > 031-32. A desired-block event-output stream comprises a policy keyword (`ring` or `gate`), an optional bracketed capacity defaulting to 1024, a name, an element type, and a `= source` expression. (§13.19.4)
    > 031-33. A desired-block event-output stream's `= source` is fed internally by the effect (from parameters or `observed:` cells); there is no external program-side wiring into it. (§13.19.4)
  - `packages/ductus-lang/docs/SPEC.md:22149-22152`
    > The declaration shape matches a regular `stream` declaration (§13.18.2)
    > — policy keyword (`ring` or `gate`), optional capacity (defaulting to
    > 1024), name, element type, and a `= source`. The effect feeds the
    > source internally; there is no external program-side wiring into it.
  - `packages/ductus-lang/docs/SPEC.md:20351-20366`
    > - **`[capacity]`** is an optional compile-time-known positive `usize`
    >   (a literal, a `const`, or a const-generic parameter — §2.5)
    >   specifying the buffer's slot count. When omitted:
    >   - For declarations whose source is a single stream or a stream-
    >     producing operator chain whose output capacity is known,
    >     capacity defaults to that output's capacity.
    >   - For declarations whose source is a reactive expression
    >     (§13.18.7) involving streams and/or signals, capacity defaults
    >     to the **sum of the stream inputs' capacities** (the event
    >     producers). A signal sampled in the expression drives no emission,
    >     so it contributes no capacity.
    >   - For a `recurrent[N] stream` (§13.18.8), the buffer capacity
    >     defaults to `sum_of_input_capacities`; the self-history depth `N`
    >     is a *separate* allocation on the second stream axis (a `Ring[B]` /
    >     `Gate[B]` policy stream carrying history depth `N` — buffer `B`
    >     beside history `N`, §13.18.3), not added to the buffer (§13.18.8.4).
    >   - In all other cases (e.g., a bare `.changes` member without
    >     surrounding context that pins it), capacity defaults to `1024`.

#### P021 — 017-136 enumerates the three admissible self-sourced destination forms as `own child, module-level instance, or a Handle-typed attr`, but the immediately-following elaboration 017-139/140/141 spells that same third form `WeakHandle`-typed (`attr wheel: WeakHandle[Drivable]`, freezes on None, needs `dynamic incoming`) — an internal term mismatch on the enumerated form.

- **Severity/Category/Verdict:** MED / contradiction / CONFIRMED (family: witness)
- **Anchors:** LOG 017-136, 017-139, 017-140, 017-141, 017-182 · SPEC § 13.3.4.2
- **Why:** 017-136 fixes the closed set of self-sourced destination forms; the third member is named a `Handle`-typed attr. The three entries that elaborate exactly that third form (017-139/140/141) all name it `WeakHandle`, and the freeze-on-None + dynamic-incoming semantics they attach are WeakHandle-only per the 017-176/182 split (a static `Handle[T]` never freezes and its membership is a static fact). An implementer reading the closed enumeration in 017-136 would admit a static `Handle[T]` attr as a sanctioned self-sourced destination form and expect it to behave like the elaborated form (freeze, dynamic incoming) — but a static Handle does neither. Either the enumerated form is `WeakHandle`-typed (and 017-136 mis-names it) or a static `Handle`-typed attr is a legal fourth/variant form whose static membership behaviour is left unstated. This is separate from F085, whose anchors are 017-139/141 and whose subject is the SPEC worked example's `Handle[Drivable]` typing; 017-136's own enumeration text is unaddressed by any audit finding.
- **Direction:** Surface to user: align the third enumerated form in 017-136 with the elaboration — spell it `WeakHandle`-typed attr to match 017-139/140/141; or, if static `Handle`-typed attrs are also admissible self-sourced destinations, add an entry stating their static-membership (static `incoming`, never-freeze) behaviour. Do not pick unilaterally.
- **Evidence check:** pass — Quotes confirmed. 017-136 (LOG 2277) enumerates the closed set of three self-sourced destination forms, naming the third 'a `Handle`-typed attr'. The three entries elaborating that form (017-139/140/141, LOG 2280-2282) all spell it `WeakHandle`, and their freeze/dynamic-incoming semantics are WeakHandle-only per the 017-182 split. Term mismatch in the LOG's own enumeration. Anchor 017-136 is untouched by F085 (which targets 017-139/141 and the SPEC example). Distinct MED.
- **Charity check:** sustain — Term mismatch on the enumerated self-sourced destination form that survives the charitable umbrella reading. 017-136 (DECISION_LOG.md:2277) enumerates exactly three admissible self-sourced destination forms; the third is 'a `Handle`-typed attr'. Its own elaboration 017-139/140/141 (DECISION_LOG.md:2280-2282) spells that same form `WeakHandle`-typed (`attr wheel: WeakHandle[Drivable]`) and attaches freeze-on-None + `dynamic incoming` semantics that are WeakHandle-ONLY per the 017-176/182 split (a static `Handle[T]` never freezes, membership is a static fact). Two readings, both leaving a defect: (a) if 017-136's 'Handle' means WeakHandle, the enumeration mis-names it; (b) if 'Handle' is the 017-182 umbrella, then a static `Handle[T]` attr is a sanctioned self-sourced form whose membership behavior no entry specifies (017-139/141 cover only the WeakHandle sub-case), and 017-147 wrongly forces it to `dynamic incoming` (see P020). No normative text forces the umbrella reading over the literal one, and the WeakHandle elaboration makes the literal 'Handle-typed' inconsistent with its own §13.3.4.2 elaboration. Category standard (ambiguity: refute only with a FORCED reading) not met; when torn, sustain. Distinct from F085 (SPEC worked example at 017-139/141); 017-136's enumeration text is unaddressed.
- **Evidence:**
  - `packages/ductus-lang/docs/DECISION_LOG.md:2277-2277`
    > 017-136. Self-sourced destinations must be nameable from the type's scope; exactly three forms are admissible: an own child, a module-level instance, or a `Handle`-typed attr. (§13.3.4.2)
  - `packages/ductus-lang/docs/DECISION_LOG.md:2280-2282`
    > 017-139. A `WeakHandle`-typed attr destination gives per-instance parameterized wiring, configured at placement: `attr wheel: WeakHandle[Drivable]` with entry `Drives: wheel`. (§13.3.4.2)
    > 017-140. A self-sourced connection through a `WeakHandle` freezes while the handle resolves to `None`. (§13.3.4.2)
    > 017-141. With a `WeakHandle` destination, membership at the destination is not a static fact: every node type the handle's type admits must declare a `dynamic incoming` connection-view of that type. (§13.3.4.2)

#### P025 — The desired: block may contain `when`/`given` selection blocks whose arms freeze independently (031-37..041: active arm live, inactive arms frozen, a frozen arm 'neither goes dirty nor triggers an update', reopen recomputes that arm), but the IR desired model (033-170: one whole-record cell from 033-210: a computation with 'no activation input anywhere') has no per-arm gate/freeze structure, so gated desired arms have no IR lowering.

- **Severity/Category/Verdict:** MED / gap / CONFIRMED (family: witness)
- **Anchors:** LOG 031-37, 031-38, 031-39, 031-40, 033-170, 033-210 · SPEC § 13.19.4, 15.4.5
- **Why:** Take `effect stream_pull(mode: Cell[Mode]): desired: when mode == Mode::Poll: derived url: Url = ...; given mode: Push -> derived channel: Channel = ...`. Per 031-38/039/040 each arm's desired cells freeze/thaw under a gate predicate on `mode`, and a frozen arm must NOT dirty the reconciler while its sibling is active — i.e. the desired computation is partitioned by an intra-desired gate. But 033-170 emits one whole-record `desired` cell and 033-210/§15.4.5 assert the desired computation has 'no activation input anywhere'. A single pure desired-builder behavior recomputes wholly whenever any input (including `mode`) changes; it cannot express per-arm freeze where an inactive arm's fields stay static and don't trigger `update`. §15.4.5 gives no gate structure over desired cells (the only gate shown, App.g0, guards the whole effect for suspend/resume, not intra-desired arms). Result: the `when`/`given`-in-desired feature has no IR lowering, and 033-210's 'no activation input' is in tension with a gate predicate driving desired-arm selection.
- **Direction:** Extend §15.4.5/033-170 to lower intra-desired `when`/`given` into per-arm desired cells guarded by a gate whose predicate feeds the freeze (distinct from the whole-effect suspend/resume gate), and reconcile 033-210's 'no activation input anywhere' with that gate predicate — or restrict 031-37..041 out of the IR-representable surface. User decides which.
- **Evidence check:** pass — All 6 anchors verified verbatim on fresh reads: 031-37 (DECISION_LOG.md:3806), 031-38 (:3807), 031-39 (:3808), 031-40 (:3809) establish per-arm freeze semantics inside desired: blocks (active arm live, inactive frozen, frozen arm 'neither goes dirty nor triggers an update', reopen recomputes). 033-170 (:4282) types desired as a single whole-record pool_index<%T> cell; 033-210 (:4322) asserts 'no activation input anywhere'; SPEC §15.4.5 (24479-24483) confirms the whole-record cell is 'a pure function of message... so there is no activation input anywhere' and the only gate shown (App.g0) guards the whole effect, not intra-desired arms. The tension is real: a single pure whole-record desired-builder cannot express per-arm freeze where an inactive arm's fields stay static and don't dirty the reconciler. No per-arm gate/freeze IR structure exists in §15.4.5. MED gap sustained. Not a dedup of any audit F-id (audit's desired findings do not cover the per-arm-freeze IR-lowering gap).
- **Charity check:** sustain — IR lowering gives no per-arm gate/freeze structure for gated desired-block arms. Surface (031-37..41, SPEC 22171-22184): each when/given arm in desired: freezes independently — an inactive arm 'neither goes dirty nor triggers an update', reopen recomputes only that arm. IR (033-170 DECISION_LOG.md:4282): desired state is ONE whole-record cell pool_index<%T> from a single desired-builder behavior; 033-202 (DECISION_LOG.md:4314): that behavior's inputs are 'the desired: block's input cells', output is the desired record — a single pure behavior that recomputes wholly on any input change and cannot hold one arm's fields frozen while a sibling is live. 033-210 (DECISION_LOG.md:4322): the desired computation has 'no activation input anywhere', yet a when/given lowers to a gate (033-62, DECISION_LOG.md:4174) whose predicate on mode IS an activation input. The worked example (SPEC 24437-24483) contains no intra-desired when/given and its whole-record cell is trivially 'a pure function of message, so there is no activation input anywhere' — it does not exercise the gated-desired case. Searched 033/§15.4.5 for any lowering of desired-block when/given arms to per-arm gates freezing per-arm desired cells that feed the scatter: none exists. 022-91/022-93 confirm desired: arms genuinely hold nested per-arm desired cells, so the construct is real. No dissolving text. Sustain: gated desired arms have no IR lowering, and 033-210's 'no activation input' is in tension with a gate predicate driving desired-arm selection.
- **Evidence:**
  - `packages/ductus-lang/docs/DECISION_LOG.md:3806-3809`
    > 031-37. A `desired:` block may contain `when` and `given` selection blocks whose arms hold desired-cell declarations. (§13.19.4)
    > 031-38. `when`/`given` arms in `desired:` carry standard freeze semantics (Model B): every arm is constructed, the active arm's cells are live, inactive arms' cells are frozen at their last value. (§13.19.4)
    > 031-39. A frozen desired arm contributes static values to the reconciler: it neither goes dirty nor triggers an `update`. (§13.19.4)
    > 031-40. When a gate over desired cells reopens, the arm's cells recompute, go dirty, and the reconciler updates with the now-live values. (§13.19.4)
  - `packages/ductus-lang/docs/DECISION_LOG.md:4322-4322`
    > 033-210. An effect's desired computation has no activation input anywhere; activation is expressed solely through the suspend/resume model (§13.19.12). (§15.4.5)
  - `packages/ductus-lang/docs/DECISION_LOG.md:4282-4282`
    > 033-170. An effect's desired state is a single whole-record cell typed `pool_index<%T>` holding the desired-builder behavior's output record; the runtime scatters it into per-field desired state. (§15.4.5)
  - `packages/ductus-lang/docs/SPEC.md:24479-24483`
    > `App.print:0` uses the ordinal path form for an
    > anonymous instance (§15.4.1.1); and the whole-record desired cell
    > `App.print:0.desired` — a `pool_index<%TextRec>` (B@d5 returns the whole
    > `%TextRec`) that the runtime scatters into per-field desired state — is a pure
    > function of `message` (bound to `App.label`), so there is no activation input
    > anywhere, consistent with the suspend/resume-only model (§13.19.12).

#### P026 — The operator-parameter-kind rule (029-12 / SPEC §13.17.3) is a closed enumeration — cell-bound, value, or function/operator-typed — that does not admit a `yielded T` parameter, yet 034-11 and SPEC §13.20.4.1 require operators to accept `yielded T` parameters (the fold ecosystem's consumer path).

- **Severity/Category/Verdict:** MED / contradiction / CONFIRMED (family: cross-pair)
- **Anchors:** LOG 029-12, 034-11, 034-12 · SPEC § 13.17.3, 13.20.4
- **Why:** 029-12 and SPEC §13.17.3 enumerate the operator parameter kinds as a closed set ('The kinds are distinguished by declared type': cell-bound `cell T`, value `T`, function-/operator-typed). A `yielded T` group is a distinct cell-KIND (034-10, SPEC §13.20.4 — a member of the cell umbrella that stores nothing) and is not a single cell, so it fits none of the three: it is not a cell-bound `cell T` (029-13/14 describe that as binding to one reactive cell read for its current value), not a plain value `T`, and not function/operator-typed. Yet 034-11 and SPEC §13.20.4.1 mandate operators accept `yielded T` parameters — this is the primary way an operator consumes a collect-produced group short of folding it. An implementer reading the authoritative operator-parameter rule (§13.17.3) would reject `operator f(g: yielded Voice)`, contradicting the exact construct §13.20 declares legal. This is distinct from audit F096 (which is the ABI/IR-vocabulary representation gap for passing a yielded group across the behavior boundary) and F164 (the parallel gap where 018-37's repeat-source enumeration omits yielded): this finding is the surface-level operator-parameter-KIND enumeration contradiction in §13.17, not covered by either.
- **Direction:** Either 029-12 / SPEC §13.17.3 add `yielded T` as a fourth permitted operator-parameter kind (aligning §13.17 with 034-11/§13.20.4.1), or 034-11/§13.20.4.1 narrow the yielded-consumer surface to drop operator parameters — a design call to surface, not to resolve here.
- **Evidence check:** pass — Anchors verified: 029-12 (DECISION_LOG.md:3391) enumerates operator parameter kinds as cell-bound Cell[T]/cell T, value T, or function/operator-typed; SPEC §13.17.3 (19613-19617) confirms 'The kinds are distinguished by declared type' listing exactly cell-bound, value, function/operator-typed. 034-11 (:4362) and SPEC §13.20.4.1 (23067-23068) require operators accept yielded T parameters ('Yielded-typed parameters — yielded T may be a parameter type on operators and on functions'). 034-10 (:4361) makes yielded a distinct cell-KIND that stores nothing (034-9, :4360). A yielded T fits none of §13.17.3's three enumerated kinds. DEDUP: distinct from audit F093/F164/F097 (those are IR-lowering / repeat-source-admission gaps for yielded, not the §13.17.3 surface parameter-KIND enumeration) and distinct from the audit's 029-12 finding (which is the Cell[T]-bracket-notation divergence, F-anchored under the 029/13.2.8 divergence). The surface operator-parameter-kind contradiction is not reported in the audit. MED sustained. Minor caveat: 029-12 opens 'may be any value' before the three-item list, but §13.17.3's 'The kinds are distinguished by declared type' reads the list as closed, so the contradiction holds.
- **Charity check:** sustain — §13.17.3 (SPEC 19611-19617) opens 'An operator parameter may be any value' then states 'The kinds are distinguished by declared type' and enumerates exactly three: cell-bound (name: cell T), value (name: T), and function-/operator-typed. yielded T fits none: 016-1 (DECISION_LOG.md:1854) and 016-178 (DECISION_LOG.md:2031) list yielded T as a distinct lowercase kind annotation SEPARATE from cell T; 034-9/034-10 (DECISION_LOG.md:4360-4361) make it a distinct cell-KIND storing nothing, not a single reactive cell and not a plain value T. Yet 034-11 (DECISION_LOG.md:4362) and §13.20.4.1 (SPEC 23067-23068) mandate 'yielded T may be a parameter type on operators and on functions.' The §13.17.3 permission for yielded params is stated ONLY cross-section in §13.20.4.1; §13.17.3 itself gives an implementer three kinds and would reject operator f(g: yielded Voice). The 'any value' opener does not force admitting yielded (a store-nothing membership structure is not obviously 'a value'), and no text adds a fourth yielded-typed parameter kind to §13.17.3. Distinct from F096 (ABI/IR vocabulary gap for passing a yielded group across the behavior boundary) and F094/F097/F164 (repeat-source enumeration omits yielded); this is the surface parameter-KIND enumeration contradiction. Sustain.
- **Evidence:**
  - `packages/ductus-lang/docs/DECISION_LOG.md:3391-3391`
    > 029-12. An operator parameter may be any value: cell-bound `name: Cell[T]`, value `name: T`, or function- or operator-typed (carried structurally, §13.17.13). (§13.17.3)
  - `packages/ductus-lang/docs/DECISION_LOG.md:4362-4362`
    > 034-11. A `yielded` group is consumed only by the fold form, by yielded-typed parameters of both operators and functions, and by `repeat` over it; it supports no indexing, and a structural compile-time `for` over a `yielded` group is a compile error. (§13.20)
  - `packages/ductus-lang/docs/SPEC.md:19613-19617`
    > An operator parameter may be any value. Two kinds carry reactive or
    > configuration input — cell-bound and value parameters (below); a
    > parameter may additionally be **function-** or **operator-typed**, since
    > an operator is a first-class compile-time value (§13.17.13). The kinds
    > are distinguished by declared type:
  - `packages/ductus-lang/docs/SPEC.md:23067-23068`
    > - **Yielded-typed parameters** — `yielded T` may be a parameter type on
    >   operators *and* on functions; the body walks the members in walk order.

#### P028 — The field-vs-method-call disambiguation rule (009-21 / SPEC §6.1.4) breaks for a record field holding a closure: `r.handler()` has two conflicting specified parses — a method-call dispatch (§3.4.1) versus a field-read-then-closure-call (precedence tier-14) — and no rule resolves it, leaving closures stored in fields/slots with no specified invocation form.

- **Severity/Category/Verdict:** MED / parse_ambiguity / PLAUSIBLE (family: smell)
- **Anchors:** LOG 009-21, 013-178, 013-200 · SPEC § 6.1.4, 3.4.1, 4.4.7, 11.10.6
- **Why:** 013-200 and 013-195 explicitly permit closures in record fields and collection slots (`Vec[dyn fn(Event) -> ()]`), yet the only closure-invocation rule (013-178) applies to a binding name, not a field/index result. For a closure-typed field `handler`, the surface `r.handler()` is a name-after-dot followed by call syntax — exactly the 'method-call operator' shape §6.1.4/009-21 says routes to §3.4.1 dispatch (which searches for a trait method or free function named `handler` and errors if none exists), NOT a field read followed by a closure call. Simultaneously the precedence table (tier 14) makes `.` and `()` independent left-associative postfix operators, which parses `r.handler()` as `(r.handler)()` — read the field, then call the closure. The two specified readings give different behavior (unknown-method error vs. successful closure call), and no rule states which governs when a field is closure-typed. 009-21's premise ('syntax following the dot' is decisive) is false for callable fields: `r.method()` and `r.closure_field()` are syntactically identical after the dot. An implementer writing the resolver has no rule; a program storing a handler closure in a field/Vec slot has no specified invocation form (013-178 covers only bindings).
- **Direction:** State in the LOG/§6.1.4 how `expr.name(args)` resolves when `name` is a closure-typed field: either field-read-then-call wins, or method dispatch wins, or the field call must be parenthesized `(r.handler)(args)`; and give the invocation form for a closure held in a field/index slot (013-178 currently covers only bindings). Surface to user; do not resolve unilaterally.
- **Evidence check:** partial — Core anchors verified: 009-21 (DECISION_LOG.md:958) 'distinguishes field access from method call by the syntax following the dot'; 013-178 (:1564) 'A closure value is invoked with ordinary call syntax on its binding' (binding only); SPEC §6.1.4 (4839-4842) confirms disambiguation 'by the syntax following the dot' with method-call being 'followed by a function name and call syntax'; precedence tier 14 (SPEC:3354) makes '.', '()', '[]' independent left-assoc postfix ops. The two conflicting parses (method-dispatch vs field-read-then-closure-call) for r.handler() are supported and no rule resolves which governs a closure-typed field. HOWEVER: the finding's evidence block cites '013-200' with text 'A mut binding may carry an explicit type annotation: mut handlers: Vec[dyn fn(Event) -> ()]' at DECISION_LOG.md:1600, but line 1600 is actually 013-214 ('A mut binding may be reassigned entirely'); the quoted 013-200 text does NOT match disk at the cited line. That one cited quote is mislabeled/mismatched. The mismatch is not load-bearing to the crux (closures-in-fields is corroborated by the verified 009-21/013-178/§6.1.4/tier-14 anchors), so the finding's substance survives but one evidence line fails the within-3-lines quote check. PARTIAL.
- **Charity check:** sustain — The only closure-invocation rule 013-178 (DECISION_LOG.md:1564) invokes a closure 'with ordinary call syntax on its binding' — a binding name, not a field/index result. For a closure-typed field, r.handler() is a name-after-dot followed by call syntax = the method-call operator shape that §6.1.4 (SPEC 4839-4842) and 009-21 (DECISION_LOG.md:958) route to §3.4.1. §3.4.1 (SPEC 2034-2078) opens 'A bare-name call f(x) or method-call x.f() may resolve to either a trait-implementation function or a free function' and its 8-step algorithm has NO step checking whether the name is a closure-typed field of the receiver — step 8 is unknown-method error if no method/free-function matches. Simultaneously precedence tier 14 (SPEC 3354) makes . and () independent left-associative postfix ops, parsing r.handler() as (r.handler)() — read field, call closure. Two conflicting specified parses (unknown-method error vs successful closure call); no rule selects when the field is closure-typed. 013-200 (DECISION_LOG.md:1600) permits closures in Vec slots too, with no specified invocation form. Searched for callable-field / stored-closure invocation semantics: none found. Sustain.
- **Evidence:**
  - `packages/ductus-lang/docs/DECISION_LOG.md:958-958`
    > 009-21. The compiler distinguishes field access from method call by the syntax following the dot. (§6.1.4)
  - `packages/ductus-lang/docs/DECISION_LOG.md:1564-1564`
    > 013-178. A closure value is invoked with ordinary call syntax on its binding: `let total = closure(buf)`. (§11.10.1)
  - `packages/ductus-lang/docs/DECISION_LOG.md:1600-1600`
    > 013-200. A `mut` binding may carry an explicit type annotation: `mut handlers: Vec[dyn fn(Event) -> ()] = Vec::new()`. (§11.10.6)
  - `packages/ductus-lang/docs/SPEC.md:4839-4842`
    > A field is accessed by dot notation: `record.field_name`. The dot operator
    > is the field-access operator, distinct from the method-call operator (which
    > also uses `.` but is followed by a function name and call syntax). The
    > compiler disambiguates by the syntax following the dot.
  - `packages/ductus-lang/docs/SPEC.md:3354-3354`
    > | 14   | `?`, `.`, `[]`, `()`, and `T%()`/`T\|()`/`T?()` casts     | left           |

#### P029 — 008-62 restricts `Type[…]` to value positions that OMIT record fields ('attr, parameter, return value, let/const binding, or generic value argument'), but 008-71 and SPEC §5.7.4 explicitly permit storing a `Type[C]` value in a record field — an intra-section divergence over whether a record type may declare a `Type[C]`-typed field.

- **Severity/Category/Verdict:** MED / divergence / PLAUSIBLE (family: smell)
- **Anchors:** LOG 008-62, 008-71 · SPEC § 5.7.2, 5.7.4
- **Why:** 008-62 (the exhaustive 'appears only in value position' enumeration) lists attr, parameter, return value, let/const binding, and generic value argument — record field is absent. 008-71 (three entries later, same §008) says a type value 'may be stored in persistent slots: record fields, attrs, and cells,' and SPEC §5.7.4 repeats 'record fields, attrs, cells.' A record field is therefore both excluded (008-62's closed list) and permitted (008-71). This is behavior-changing: an implementer building the type checker cannot decide whether `type Registry: builder: Type[Drivable]` is legal. The tension is sharpened by 008-65's desugar rule — an existential `Type[C]` slot becomes a compiler-introduced generic parameter on 'the enclosing type' — which for a record field would silently make the record generic, a consequence 008-62's omission may be deliberately avoiding but 008-71 contradicts.
- **Direction:** Reconcile 008-62 and 008-71: either add 'record field' to 008-62's value-position list (and state how the 008-65 existential-desugar applies to a record field — does the record gain a hidden generic parameter?), or narrow 008-71 to attrs/cells only. Surface to user; do not resolve unilaterally.
- **Evidence check:** pass — All anchors verified verbatim: 008-62 (DECISION_LOG.md:922) 'Type[…] appears only in value position — attr, parameter, return value, let/const binding, or generic value argument' (record field absent); SPEC §5.7.2 (4667-4670) repeats the same closed list omitting record field. 008-71 (:931) 'A type value may be stored in persistent slots: record fields, attrs, and cells'; SPEC §5.7.4 (4706-4709) 'it may be stored in persistent slots (record fields, attrs, cells).' Record field is both excluded (008-62 closed enumeration) and permitted (008-71/§5.7.4). Behavior-changing: type-checker cannot decide legality of a Type[C]-typed record field. 008-65 (:925) desugar sharpens it (existential slot -> generic param on enclosing type -> would silently make the record generic). MED divergence sustained.
- **Charity check:** refute — The finding treats 008-62's list as a closed enumeration that EXCLUDES record fields, but §5.7.2 (SPEC 4665-4674) 'Type[…] appears only in value position — ... It never appears in bound position' scopes 'only' to the value-vs-BOUND axis: the em-dash list illustrates value positions, and the section's whole purpose is 'The instance-versus-type ambiguity ... exists only in value position ... in bound position ... no Type[…] is needed or permitted.' A record field is uncontroversially a value position (it holds a compile-time value, not a bound). §5.7.4 (SPEC 4706-4713, 008-71 DECISION_LOG.md:931) is a SEPARATE storage axis that normatively states a type value 'may be stored in persistent slots (record fields, attrs, cells)', placed three entries after 008-62 by the same authors. The two passages are CONSISTENT — 008-71 does not contradict 008-62 because a record field satisfies 'value position (not bound)'. Since the dissolving text is consistent with the finding's quoted passage (not inconsistent), the verdict is refute, not refile_divergence. The consistent reading is forced by 'only ... value position' contrasting bound + §5.7.4's explicit record-field permission. 008-65's desugar (existential slot → generic param on enclosing type) is a consequence 008-71 explicitly accepts, not a contradiction.
- **Evidence:**
  - `packages/ductus-lang/docs/DECISION_LOG.md:922-922`
    > 008-62. `Type[…]` appears only in value position — attr, parameter, return value, `let`/`const` binding, or generic value argument. (§5.7.2)
  - `packages/ductus-lang/docs/DECISION_LOG.md:931-931`
    > 008-71. A type value may be stored in persistent slots: record fields, attrs, and cells. (§5.7.4)
  - `packages/ductus-lang/docs/SPEC.md:4706-4709`
    > #### 5.7.4 Storage
    > 
    > Because a type value is compile-time data — not a borrow of any instance —
    > it may be **stored in persistent slots** (record fields, attrs, cells).
  - `packages/ductus-lang/docs/SPEC.md:4667-4670`
    > `Type[…]` appears only in **value position** — the type of an attr,
    > parameter, return value, `let`/`const` binding, or generic *value*
    > argument. It never appears in **bound position**

#### P004 — 'attr' restatements drifted: 016-15 says attrs are written ONLY at placement time; 021-16 makes a reactive-fed attr continuously track its source cells after placement via an implicit derived bridge.

- **Severity/Category/Verdict:** LOW / contradiction / CONFIRMED (family: collision)
- **Anchors:** LOG 016-15, 021-16 · SPEC § 13.2.2, 13.8.2.1
- **Why:** 016-15 restates the attr write-model as an absolute closed rule ('written only at placement time'). 021-16 describes an attr whose RHS references reactive cells reactively tracking those sources after placement through an implicit derived bridge — i.e. the attr's value changes on later commits, not only at placement. The restatements no longer say the same thing about WHEN an attr's value changes. A charitable reading exists (the bridge is instantiated at placement and the later updates are the bridge's, not new 'writes' to the attr slot), which is why this is LOW rather than a hard conflict, but 016-15's unqualified 'only at placement time' is stronger than 021-16 supports and can mislead an implementer about attr update timing. 021-16 is anchored in the audit (F174) but only for a citation-aptness defect (its §13.8.2.1 pointer), not for this write-timing drift against 016-15.
- **Direction:** Qualify 016-15 so 'written only at placement time' does not deny the post-placement reactive tracking of 021-16 (e.g. distinguish the placement-time bind of the value/bridge from the ongoing reactive updates), or confirm the two are intentionally reconcilable. Surface to user; do not edit.
- **Evidence check:** pass — 016-15 (DECISION_LOG.md:1868) 'Attrs are written only at placement time' vs 021-16 (DECISION_LOG.md:2755) reactive-fed attr 'reactively tracks the sources' after placement via an implicit derived bridge. Quotes confirm the tension over WHEN an attr's value changes. LOW is apt: the charitable reading (bridge instantiated at placement; later updates are the bridge's, not new writes) is available. Not a dup: F174 (audit:4296) cites 021-16 only for its §13.8.2.1 citation-aptness, not this write-timing drift against 016-15. CONFIRMED.
- **Charity check:** sustain — 016-15's absolute 'attrs are written only at placement time' vs 021-16/021-18's post-placement reactive tracking where 'the target attr updates' when sources change.
- **Evidence:**
  - `packages/ductus-lang/docs/DECISION_LOG.md:1868-1868`
    > 016-15. Attrs are written only at placement time. (§13.2.2)
  - `packages/ductus-lang/docs/DECISION_LOG.md:2755-2755`
    > 021-16. A placement attr RHS that references reactive cells (signals, attrs, recurrents, deriveds) creates an implicit derived bridging the source cells to the target attr, so the attr reactively tracks the sources: `Display as d1 | label=format(c1.count)`.

#### P006 — 005-65 states absolutely that `satisfies Trait` without a corresponding `fulfill` block is a compile error, but 005-6 and 005-72 assert that a methodless marker trait's `satisfies` (e.g. `satisfies Copy where T: Copy`) is legal with 'no `fulfill` block involved', so the pairing restatement flatly contradicts the marker-trait waiver restatements.

- **Severity/Category/Verdict:** LOW / contradiction / CONFIRMED (family: collision)
- **Anchors:** LOG 005-65, 005-6, 005-72, 009-9 · SPEC § 3.2
- **Why:** 005-65 states the satisfies-requires-fulfill rule with no in-entry exception, but 005-6 (marker: checked only for existence, no 
fulfill) and 005-72 (methodless marker: 'no `fulfill` block is involved') describe legal `satisfies` clauses with no fulfill. Under LOG Invariant 2 each entry must be self-contained; a reader landing on 005-65 and 005-6/005-72 sees a flat contradiction over whether `satisfies Copy` alone compiles. 005-67 waives the pairing for defaulted-method traits and 005-71 keeps it mandatory for abstract-method/required-cell traits, but neither 005-65 nor those waivers carry the methodless-marker carve-out into the pairing entry. LOW because the intended reading (markers/umbrella traits exempt) is recoverable from the nearby waiver entries.
- **Direction:** Amend 005-65 to state its exceptions self-containedly (marker/methodless-marker traits per 005-6/005-72 and pure-requirement umbrella traits per 009-9 need no fulfill), so the pairing restatement no longer contradicts the waiver restatements. Do not edit; surface to user.
- **Evidence check:** pass — 005-65 (DECISION_LOG.md:406) makes satisfies-without-fulfill an absolute compile error; 005-6 (DECISION_LOG.md:347) marker satisfies 'checked only for existence'; 005-72 (DECISION_LOG.md:413) methodless marker 'satisfies Copy where T: Copy ... no fulfill block is involved'; 009-9 (DECISION_LOG.md:946) adds the umbrella-trait exception. Quotes confirm 005-65's unqualified pairing rule contradicts the marker-trait/umbrella waivers. LOW is apt: the carve-out is recoverable from nearby 005-67/005-71/009-9. Not a dup: F015 (audit:2175) covers 005-72 only for the unpinned 'methodless-trait waiver' term, not this 005-65 pairing contradiction. CONFIRMED.
- **Charity check:** sustain — 005-65's absolute 'satisfies without fulfill is a compile error' vs 005-6/005-72 describing legal satisfies-without-fulfill for methodless marker traits.
- **Evidence:**
  - `packages/ductus-lang/docs/DECISION_LOG.md:406-406`
    > 005-65. `satisfies Trait` in a type body without a corresponding `fulfill Trait for Type` block reachable through the module graph is a compile error. (§3.2)
  - `packages/ductus-lang/docs/DECISION_LOG.md:347-347`
    > 005-6. A `satisfies` clause naming a marker trait is a declarative assertion about the type, checked by the compiler only for the trait's existence. (§3.1)
  - `packages/ductus-lang/docs/DECISION_LOG.md:413-413`
    > 005-72. A `satisfies` clause for a methodless marker trait (auto-satisfiable under the methodless-trait waiver) may carry a `where` clause that makes conformance conditional on the type's parameters: `satisfies Copy where T: Copy`. The type satisfies the marker for exactly those instantiations whose parameters meet the bounds, and for no others; no `fulfill` block is involved. (§3.2)
  - `packages/ductus-lang/docs/DECISION_LOG.md:946-946`
    > 009-9. Every `satisfies`-listed trait must have a matching `fulfill` block reachable through the module graph, except pure-requirement umbrella traits, which are satisfied automatically when their requirements are. (§6.1.1)

#### P010 — LOG 016-209 pins reactive-composite materialization to 'exactly three' boundaries, but 016-217 (and SPEC §13.2.9.8) describe a fourth reactive-cells-to-concrete-value path: a plain `let` of a `with` result.

- **Severity/Category/Verdict:** LOW / contradiction / PLAUSIBLE (family: collision)
- **Anchors:** LOG 016-209, 016-217, 016-216, 016-205 · SPEC § 13.2.9.7, 13.2.9.8
- **Why:** 016-209 uses a closed enumeration ('exactly three') for where a reactive composite's live cells collapse to a concrete value. 016-217/SPEC §13.2.9.8 add a path where reactive RHS cells are read at evaluation time to produce a concrete PeakResult — that is a reactive-to-concrete collapse not in the three. A charitable reading reconciles it (the plain-`let`-of-`with` result was never a live composite, so nothing is being 'materialized' — it is born concrete per §6.1.5), so this is soft, hence LOW. But the word 'exactly' invites an implementer to treat the three as exhaustive of every reactive-to-concrete transition and be surprised that a plain `let x = A with f: sig` snapshots sig while a plain `let x = A` aliases sig live (016-205). This is restatement drift: the 'exactly three' closure and the 'fourth' with-path were written in different LOG entries and no longer align on completeness.
- **Direction:** In 016-209 / SPEC §13.2.9.7, soften 'exactly three' or scope it explicitly to *already-live* composites, and cross-note that a `with` result bound in a non-reactive form is born concrete (§6.1.5 / 016-216-217) rather than materialized — so it is outside the enumeration by construction, not a fourth boundary. User decision on wording; do not resolve here.
- **Evidence check:** pass — 016-209 (DECISION_LOG.md:2062) verbatim: 'Reactive composites stay live across Ductus source except at exactly three materialization boundaries...' 016-217 (DECISION_LOG.md:2070) verbatim: 'A plain `let` of a `with` result... yields a concrete value, reading reactive RHS cells at evaluation time.' SPEC §13.2.9.7 (12705-12706) 'Three boundaries force materialization'; SPEC §13.2.9.8 (12764-12768) confirms the plain-let-of-with concrete path. The word 'exactly three' invites treating the three as exhaustive of every reactive-to-concrete transition; the with-path is a fourth. Charitable reconciliation exists (born concrete, never a live composite), so LOW is correct. Restatement-drift confirmed on disk. Not in audit report (no 016-209/016-217 hits).
- **Charity check:** refute — Does 016-217's plain-let-of-with reactive-to-concrete path constitute a fourth reactive materialization boundary that 016-209's 'exactly three' wrongly excludes?
- **Evidence:**
  - `packages/ductus-lang/docs/DECISION_LOG.md:2062-2062`
    > 016-209. Reactive composites stay live across Ductus source except at exactly three materialization boundaries: non-reactive collection storage, FFI/host handoff, and serialization/persistence. (§13.2.9.7)
  - `packages/ductus-lang/docs/DECISION_LOG.md:2070-2070`
    > 016-217. A plain `let` of a `with` result that does not flow into a reactive declaration yields a concrete value, reading reactive RHS cells at evaluation time. (§13.2.9.8)
  - `packages/ductus-lang/docs/SPEC.md:12705-12706`
    > reactive composites stay live across most of the language. Three
    > boundaries force materialization to a concrete value:
  - `packages/ductus-lang/docs/SPEC.md:12764-12768`
    > A plain `let` binding to the `with` expression's result, not
    >   itself flowing into a reactive declaration, produces a concrete
    >   value per the standard §6.1.5 semantics — `signal_c` is read for
    >   its current value at the let-binding's evaluation and the result
    >   is a concrete `PeakResult`.

#### P013 — 009-43 presents the record @derive set as a fixed six-trait set, but 005-193..197 admit @derive(Copy), @derive(Circularity), and any opt-in language-defined marker on records too.

- **Severity/Category/Verdict:** LOW / contradiction / PLAUSIBLE (family: collision)
- **Anchors:** LOG 009-43, 005-193, 005-194, 005-196, 005-197 · SPEC § 6.1.8, 3.8.1
- **Why:** 009-43 restates the record @derive-eligible set as a closed 'fixed trait set' of six traits (Eq, Ord, Hash, Clone, Display, Debug). But 005-196 (@derive(Copy), which checks 'every field's type must be Copy' — a record-shaped check) and 005-197 (@derive(Circularity)) and 005-194 (any opt-in language-defined marker) show additional traits are @derive-eligible on records. A reader building the record @derive whitelist from 009-43 rejects @derive(Copy)/@derive(Circularity), which 005-196/005-197 explicitly permit. The two restatements of 'the record @derive set' disagree. Weaker than the other two because 009-43 is arguably scoped to structural-implementation-generating derivations (Copy/Circularity generate no method body per 005-196/197), so the sets could be reconciled as 'structural-impl set' vs 'full @derive-eligible set' — but 009-43's bare 'fixed trait set' wording does not carry that qualification and reads as the exhaustive record @derive set.
- **Direction:** Either scope 009-43 explicitly to structural-implementation-generating derivations (excluding the no-method-body markers Copy/Circularity), or restate 009-43's set to include the full record @derive-eligible set per 005-193..197. User decision on the intended scope of 009-43; do not resolve unilaterally.
- **Evidence check:** pass — 009-43 (line 980) verbatim: '@derive on a record generates structural implementations for a fixed trait set: @derive(Eq, Ord, Hash, Clone, Display, Debug)' (six, 'fixed'). 005-194 (535) 'Every opt-in language-defined marker trait is @derive-eligible'; 005-196 (537) '@derive(Copy) performs the structural Copy-eligibility check — every field's type must be Copy' (record-shaped, field-by-field); 005-197 (538) '@derive(Circularity) is a bare opt-in declaration.' A reader building the record @derive whitelist from 009-43's 'fixed trait set' rejects Copy/Circularity which 005-196/197 permit. Reconcilable as structural-impl-set vs full-eligible-set, so LOW is correct. Verified on disk. Not in audit report.
- **Charity check:** refute — Does 009-43's 'fixed trait set' of six wrongly exclude @derive(Copy)/@derive(Circularity), which 005-196/197 permit on records?
- **Evidence:**
  - `packages/ductus-lang/docs/DECISION_LOG.md:980-980`
    > 009-43. `@derive` on a record generates structural implementations for a fixed trait set: `@derive(Eq, Ord, Hash, Clone, Display, Debug)`. (§6.1.8)
  - `packages/ductus-lang/docs/DECISION_LOG.md:535-535`
    > 005-194. Every opt-in language-defined marker trait is `@derive`-eligible. (§3.8.1)
  - `packages/ductus-lang/docs/DECISION_LOG.md:537-537`
    > 005-196. `@derive(Copy)` performs the structural Copy-eligibility check — every field's type must be `Copy` — and generates no method body. (§3.8.1)
  - `packages/ductus-lang/docs/DECISION_LOG.md:538-538`
    > 005-197. `@derive(Circularity)` is a bare opt-in declaration: no structural check, no method body. (§3.8.1)

#### P027 — 029-30 scopes the fold-forces-indented-block rule to a fold that is the operator body's FINAL expression, while 035-8 and both SPEC leaves (§13.17.4, §13.21.6) scope it to ANY fold written in an operator body — a scope-of-forcing drift within the same rule duplicated across §029 and §035.

- **Severity/Category/Verdict:** LOW / divergence / CONFIRMED (family: cross-pair)
- **Anchors:** LOG 029-30, 035-8 · SPEC § 13.17.4, 13.21.6
- **Why:** The fold-in-operator-body forcing rule is duplicated across §029 (029-30) and §035 (035-8) plus two SPEC leaves — exactly the drift risk the director flagged from 035-8 splicing fold into 029's block-form rules. 029-30 conditions the forcing on 'a body whose final expression is a `fold` form'; 035-8 and SPEC §13.17.4/§13.21.6 condition it on 'a `fold` written in an operator body' (any position). The outcomes coincide (a non-final fold body-item already implies a multi-item indented block, so the block is forced either way), so this is not implementer-blocking — hence LOW — but the two statements of the same rule differ in stated scope, and the narrower 029-30 wording could mislead a reader into thinking a fold appearing as a non-final body item is not itself the trigger.
- **Direction:** Align the two statements: either broaden 029-30's clause to 'any `fold` in the body forces the indented block' (matching 035-8 and both SPEC leaves), or note in 035-8 that the forcing is only operative for the final-expression case; do not resolve unilaterally.
- **Evidence check:** pass — Verified: 029-30 (DECISION_LOG.md:3409) scopes the forcing to 'a body whose final expression is a fold form always forces the indented block', while 035-8 (:4375) scopes it to 'a fold written in an operator body forces the indented block form' (any position), and SPEC §13.21.6 (19743-19745) says 'A fold in an operator body forces the indented block form.' Scope-of-forcing drift confirmed (final-expression-only vs any-position). Finding correctly self-classifies LOW: outcomes coincide because a non-final fold body-item already implies a multi-item indented block. Not implementer-blocking; a duplicated-rule wording drift. LOW sustained.
- **Charity check:** sustain — 029-30 (DECISION_LOG.md:3409) scopes the forcing to 'a body whose final expression is a fold form always forces the indented block'; 035-8 (DECISION_LOG.md:4375) and SPEC §13.17.4/§13.21.6 (SPEC 19743-19745) scope it to 'a fold written in an operator body' (any position). Real wording drift of the same rule duplicated across §029 and §035. Outcomes coincide (a non-final fold body-item already implies a multi-item indented block, so the block is forced either way), so it is not implementer-blocking — LOW/divergence. The narrower 029-30 wording could mislead a reader into thinking a non-final fold body item is not itself the trigger. No dissolving text reconciles the two scopes. Sustain.
- **Evidence:**
  - `packages/ductus-lang/docs/DECISION_LOG.md:3409-3409`
    > 029-30. An operator body that is just its final expression (no declarations) may be written inline after the colon — `operator double(source): source * 2` — following the same inline-or-indented-block form permitted for function bodies; a body carrying any `recurrent`/`derived`/`stream`/`let` declaration is an indented block, and a body whose final expression is a `fold` form always forces the indented block (a `fold` form is never written inline). (§13.17.4)
  - `packages/ductus-lang/docs/DECISION_LOG.md:4375-4375`
    > 035-8. A `fold` joins the permitted operator-body items and may appear as a derived right-hand side, and a `fold` written in an operator body forces the indented block form rather than the inline final-expression form. (§13.21)
  - `packages/ductus-lang/docs/SPEC.md:19743-19745`
    > (§13.21) — a `collect` builds a `yielded` group and a `fold` reduces
    >   one (or a uniform-slot composite) to a value cell; both may appear as
    >   a `derived` RHS or as a body item. A `fold` in an operator body forces
    >   the indented block form (its header cannot share the inline-after-colon
    >   single-expression body).

#### P030 — The three-form call equivalence (001-33) plus the trait-shadows-free-function rule (005-124) creates a silent-capture footgun: an in-scope trait impl with a method named `f` silently wins over an unrelated same-named free function at `f(x)`/`x.f()`, demoting the free function to path-only reachability, so importing a trait can silently redirect existing bare-name calls.

- **Severity/Category/Verdict:** LOW / design_smell / CONFIRMED (family: smell)
- **Anchors:** LOG 001-33, 005-120, 005-124 · SPEC § 3.4.1
- **Why:** Footgun witness: a module defines `fn render(s: Song) -> Frames` (a free function) and calls `render(song)` throughout. A later `use SomeRenderTrait` brings a trait into scope whose `render` method `Song` fulfills. Per 005-120/005-124 the trait-impl candidate now silently wins at every `render(song)` and `song.render()` site; the original free function is demoted to `this_module::render(song)` and every existing bare call is redirected to a different operation with no diagnostic. 001-33 promises methods and free functions are 'interchangeable syntactic forms for the same operation,' but this rule silently swaps the operation when the two share a name across the trait/free-function boundary. The capture is triggered by an import, action-at-a-distance, with no warning. This is an intended consequence of the priority rule (so not a contradiction), but it tensions 001-33's interchangeability promise and is a maintainability hazard the docs do not flag.
- **Direction:** Consider whether a same-name collision between a trait impl and an unrelated in-scope free function should warn/error at the call site (or at import) rather than silently redirect; at minimum document the capture hazard at 005-124/§3.4.1. Design call — surface to user.
- **Evidence check:** pass — All anchors verified: 001-33 (DECISION_LOG.md:63) 'Methods and free functions are interchangeable syntactic forms for the same operation'; 005-120 (:461) resolution 'prioritizes trait implementations over free functions'; 005-124 (:465) 'A matching trait-impl candidate wins over a same-named in-scope free function, which is shadowed at that call site and callable only via path qualification'; SPEC §3.4.1 (2057-2060) confirms 'The trait impl wins over any same-named free function... shadowed... callable only via path qualification.' The import-triggered silent-capture footgun is a genuine consequence: a use bringing a trait into scope silently redirects existing bare-name calls, tensioning 001-33's interchangeability promise. Finding correctly classifies it as an intended-consequence design smell (not a contradiction) -> LOW. LOW sustained.
- **Charity check:** sustain — 005-124 (DECISION_LOG.md:465) and §3.4.1 step 4 (SPEC 2057-2060) 'The trait impl wins over any same-named free function. A free function with the same name in scope is shadowed at this call site; it remains callable only via path qualification' — no warning, no error. 005-121 (DECISION_LOG.md:462) collects trait candidates 'for every trait reachable in the current scope (imported or accessible by path)', so a use-import of a trait whose method name matches an existing free function silently redirects every existing bare render(song)/song.render() call site to a different operation, action-at-a-distance via import. This tensions 001-33 (DECISION_LOG.md:63) 'Methods and free functions are interchangeable syntactic forms for the SAME operation.' Searched for a mitigating diagnostic: SPEC 16293-16298 guards a DIFFERENT collision (body-member vs module-level value NAME → 'bare names never silently shadow across the body/module boundary', a compile error) — it does not reach the trait-vs-free-function dispatch case, which by contrast resolves silently. So the mitigation text sharpens rather than dissolves the smell: the language explicitly rejects one silent-shadow class but permits this import-triggered redirect with no diagnostic. Intended consequence of the priority rule (not a contradiction), undocumented maintainability hazard — LOW/design_smell. Sustain.
- **Evidence:**
  - `packages/ductus-lang/docs/DECISION_LOG.md:63-63`
    > 001-33. Methods and free functions are interchangeable syntactic forms for the same operation: `f(x, y)` may equivalently be called as `x.f(y)`. (§1.3)
  - `packages/ductus-lang/docs/DECISION_LOG.md:465-465`
    > 005-124. A matching trait-impl candidate wins over a same-named in-scope free function, which is shadowed at that call site and callable only via path qualification: `some_module::f(x)`. (§3.4.1)
  - `packages/ductus-lang/docs/SPEC.md:2057-2060`
    > 4. **One parent-trait candidate matches → resolve to it.** The trait impl
    >    wins over any same-named free function. A free function with the same
    >    name in scope is *shadowed* at this call site; it remains callable only
    >    via path qualification (e.g., `some_module::f(x, ...)`).

## Remaining gaps after patch-up (completeness critic)

- Sampled-pair blindness survives the patch-up. There are C(35,2)=595 cross-section pairs (confirmed: 35 distinct LOG section prefixes). The main audit examined 15 pairs as pairs (matrix: 'sampled (15 of 595 pairs)'); the patch-up revives exactly one more dead lane, pair-029x035, bringing the total to 16 of 595. ~579 section pairs are never examined for cross-section jointly-unsatisfiable conflict. Two entries that conflict but do NOT share a normalized lexical subject (e.g. an §011 two-track rule vs an §031 reconciler rule phrased about different nouns) fall through BOTH the claims-index collision adjudication (which keys on shared subject) AND the unsampled 579 pairs. — why missed: The claims-index collision adjudication is exhaustive only 'at subject-collision fidelity' (coverage matrix): it clusters entries by normalized subject, so a conflict with no subject overlap is structurally invisible to it. The cross-pair lane is sampled, and reviving one dead pair (029x035) does not change the sampling regime. No lane in either the base audit or the patch-up does an all-pairs semantic sweep. — next: A next run would need either an all-pairs (595) cross-section semantic sweep, or a subject-agnostic conflict detector that pairs entries by shared *referent/mechanism* (gate, cell-path, dirty-set, track) rather than shared lexical subject, to catch cross-section jointly-unsatisfiable rules that the collision index cannot see.
- Witness-scenario sampling survives unchanged. The base audit ran ~10-12 constructive multi-section witness scenarios (matrix: 'sampled (10 scenarios)'). The joint-satisfiability space of 3+-section compositions over 35 sections is combinatorially large (thousands of 3-tuples). The patch-up adds NO new witness lane — it adds claims-index, orphan-lossless, dead-pair, residual-gate, known-open, and F115 lanes only. So constructive joint-satisfiability of composed constructs (e.g. yielded x fold x gate x hot-reload interactions beyond the ~12 probed) remains sampled, not exhaustive. — why missed: Witness probing is inherently constructive and human-designed; it cannot be made exhaustive and no patch-up lane targets it. The patch-up's F115 touch resolves one §034 claim but does not backfill the yielded/fold/gate witness neighborhood that generated the §034 HIGH cluster (F093/F094/F114/F211). — next: A next run would need a directed witness expansion specifically over the amendment-seam constructs (yielded, fold, gate-close, hot-reload identity) — the regions where the base audit already found dense HIGHs — since found-defect density predicts where more composed defects hide.
- The orphan-subtree sweep is LOSSLESS, not SEMANTIC. It closes the base audit's hole (1) only in the narrow sense of LOG<->SPEC preservation over uncited SPEC zones. ~745 numbered SPEC subsections exist vs ~716 distinct section-refs cited by LOG entries; the uncited remainder gets a preservation/lossless check but NOT a defect-read for SPEC-internal contradictions, undefined terms, or implementer-gaps. A contradiction living entirely inside an uncited SPEC subsection (with no LOG anchor) is still undetectable after the patch-up. — why missed: The revived lane is a 'reverse losslessness (churn + orphan zones)' sweep — its charter is preservation, not correctness reading. Uncited SPEC prose has no LOG entry to anchor a LOG-SPEC divergence finding, and the audit's finding model is LOG-anchored, so purely-SPEC-internal defects in orphan zones have no reporting slot. — next: A next run would need a dedicated read-for-defects pass over the ~29+ uncited SPEC subsections treating SPEC-internal contradiction/undefined-term as first-class findings (as Appendix-A F038 already shows can occur), decoupled from LOG-anchoring.
- The residual-term gate is mechanical presence-detection, not adjudication. Grep confirms live residue survives: gate_stream x1 (SPEC), combineLatest x1 (SPEC), Monoid x1 (LOG), plus 'interpret' x44 LOG / x82 SPEC and 'signal-as-noun' which is high-frequency legitimate. The gate can flag surviving tokens but the judgment of whether each survivor is benign residue (e.g. a deletion note, a rejected-design mention) or a genuine live defect is a per-hit adjudication the gate does not itself perform. — why missed: A mechanical grep gate returns hits; classifying each hit as defect-vs-legitimate requires reading surrounding context and cross-referencing the known-open register (C12 deletions, rejected combineLatest/Monoid designs). The gate's output is a candidate list, so residual hits that are actually legitimate deletion-residue vs actually-live defects are not separated by the gate alone. — next: A next run (or the writer agent) must adjudicate each surviving residual hit against the known-open register: gate_stream/combineLatest/Monoid are named as rejected/deleted designs, so confirm each surviving occurrence is a mention-in-rejection, not a live normative use, before down-weighting.
- F115 verification resolves one UNVERIFIED claim but does not reduce the UNVERIFIED-class risk structurally. The base audit lists five dead agents (pair-029x035, smell-behavior-and-data, dedup-2, director-triage, completeness-critic). The patch-up revives four functions (029x035, smell-behavior-and-data, F115, plus this completeness lane) but director-triage and dedup-2 remain unrun — so residual near-duplicate survivors flagged in the reading guide (F057/F086/F162; F190/F260/F092; F094/F097/F164; F171/F211) are still not collapsed, and no independent triage re-ranked the merged set. — why missed: dedup-2 and director-triage deaths are not in this patch-up's charter (it lists claims-index, orphan, two dead lanes, residual-gate, known-open, F115). The two revived dead lanes are the sweep lanes, not the triage lanes, so the de-duplication and cross-finding ranking gap persists. — next: A next run would need the dedup-2 and director-triage lanes re-run to collapse the acknowledged near-duplicate clusters and produce an independent severity re-rank, so the survivor x-counts are not the only dedup signal.
- Known-open tagging is a suppression filter, not a coverage guarantee. The base report states 'Zero findings were tagged known-open' — meaning nothing was suppressed OR the register items simply did not resurface. After the residual-gate and dead-lane revival surface NEW hits (gate_stream, combineLatest, Monoid, Phase-19a/19b subscribe->dependency-edge residue), those new hits must be run through the known-open register; a hit that IS a known-open item but is NOT recognized as such would be mis-reported as a fresh defect. — why missed: The known-open register was applied to the base pass's findings; the patch-up generates a fresh batch of residual/orphan/pair hits that post-date that application. The tagging step must be re-run against the new hits, and this lane (completeness critic) does not itself perform that tagging. — next: The writer/tagging agent must re-apply the known-open register (Phase-19a Cell-storage residue, Phase-19b subscribe->dependency-edge, C12 to_ring/gate_stream deletions, rejected interpret/combineLatest/Monoid, 029-74 fn-RHS wording) to every new residual-gate and orphan-sweep hit before the addendum reports them.

Disclaimers:
- Even after the patch-up, cross-section completeness is bounded at subject-collision fidelity plus 16 of 595 sampled pairs; ~579 section pairs are never adjudicated as pairs and any jointly-unsatisfiable conflict between two entries that share no normalized subject is outside the method's reach.
- Joint-satisfiability of composed constructs is sampled (~10-12 witness scenarios), not proven; the patch-up adds no witness lane, so absence of a composition defect is not evidence of its absence.
- The orphan sweep attests LOG<->SPEC preservation over uncited SPEC zones only; it does NOT read uncited SPEC prose for internal contradictions, so purely-SPEC-internal defects in the ~29+ uncited subsections remain unattested-for-correctness (only attested-for-preservation).
- The residual-term gate reports token survivors (gate_stream, combineLatest, Monoid, interpret, signal-as-noun) mechanically; whether each survivor is benign deletion-residue or a live defect is a separate adjudication the gate does not perform and which must be reconciled against the known-open register.
- F115 is adjudicated by a single verification pass, not independently re-derived; and dedup-2/director-triage remain unrun, so the acknowledged near-duplicate clusters (F057/F086/F162; F190/F260/F092; F094/F097/F164; F171/F211) are still uncollapsed and the severity ranking rests on survivor x-counts, not an independent triage.
- Parse-ambiguity and grammar coverage remain heuristic (2 parser-adversary lanes, no grammar proof); the patch-up does not add a parser lane, so lexer/parser conflicts beyond the sampled adversaries are undisclaimed.
- The amendment plan document remains out of scope as evidence in both the base pass and the patch-up; all findings rest on LOG+SPEC only, so a defect that exists solely as a divergence between the plan's intent and the landed docs is not in the corpus of record.

## Agent ledger

- extract-orphans: ok
- extract-claims: ok
- collide-1: ok
- collide-5: ok
- collide-2: ok
- collide-8: ok
- collide-7: ok
- collide-3: ok
- collide-4: ok
- collide-6: ok
- collide-10: ok
- collide-9: ok
- gate-residual: ok
- witness-col-3: ok
- witness-col-2: ok
- witness-col-4: ok
- witness-col-5: ok
- witness-col-1: ok
- tagger-known-open: ok
- pair-029x035: ok
- completeness-critic: ok
- smell-behavior-and-data: ok
- v-evidence-f115: ok
- v-charity-f115: ok
- v-evidence-2: ok
- v-evidence-4: ok
- v-evidence-1: ok
- v-charity-1: ok
- v-evidence-3: ok
- v-charity-3: ok
- v-charity-4: ok
- v-charity-2: ok
