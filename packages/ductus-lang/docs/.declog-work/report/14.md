| § | skeleton_n | draft_n | final_n | adds | splits | removals |
|---|---|---|---|---|---|---|
| 13.4 | 0 | 0 | 0 | 0 | 0 | 0 |
| 13.4.0 | 8 | 7 | 7 | 0 | 0 | 0 |
| 13.4.1 | 15 | 15 | 15 | 0 | 0 | 0 |
| 13.4.2 | 6 | 5 | 5 | 0 | 0 | 0 |
| 13.4.3 | 3 | 3 | 3 | 0 | 0 | 0 |
| 13.4.4 | 6 | 6 | 6 | 0 | 0 | 0 |
| 13.4.5 | 1 | 0 | 1 | 1 | 0 | 0 |
| 13.5 | 4 | 3 | 3 | 0 | 0 | 0 |
| 13.5.1 | 8 | 8 | 8 | 0 | 0 | 0 |
| 13.5.1.1 | 5 | 5 | 5 | 0 | 0 | 0 |
| 13.5.2 | 7 | 6 | 6 | 0 | 0 | 0 |
| 13.5.3 | 6 | 6 | 6 | 0 | 0 | 0 |
| 13.5.4 | 6 | 5 | 5 | 0 | 0 | 0 |
| 13.5.4.1 | 39 | 38 | 38 | 0 | 0 | 0 |
| 13.5.4.2 | 10 | 8 | 9 | 0 | 1 | 0 |
| 13.5.4.3 | 2 | 0 | 1 | 1 | 0 | 0 |
| 13.5.4.4 | 2 | 2 | 2 | 0 | 0 | 0 |
| 13.5.4.5 | 5 | 4 | 4 | 0 | 0 | 0 |
| 13.5.4.6 | 10 | 10 | 10 | 0 | 0 | 0 |
| 13.5.4.7 | 14 | 14 | 14 | 0 | 0 | 0 |
| 13.5.4.8 | 12 | 11 | 11 | 0 | 0 | 0 |
| 13.5.4.9 | 14 | 14 | 14 | 0 | 0 | 0 |

Totals: draft 170 → reviewed 173 (adds 2, splits 1, removals 0). Per-§ counts in the reviewed COVERAGE block verified against the actual entry tags; sum = 173 matches `<!-- END 14 n=173 -->`.

Delta detail (this report reconstructed independently; diff of entry bodies with numbering stripped):
- ADD reviewed 14-37 (§13.4.5): coexistence of type-bulk and named access with indexing legality under `+` / `[=1]` — closes the §13.4.5 zero the drafter left; it is the worked example's only summary claim not restated elsewhere.
- ADD reviewed 14-118 (§13.5.4.3): attr-as-`repeat`-source / reads yield `Signal[Vec[VoiceConfig]]` with the collection supplying the iterator inside the `Signal` (SPEC.md lines 13583-13586) — normative source-typing claim stated nowhere else; closes the §13.5.4.3 zero.
- SPLIT draft 14-110 → reviewed 14-111 + 14-112 (§13.5.4.2): the diff step and the `old ∩ new` carry-over rule are two commitments.
- REWORD draft 14-10 → reviewed 14-10 (§13.4.1): dropped the drafter's interpolated "only" (spec says "indexable under the guaranteed minimum", SPEC.md line 13111) and relocated the `c.parts.Oscillator[0]` legality example into ADD 14-37. No loss.
- REWORD draft 14-77 → reviewed 14-78 (§13.5.4.1): the worked-example fragment `repeat (sid, info) in sessions` (from §13.5.4.3's SessionPanel) replaced with the spec's own §13.5.4.1 gloss ("same destructuring grammar the for-loop's iteration variable accepts", SPEC.md lines 13412-13414). Example-only content; rule preserved at 14-78/14-79.

Reconciliation notes for skeleton_n vs final_n gaps (no entry changes required except as listed in UNRESOLVED):
- §13.4.0: skeleton includes the parts-vs-top-level usage-guidance block (SPEC.md lines 13075-13086) → see UNRESOLVED.
- §13.4.2: "compiler unrolls to one reference per declared part" folded into 14-27; the conscious-decision rationale for per-type-only iteration folded into 14-25. No loss.
- §13.5: skeleton counts the contested "zero, one, or many times per source element" multiplicity (SPEC.md lines 13267-13269) → deliberately held in finding 1, not serialized.
- §13.5.2: the fast-path facet "per-commit floor still applies; fast path eliminates only per-scope storage" is carried at 14-132, where §13.5.4.6 restates it. No loss.
- §13.5.4: the use-repeat-vs-use-a-gate directive is reconstructible from 14-69 + 14-70. No loss.
- §13.5.4.1: the "storage sites require real owners; cluster members cannot be stored (§11.3.4)" rationale paragraph folded into 14-81/14-88 conclusions (restates §11.1/§11.3.4 rules owned elsewhere). No loss.
- §13.5.4.2: step 1's bind-sees-owned-element clause is a restatement carried at 14-80 (§13.5.4.1). No loss.
- §13.5.4.3: skeleton's second item — per-`voice_id` persistence of recurrents inside placed `Voice` children (SPEC.md lines 13582-13583) — is the contested side of the state-shape ambiguity and is held in finding 6, deliberately not serialized. The section's remaining normative-sounding sentences restate rules owned elsewhere: internals-vs-caller-supply and never-counted-against-`parts:` → 14-135–14-138 (§13.5.4.7) with "absent from the `parts` namespace" defined at §13.3.4.2 (chunk 13 territory, cross-referenced by the spec itself); single-exposition rule → §13.3.3.3; per-scope cell paths `<effect-instance>.<row.id>.<cell>` → 14-61–14-64; implicit-keying examples → 14-103–14-106.
- §13.5.4.5: the opening "follows §13.15.2's path-based cell identity" restates 14-60/14-119; the run-new-derivation-next-commit-and-diff procedure folded into 14-123/14-124. No loss.
- §13.5.4.8: skeleton counts the contested "`<key-expr>` / `Keyed::key` body must not read it" clause (SPEC.md lines 13773-13781) → deliberately held in finding 3, not serialized. The React `key={index}` anti-pattern rationale folds into 14-159's "accepting that consequence".

NO-NORMATIVE-CONTENT:
§13.4: heading only — SPEC.md line 13055 ("### 13.4 Parts") is immediately followed by §13.4.0; there is no prose at the §13.4 level. All content lives in §13.4.0–§13.4.5, each of which has final_n ≥ 1.

REMOVALS: none

UNRESOLVED:
- coverage gap (judgment call, advisory severity) | §13.4.0 | The parts-vs-top-level usage-guidance block (SPEC.md lines 13075-13086) appears in NEITHER draft nor reviewed: the three "use parts when" criteria (contained instance conceptually owned by the parent; parent's reactive expressions aggregate over the child instances; compositional structure is part of the parent's identity) and the complement "use top-level placements when the instance stands on its own and participates in the graph through connections rather than containment". The mechanics half is covered (14-1–14-3) but the criteria are not, and this chunk's log does record comparable "use X for Y" guidance elsewhere (14-26 `dyn Trait`, 14-77 stream projection), so the omission is inconsistent rather than clearly principled. Against logging it: the block is advisory framing with subjective predicates, and the gate passed without it. Proposed entry wording: "Guidance: use parts when the contained instance is conceptually owned by the parent, when the parent's reactive expressions need to aggregate over the child instances, or when the compositional structure is part of the parent's identity; use a top-level placement when the instance stands on its own and participates in the graph through connections rather than containment. (§13.4.0)" Orchestrator action: add the entry or ratify the omission as motivational prose.

FINDINGS:
- kind: ambiguity | sections: §13.5, §13.5.4 | readings: the primitive permits "zero, one, or many" instantiations *per source element* (multi-scope-per-element capability runtimes must support) vs loose phrasing for "zero-to-many scopes overall, exactly one per unique key" (as `repeat`'s one-scope-per-element behavior implies) | impact: whether conformant runtimes must support multiple scopes per element for future constructs | quarantined: "The keyed-scope primitive permits a template to be instantiated zero, one, or many times per source element. (§13.5)" (entry 14-39 was written without the per-element multiplicity claim) | assessment: reconstructed-audit confirmed — SPEC.md lines 13267-13269 read "instantiated zero, one, or many times per source element"; reviewed 14-39 carries only the own-state-cells clause, and no entry serializes the per-element multiplicity; the renumbered pointer (draft said 14-38) correctly tracks the reviewed numbering.
- kind: incoherence | sections: §13.5.4.1, §13.5.4.2 | readings: §13.5.4.2 step 2's parenthetical gloss ("explicit `keyed by`, then `Keyed` trait, then stringifiable element, else compile error") is an incomplete restatement and the full 5-path order of §13.5.4.1 (including carried keys) applies at iteration time vs §13.5.4.2 normatively excludes the carried-key path from per-commit re-keying (contradicting §13.5.4.1 path 2 and §13.5.4.8's carried-key diffing) | impact: whether `dynamic` namespace sources use carried keys during per-commit key derivation | quarantined: "Key derivation at iteration time proceeds: explicit `keyed by`, then `Keyed` trait, then stringifiable element, else compile error. (§13.5.4.2)" (entry 14-110 cites §13.5.4.1's ordered selection without enumerating the paths) | assessment: reconstructed-audit confirmed — the step-2 parenthetical (SPEC.md lines 13524-13526) omits the carried-key path that §13.5.4.1 path 2 defines and §13.5.4.8's "Carried keys diff identically" bullet (lines 13767-13772) presupposes; 14-110 is the only entry-list match for iteration-time key derivation and reproduces neither the defective gloss nor a resolution.
- kind: contradiction | sections: §13.5.4.8 | readings: "the `<key-expr>` / `Keyed::key` body that those paths use must not read it" bars all key-exprs from reading the `at` index, with bare `keyed by <index>` as the sole sanctioned exception vs the must-not-read clause binds only implicit-path derivation (`Keyed::key`), and explicit `keyed by` expressions may freely read the index (as `keyed by <index>` in the same subsection demonstrates) | impact: legality of explicit key expressions that read the index (e.g. arithmetic over it) | quarantined: "The `<key-expr>` and the `Keyed::key` body must not read the `at` index bind. (§13.5.4.8)" | assessment: reconstructed-audit confirmed — SPEC.md lines 13773-13781 contain both the must-not-read clause and the sanctioned `keyed by <index>` opt-in; entries 14-158/14-159 carry only the uncontested implicit-path exclusion and the explicit opt-in, and "must not read" appears in no entry.
- kind: ambiguity | sections: §13.5.4.1, §13.5.4.2 | readings: two elements deriving equal keys in one evaluation collapse to one scope (with an unspecified element winning the bind) vs duplicate keys are a runtime or compile-time error; the spec defines key-set diffing but never duplicate-key behavior | impact: divergent implementations on sources yielding duplicate keys | quarantined: none | assessment: reconstructed-audit confirmed — §13.5.4.1's derivation paths and §13.5.4.2's set-diff steps (SPEC.md lines 13514-13543) nowhere define behavior when two elements of one evaluation derive the same key; no entry asserts a duplicate-key semantics, so "quarantined: none" is accurate.
- kind: ambiguity | sections: §13.5, §13.5.4.1 | readings: §13.5's intro glosses the `repeat` source as "a reactive iterable source (`Signal[I]` where `I: Iterable`)", subsuming `dynamic` namespace cells because such cells are themselves `Signal[I]`-typed vs the intro omits the second source kind §13.5.4.1 admits (`dynamic` namespace cells as a distinct, non-`Signal[I]` source) | impact: whether conformance requires `dynamic` namespace cells to be representable as `Signal[I]` | quarantined: none | assessment: reconstructed-audit confirmed — the intro gloss (SPEC.md lines 13271-13273) names only `Signal[I]` while §13.5.4.1's source bullet (lines 13399-13404) lists `dynamic` namespace cells disjunctively and gives them carried-key behavior plain `Signal[I]` sources lack; entry 14-40 says "reactive iterable source" without the `Signal[I]` restriction and 14-72 records both kinds, so neither reading is serialized as exclusive.
- kind: ambiguity | sections: §13.5.2, §13.5.4.1, §13.5.4.3 | readings: state-shape counts only direct `signal`/`attr`/`recurrent` declarations in the template body — but a `repeat` body is a placement-body block of placements and connections, which may make every `repeat` template trivially stateless — vs state-shape encompasses the cells of the template's placed child instances, as §13.5.4.3's per-key persistence of `Voice` recurrents and §13.5.3-style paths (`<effect-instance>.<row.id>.<cell>`) imply | impact: whether `repeat` scopes whose placed children hold state take the no-pool fast path of §13.5.2 | quarantined: none | assessment: reconstructed-audit confirmed — §13.5.2's definition (SPEC.md lines 13312-13317) is literally declaration-scoped while §13.5.4.3 asserts "Each `Voice` scope's state (recurrents inside `Voice`) persists across commits for the same `voice_id`" (lines 13582-13583); entry 14-54 carries only the literal definition, and the placed-children extension lives solely in this finding. Note: the Voice-persistence sentence is the de-facto withheld claim, so "quarantined: none" is technically accurate (no drafted entry was withheld) but the finding would be stronger citing it; compliance holds either way.

ORCHESTRATOR-RESOLUTION: the §13.4.0 coverage-gap UNRESOLVED item is resolved by ADDING the guidance, split into two atomic entries (now 14-4/14-5) per the atomicity test; idiomatic guidance is in scope per the user requirement that the log covers all decision kinds.
