# Review report — chunk 22 (§13.18.9–§13.18.16, SPEC.md lines 18747–19548)

Reviewer read the full spec range first (two slices, both verified complete to slice end; line 19549 confirmed as the §13.19 boundary) and wrote skeleton/22.md (107 keyphrases) before opening the draft.

| § | skeleton_n | draft_n | final_n | adds | splits | removals |
| §13.18.9 | 27 | 36 | 36 | 0 | 0 | 0 |
| §13.18.10 | 1 | 1 | 1 | 0 | 0 | 0 |
| §13.18.10.1 | 2 | 2 | 2 | 0 | 0 | 0 |
| §13.18.10.2 | 6 | 5 | 5 | 0 | 0 | 0 |
| §13.18.10.3 | 3 | 5 | 5 | 0 | 0 | 0 |
| §13.18.10.4 | 2 | 2 | 2 | 0 | 0 | 0 |
| §13.18.10.5 | 4 | 4 | 4 | 0 | 0 | 0 |
| §13.18.10.6 | 3 | 3 | 3 | 0 | 0 | 0 |
| §13.18.11 | 5 | 8 | 8 | 0 | 0 | 0 |
| §13.18.12 | 11 | 15 | 15 | 0 | 0 | 0 |
| §13.18.13 | 10 | 13 | 13 | 0 | 0 | 0 |
| §13.18.14 | 15 | 21 | 21 | 0 | 0 | 0 |
| §13.18.15 | 8 | 8 | 8 | 0 | 0 | 0 |
| §13.18.16 | 10 | 10 | 10 | 0 | 0 | 0 |

skeleton_n vs draft_n deltas are granularity only (skeleton folded signature+default pairs; draft split them — verified one-to-one against spec text). Skeleton lines with no in-list draft entry all map to quarantined wording in findings 1–3 (protocol-compliant): "recurrent observe arms self-history access" (§13.18.10.5), "decreasing k reload-safely truncates" (§13.18.14), "past/previous only recurrent stream" (§13.18.15), and the per-consumer increment clause of "cursor jumps, dropped_total increments" (§13.18.12, uncontested remainder kept as 22-74). Skeleton's "§13.18.9 projections structural initials" is distributed across 22-19..22-22 (each entry states its structural initial). Final K = 133; numbering already dense and in spec order (merge/throttle interleave anchored at code-block lines, verified). One wording correction, not counted as add/split/removal: 22-112 — draft said "fully preserved"; the word "fully" is not in the spec and silently harmonizes finding 3 toward one reading; restored to the spec's "preserved iff". Judgment call kept from draft: 22-106 holds the three reload-unsafe change kinds (element type / policy / capacity) as one entry — three list rows sharing one stated consequent (per-instance restart), treated as a governing rule over a closed set, not two decisions. Every example in all 133 entries was traced to its cited subsection's text (none invented); all refs are deepest owned §s; quarantine hygiene verified (no contested wording in entries, no trailing comments present, ` <!-- ooo -->` not needed).

NO-NORMATIVE-CONTENT:
- §13.18.9: skip/take "duals" descriptor; merge default rationale ("ensuring no overflow if both inputs fill simultaneously"); cross-policy-merge rationale ("overflow semantics would be ambiguous"); to_signal default rationale (signals always defined, §13.2.6/§13.9.7 refs); pipe type-directed-dispatch mechanism note (§13.17.7's content); scan/pairwise equivalence-to-recurrent and when-to-prefer guidance.
- §13.18.10.2: "subset operation" contrast paragraph — the spec itself says §13.18.7.2 (another chunk) "states that rule normatively"; the per-LHS-event driver rule is kept as 22-42.
- §13.18.10.3: capacity rationale ("because they don't drive emission").
- §13.18.11: "output-buffer property / independent of input semantics" rationale paragraph; author-responsibility framing (normative tail kept as 22-66).
- §13.18.12: "consumers that care about completeness must monitor dropped_total or use gate" (author guidance); "a stream whose consumers may be frozen ... wants ring, not gate" (guidance); "frozen consumer is the slowest-possible consumer" descriptor; @reset_on_reopen trade-off analogy to recurrent history.
- §13.18.14: "the trailing history would have no place to live" rationale; §13.15.3 when-predicate precedent analogy.
- §13.18.15: "a function that needs to produce events ... returns a value the caller feeds" guidance; "streams are not signal-shaped or attr-shaped" rationale; lockstep cross-ref (§13.2.4.1); "explicit to_stream is still available" (implied by the operator's existence, 22-3).
- §13.18.16: hint prose beyond the exemplar code (remediation suggestions).

REMOVALS: none.

UNRESOLVED:
- All five findings are spec-text defects/ambiguities; not resolvable by this pipeline — carried in reviewed/22.md FINDINGS for upstream adjudication.

FINDINGS:
- kind: contradiction | sections: §13.18.10.5, §13.18.15, §13.18.16 | readings: §13.18.15's ".past/.previous only valid inside recurrent[N] stream bodies; use in signal arms is a compile error" read literally (banning recurrent-signal history) vs scoped to non-recurrent contexts only, since §13.18.10.5 (and §13.2.4.3) allow arm expressions in a recurrent's observe body to use .previous/.past | impact: legality of history access in recurrent-signal observe arms (e.g. `on tick where active_mode: counter.previous(0) + 1`) differs | quarantined: "When an observe block is a recurrent's body, arm expressions may reference the recurrent's self-history via `.previous(fallback)` and `.past(k, fallback)`. (§13.18.10.5)" and "`.past(n, fallback)` and `.previous(init)` are valid only inside the expression body of a `recurrent[N] stream` declaration; use in plain `stream` declarations, `derived` expressions, or signal arms is a compile error. (§13.18.15)" | assessment: confirmed — independently verified: §13.2.4.3 (SPEC.md line 10891) grants .previous/.past to recurrent value cells, and line 11891 shows `on tick where counter.previous(0) < 100` in arms; §13.18.15's literal exclusivity (lines 19413–19416) and §13.18.16's diagnostic message text contradict; 22-131 deliberately states only the uncontested plain-stream case.
- kind: ambiguity | sections: §13.18.12, §13.18.13 | readings: `dropped_total` is a per-stream cumulative displacement counter (per the §13.18.6 surface table and §13.18.13 per-stream metadata), incrementing at overwrite time vs §13.18.12's "the dropped_total signal increments by the number of skipped events" implying per-consumer gap accounting at cursor-jump time | impact: observable counter value differs when multiple consumers lag by different amounts | quarantined: "When a ring overwrite forces a cursor jump, `dropped_total` increments by the number of events the cursor skipped. (§13.18.12)" | assessment: confirmed — part-1 surface table (line 18401: "Cumulative count of events displaced by overflow (ring only; always 0 for gate)") and §13.18.13 line 19251 are per-stream; §13.18.12 lines 19206–19209 tie the increment to a consumer's gap observation; uncontested remainder retained as 22-74.
- kind: ambiguity | sections: §13.18.14 | readings: decreasing an input's max `k` "reload-safely truncates allocation" retaining the most recent `k` values vs the same bullet's "preserved iff ... max k does not decrease" implying the history is not preserved (contents reset to fallbacks, allocation shrunk) | impact: post-reload `.past` reads return retained pre-reload values vs fallbacks | quarantined: "Decreasing an input's max `k` is reload-safe and truncates its history allocation. (§13.18.14)" | assessment: confirmed — both sentences sit in the same §13.18.14 bullet (lines 19375–19381); note the contrast with output-history decrease, which is reload-UNSAFE, making the per-input decrease text doubly anomalous; additionally the draft's 22-112 had softened "preserved" to "fully preserved" (a silent partial resolution toward the truncation reading) — corrected to spec wording in the reviewed list.
- kind: ambiguity | sections: §13.18.9 | readings: `throttle`'s `clock: Signal[u64]` measures `window` in ticks aligned with `duration`'s nanosecond representation (§9.4.1) vs the tick unit and the clock-to-window relation are implementation-/caller-defined — the catalog never states how `clock` and `window: duration` interact | impact: which events a conforming `throttle` emits vs suppresses | quarantined: none (22-27 states the contract as listed; no entry removed) | assessment: new (reviewer) — grep confirms `clock: Signal[u64]` appears only at the §13.18.9 signature (line 18869) plus a cross-ref at 18412–18419; no global clock convention exists, and the spec's own time-point type `instant` is not used here.
- kind: ambiguity | sections: §13.18.9, §13.18.11 | readings: all Stream-to-Stream catalog operators (including `throttle`, `scan`, `pairwise`) preserve source policy and capacity by construction, §13.18.11's "(map, filter, skip)" list being exemplary vs preservation holds only where expressly stated (`map`, `filter`, `skip`, `take`, `merge`), leaving `throttle`/`scan`/`pairwise` output policy and capacity unspecified | impact: output buffer configuration (overflow policy and capacity) of `throttle`/`scan`/`pairwise` results | quarantined: none (no entry asserts preservation for these operators; no entry removed) | assessment: new (reviewer) — §13.18.9's by-construction sentence (lines 18873–18875) is a restrictive clause ("operators that preserve ... do so by construction"), naming no operator set; only map/filter carry the preserved-from-source comment and skip/take are covered at lines 18815–18817; throttle/scan/pairwise have no statement anywhere in the chunk.

Additional verification notes: §13.18.16 contains exactly TEN diagnostic classes (independently recounted; drafter's count of ten confirmed). COVERAGE block totals 133 and matches per-§ entry counts; sentinel n=133 consistent.
