# Review report — chunk 23 (§13.19 Effects, SPEC.md lines 19549-20506)

| § | skeleton_n | draft_n | final_n | adds | splits | removals |
| §13.19 | 4 | 8 | 6 | 0 | 0 | 2 |
| §13.19.1 | 3 | 5 | 4 | 0 | 0 | 1 |
| §13.19.2 | 10 | 6 | 7 | 1 | 0 | 0 |
| §13.19.3 | 7 | 11 | 11 | 0 | 0 | 0 |
| §13.19.4 | 8 | 17 | 17 | 0 | 0 | 0 |
| §13.19.5 | 8 | 14 | 15 | 1 | 0 | 0 |
| §13.19.6 | 3 | 4 | 4 | 0 | 0 | 0 |
| §13.19.7 | 6 | 8 | 8 | 0 | 0 | 0 |
| §13.19.8 | 4 | 7 | 7 | 0 | 0 | 0 |
| §13.19.9 | 2 | 3 | 3 | 0 | 0 | 0 |
| §13.19.10 | 2 | 2 | 3 | 1 | 0 | 0 |
| §13.19.11 | 14 | 19 | 19 | 0 | 0 | 0 |
| §13.19.12 | 8 | 15 | 15 | 0 | 0 | 0 |
| §13.19.13 | 3 | 5 | 5 | 0 | 0 | 0 |
| §13.19.14 | 5 | 11 | 12 | 1 | 0 | 0 |
| §13.19.15 | 5 | 9 | 9 | 0 | 0 | 0 |
| §13.19.16 | 12 | 12 | 12 | 0 | 0 | 0 |

Notes on the table: skeleton_n counts my independent pre-draft decision lines (104 total; coarser than the draft, which had already split lists into atomic entries — hence draft_n > skeleton_n in most rows; no draft entry required further splitting). skeleton_n for §13.19.2 includes the contested at-least-one-block line, quarantined rather than serialized. Adds: 23-13 (block optionality, §13.19.2 — required by finding 1's quarantine so the uncontested per-block optionality is not lost), 23-59 (host writes keyed by instance ID and cell name, §13.19.5), 23-84 (module-private unreachable / public re-exportable, §13.19.10), 23-125 (reconciler registered per type via host API keyed by type name, §13.19.14 — relocation of draft 23-12 to its deepest owned section). Amendments in place: 23-15 (+ top-level `signal` to the excluded-declarations list), 23-71 (+ "stream value is the program-readable cursor view"), 23-74 (+ what counts as different scopes: different `effects:` clauses or different `repeat`/gate arms), 23-78 (+ example folded from removed draft 23-6), 23-97 (+ governing "reloads by its ordinary identity rules", the easy-to-miss SPEC lines 20141-20147 content), 23-133 (+ "permitting the runtime to invoke the reconciler freely").

NO-NORMATIVE-CONTENT:
- §13.19.1: "unifies request/response with long-lived resources ... mental shift ... pays for itself" — rationale, stripped (drafter's strip confirmed).
- §13.19.2: worked examples (`fetch`, `websocket`) and their captions — exemplars, mined only for entry examples.
- §13.19.3: "used for configuration values that do not vary at runtime (HTTP method, content type, retry budget)"; "authors place the primary upstream cell as the first parameter" — usage guidance.
- §13.19.4: "canonical pattern for io-driven topology — rendering one component per database row..." — motivation; "no new reconciler mechanism is involved" — explanatory gloss on 23-42/23-43.
- §13.19.5: "typically a sentinel like `None` ... or `false`" — guidance; "observed blocks declare cells that receive host-pushed data; they do not host reactive-structure declarations" — rationale for the ban serialized at 23-54.
- §13.19.12: "freezing them (Model B) is complete" — rationale within 23-116's rule.
- §13.19.13: named-entry flat-namespace paragraph and stream-operator-access paragraph — restatements of §13.19.7 rules already serialized at 23-65 and 23-71; not separately serialized, nothing lost.
- §13.19.15: "which keeps reconciler ordering and lifecycles explicit rather than nesting them" — rationale for 23-140/23-141.
- §13.19.16: exemplar error/hint texts — serialized as the twelve classes per registry rule (hint texts non-normative; see finding on divergent hints).

REMOVALS:
- draft 23-4 ("Effects compose with operators via `|>`.", §13.19) — removed as duplicate: the intro bullet summarizes §13.19.13, whose entries (final 23-120, 23-124) carry the full commitment; deepest-owned-section rule places the content there. No content lost.
- draft 23-6 (declaration introduces type and constructor under one name, §13.19) — removed as duplicate of the §13.19.8 "type and constructor unified" rule (final 23-78); the `effect fetch` example was folded into 23-78. No content lost.
- draft 23-12 (reconciler keyed by the effect's type name, §13.19.1) — moved, not dropped: §13.19.1 forward-references §13.19.14, where the registration rule is stated in full; re-serialized as final 23-125 with the per-type and via-host-API aspects restored. Counted as the §13.19.1 removal and the §13.19.14 add.

UNRESOLVED: none.

FINDINGS:
- kind: contradiction | sections: §13.19.2, §13.19.3 | readings: an effect must declare at least one of `desired:`/`observed:` (an effect with neither has no surface) vs the `log` example declares an effect with neither block ("// no desired, no observed — pure fire-and-forget consumer"), implying stream-parameter-only effects may omit both | impact: whether `effect log(entries: GateStream[LogEntry, 4096])` with an empty body is legal | quarantined: "At least one of `desired:` or `observed:` must be present in an effect declaration. (§13.19.2)" or "An effect taking only stream parameters may declare neither block, acting as a fire-and-forget consumer: `effect log(entries: GateStream[LogEntry, 4096])`. (§13.19.3)" | assessment: confirmed — verified at SPEC lines 19642-19645 vs 19728-19729; quarantine correctly enforced in the draft (neither side serialized; stream-typing of parameters kept at draft 23-29/final 23-27, which is uncontested)
- kind: incoherence | sections: §13.19.15, §13.19.16 | readings: "an effect-using function would need to be promoted to an operator" means promotion enables consuming/transforming effect cells only (instantiation still belongs in `effects:`) vs it implies operators may host effect instantiations, contradicting the same restrictions list and the operator-body diagnostic | impact: whether promotion to operator grants any effect-hosting capability | quarantined: "An effect-using function must be promoted to an operator. (§13.19.15)" | assessment: confirmed — verified at SPEC line 20340 vs lines 20322-20327 and 20479-20489; the function-body diagnostic's own hint (lines 20412-20414) points to the `effects:` clause, not to operator promotion, supporting the consume-only reading but leaving the §13.19.15 advice incoherent as written; quarantine correctly enforced (final 23-138 carries only the ban)
- kind: incoherence | sections: §13.19.2, §13.19.4, §13.19.16 | readings: §13.19.2's "no other declaration kinds are permitted inside the blocks" and the §13.19.16 hint "effect blocks accept only `derived`, `sink` (in `desired:`) and `signal`, `stream` (in `observed:`)" read literally ban `repeat`/`when`/`given` inside `desired:` vs §13.19.4 expressly admits `repeat` declarations and `when`/`given` selection blocks in `desired:` | impact: whether the disallowed-declaration diagnostic fires on `repeat`/`when`/`given` in a `desired:` block | quarantined: "Effect blocks accept only `derived`, `sink` (in `desired:`) and `signal`, `stream` (in `observed:`). (§13.19.16)" | assessment: confirmed (new, this review) — the cell-declarations-only reading reconciles the texts, but the §13.19.16 hint's exhaustive list omits the §13.19.4 admissions; entries 23-14/23-15, 23-37/23-40, and 23-152 each track their own section's letter, and the over-broad exhaustive-list sentence is kept out of the entry list
- kind: ambiguity | sections: §13.19.4, §13.19.5 | readings: §13.19.5's direction to place a `repeat`/`when`/`given` in "the same effect's `desired:` block, consuming the observed cell as the source" implies sibling `observed:` cells are in scope as inputs inside `desired:` vs §13.19.4 defines `derived` inputs only as "parameters and any in-scope cells" and never states that sibling observed cells are in scope, nor how the resulting host-write to desired-recompute to reconciler-update loop is ordered | impact: whether a desired-block `repeat` or `derived` may consume the same instance's observed cells, and the commit ordering of the resulting feedback loop | quarantined: none | assessment: confirmed (new, this review) — final 23-55 serializes exactly what §13.19.5 states; the unstated scoping premise is recorded here rather than inferred into an entry
- kind: incoherence | sections: §13.19.5, §13.19.7, §13.19.16 | readings: the "cannot assign to cell `response`" diagnostic's hint in §13.19.5 ("the host's reconciler is the sole writer ... to inject test data, construct a different effect instance or use a stub effect") vs the hint in §13.19.7/§13.19.16 ("change the upstream signal(s) ... or pipe a stream into the effect's sink(s)") | impact: low — one diagnostic class shown with divergent exemplar hint texts; class trigger and behavior identical | quarantined: none | assessment: confirmed (new, this review) — minor wording-level incoherence flagged per protocol rather than silently normalized

Process notes: a stale skeleton/23.md from the killed reviewer run existed; per brief it was read only to unlock overwrite, its content ignored, and my skeleton written from scratch before the draft was opened. Spec slices verified complete (first Read ended at line 20048, second at 20506 — "*End of §13.*"). Independent diagnostic-class count in §13.19.16: twelve, agreeing with the drafter; all twelve serialized as classes in spec order. Draft quarantine and trailing-comment rules verified: no contested wording leaked into draft entries, no trailing comments present (none of the legal ` <!-- ooo -->` form either). Draft contained no invented examples; every example traced to the cited subsection. Key easy-to-miss content (suspended-instance reload, SPEC lines 20141-20147) present in draft 23-97..23-100, carried forward with the governing sentence restored in final 23-97.
