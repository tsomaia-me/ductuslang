| § | skeleton_n | draft_n | final_n | adds | splits | removals |
|---|---|---|---|---|---|---|
| §13.9 | 8 | 11 | 11 | 0 | 0 | 0 |
| §13.9.1 | 5 | 8 | 7 | 0 | 0 | 1 |
| §13.9.2 | 3 | 3 | 3 | 0 | 0 | 0 |
| §13.9.3 | 3 | 5 | 5 | 0 | 0 | 0 |
| §13.9.4 | 4 | 5 | 5 | 0 | 0 | 0 |
| §13.9.5 | 4 | 4 | 4 | 0 | 0 | 0 |
| §13.9.6 | 3 | 4 | 4 | 0 | 0 | 0 |
| §13.9.7 | 29 | 31 | 31 | 0 | 0 | 0 |
| §13.9.8 | 5 | 2 | 2 | 0 | 0 | 0 |
| §13.9.9 | 4 | 5 | 5 | 0 | 0 | 0 |
| §13.9.10 | 1 | 3 | 3 | 0 | 0 | 0 |
| §13.9.11 | 2 | 6 | 6 | 0 | 0 | 0 |
| §13.9.12 | 14 | 17 | 17 | 0 | 0 | 0 |
| §13.9.13 | 10 | 15 | 15 | 0 | 0 | 0 |
| total | 95 | 119 | 118 | 0 | 0 | 1 |

Skeleton-vs-draft reconciliation notes: every skeleton keyphrase maps to a draft entry except three §13.9.8/§13.9.7 items ("gated subtree excluded from DAG", "gate-close adds no DAG work", "no recompute pass on close"), which are correctly quarantined inside findings 1 and 2 rather than asserted as entries — quarantine verified appropriate, not gaps. Draft entries with no skeleton counterpart (scope-of-evaluation 17-18, connection-placement modifier 17-23, gate-true definition 17-42, recurrent-no-snap-recompute 17-51, arm-body-list 17-108, payload-change re-evaluation 17-113) all verify against spec text and are retained.

NO-NORMATIVE-CONTENT: none

REMOVALS:
- draft 17-19 ("No marker attribute (such as `active`) designates a gate; the `when` keyword carries the structural role explicitly." §13.9.1): removed. It is drawn from the explicitly labeled rationale block "Two design moves justify the clause" (SPEC lines 15405-15414). The marker-trait rejection reserves nothing and requires nothing of a conforming implementation; the gate surfaces and grammar are already fixed by entries 17-6, 17-19, and 17-22. Fails the losslessness test (rationale/"why", per STRIP rules). The sibling design-move bullet (host routing rejected) is separately and normatively stated in the §13.9 intro and survives as 17-11.
- clause-level trim (not an entry removal): draft 17-11's clause "and the runtime enforces gate state at edge level" removed from reviewed 17-11; the identical commitment is owned at the deepest § by 17-41 (§13.9.7, "evaluates gate state at edge level on each propagation cycle"). The routing-lives-in-source commitment is retained in 17-11.
- correction (not a removal): draft 17-34 (now 17-33) used an invented example `Pulse when a and b: l1` that appears nowhere in the spec, violating the example-traceability rule. Replaced with the spec-literal `Pulse when my_app.is_emitting and my_app.debug_audio: l1` (SPEC line 15548); that example's incoherence stays flagged in finding 4 rather than being silently neutralized.

UNRESOLVED:
- Close-transition recompute contradiction (finding 1): contested sentences remain quarantined; spec author must decide whether a one-shot effect-desired recompute exists at gate-close.
- Scope of "gated subtree excluded from the DAG" (finding 2): absolute vs predicate-support exception; quarantined.
- Gate-open snap commit timing (finding 3): "within the commit that flips" vs "between commits"; quarantined.
- §13.9.5 combination example (finding 4): needs an editorial fix in the spec; reviewed entry transcribes it verbatim meanwhile.
- Placement-level endpoint scope for connection `when` (finding 5): rule text and example disagree on whether `from`/`to`/`pair` are placement-resolvable.
- "provably total" guard criterion (finding 6): undefined anywhere in the spec.

FINDINGS:
- kind: contradiction | sections: §13.9.7, §13.9.8 | readings: gate-close performs no recompute pass at all ("There is no recompute pass"; "Gate-close adds no DAG work") vs the deriveds bullet referencing "the one-shot effect-desired recompute of the close transition (below)" at the false-flip commit, a recompute the close paragraph never defines | impact: whether effect desired state recomputes once at suspend time | quarantined: "Gate-close triggers no recompute pass; freezing plus effect `suspend` is the entire close behavior. (§13.9.7)" vs "At the false-flip commit a one-shot effect-desired recompute runs before the subtree settles frozen. (§13.9.7)"; also "Gate-close adds no per-commit DAG work; its only consequence is firing `suspend` for contained effects. (§13.9.8)" | assessment: confirmed — verified at SPEC lines 15641-15644 ("one-shot effect-desired recompute of the close transition (below)") against 15675-15677 ("There is no recompute pass") and 15740 ("Gate-close adds no DAG work"); the forward reference "(below)" points at text that denies the referenced recompute; quarantine of all three contested wordings verified.
- kind: ambiguity | sections: §13.9.8, §13.9.7 | readings: "A subtree that is gated off is excluded from the DAG; nothing in it recomputes" read as absolute vs as implicitly excepting the predicate-support subgraph that §13.9.7 requires to stay current | impact: whether predicate-read deriveds in a gated subtree recompute via the per-commit DAG or outside it | quarantined: "A gated-off subtree is excluded from the per-commit DAG; nothing in it recomputes. (§13.9.8)" vs "A gated-off subtree is excluded from the per-commit DAG except the minimum subgraph supporting its `when` predicate. (§13.9.8)" | assessment: confirmed — SPEC lines 15734-15735 vs 15637-15640; §13.9.7 frames the exception as a §14 implementation concern "transparent at the language level", which softens but does not resolve the absolute §13.9.8 wording.
- kind: ambiguity | sections: §13.9.6, §13.9.7, §13.9.8 | readings: the open snap is "scheduled within the commit that flips the predicate" (§13.9.6, §13.9.8) vs §13.9.7's "transitions from false to true between commits", readable as the snap landing in the following commit | impact: same-commit cascade vs one-commit latency at gate-open | quarantined: "The gate-open transition is a propagation event scheduled within the commit that flips the predicate. (§13.9.6)" vs "A gate-open detected across a commit boundary is processed as a propagation event in the next commit. (§13.9.6)" | assessment: confirmed — SPEC lines 15565-15567 and 15739-15741 vs 15655-15657; "between commits" can be read as boundary-comparison (consistent) or next-commit scheduling (inconsistent), and nothing in the chunk pins one reading.
- kind: incoherence | sections: §13.9.5 | readings: the explicit-combination example reads `my_app.is_emitting` although the type-level gate reads `from.is_emitting` and the example `App` declares no `is_emitting`; likely intended `d1.is_emitting` or `from.is_emitting` | impact: non-normative example only; the total-replacement rule is unaffected (reviewer note: entry 17-33 now transcribes the spec example verbatim, per the no-invented-examples rule; this finding flags its incoherence) | quarantined: none | assessment: confirmed — SPEC line 15535 (`when: from.is_emitting`) vs 15548 (`when my_app.is_emitting and ...`); the App body at 15537-15541 declares no `is_emitting`. Reviewer amended the finding's impact parenthetical: the draft entry had dodged the incoherence with an invented neutral example, which violates example traceability; the reviewed entry quotes the spec and the finding carries the flag. Cross-links to the new endpoint-scope finding below.
- kind: ambiguity | sections: §13.9.3, §13.9.4, §13.9.5 | readings: the §13.9.3 example `ShowsCount when from.count > 0: d1` has a placement-level predicate reading the connection's `from`, but §13.9.4's placement-level scope enumeration (siblings, parent attrs, top-level cells, identifiers resolvable at that point) never grants `from`/`to`/`pair`, which the type-level bullet grants explicitly "for connections" — endpoint access at placement level is either implied by the example or not granted by the rule | impact: whether a connection placement's `when` may reference endpoint cells; if not, a type-level endpoint predicate such as `from.is_emitting` cannot be reproduced in the explicit placement-level combination §13.9.5 requires under total replacement (plausibly why the §13.9.5 example swaps to `my_app.is_emitting`) | quarantined: none (entries 17-23 and 17-31 transcribe the spec verbatim) | assessment: confirmed (reviewer-added) — SPEC line 15468 is the only placement-level endpoint reference in the spec (grep over `from.count` confirms); lines 15512-15518 enumerate the two scope levels and grant endpoints only at type level.
- kind: ambiguity | sections: §13.9.12 | readings: `default:` is required "unless some guard is provably total" — "provably total" occurs nowhere else in the spec and no proof criterion is defined (const-evaluable literal `true`? type-derived totality? implementation-chosen analysis?) | impact: whether a multi-way `when:` block without `default:` is accepted varies by implementation; the boundary of the missing-default diagnostic class is unspecified | quarantined: none (entry 17-99 transcribes the rule verbatim) | assessment: confirmed (reviewer-added) — grep confirms SPEC line 15905 is the sole occurrence of "provably total" in the document.
